# primary v9 写作要求增量草案（M-d T6 草案半场；2026-09-19）

状态：**草案**——未投产。基线 = am4-native-v8（R7_PRIMARY_DEFAULT_PROMPT，协议键冻结）。
v9 不改任何输出协议键（PrimaryDecision.parse 逐字对齐不破坏），只做三处增量，
应用路径：Prompt 工作台 diff v8↔v9 → 审批（R2 门）→ release_asset 新版本激活 →
rca_model_call.role_digest 对账。该装配文件当前属主会话在途改动面，等可写窗口再动。

---

## 增量一：把握显式措辞（供 conclusion_six_parts 评分检出）

v8 写作要求已列六要素，但"有多大把握"没有硬措辞——报告里可写可不写，评分器检不到。
v9 在【报告写作要求（六要素）】段的硬规则列表追加一条：

```text
- ROOT_CAUSE 的 statement 结尾必须带把握短语，格式固定：
  「把握：HIGH（多源一致且机理通顺）」/「把握：MEDIUM（多源一致）」/「把握：LOW（单源证据）」
  三选一，括号内写依据；不确定就是 LOW，不丢这个短语（丢了=报告不完整，评分按缺要素算）。
```

理由：这是 T5 conclusion_six_parts 指标的机器检出锚（statement 文本 regex
"把握：HIGH|MEDIUM|LOW"），也是六要素纪律第 5 条（诚实降级好过硬编）的落地点。

## 增量二：补一个坏示例（v8 只有正面范例）

v8 措辞范例给了一个好例子。按 Extra08"示例优于解释、坏例划边界"，在同段追加：

```text
反例（不合格，出现即返工）：
  "经分析，该问题系多因素耦合导致，建议持续关注并加强治理。"
  哪里坏：没有根因（多因素=没说）、没有证据（凭什么是）、没有量化（影响多大？）、
  没有把握、建议是空话。六件里五件没有，这一段等于没写。
```

## 增量三：禁黑话三条（写作要求的"不做什么"边界）

```text
- 禁黑话：赋能、抓手、闭环、沉淀、拉齐——出现即返工；
- 禁无量化描述："成功率显著下降"不合格，"成功率 99.2%→87.4%"合格；
- 禁伪证据引用：statement 正文不得出现"日志显示""指标表明"却不给出对应
  evidence_refs 的写法——说不出是哪条证据，就写"证据不足"。
```

## 风格依据

- hello-agents Extra08：假设模型已聪明，只写模型不知道的（六要素格式是新增约束，
  值得写）；示例优于解释（好例+坏例成对）；"不做什么"比"做什么"精确（禁黑话是
  可检测边界，不是"专业得体"这种模糊形容词）。
- 吴恩达两原则：明确具体（把握三选一枚举+固定短语格式）；给模型思考时间（v8 的
  交叉印证步骤保留不动）。
- 最小 token：v9 = v8 + 约三十行，不重写已验证的十段结构——v8 的协议段、收敛标准、
  诚实纪律经批实测（aa7f25b4 修复后 F1=0.9655），动得多不如动得准。
