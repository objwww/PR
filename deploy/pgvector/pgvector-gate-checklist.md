# pgvector 可行性门检查清单（M5-21）

> 红线（AM5 技术方案 §1/INV-AM5-9 原文）：**pgvector 默认不启用——M5-21 是可行性门
> 不是建设任务**。仅当 ①FTS 实验被证实不足 且 ②向量检索 A/B 有价值 两前提成立，
> 且本清单四门数据齐全经评审接受，方可进入启用评审；任何一门缺数据 = 未过门 = 不启用。

## 门前置（两前提，缺一不开门）

| # | 前提 | 判据 | 数据槽位 | 状态 |
|---|------|------|----------|------|
| P1 | FTS 不足 | zhparser spike 结论 + FTS on 态 recall/命中率实测不足以支撑假设召回 | [195] E2E-AM5-10 采集段 | 未测（FTS 默认关闭，spike 未做——残余风险②） |
| P2 | A/B 有价值 | 向量通道相对 FTS 的增量召回对 RCA 假设质量有可度量增益 | [195] 同上 | 未测 |

## 四门（方案原文"recall/内存/延迟/备份数据齐全才接受"）

| # | 门 | 指标定义 | 采集法 | 判定线 | 数据槽位 | 状态 |
|---|-----|----------|--------|--------|----------|------|
| G1 | recall | recall@k（对金标查询集的历史假设命中） | `pgvector-metrics-collect.sh` phase2（需金标查询-假设对，源自 M5-04 金标场景） | **195 实测后评审冻结**（E-17 未核实项，不预造阈值） | [195] | 未测 |
| G2 | 内存 | HNSW 索引体积 + 建索引/查询期后端 RSS 增量，对照 2C4G 与 195 内存预算表 | `pgvector-metrics-collect.sh` phase3 | 同上（195 实测定门——残余风险③） | [195] | 未测 |
| G3 | 延迟 | p50/p95/p99 查询延迟（单查与并发两 profile） | `pgvector-metrics-collect.sh` phase4 | 同上 | [195] | 未测 |
| G4 | 备份 | dump/restore 往返后行数+抽样距离对拍恒等；HNSW 索引 restore 后重建可用 | `pgvector-metrics-collect.sh` phase5 | 往返零丢失 + 重建成功（零容忍） | [195] | 未测 |

## 执行纪律

1. 本清单与采集脚本随 M5-21 入库即视为**门保持关闭**；E2E-AM5-10 的 pgvector 腿
   只做"关闭面+指标采集"断言，不做启用断言。
2. 四门槽位数据在 195 释放后的部署段统一回填；回填后本清单随 PROGRESS 归档为
   可行性报告附件。
3. 判定线冻结属评审事件（同 ConfigBundle 版本纪律），单方不得在脚本/清单内私定。
