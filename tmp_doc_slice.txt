9: 但以下链条仍不完整：**发现异常 → 合法裁决 → 持久化终止 → 停止新动作 → 尽力停止在飞调用 → 拒绝迟到提交 → 回收资源 → 可验证地展示收敛结果**。首批扫描饥饿、锁序倒置、取消应用非原子、工具缺少统一取消信号，是本轮应优先处理的内容。
34: “取消成功”必须拆成可检验的事实，否则前端显示成功，后台仍然烧 token，双方都以为对方出错。
44: 建议 API 保持既有 `command.state=APPLIED` 兼容性，附加读取字段 `terminationRequestedAt`、`localExecutionState`、`inflightCount`、`unknownActionCount`。这些是建议新增字段，不是当前 API 已有能力。页面显示“已取消；本地还有 1 个调用停止中”，而
83: 进程内通知不覆盖其他进程，因此其他实例仍通过同一持久事实收敛。这对当前单机展示项目已足够；引入 Redis Pub/Sub 仍会需要丢消息恢复，RabbitMQ 仍不能替代数据库终止事实和提交围栏，暂不增加部署负担。
119: 固定 N 条、每批 B 条、间隔 P 时，覆盖周期近似 `ceil(N/B) × P + 各批处理时间`。比如 N=60、B=50、P=30s，正常连续运行约两批覆盖；这只是配置推导，不是延迟实测承诺。指标必须展示最长未扫描时长，按目标覆盖时间调整批量和间隔。
127: | READY/RETRY_WAIT，且到期时间合理 | 等待正常调度；展示等待原因和下一次运行时间 |
160: | WC-5 | Run 读接口及 RunDetail 页面、指标接线、测试目录 | 展示停止进度与原因，完成下表 E2E；最后才灰度提升模式。 |
198: | WC-T29 | E2E | 控制扫描循环持续失败但 HTTP 健康 | 看门狗健康指标与告警触发，页面不只显示“服务正常”。 |
211: 完成标准不是“看门狗类存在”，而是：后续 Run 必被检查、终止原子可重放、新动作被拦截、在飞工作有停止/UNKNOWN 结果、迟到提交无效、资源最终对账、前端显示真实收敛状态。上述证据未齐前，应标为“具备恢复与取消基础能力，完整闭环待验收”。