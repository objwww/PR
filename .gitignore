# 密钥与本地配置
.env
*.pem
*private-key*

# 构建产物
target/
build/
node_modules/

# 备份
backups/
# probe-sync 常驻守护运行时状态（TB-21，195 栈级运行态，不入库）
deploy/probe-sync-state/
# 本地工作脚本与运行日志（G0 期工具；含密钥恢复脚本，密钥纪律：不入 git）
var/
# 会话与后台任务工作区
.zcode/
.scheduler-research/
