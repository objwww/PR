# BUGLOG 逐项索引（2026-09-13）

原始状态快照，不代表本轮逐项复验通过。行号用于区分重复编号；完整描述与证据仍以 BUGLOG 原行为准。

记录 141 条；唯一编号 140 个。重复编号：BA-22

| 编号 | 原行号 | 原状态 | 问题摘要（截取） |
|---|---|---|---|
| BA-01 | 10 | 已关闭 | 195 从 ghcr.io 直拉 OTel Demo 镜像停滞 |
| BA-02 | 11 | 已关闭 | HolmesGPT 调百炼 404 |
| BA-03 | 12 | 已关闭 | publisher-app 起不来：挂载源 github-app-key.pem 缺失 |
| BA-04 | 13 | 已关闭 | T04 gzip 炸弹用例跑出 202 而非 413 |
| BA-05 | 14 | 已关闭 | critical 任务的 SLA deadline 落 PG 会变成史前时刻 |
| BA-06 | 15 | 已关闭 | 投影器首见路径两处逻辑错：receivedCount 双计（期望 1 实得 2）+ 首见 firing 误判为 RERUN |
| BA-07 | 16 | 已关闭 | 重写 AlertFlowConfig 后编译报"找不到符号 AlertIntakeLimits" |
| BA-08 | 17 | 已关闭 | RcaWorkerTest 全文件编译报"非法字符 \ufeff" |
| BA-09 | 18 | 已关闭（G0-02/08） | AM1 修复回流后主会话复核：多处未收口 |
| BA-11 | 19 | 已关闭（G0-05~07） | 代码盘点纠正两处前判（2026-09-04，agent-64 盘点） |
| BA-12 | 20 | 已关闭（G0-07/09） | 代码审计新发现（2026-09-04，agent-67）：episode 乱序漏洞 + 三项评审已采纳项缺口 |
| BA-13 | 21 | 已关闭（G0-12） | 代码审计 P2 优化清单（2026-09-04，agent-67） |
| BA-10 | 22 | 已关闭（G0-03/04） | 评审对照代码发现两处新冲突（2026-09-04 二轮评审） |
| BA-14 | 23 | 已关闭（G0-10） | E2E 首跑 RCA 全军覆没：attempt 全部 REJECTED_MALFORMED、task 三次重试耗尽 DEAD——模型实际输出散文或 ```json 围栏包裹的 JSON，验证链整包 JSON.parse 失败 |
| BA-15 | 24 | 已关闭（G0-10） | 报告根因跑偏：把"kubectl 命令不存在"当成系统故障根因——holmes 给模型配了 bash 工具，模型反射式先跑 kubectl（容器内无 K8s），空转数轮后把工具缺失误报为根因；另查实 holmesgpt 不设 PROMETHEUS_URL 时 prometheus |
| BA-16 | 25 | 已关闭（预防固化） | 195 源码同步时 `rm -rf deploy` 连带删掉 deploy/.env、deploy/alert/.env、alertmanager secrets/webhook-bearer 三类密钥文件（G0 期内发生两次），control-app 重启即缺参起不来；早期 E |
| BA-17 | 26 | 已关闭（记录在案） | 崩溃演练 SIGKILL control-app 后容器未自动重启（RestartCount=0），E2E 断链，需人工 docker start |
| BA-18 | 27 | 已关闭（纪律固化） | 一批远程命令被本地 cmd/ssh 双层劫持产出垃圾：`>` 被当本地重定向（仓库根生出 now()、{console.log('TIMEOUT') 两个垃圾文件）、`$()` 被本地展开、tail/grep 等 Unix 工具本地不存在直接报错 |
| BA-19 | 28 | 已关闭 | flagd 故障开关修复连环翻车：①sed 把 paymentFailure defaultVariant 改 "on" 非法（该 flag 无 on variant，flagd 校验拒绝）；②改回 "off" 的正则误伤 paymentServiceFailure（原本无 va |
| BA-20 | 29 | 开放（观察项，AM2 前置） | 程序化造"成功流量"受阻：缺字段 checkout 请求能打进 gRPC 产生 SLO 烧损（历次 firing 的真实来源），但带完整 PlaceOrderRequest（address/email/creditCard 三件套；creditCardExpiration 是 Y |
| BA-21 | 30 | 开放（架构观察） | E2E 实证：真实 page 与 ticket 两档告警（alertname 同为 checkout、仅 severity 不同）合并进同一 incident；合成演练告警（不同 alertname）独立成单 |
| BA-22 | 31 | 已关闭（M2-25~27） | E2E 首轮指纹对账 FAIL：驱动本地重算/冻结常量=0815624c44921472，Alertmanager 实测=0d7404ae811ae84a |
| BA-23 | 32 | 已关闭（V6） | E2E attempt 2 F1 激活 409：uq_scenario_map_fp 唯一冲突——同指纹（同 alertname 身份）第二次激活被全局唯一索引拒绝，TTL 演练（复用 F1）与 E2E 重跑必踩 |
| BA-24 | 33 | 已关闭（M2-21~23） | F2 恢复收口半途态：RECOVERING 扫描只补会话级 RECOVERED 审计，无逐单配对行；restoreF2 的 CAS 只看 booking_status='CREATED'，不验证支付事实即回跳 ENABLED |
| BA-25 | 34 | 已关闭（M2-21~23） | F3 窗口语义漏洞：ACTIVE F3 期间 UNKNOWN 老化订单被对账器提前收掉（窗口保持 FAIL）；且首轮 SQL 的 NOT EXISTS 只绑定到 OR 第二支 |
| BA-26 | 35 | 已关闭（记录在案） | incident/Holmes 侦听器抖动：watch v1 依赖 incident.current_rca_run_id 轮询 run 状态，run 完成后该列被控制面清除 → 误报"incident 未铸出"反复空转 |
| BA-22 | 36 | 已关闭（B1/cf02851，2026-09-12） | EvidencePackageValidator（AM1）在 alert.domain 内使用 Jackson，违反分层铁律 R3（domain 零框架） |
| BA-27 | 37 | 已关闭 | PostgresClaimStore 真 PG 写入即败：sources/refs 以 varchar 直插 V17 jsonb 列，SQLSTATE 42804 |
| BA-28 | 38 | 已关闭 | PostgresRunBudgetLedger.settleFrom 无条件 `settled_at = now()`，PROVISIONAL 悬挂态违反 V13 ck 约束 |
| BA-29 | 39 | 已关闭 | 限额永远放行（fail-open）：PostgresIncidentBudgetLedger.sumWindow 的 `:window` 参数一名两用——window_kind 等值要 `'24H'`、interval 字面量要 `'24 hours'`，等值恒不匹配 → SUM |
| BA-30 | 40 | 已关闭 | 并发回执必现死循环：DagExecutionService.transition CAS 失败分支原样 return，本地 states 残留 BLOCKED，不动点迭代永远重评同一任务 |
| BA-31 | 41 | 已关闭 | PostgresRunBudgetLedger.findStaleReservations：pgjdbc 不识别 `Instant`（setObject 直接抛） |
| BA-32 | 42 | 已关闭 | reserve 拒绝路径自己炸：PostgresRunBudgetLedger.probe 对无限额行（未 ensureLimit 的 run）`single()` 抛 EmptyResult——本应 fail-closed 给拒绝，结果异常逃逸 |
| BA-33 | 43 | 已关闭 | DAG 任务真 PG 落库必炸：rca_task deadline 的 Instant.MAX（critical 永不到期）写侧编码 `Timestamp(Long.MAX_VALUE)` 被 pgjdbc 原样渲染成 292278994 年字符串，超出 PG timestamp |
| BA-34 | 44 | 已关闭（合集登记） | AM4 195 真跑暴露的 10 项测试层缺陷（测试代码/夹具问题，非生产代码）：① V12StateExpansion seed run 占住 uq_rca_run_active_incident 谓词；② V18TaskEdgeRunScope 同 incident 双活跃  |
| BA-35 | 45 | 已关闭 | 2fca37d 模型切换漏改钉值测试：`EvalRunnerProfileIsolationTest.evalProfileAssemblesFullRunnerStack` 断言 eval 装配默认模型 `deepseek-v3`，而 2fca37d 已把 `EvalRunne |
| BA-36 | 46 | 已关闭（同会话补交 1adb940） | a2657b8（M5-08）stage 清单遗漏主类：commit 含 EvalGateRunnerTest.java（200 行）但主类 EvalGateRunner.java 未跟踪——HEAD 单独 checkout 后 EvalGateRunnerTest 引用缺类不可编 |
| BA-37 | 47 | 已关闭 | glm-5 系统性仿告警标签造含空格 artifact_ref：holmes run 收敛失败 REJECTED_SCHEMA_MISMATCH——`prometheus://alert: ArenaDuplicateOrders`（ref 冒号后带空格）被 SAFE_REF ` |
| BA-38 | 48 | 已关闭 | runall 批中容器到期自灭：arena-e2e-cli 是 `sleep 14400` 一次性容器（RestartPolicy=no），4h 寿命到期 exit=0 自灭，恰砸中场景 04 phase1 注入 exec（00:56 启动→04:56:47 到期）；common |
| BA-39 | 49 | 已关闭（部署段修复 9ebe952） | M5-10 Canary 审计仓储无 @Bean 装配：`PostgresCanaryDecisionLogRepository` 无组件注解且 main 树中无任何 @Bean 产出 `CanaryDecisionLogRepository`（唯一 new 点在 `Postgr |
| BA-40 | 50 | 已关闭（部署段就地修复 14edd86①） | V25 重建活跃 Run 唯一索引时漏掉 `REPORTING`：V12 明确把 `REPORTING` 纳入 `uq_rca_run_active_incident` 活跃谓词；V25 为 Canary 升级到 `(incident_id,engine)` 后仅保留 `QUEU |
| BA-41 | 51 | 已关闭 | 孤儿测试替身致 HEAD 编译破面：e09aeab（M5-13 C-18）给 `RcaRunRepository` 接口加 `currentRevision(UUID)` 而漏提交两处测试替身（RunQueryServiceTest.FakeRuns、EventQueryCont |
| BA-42 | 52 | 已关闭 | V28 月分区换身撞 42P07：`relation pk_rca_event already exists`（V28 line 30，22 errors 连坐全 IT 面）——PK/UNIQUE 的约束索引名是 schema 全局关系名，旧表 rca_event 同名索引在场时 |
| BA-43 | 53 | 已关闭 | IT 基座两缺陷连坐 195 首跑：①PostgresITBase.ALL_TABLES 清场清单冻结在 AM2 时代漏 AM5 表——跨类污染在 195 类序（filesystem 序）下以 golden_candidate FK 违约爆 5 error（NTFS 类序掩盖，平 |
| BA-44 | 54 | 已关闭 | canary 序列授权缺 + DETACH 撞 owner 权限墙 + IT 两洞（6150a97 四缺陷）：①canary_route_decision bigserial 的 append 需序列 USAGE，V25 表授权不覆盖序列面（permission denied f |
| BA-45 | 55 | 已关闭 | advanceState 单向栅栏仓库面缺失：裸 `WHERE state=:from` 放行 ARCHIVED→EXPORTED 倒退（itA01 恰一次栅栏断言实证）——archive_manifest 状态机可被倒转，重导出/重挂载破坏恰一次语义 |
| BA-46 | 56 | 已关闭 | rca_event 幂等面并发竞态：V28 唯一约束按 E-17 并入分区键 created_at 后，同键并发双写的 DB 兜底消失（created_at 各异即不撞 UNIQUE）——itC6 八线程并发实证 3 行同键事件；重放查询 optional() 撞 Incorre |
| BA-47 | 57 | 已关闭 | Operator Case 并发认领的败者语义错面：applyCommand 状态机判定先于修订 CAS——并发败者在胜者提交后才读（期望修订过期+状态已 ACKED）时抛 IllegalCaseAction 而非败者语义 CaseRevisionConflict；Postgre |
| BA-48 | 58 | 已关闭 | provider 指纹部署面缺口：holmesInvestigationExecutor 读 `${app.alert.eval.provider-fingerprint}` 无默认值（INV-AM5-3 fail-closed posture 正确：部署特定身份必须真值），但  |
| BA-49 | 59 | 已关闭 | AM5 E2E 线束与 195 真栈拓扑三处失配：①宿主无 psql 客户端且 deploy postgres 无宿主发布面（INV-AM0-1 loopback-only）——am5_psql_ro 无执行面；②litellm 实际发布 4100（预检默认 4000）且 /he |
| BA-50 | 60 | 已关闭 | preflight 子进程调用丢失函数面：preflight.sh 调 am5_psql_ro/am5_redact 但从不 source e2e-am5-common.sh，而 runall 以子进程 `sh` 调用 preflight——函数不跨进程边界，"迁移最大号读取"探 |
| BA-51 | 61 | 已关闭 | git archive 行尾转换击穿 Gatus 契约门：Windows 侧 core.autocrlf=true 下 `git archive` 把文本条目转 CRLF 落 Linux 树；GatusProbeConfigContractTest 逐行解析 `line.matc |
| BA-52 | 62 | 已关闭 | IT 夹具落后 C-70 类型化领取面：claimNext 只认 driver 键后，AlertGenerationFenceIT 任意 key（OLD_TASK/NEW_WORK）真 PG 领不到→claimNext 空 2 红；Am4E2E06FaultDrillIT 以 c |
| BA-53 | 63 | 已关闭 | canary 决策审计 FK 即时检查炸新 run 铸造：canary_route_decision.run_id 外键 NOT DEFERRABLE，而生产两铸造点（IncidentProjector/RcaRunOrchestrator castRunAndTask）顺序=r |
| BA-54 | 64 | 开放 | HOST1 三档内存闸自动触发面未落码（架构 v1.2 :946-953）：MemAvailable 判定器（<1.2GiB 持续 5min / <768MiB 持续 2min / <512MiB + 滞回 1536MiB×10min）、RESOURCE_MODE_CHANGED |
| BA-55 | 65 | 已关闭 | fallback_of 事件载荷非 JSON 形：FallbackService.eventPayload 用 Java Map.toString()（`{key=value}` 形）落 rca_event.payload（json 列）——195 真 PG INSERT 报 ` |
| BA-56 | 66 | 已关闭 | M6-05 部署 195 启动即崩：holmesShadowWorker 缺 EngineComparisonRecorder bean——control-app APPLICATION FAILED TO START（UnsatisfiedDependency 参数 7），健康 |
| BA-57 | 67 | 已关闭 | V34 定向真 PG IT 三轮递进：R1 6/6 error——五案 holmes_shadow_work INSERT 撞 native_run_id FK（IT 用 UUID.randomUUID() 引用不存在 rca_run 行）+ worker 链 UPDATE rc |
| BA-58 | 68 | 已关闭 | PostgresRcaTaskRepositoryIT 匿名 bundle 仓库 findByDigest 回 empty：195 全量 verify（133 IT 零跳过真跑）staleFiringAfterResolvedKeepsIncidentResolvedOnReal |
| BA-59 | 69 | 已关闭 | M6-07 部署段启动即崩：B25 运行时门 Holmes 凭证检查残留——SelfCheckConfig 读 `${app.alert.holmes.enabled:true}` 默认真，AlertSelfCheck 以 holmesEnabled 强制 HOLMES_API_ |
| BA-60 | 70 | 已关闭 | M6-07 C-77 零铸造的幽灵 runId 提交点 23503：两铸造点（IncidentProjector/RcaRunOrchestrator）先预生成 runId 传入 route() 再守卫早退不铸 run，决策行却照抄该 id——V31 DEFERRABLE INI |
| BA-61 | 71 | 已关闭 | prometheus-am0 单文件 bind mount 钉住旧 inode，配置热更新假象：MIG-01 收尾给 prometheus.yml 追加 node-exporter-host2 job 后，容器内 promtool 校验"通过"、但容器看到的文件 md5 ≠ 宿主 |
| BA-62 | 72 | 已关闭 | mount 审计工具链两级假绿/假红：①`docker cp` 对 ro 单文件 bind mount 返回的是宿主机源文件内容而非容器视图（litellm config.yaml 容器内 md5=fde6… mtime 9-08，docker cp 出来却=宿主新版 fb31… |
| BA-63 | 73 | 已关闭 | litellm-am3 运行配置语义漂移：服务名 qwen3.7-plus（旧）vs 宿主配置 glm-5（新）：宿主 config.yaml 于 2026-09-09 08:46 CST 被同步配方刷新（LF 行尾 + model_name 更名 glm-5 + TODO 注释 |
| BA-64 | 74 | 已关闭 | alertmanager-am0→control-app 通知链全断：credentials_file /etc/alertmanager/secrets/webhook-bearer 不存在→（补文件后）permission denied：receiver=zhongtai w |
| BA-65 | 75 | 已关闭 | EX-C4 演练 sink 绑宿主 loopback 容器不可达：hook 首射 rl-bot/echo-bot 双双 `DUTY_CHANNEL_FAILED io:ConnectException` → dead-bot `env_missing` → 503 链尽（降级与台 |
| BA-66 | 76 | 已关闭 | Am4ShadowTrigger 快照身份混用：F05 fail-closed 上线后 Am4ShadowTriggerTest 5 案全 ERROR「快照行缺失（黑板契约 fail-closed）」 |
| BA-67 | 77 | 已关闭 | EX-A4a 新案 `unknownSegmentFailureLedgersUnknownAndRethrows` 红：「Expecting code to raise a throwable」——executor 抛 IllegalStateException 后 agent |
| BA-68 | 78 | 已关闭 | 195 真 PG IT `f16_ledgerAnchorsActionSeqAttemptAndReclaimsStalePending` ERROR：`fk_rca_tool_invocation_run` FK 拒绝（Key (run_id)=… is not presen |
| BA-69 | 79 | 已关闭 | EX-A3 四边界案首跑双红：b2 `expected: UNKNOWN but was: PENDING`；b1 `expected: 4L but was: 5L` |
| BA-70 | 80 | 已关闭 | 195 run1 定向 IT：ExA3DriverRecoveryIT 2/5 红——fullChainOnce 断言三任务 DONE 实际全 DEAD |
| BA-71 | 81 | 已关闭 | 195 官方 verify run1：ExA2LeaseCancelFenceIT.cancelVsFinishRaceIsConsistent 红——`竞态双方零异常`，实为 DataIntegrityViolation `value too long for type cha |
| BA-72 | 82 | 已关闭 | ExB1ChangeEventIT 编译错两处（找不到符号 List） |
| BA-73 | 83 | 已关闭 | ChangeQueryExecutor 两案 ERROR「No ObjectCodec defined」+ 字节超限案断言异常类型不符（FAILURE） |
| BA-74 | 84 | 已关闭 | 空 allowlist 装配返回 NPE 而非契约的 IllegalArgumentException |
| BA-75 | 85 | 已关闭 | deploy_app（insert-only）的 `insert ... on conflict (source, deploy_id) do nothing` 报 42501 permission denied |
| BA-76 | 86 | 已关闭 | ExB1ChangeEventIT 两案断言错位 |
| BA-77 | 87 | 已关闭 | 195 targeted run2 两案红：①`expected: null but was: "null"`；②批量截断案被 INVALID_ARGS 正确拒绝 |
| BA-78 | 88 | 已关闭 | 落档谎报成功：195 drill1 后 change_event 0 行 vs 日志印 `CHANGE_EVENT_RECORDED` |
| BA-79 | 89 | 已关闭 | drill2 两次落档（trap FAILED+显式 SUCCEEDED）双双 42601 `syntax error at ":"` |
| BA-80 | 90 | 已关闭 | drill3 `INSERT..RETURNING` 被拒 `permission denied for table change_event` |
| BA-81 | 91 | 已关闭 | 同 deploy_id 幂等重放也印 RECORDED（假阳性；锚本身零新增由 count 1→1 实证） |
| BA-82 | 92 | 已关闭 | LogQueryExecutor 参数形状违约裸 `DateTimeParseException` 外漏（期望 ToolControlPlaneException 实收 DateTimeParseException） |
| BA-83 | 93 | 已关闭 | ExB2LogQueryIT 容器启动即死——Testcontainers `Wait.forHttp("/ready")` 60s 连接拒绝超时 |
| BA-84 | 94 | 已关闭 | ExB2LogQueryIT 4 案中 2 案在 push 断言即红（0.014s/0.171s 秒红，查询面根本未走到） |
| BA-85 | 95 | 已关闭 | otelcol-control 重载后 crash-loop：`unknown type: "docker_logs"`；restart loop 期间 control-app→上游遥测三支柱全断（INV-AM5-8 红线受压） |
| BA-86 | 96 | 已关闭 | 红驱扫描永不可用：195 drill6 部署段 control-app 红驱循环每 30s ERROR「等待重驱循环异常，PT30S 后重试」（循环捕获重试的韧性面按设计存活） |
| BA-87 | 97 | 已关闭 | compose 对 .env 插值吃 bcrypt 裸 $ → 登录恒 401：drill8 首轮真机登录正确口令也 401 login_failed；compose recreate warning「The "omw7LT9Iu2gHeG" variable is not se |
| BA-88 | 98 | 已关闭 | C2a 官方全量 `mvn verify`（195）红——order-arena ComposeTopologyContractTest 2/10 失败（`service(alertCompose,"holmesgpt")` containsKey 断言失败） |
| BA-89 | 99 | 已关闭 | 部署包装脚本错名 → 标准管道旧源球回滚 195 工作树：exc2a-deploy.sh `cp m6-ev-exc2a-src.tar.gz` 失败（真名无前缀）→ set -u 下继续跑 deploy-am4.sh → 拿 C3a 时代旧源球 stage → `rsync - |
| BA-90 | 100 | 已关闭 | drill2 leg A 行到达 echo（journal Δ>0）却 DEAD `unparseable_ack_body/outcome_unknown`；手工 probe 同一 stub 却拿到 `{"errcode":0,"errmsg":"ok"}` |
| BA-91 | 101 | 已关闭 | 执行器时钟冻结（pre-existing，M3 起撒谎）：drill3 leg A PASS 但 `sent_at=04:25:18` 早于 drill 开始整 29 分钟=容器重启时刻；leg B RETRY_WAIT 一闪即过，attempt 5/5 秒级烧完 → DEAD  |
| BA-92 | 102 | 已关闭 | M7-13 编辑滑手合集（编译期拦）：ControlAlertRouter.Decision 误删 public 修饰符、List import 缺失、DutyTestSupport 子类域 DispatchOutcome 未限定名 |
| BA-93 | 103 | 已关闭 | PostgresDutyStore 本地快照编译错 |
| BA-94 | 104 | 已关闭 | DutyFallbackWatcherTest 空层快照走 fallback-only 解析 → 降级断言 0≠1 |
| BA-95 | 105 | 已关闭 | M7-14 4 处 `throw new StaleClaimException("..."+id)` 编译红 |
| BA-96 | 106 | 已关闭 | M7-14 NotifyConfig 出现第二组 NotifyOutboxStore/FencedNotifyExecutor/Claimer bean——原 claimer 注入点未打 @Qualifier 启动歧义 |
| BA-97 | 107 | 已关闭 | DutyChannelRouter 用 computeIfAbsent 装渠道行，null 加载结果 NPE |
| BA-98 | 108 | 已关闭 | markRead 幂等分支控制器判 404 而非幂等 200 |
| BA-99 | 109 | 已关闭 | DutyAdminControllerTest 预检拦：jsonPath doesNotExist 对 null 值字段误判 + insertOverride 未打桩将致 Map.of NPE |
| BA-100 | 110 | 已关闭 | M7-17 编译期拦：CSRF 豁免配置不编译 |
| BA-101 | 111 | 已关闭 | M7-17 编译期批四件：GatusPayload.java 声明 public record GatusEvent（public 类型须同名文件）；DutyWebhookClient 旧 static env() 与新 protected env() 重载撞名；Snapshot |
| BA-102 | 112 | 已关闭 | WriteBackClient.countLines 文件句柄泄漏锁 spool 文件：Windows 实测 AccessDeniedException 拦后续 rewrite（Linux 无锁语义故线上不可见，跨平台潜伏雷）——产线真 bug 测试拦 |
| BA-103 | 113 | 已关闭 | docker 装配 bean 歧义连环三轮：195 真启动三轮 `APPLICATION FAILED TO START`——①control DutyAdminController 注入点见 2×DutyAdminStore（DutyFlowConfig 双 PostgresD |
| BA-104 | 114 | 已关闭 | 部署脚本静默吞错：M7-18 部署后全部反代探针 000——整目录工件（含 nginx-narrow-195.conf）没落 195，drill cp 静默败+`set -u`（无 -e）照样 reload 旧配置 |
| BA-105 | 115 | 已关闭 | M7-18 195 真 PG 首跑 DutyDeliveryClaimIT 红（2 案）——IT 自身三缺陷，适配器无罪：①断言比 jsonb 无空格字面量（PG `::text` 渲染 `"type": "duty"` 带空格）；②单次领取内双 markRetryWait 违  |
| BA-106 | 116 | 已关闭 | 演练种子 SQL 幻影列致两轮空跑：drill 两轮在 duty 空表上空跑（快照 onCall=null/派发 NO_CHANNEL 全由此） |
| BA-107 | 117 | 已关闭 | JdbcClient `.query(mapper)` 漏 `.list()` 死语句——SQL 根本不执行：快照 onCall 恒 null → viaFallback → channels:[] → 派发恒 `DUTY_DISPATCH_NO_CHANNEL`；同库同刻 ps |
| BA-108 | 118 | 已修复（B2/40407e2 假件语义分叉清账，2026-09-12；195 真窗复核留主模式真跑窗） | 主模式 TOKEN 限额从未落账 → 真 PG 全量 BudgetExhaustedException（余额=0）：195 真窗 A0 首跑 run 终态 FAILED，control-app 日志逐分钟 `预算耗尽: TOKEN 已扣=0 余额=0 本次请求=1500`；run |
| BA-109 | 119 | 修复待真窗复核 | RCA→平台账本 23503 FK 违反（R7 方案预登记风险面首跑实证）：A0 二跑 rca_model_call 全 UNKNOWN(TRANSPORT_UNKNOWN) + 日志「账本 STARTED 写失败，零触网（D5）: DataIntegrityViolationE |
| BA-110 | 120 | 修复待真窗复核 | 真模型全程不可解析：缺省 prompt 零输出协议描述 → glm-5 自然语言作答 8 步 JsonParseException 耗尽 → 确定性兜底 FINAL（空 claims）：A0 四跑 phase1-4 PASS（真模型 SUCCESS×6/PENDING×1、run |
| BA-111 | 121 | 已关闭 | A0 脚本五跑 phase2 超时：成功后同输入再注入被产品正确去重——run4 SUCCEEDED 后 incident 仍 FIRING 且 last_investigation_hash=输入哈希，第 5 次同 summary 注入 received_count+1 但零路 |
| BA-112 | 122 | 修复待真窗复核 | glm-5 首发 TOOL_CALL 参数形状不合法 → 工具控制面 INVALID_ARGS → 主任务直接 DEAD（run 却 SUCCEEDED+空 claims）：A0 六跑 phase1-3 PASS（真模型按协议输出 tool_call 了！）但 PRIMARY_I |
| BA-113 | 123 | 修复待真窗复核 | BA-112 修复②的 reason 串撞 executor 可重试封闭集 → INVALID_ARGS 计步重驱后仍 DEAD：A0 七跑 phase4 FAIL（run=b126fecc），日志先打「TOOL_CALL 参数形状拒绝（INVALID_ARGS），计步重驱」（r |
| BA-114 | 124 | 已修复（用户裁定选B：f2b1f6b 领取即迁移；195 真 PG 新契约 DrillIT 9/9 绿 2026-09-12） | drill_job claimNext 领取不推进状态 → 同作业可被顺序重复领取（真 PG 探针实证双领）：EVAL 对照组 claimNextLaunch 在 CLAIM_SQL 单语句内原子 SET state='CLAIMED'（安全），而 drill 的 CLAIM_S |
| BA-115 | 125 | 已关闭（R4 定价显式语义落地=候选B：UNPRICED 账面+phase6 断言翻面；195 A0 phase6 绿+价目注入实测 5 账行结清 2026-09-12） | rca_model_call 成本三列全空 → A0 phase6 结清断言必红：A0 八跑（run=87572b9d）8 笔模型调用全 SUCCESS、usage jsonb 在场、usage_missing=false、settled_at/invocation_id 在场， |
| BA-116 | 126 | 已定位待裁定（产品无罪） | A0 套件同 episode 复燃 → 发布一次语义 → phase7 对第 2+ 个 SUCCEEDED run 恒红：incident E2EA0CheckoutProbe 自 04:23 起 8 次注入全 firing 无 resolved（generation 恒 0 单 |
| BA-117 | 127 | 已修复（195 真机实证@r7fix7 窗 1f873c3：RX02/RD09 五 attempt SUITE PASS，子任务真执行 DONE）。主会话裁定修法A：装配面 epoch→ISO-8601 单点转换，工具边界保持单一 ISO 契约；AlertFlowConfigTest 2 例+全量 1342 绿） | 委派专家子任务（logs/change 角色）确定性秒死：兼容单工具 handler 以纪元秒串喂 ISO-8601 契约 → 触网前 INVALID_ARGS → 子任务零重驱直接 DEAD：R7 重启套件 attempt-3（qwen）/attempt-4（deepseek） |
| BA-118 | 128 | 已修复（195 真机实证@r7fix7 窗 1f873c3：SIGKILL 打进委派等待窗恢复无假收尾）。主会话裁定：triage 按 runtimeKind 分流——BOUNDED_LLM 主任务跳过阶段③幂等收尾与 FAILED 孤儿 DEAD，检查点相位续驱；R7PrimaryModeExecutorTest 新增恢复案+存量恢复 8 例零回归） | 恢复三相位 triage 对多步 LLM 主任务误收尾：阶段③ result_ref 幂等收尾把 RUNNING→DONE 零触网结算，绕过 drive 环与 FINAL 检查点：attempt-4 SIGKILL（打在 WAITING_CHILDREN+委派在场）后重启，主任务 |
| BA-119 | 129 | 已修复（195 真机实证@r7fix7 窗 1f873c3：单拒反馈即收敛不再盲重提）。主会话裁定候选A：DELEGATE 全拒写 lastError 回喂，含拒绝码+「勿重提同 gap/配额耗尽→直查或 final」指引；R7RoleRunnerTest 新增全拒案） | 委派台账拒绝不回喂 → 主任务盲重提同 gap 空转至驱动上限（双模型复现）：attempt-3 qwen「委派裁决撞既成台账（gap=g1），以先到者为准」×15 →「驱动迭代上限（22，DELEGATE 空转防御）→ DEAD」；attempt-4 deepseek 同面 × |
| BA-120 | 130 | 修复已落码待真机复核（按用户裁定并入 R5：空 content+推理痕迹/finish=length→OutputBudgetExhausted 同签名×2 熔断，L0 绿）；195 无 deepseek 钥匙=staging 窗 BLOCKED（2026-09-12 登记，需钥匙或裁定） | deepseek-v4-flash-0731 推理模式烧穿步预算 → 零 content → PROTOCOL_ERROR 终态失败风暴：r7fix7 后重启套件首跑（run=7dc6c46d）前期模型调用全部正常（13:58:10 委派决策、13:58:42 重提、logs.q |
| BA-121 | 131 | 已修复（195 真 PG 部署面红→绿实证） | V87__ev08_eval_review.sql 三处 `COMMENT ON ... IS '...' \ |
| BA-122 | 132 | 已修复（V89 迁移落库 + 195 真 PG IT 红绿实证） | release_asset.asset_kind 词表缺 runbook 语料两 kind（RUNBOOK_DOC/RUNBOOK_CATALOG）——EN-07 IT 种子直撞 ck_release_asset_kind（En07RagSourcesIT 3 红同根）：EN-0 |
| BA-123 | 133 | 已修复（源面+IT 面红绿实证；195 运行时 jar 未含修复——生产面休眠无暴露，jar 重部署随下一部署窗） | PostgresDrillJobRepository CLAIM_SQL 的 `RETURNING` 尾空格写在 text block 行尾被编译期剥离 → 拼成 `RETURNINGid`（42703 column does not exist）→ drill 领取 CAS 全 |
| BA-124 | 134 | 已修复（195 真 PG IT 红绿实证，批 21/21 终绿） | ConfigBundle 4 参 activateQualified 激活事务缺资格行 FOR UPDATE 锁——EN-02 P07 接口契约「先锁资格行再 CAS」只落了 5 参事实路径，4 参路径裸奔：195 真 PG IT 单批 21 例首轮即红 PostgresRele |
| BA-125 | 135 | 已修复（195 真 PG IT 7/7+真机 API 面 200 实证） | R6 usage 读面第二处 jsonb 裸列（listUsageCalls/listUsageCallsForRuns）→ 真 PG BadSqlGrammar → /api/eval/runs 500 经 /error 分派撞 anyRequest().denyAll() 被 |
| BA-126 | 136 | 已关闭（V108+RunReconciler 上线 195 ALERT_ONLY；UT 21 + 真 PG IT 4/4 + 全量 1874 绿） | 活跃Run可能缺少可恢复driver而长期停在REPORTING，未见独立Run级对账入口 |
| BA-127 | 137 | 已关闭（影子收口全链落码 + 195 部署 + SR01 UT 7 例/SR02 真 PG IT） | Am4ShadowTrigger刻意留REPORTING，但isActive与V25同incident同engine唯一索引仍将其视为活跃 |
| BA-128 | 138 | 已修复（195 真机红→绿 + RegressionCaseControllerTest 回归锁） | OP 批 body 缺省字段 NPE 被安全链伪装成 401：POST /api/eval/regression-candidates 缺 reportId → 500 NPE 经 /error 分派撞 denyAll → 客户端见 401 {"error":"unauthori |
| BA-129 | 139 | 已修复（V106 迁移 + PostgresOpBatchIT 授权锁 + 195 真机红→绿） | materialize 以 control_app 写 dataset_version 被 PG 拒（permission denied for table dataset_version）→ 又经 /error 伪装 401：OP-01 入集链真机第二轮卡死 |
| BA-130 | 140 | 已修复（V107 迁移 + IT 同锁 + 195 真机红→绿） | BA-129 修复后第二层雷：case_version 有 V21 分区 RLS 矩阵，control_app 只有非 HOLDOUT 读策略无 INSERT 策略 → insert 违 RLS（new row violates row-level security policy |
| BA-131 | 141 | 已修复（真 PG IT 红→绿 4/4 + 195 jar 重部署） | PostgresCompactionAttempt.casState 无条件 `settled_at = now()` → RESERVED→IN_FLIGHT 迁移在真 PG 违 ck_rca_compaction_attempt_settle（非终态必带空 settled_a |
| BA-132 | 142 | 已修复（IT 期待面修正，非产品缺陷） | OP/CL 批四 IT 首跑 3 处测试面错误：①PostgresOpBatchIT row() 夹具每次随机新 reportId，按报告回读断言 hasSize(2) 恒 1；②PostgresRunSkillBindingIT 并发用例断言"racer-0 的 digest  |
| BA-133 | 143 | 已定位待修（195 真机定谳；run26 收官窗） | logs.aggregate 映射自 EN-05 log_error_aggregate 但 Loki 查询无 error 过滤——数的是窗内全量日志行（任意级别），且空 vector 抛 NO_DATA 而非 count=0：历 run 的「聚合计数 72/100」实为 che |
| BA-134 | 144 | 已定位待修（run26 收官窗；e2e 脚本资产雷非产品雷） | e2e-r7-a0-provider-receipt-chain.sh phase7 两雷：①探针查 rca_report 旧列名 package（现行 schema 为 package_json）→ psql ERROR column "package" does not ex |
| BA-135 | 145 | 已立案待修（run16/17 实证） | run 级工具 allowlist 拒绝走 UNKNOWN_TOOL 终止族——BoundedLlmRoleRunner 计步反馈环被绕过：模型请求 allowlist 外工具时 Gateway 回 UNKNOWN_TOOL（ToolControlPlaneException）→ |
| BA-136 | 146 | 已修复（195 热旋转 ~45条/s→2条/30s 实证） | RunReconciler 巡逻循环仅 decided==0 才 sleep——常驻候选（ALERT_ONLY 下两历史孤立 Run）每轮都被“决策”→ 永不睡 → 热旋转：195 首部署 10min 产出 27282 条 run_reconcile_decision 日志（~4 |
| BA-137 | 147 | 已关闭（现场完全恢复；BA-16 同族再犯，教训升级固化） | 部署脚本 rsync -a --delete 同步源树，删掉 195 机器特定文件与白名单外模块：deploy/.env（46 个运行时变量）、根 .env、4 个 maven 模块（order-arena/arena-chaos-admin/notify-app/duty-ad |
| BA-138 | 148 | 已修复关单（bcda248；RR03 195 真机验收） | audit-runtime.sh docker 面采集失败（零容器）时 container_mem_limits 误判 MATCH（“0 容器全有上限”）——采集失败冒充一致，恰为 RR03 要防的失效模式：对拍器出口 exit 0 会放行一次实际未采集的“一致” |
| BA-139 | 150 | 已修复关单（195 全量套件 run #2 复验绿） | 在途取消注册表 submit↔register 小窗丢取消通知：195 满载全量套件跑 ToolGatewayTest.wc3_t13c「Run 取消的在途通知」偶发红——caller join(5000) 后 thrown 为 null（工具线程仍卡在 release.awai |
| BA-140 | 152 | 已关闭（六项测试缺陷当轮清账，195 定向 IT 27/27） | WC 批四个真 PG IT 首上 195 六项测试自身缺陷连红（本地无 docker 全 NOT_RUN 从未真跑）：t27 终态 fixture 撞 ck_rca_run_finish、t25 ctor 传 null reports NPE、t02 翻页序不稳、t02 回卷差一 |
