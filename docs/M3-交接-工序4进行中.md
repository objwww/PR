# M3 工序 4→5 交接（2026-09-02，主会话交接版）

> 本文档是**会话间工作交接**，不是测试交接文档（《M3-测试交接文档.md》是工序 5 的正式前置，
> 已由子任务起草 v1.0，待按本文 §4 裁定口径定稿）。
> 读者：下一个接手 M3 收官的 work。读完本文即可继续，不需要翻旧会话。

## 0. 一分钟现状

- M2 已收官（G2 过门，commit `8a4f694` 在 main）。
- **M3 代码**（ModelGateway 模型治理 + V5 model_call_ledger）已在本地工作区 + 195 同步，
  195 全量 `mvn verify` 全绿（shared UT 101、control UT 381 + IT 52、publisher UT 143 + IT 71）。
- **M3 工序 4（195 部署验证）已完成**：栈带 V5 起通；混合模式 smoke **213/0 全绿** +
  全 stub 窗口 smoke **235/0 全绿**；栈已切回混合模式。
- **用户已裁定**：M3 测试矩阵 206 条中 101 条无执行体（工序 2/3 编码欠债）——
  处置口径 = **核心集补落码 + 其余豁免**。核心集 = G2 硬门挂载的
  E2E-41/42/45/46/48/49/50/51/54/56/60/61/70（13 条）；BT-M3-01~05 是演示别名不重复落码；
  其余 88 条在方案 §11 修订中逐条记豁免。G2 按修订后矩阵过门。
- M3 全部改动**未 commit**（工序 4 完成 + 用户确认后才准 commit，见 §5）。
- **e2e-m3.sh 核心集已落码**（13 条，793 行，§2.2），待 review + 195 真跑。

## 1. 关键事实（都已实证，直接信任）

### 环境锚点（195 = root@146.56.195.225，仓库 /opt/build/pr，compose 目录 deploy/）

- ssh：`ssh -i ~/.ssh/id_ed25519 root@146.56.195.225`（本机侧执行）。
- `.env` 是符号链接：混合（默认）模式 → `/opt/projects/pr_agent/.env`；
  全 stub 演练窗 → `deploy/.env.allstub.bak-r8`。切换 = `ln -sf <目标> .env` + `docker compose up -d --force-recreate control-app`。
- **演练窗合法旋钮三元组（INC-61 定型，F-22 不等式链）**：
  `APP_WORKER_MAXLEASESECONDS=60` + `APP_MODEL_GATEWAY_TOTALDEADLINEMS=30000` +
  `APP_MODEL_PERCALLTIMEOUTMS=20000`。不等式链：lease > deadline+10s 余量 ∧
  perCall ≤ deadline ∧ recoveryAfter(默认240) ≥ 2×perCall。少一环就被启动校验拒启。
  三环均已 compose 透传（`deploy/docker-compose.yml` control-app 环境块）。
- **M3 起 stub 模型延迟注入必须 < percall=20s**（`m2_model_delay_on` 参数）；
  M2-era 脚本里的 45000/60000/30000 若复用必须改小（本次已改 smoke-test.sh DP-17 为 12000，
  `deploy/e2e-m2.sh`/`bt-m2.sh` 未改——M2 已收官不动它们，若在 M3 栈复跑需先改）。
- `.env*` 600 权限含密钥：**只 grep 键名，永不打印内容**（前执行方误泄过 AGENT_MODEL_API_KEY）。
- 195 构建：`docker run --rm -v /opt/build/pr:/ws -w /ws -v /root/.m2:/root/.m2 [-v /var/run/docker.sock:/var/run/docker.sock] maven:3.9-eclipse-temurin-21 mvn -s maven-settings-aliyun.xml ...`；打 jar 用 `package -Dmaven.test.skip=true`；跑测试用 `verify` 挂 docker.sock；`-pl control-app` 必须带 `-am`。
- Windows 侧文件经 ssh 同步 195 一律 `sed 's/\r$//' file | ssh ... "cat > ..."`，验证 `tr -cd '\r' < f | wc -c` 应为 0（**别用 grep '\r'**，GNU grep 把 \r 当字母 r 误报）。
- 本机（Windows）无 docker，IT/E2E 只能在 195 跑；本机 mvn 要 `export JAVA_HOME='E:\ProgramInstall\jdk21'`。
- 195 备份：`/opt/build/backups/m3-predeploy-20260902-130824/`（preV5 pg_dump + M2-era 源码树）。

### 工序 4 已修缺陷（BUGLOG 四条均已登记）

| 编号 | 内容 | 状态 |
|---|---|---|
| INC-58 | CT22V4UpgradeIT 硬编码 flyway 版本 4→5（M2-era IT 的 M3 适配遗漏） | 已修复，verify 绿 |
| INC-59 | EX06ModelFailureIT 预算用例改入队 ModelBudgetExceededException | 已修复，verify 绿 |
| INC-60 | `markUnknownOlderThan` 直绑 Instant 被 pgjdbc 拒（07006），Recovery 每分钟静默失败；已改 `Timestamp.from` + 新真 PG 回归 `PostgresModelCallLedgerRepositoryTest` + `PostgresITBase` 清场清单补 V5 表（16 张） | 已修复，195 实证两周期零失败 |
| INC-61 | M3 旋钮 compose 未透传 + 三环不等式只配平一环；compose 补两行透传 + 三元组定型 + DP-28 重写为模式感知 + DP-21 lease 负例自带非法组合 + DP-17 延迟 45s→12s | 已修复，stub R2 验证中 |

## 2. 正在跑的任务与验收方法

### 2.1 全 stub 窗口 smoke（工序 4 已完成 ✅）

**两窗均已全绿**：混合 213/0（smoke-evidence/20260902-141159）+ 全 stub 235/0
（smoke-evidence/20260902-151633）。195 栈已切回混合模式（.env → /opt/projects/pr_agent/.env），
四容器健康，control 自检通过。本节目验收方法仅留档备查：

```bash
# （已完成，勿重跑；如需复查日志）
ssh -i ~/.ssh/id_ed25519 root@146.56.195.225 "grep -E '\[FAIL\]|结果：' /tmp/smoke-stub-r2.log"
```

### 2.2 e2e-m3.sh 核心集（已落码 ✅，待 review + 195 真跑）

- 产出已落盘：`deploy/e2e-m3.sh`（793 行，13 条核心集，bash -n 过、LF 干净）、
  `deploy/m3-lib.sh`（110→212 行：m3_model_fault_on_route/off_route 路由限定、
  m3_model_fault_once_on/off scenario 首败后成、m3_model_leak_on/off 密钥泄漏注入）、
  `deploy/docker-compose.yml`（fallback 三行透传已加：AGENT_MODEL_FALLBACK /
  OPENAI_COMPAT_BASE_URL_FALLBACK / AGENT_MODEL_API_KEY_FALLBACK，空默认=单路由）。
- 文件头含 G2 映射、路由形态三档（single/dual-inherit/dual-distinct）说明、
  PR 号段表（41000+ 起，与 e2e-m2 20000+、smoke 100~899 错开）、9 条诚实清单。
- **接手者必须先通读 review 再上 195**（G2 硬门挂载面）；作者自报四个 195 首跑
  最可能翻修点：`docker compose config` 渲染、WireMock scenario 字段实测、
  retry_after_ms 毫秒口径、run.state=FAILED 终态语义。
- .env 侧前提（在 195 操作，密钥纪律不变）：
  - dual-distinct 档（E2E-42/51/60）：`.env.allstub` 增补 `AGENT_MODEL_FALLBACK=<非主模型名>`
    + `OPENAI_COMPAT_BASE_URL_FALLBACK=http://github-stub:8080/fallback`
    + `AGENT_MODEL_API_KEY_FALLBACK=`（留空=C-2 继承）+ `docker compose up -d control-app`；
  - dual-inherit 档（E2E-46）：仅设 `AGENT_MODEL_FALLBACK`，另两键摘除重建；
  - 两档互斥，建议各备一份 .env 备份切换；形态不符脚本记 [SKIP] 不伪造。

## 3. 剩余工序（按序）

1. **验收 stub smoke R2**（§2.1）→ 全绿则工序 4 完成。
2. **review + 195 真跑 e2e-m3.sh 核心集**（13 条，逐条过；败了修，大概率有 1~3 条断言要调）。
3. **方案 §11 修订**：其余 88 条无执行体用例逐条记豁免（用户已裁定口径），
   修订记录注明 v1.5、日期、裁定人；G2 硬门段标注挂载到 e2e-m3.sh 核心集。
4. **PROGRESS.md 追加 M3 工序 4 完成记录**（含 INC-58/59/60/61、两轮 smoke 计数、
   核心集裁定引用）。
5. **《M3-测试交接文档.md》定稿**：现有 v1.0 草稿在案（620 行，含 101 条 BLOCKED 清单），
   按核心集口径改：核心集 13 条改为"执行体 = deploy/e2e-m3.sh"，其余保持 BLOCKED-豁免口径；
   环境锚点补三元组铁律（草稿已有）+ fallback 双形态配置窗口说明。
6. **M3 commit + push**：必须**当场问用户确认**。切割 = 全量 M3 面（工作区全部 M3 适配 +
   V5 + deploy 三件套 + docs）。push 走代理：
   `git -c http.proxy=http://127.0.0.1:7890 push origin main`（直连被 reset）。
   `m3-wip-anchor` 分支可留。
7. 之后把《M3-测试交接文档.md》交执行方开工序 5。

## 4. 纪律红线（接手者必须遵守）

- 三文档（测试进度/测试记录/测试BUG清单）是**执行方写域**，主会话不写；
  主会话写域 = BUGLOG / PROGRESS / 方案 / 交接文档。
- git commit/push 每次当场问用户，不用历史授权。
- 禁碰 195 的 /opt/projects/mall_R*、/opt/backups、117 机器。
- 断言禁止"或"式二选一；无法注入的窗口记 NOTE/诚实清单，不伪造。
- 所有断言失败先怀疑基建脚本再怀疑产品码——本轮 6 个 smoke FAIL 里 5 个是脚本断言缺陷，
  但 INC-60 证明产品码真缺陷也存在：两条都要取证区分。

## 5. 工作区未提交清单（M3 全量面，commit 前核对用）

- 代码：control-app M3 全新面（domain/ai Gateway 体系、M3ModelGatewayConfig、
  V5 迁移、PersistenceConfig 装配）+ M0 旧三件套删除（ModelClient/ModelBudgetGuard/
  SpringAiModelClient/ModelTimeoutException 及对应 4 测试）+ publisher 适配 +
  CT22/EX06 适配修正 + PostgresModelCallLedgerRepository（INC-60）+ 新 IT +
  PostgresITBase 清场清单。
- deploy：docker-compose.yml（旋钮透传×2 + 可能已有 fallback×3）、smoke-test.sh
  （DP-20~28 全新段 + DP-17/21 修正）、m3-lib.sh（新）、e2e-m3.sh（新，子任务产出）。
- docs：BUGLOG（INC-58/59/60/61）、PROGRESS（待补）、M2 方案 §9.3 与 M2 交接文档计数修平、
  M3-测试交接文档.md（草稿待定稿）。
