# 执行记录——RR22/RR23/RR24 依赖故障有界（195 真机·隔离栈 rriso）

- 日期：2026-09-13（执行窗约 17:40~22:15 +0800；执行器 v1~v9 共九轮，前四轮为 harness 缺陷迭代，v6/v7/v9 为有效证据轮）
- 执行者：主会话（OR-06 依赖故障有界 B2 批）
- 环境：195 真机隔离栈 rriso（端口 18091；外联织物裸网 rriso-net 挂 litellm-am3/prometheus-am0/deploy-loki-1，在栈零触碰）；受控故障 mock =宿主 18100（HTTP）/18443（自签 TLS）路径分发端点（rr2224-mock.py，访问日志=物理对拍真值源）
- 注入方法：统一收口方案 §6 修订版——iso env 旋钮（OPENAI_COMPAT_BASE_URL 指 mock 故障路径 / APP_ALERT_AM4_LOGS_LOKI_BASE_URL 指 loki 故障路径 / APP_MODEL_PER_CALL_TIMEOUT_MS=8000 测试档）+ 逐案容器重建
- 模型：deepseek-v4-flash-0731（唯一有配额）；RR23 段模型回真源 litellm

## 终局断言结果

### RR22 模型网关 HTTP 故障四案（v7 轮，4/4 PASS）

| 案 | 注入面 | 结果 | 物理证据 |
|----|--------|------|----------|
| C429 | 429+Retry-After:1 | **PASS** | rows=3=mock 物理击 3 完美对拍；全 FAILED/DEFERRED（durable-defer 记账）；有界≤6；零 fallback |
| CSLOWFIRST | 30s 首字节（>8s 测试旋钮） | **PASS** | rows=3=mock 击 3；全 FAILED/DEFERRED |
| CSLOWBODY | 声明 Content-Length 500 实发 ~300 | **PASS** | rows=3=mock 击 3；全 FAILED/DEFERRED |
| CRESET | accept 后 SO_LINGER(0) RST | **PASS** | rows=3（CIRCUIT_OPEN+DEFERRED）、mock 击 5（连接层重试合法偏离，KILL 面语义）；物理证据行≥1 |

### RR24 协议/TLS/DNS/Clock 四案（v7 轮，3 PASS+1 NOT_RUN）

| 案 | 注入面 | 结果 | 物理证据 |
|----|--------|------|----------|
| CBADPROTO | 200+非法 JSON 体 | **PASS** | rows=2=mock 击 2；FAILED/UNKNOWN_ERROR:RestClientException（协议面记账）；物理证据行=2 |
| CTLS | 自签证书 TLS 拒握 | **PASS** | rows=3（CIRCUIT_OPEN+DEFERRED）；mock 击 0=预期（握手层拒绝，HTTP 层零请求）；物理证据行=1（DEFERRED=网络面物理失败记账） |
| CDNS | 不可解析主机名 | **PASS** | rows=3（CIRCUIT_OPEN+DEFERRED）；物理证据行=1（DNS 解析失败物理尝试） |
| CLOCK | 容器时钟偏移 | **NOT_RUN** | Linux time namespace 不虚拟化 CLOCK_REALTIME、容器 date -s 非局部偏移实验，需专用 VM——归专用线，不硬凑 |

### RR23 日志源（loki）故障四案（v9 轮，1 PASS+3 模型面 FAIL）

| 案 | 注入面 | 结果 | 证据 |
|----|--------|------|------|
| LEMPTY | loki 空结果（success+empty streams） | **PASS** | logs.aggregate×6 全 SUCCESS 打到注入端点；evidence 零 logs 源（零伪造）；空结果语义与不可达可区分 |
| LDEAD | loki 端口连接拒绝 | FAIL（模型面） | run 铸造✓ 但该 run 内模型未发出 logs.* 调用（deepseek 逐 run 非确定性） |
| LTRUNC | 半包 JSON | FAIL（模型面） | 同上（v9 该案 run 未在窗口内铸出） |
| LSLOW | 响应 30s 挂起 | FAIL（模型面） | 同上 |

**RR23 残余缺口的定性**：RR23 断言前提=agent 在预算内发出 logs.* 调用。两处环境面修复后链路已打通（prometheus 直连修复+步输出预算 4000 修复，LEMPTY 实证 logs.aggregate×6 全链可达）；残余 FAIL=deepseek 逐 run 是否发出 logs 调用不可控（OUTPUT_BUDGET_EXHAUSTED=BA-120 已知族：推理烧穿步预算→content 空）。**模型面依赖的确定性触发归 native 计划路径（RR25/26 线）或更大步预算模型**——非 harness 缺陷、非产品缺陷，如实记 NOT_VERIFIED。

## BA-144 立案：执行器六雷（全部我方测试面缺陷，v1~v9 逐轮定谳）

| # | 雷 | 定谳证据 | 修法 |
|---|-----|---------|------|
| 1 | 无锁执行器：上窗脚本体 setsid/nohup 存活（pkill 只杀父 ssh），双实例并发互踩 env 旋钮、verdict 读对方 run | 决策表 7-10 号同分钟交错；/tmp 输出两写者 | flock 单实例硬闸 |
| 2 | inject 轮询无时域锚：`order by id desc limit 1` 对同 alertname 抓历史决策——v2 全 suite 秒回、verdict 全读 v1 旧行（rows=7 succ=4 PENDING 混合=v1 僵尸 run b38ab80c 的 litellm 重试行） | v2 套件 9 分钟"完成"+逐字节相同 AGG | POST 前抓决策表 max(id) 水位，只认水位后决策 |
| 3 | 决策表非空基线+canary stickiness 吸收：同 key 已有近期 WHITELISTED 决策时新 firing 被粘住不建 run（v3 六案 NORUN） | v3 六案 inbox=PROCESSED/ACCEPTED 但零决策行 | suite 开局清空决策表（iso 测试库卫生，复刻首跑空表条件） |
| 4 | **白名单大小写**：incidentKey 保留标签原始大小写（AlertIdentityFactory 不折叠），CanaryRouter `contains(id.trim())` 逐字节敏感——v4/v5 白名单误小写→全量 BUCKETED_HOLMES 分桶；stickiness_key 列显示的小写是 normalizedKey 展示形，误导排查 | 决策行 bundle_digest=自指 v5 bundle+内容正确却 BUCKETED；incident 表键全大写 | 白名单与 alertname 标签逐字节同形（大写） |
| 5 | 静态 investigationHash 不触发 RERUN：FIRING 持续+无活跃 run+材料未变=产品正确拒绝重查（IncidentProjector:300-308）——CBADPROTO（唯一 X 时代 run=SUCCEEDED 的 incident）不再铸 run | v6 六案 NORUN 的 incident 均有已记录 lastInvestigationHash | 告警 summary 带套件时间戳=真新材料→RERUN（v7 实证 CBADPROTO 恢复铸 run 并 PASS） |
| 6 | 物理证据语义误判：DEFERRED 是物理调用后的 durable-defer 记账（3 行 DEFERRED=mock 3 击对拍实证），非短路码；短路码只有 CIRCUIT_OPEN（零物理）——v2 误把 DEFERRED 排除出物理证据集造成假阴性 | v3/v6 C429 rows=3=mock 击 3 | 物理证据行排除集={CIRCUIT_OPEN}；连接层杀 face（RST/TLS 拒握）用"物理证据行≥1"替代"击数==行数" |

**环境面两雷（同批定谳，归 harness/环境）**：
- iso 栈 prometheus URL：`app.alert.am4.prometheus.base-url` 缺省=standing 栈服务名 `http://prometheus:9090`，rriso-net 不可解析→prometheus.catalog 全 TRANSPORT_UNKNOWN→agent 卡死 metrics 面→RR23 四案假阴性。修=compose 透传+set_kv 指 prometheus-am0（v7 LSLOW run prometheus.catalog SUCCESS 实证）。
- deepseek 步输出预算：1 次 catalog 后即 OUTPUT_BUDGET_EXHAUSTED（BA-120 已知族）→发不出后续工具调用。修=compose 透传 APP_ALERT_R7_PRIMARY_STEP_MAX_TOKENS=4000（v9 LEMPTY 实证 logs.aggregate×6 全链可达）。

## 在栈不变量

每轮收尾复原段：loki/prometheus/budget/step-tokens 旋钮删除、模型回 litellm-am3、standing health=200/restarts=0 全程不变（各轮输出尾行）。

## 证据清单（本目录）

| 文件/目录 | 内容 |
|-----------|------|
| rr2224-verdicts.txt | v9 轮（RR23）终局 verdict |
| v7-rr22rr24/ | v7 轮 RR22×4+RR24×3 全 PASS 的 verdict+全日志 |
| v9-final-db-evidence.txt | v9 窗全部 run/模型链/工具链/决策/证据零伪造 SQL 落盘 |
| mock-access.log | 受控 mock 物理访问日志（对拍真值源；MOCK-START 起） |
| logsrows-*.txt | RR23 各案 logs.* 工具调用行 |
| body-*.json / resp-*.body | 各案注入载荷（含 v4 TAG 轮）与响应 |
| v1-contaminated/ v2-stalegrab/ v3-stickiness/ v4-bucketed/ v6-partial/ v8-budget8/ | 六轮 superseded 记录（BA-144 逐雷的原始证据） |
| compose-recreate.log / mock-stdout.log | 逐案重建与 mock 生命周期 |

排除项：mock.key（自签 TLS 私钥，纪律排除）；mock.crt 为公钥证书保留。

## 脚本

- e2e-scripts/rr2224-run.sh（v9 终版：flock+清场+MARKID+物理证据语义+RR23ONLY 门+首案闸）
- e2e-scripts/rr2224-mock.py（受控故障端点）
- e2e-scripts/docker-compose.iso.yml（+PROMETHEUS_BASE_URL/+STEP_MAX_TOKENS 透传，缺省=产品默认）
- e2e-scripts/rr2124-rr21.sh（RR21 执行器）
