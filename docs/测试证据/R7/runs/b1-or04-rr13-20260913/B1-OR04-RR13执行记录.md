# B1-3 OR-04：角色×端点矩阵 + RR13/RR16 验收记录

执行时间：2026-09-13T02:4x UTC（195 CST 09-13 10:4x）
执行机：195。方式：零突变——读面 GET + 角色层拒绝面（403 在任何业务逻辑前），
秘密扫描只读日志、只打印计数不打印值。矩阵交付：[OR04 身份角色端点矩阵 v1]
(../../../告警-OR04身份角色端点矩阵-v1.md)（基于 SecurityConfig + 全 Controller 映射）。

## RR13 角色矩阵探针（11/11 PASS）

探针脚本：rr13-probe.sh（本目录）；逐探针 .code 文件在册。

| 探针 | 线 | 端点 | 期望 | 实测 |
|---|---|---|---|---|
| p01 | 无凭据 | GET /api/eval/runs | 401 | 401 PASS |
| p02 | operator-bearer | GET /api/eval/runs | 200 | 200 PASS |
| p03 | release-bearer | GET /api/eval/runs | 403 | 403 PASS |
| p04 | webhook-bearer | GET /api/eval/runs | 403/401 | 403 PASS |
| p05 | release-bearer | GET /api/config-bundles/active | 200 | 200 PASS |
| p06 | operator-bearer | GET /api/config-bundles/active | 403 | 403 PASS |
| p07 | operator-bearer | POST /api/config-bundles（空体） | 403 零副作用 | 403 PASS |
| p08 | duty-adapter-bearer | GET /api/duty/notifications | 403（单面铸造） | 403 PASS |
| p09 | 无 CSRF | POST /api/auth/logout | 403（CSRF 保留面） | 403 PASS |
| p10 | 无票 | GET /api/rca-runs/{run26}/events/stream | 401/400 | 401 PASS |
| p11 | operator-bearer | GET /api/auth/me | 200 | 200 PASS |

判定要点：deny-by-default、operator/release/webhook/duty-adapter 四线互不越权、
operator 不覆盖发布线、越权写在角色层即拒（p07）、CSRF 豁免与保留面分工（p05 豁免
面可 bearer 直写 vs p09 保留面无票即拒）、SSE 票必经（p10）。

## RR16 秘密零落日志（日志面半面 PASS）

脚本：rr16-logscan.sh（本目录）。10 个秘密值（四 bearer + webhook 双 token + 模型
KEY + 通知双 SECRET + bcrypt + PG 密码）在 control-app / notify / web 三容器全量日志中
出现计数全 0（值不回显，长度校验 ≥12 防误报）。
模型输入捕获面的 RR16 半面：待 FULL 捕获 run 的 prompt 秘密扫描一并断言
（DIGEST_ONLY 下无从扫起——正文不落库）。

## 未做与理由（如实登记）

- RR14（机器凭据轮换撤旧）：需独占运行窗（换 bearer→验证→撤旧），与 B1-1 FULL
  捕获窗合并排队；触发条件=窗口静默。
- RR15（缺必填秘密 fail-closed）：静态面已核（compose `:?` 强制注入清单在矩阵 §1），
  运行面实证（去掉一个 :? 变量起容器应失败）同样并入独立窗口。
- 发现并登记：另一执行者 02:30Z 重建 .env 时吸收了本批的 APP_ALERT_R7_INPUTCAPTURE
  键（值 full），但基础 compose 无该键映射行——容器 env 不生效属 compose 映射机制，
  非错误；FULL 捕获窗将以 override 文件重新挂载（键在 .env 与 override 双处在场时
  override 优先，值一致无冲突）。
