# B2 合窗执行记录——CL-04/CL-06 验收 + CL-06 跨轮结构实证（2026-09-13，195 真机）

- 主机：root@146.56.195.225（树=/opt/build/pr，JAVA_HOME=/opt/jdk-21.0.12.1+1）
- 窗口：B2 批（总方案 docs/告警-Agent生产级收口与后续优化总方案-v1.md）
- 收官发：**run=1a5175cb-2dc8-4f90-8288-40966b20311f，SUITE PASS（c 轮 attempt 1）**
- 证据根：本目录（tar 源 /tmp/b2cl06-evidence.tar.gz，md5=ada4bd3a9abf4e9d03ead0a9c9e8f42c，601 文件）

## 一、结果总览

| 面 | 结果 | 证据 |
|---|---|---|
| CL-04 MC 对位（本地+195 树） | 53/53 + 58/58 绿（mc10 输入超限零触网/mc11 估算边界/r2 三档/越权零读取/r10 failFast） | pr-logs/b2-cl0406-it.log；R7ModelGatewayTest |
| CL-06 持久面 | PostgresWorkingMemoryIT 5/5（append 幂等/latestByTask/唯一约束/append-only 权限/锚点回环） | pr-logs/b2-cl0406-it.log |
| CL-06 跨轮结构（driver phase1-9） | **SUITE PASS**：委派 1 子任务、checkpoint round=1/batches=1、记忆行 3（rev1→2→3 父链连号）、锚点回环、槽纯度、FULL 捕获、证据 2 行引用全锚定、5 调用全结清、phase7 loser 面（BA-134 第二雷仲裁语义） | pr-logs/b2-cl06-a0-try1.log；runs/runs-b2cl06/b2cl06-20260913T050218Z/ |
| CL-06 跨轮 prompt 可达性 | ASSERT-OVERALL PASS：5 笔 primary FULL 捕获 digest 全对账、真跨轮 rounds=[0,1]、round0→1 槽并集=父版为空（无丢失面） | verify-output.txt；scripts/b2-cl06-verify.py |
| 记忆链定格 | 3 行：rev1(无父=首行)→rev2(父=rev1)→rev3(父=rev2)，schema 全 2，digest 一致；checkpoint rev5 锚定最新行 rev3 | memory-chain.txt；checkpoint-anchor.txt |
| 复原 | 零委派姿态（MAX_DELEGATION=0）+ INPUTCAPTURE 回 digest-only + prompt 回四工具配方 + flagd paymentFailure off；另清 .env 惰性残留行 APP_ALERT_R7_INPUTCAPTURE=full（无 compose 映射，BA-137 同型，不进容器） | 本记录 §四；tmp/env-restored.txt |
| 密钥终扫 | 195 侧 .env 实值定向（密钥族零命中；11 个命中全为非敏感配置标量=被测旋钮本身）+词形零命中；本地词形仅 2 处注释行变量名文档说明 | 本记录 §五 |

## 二、八轮迭代史（每轮一个真发现）

1. **缺省 prompt 零委派**：缺省简短 prompt 下 qwen 不触发 delegate（采样非确定性）。
2. **多信号 summary+硬性委派顺序（v3）**：summary 注 multi-signal、prompt①「第一步必须以 delegate 分支委派 logs 专家（gap_id=g1）」后 5/5 phase4 PASS。
3. **phase5 证据行恒零**：logs 专家固定查询零行+主 Agent 自由 PromQL NO_DATA → v3 prompt 嫁接 rpc_client_call_duration_seconds_count{service_name="checkout"} 的 rpc_method/error_type 维度配方（错误系列累积=依赖故障面）；phase5 增设诚实未决双分支（证据≥1 锚定断言 / 证据=0 时 final_claims≥1 AND final_missing_information≥1）。
4. **phase7 publication 超时=BA-134 第二雷**：同 incident 非首跑=report_publication_loser（落档不发布，outbox 不外发）→ phase7 winner/loser 双分支（loser 面放行并跳过 outbox 断言）。
5. **phase9 记忆行=1**（v3 try1-5，本会话起点）：诊断 steps_used=1、唯一记忆行 rev1 无父——**委派步不 advanceStep 不提交记忆行，仅 STEP_COMPLETED（工具步）提交**；v3 prompt 只设计 1 个查询步。这是既定实现语义（记忆=调查状态，委派本身不改槽）。
6. **v4 两步自查**：prompt② 拆两次独立 prometheus.query（错误系列+OK 基线对照）→ 记忆行 3；但「父链断裂/跳号 1 处」——断言只豁免 rev0 首行，而**委派获批先于首个 STEP 消耗版次**（首行合法落 rev1 无父）。修正：唯最低版次行可无父，其余父=前一版连号（v4a 数据实证 rev1(无父)→rev2→rev3 全连号）。
7. **锚点断言漂移**（b 轮首评）：断言要求 m.checkpoint_revision=cp.revision，但 **final 提交也消耗版次**（checkpoint 终版 rev4/5 > 末记忆行 rev2/3；b1 无委派流恰好终版=末行才成立）。修正：checkpoint 须锚定本 run 最新记忆行+digest 对账（b 轮数据实证候选断言=0）。
8. **c 轮收官**：attempt 1 SUITE PASS。

两处断言修法均为 driver 校准（委派流的版次语义），非产品缺陷；产品链（append-only、parent=上一行、锚定最新行+digest）在三轮真数据上自洽。

## 三、跨轮语义实证要点（CL-06）

- round_id 随获批委派批 +1（子任务落新轮）；本窗 3-5 笔 primary 调用 rounds=[0,1]，DELEGATE-g1 子任务 DONE 于 round 1，checkpoint round=1/batches=1。
- 记忆行只在工具步 STEP_COMPLETED 提交事务内 append；父链 parent=提交时 checkpoint.memoryId()（上一行），版本号含委派/final 等其他耗版次提交故首行可落 rev≥1。
- 槽纯度：TOOL_NOT_ALLOWED/INVALID_ARGS/DECISION_UNPARSEABLE 控制拒绝码结构上不入槽（断言绿）。
- **诚实边界（如实记录）**：round0→1 槽并集=「父版为空（无丢失面）」——round-0 唯一 prompt 是委派派发（先于任何记忆提交），且单批窗内回执的 counter_evidence 不回填 round-0 已发 prompt。**非空跨轮槽流动需多委派批**（batch1 回执填槽→round-1 派发携带），本窗 batches=1 数据面不达；并集语义已由代码面（ContextAssembler.mergeAccumulated 父版槽在前+delta 去重）+PostgresWorkingMemoryIT 覆盖，真机多批窗留待后续（RR 窗排队）。

## 四、复原记录（b2-cl06-restore.sh 输出定格）

```
== 摘除 override 重建 ==
 Container deploy-migrate-1  Exited
 Container deploy-control-app-1  Starting / Started
startup-ok
== env 验证（应回：MAX_DELEGATION=0+四工具配方+无 INPUTCAPTURE）==
OK: 委派批回 0（零委派姿态）
OK: INPUTCAPTURE 已回默认 digest-only
OK: prompt 回四工具配方
StartedAt=2026-09-13T05:04:53.182925881Z Status=running
== flagd paymentFailure 回收 ==
{"flag": "paymentFailure", "applied": "off", "previous": "100%"}
RESTORE-DONE
```

追加卫生：deploy/.env 第 32 行 `APP_ALERT_R7_INPUTCAPTURE=full` 残留已删（grep 验证 R7_INPUTCAPTURE 计 0；compose 无该键映射行——BA-137 同型惰性键，容器侧本就未注入）。

## 五、密钥终扫（双面）

- **195 侧 面1（.env 实值定向）**：密钥族（PASSWORD/SECRET/TOKEN/_KEY/DSN 共 17 键）零命中；11 个命中键经逐值核验全为非敏感配置标量（POSTGRES_DB=pr_agent、AUTH_OPERATOR_USERNAME=test、布尔/端口/预算数/prompt 正文——即被测旋钮本身，理应在证据里）。
- **195 侧 面2（词形）**：sk-/Bearer/ghp_/AKIA 零命中。
- **本地侧（词形）**：仅 scripts/e2e-r7-common.sh L17-18 注释行的环境变量名文档说明（R7_WEBHOOK_BEARER/R7_RELEASE_BEARER 字样），非密值。

## 六、工件清单

- `runs/runs-b2cl06/`：30 个 run 目录（v3/v4a/v4b/v4c 全迭代史；收官=b2cl06-20260913T050218Z）
- `pr-logs/`：a0 主线与 try1-5 各轮日志、retry v4/v4b/v4c 包裹器日志、b2cl06-v3|v4a|v4b 备份子目录、b2-cl0406-it.log（CL-04/CL-06 IT）
- `scripts/`：e2e-b2-cl06.sh（终版，含两处断言校准）、e2e-r7-common.sh、b2-cl06-override.yml（v4）、retry/restore/verify、orch/relaunch/recon/diag6/diag7
- `tmp/`：run-id.txt、env-override.txt、env-restored.txt、prompt-r0-s0/r1-s1..4.txt（FULL 捕获摘录）
- `memory-chain.txt`、`checkpoint-anchor.txt`、`verify-output.txt`：收官发三面定格

## 七、遗留与排队

- 非空跨轮槽流动（多委派批真机窗）→ RR 窗排队项，设计方案见 docs/告警-B2-RR21-28用例设计方案-v1.md。
- RR14/15（B1 遗留）+RR21~28 需独立注入窗。
- 后续批次 B3（OR-02/03/08/09/10/11/12）→B4（OP 批+CL-10）→B5（条件项）。

## 八、2026-09-13 审查勘误注（后置，docs/告警-BUGLOG与B2测试审查-统一收口技术方案-v1.md §4）

本记录所报 SUITE PASS 的**数据本体幸存**（记忆链/锚点有 memory-chain.txt、checkpoint-anchor.txt 独立定格），但当时驱动与验证器的**断言层存在四处假绿面**，证据的"验证器证明力"层降级，以 v2 套件（e2e-scripts/）取代：

1. b2-cl06-verify.py:90 无 `sys.exit`——digest 错/反证丢打印 FAIL 仍退出 0（离线复现 b2-verifier-audit-results.json）。
2. 同文件 :79-80 父序反转静默过（无 else 置败）；:74 空父集算语义通过（vacuous containment）。
3. e2e-b2-cl06.sh phase7（时行号 159-160）loser 分支 `docker logs --since 30m | grep report_publication_loser` **无本 Run 作用域**——别案 loser 行可放行本案；且日志随容器重建蒸发（复核时已查无该行），本记录 §四 复原后的 loser 面只能靠本节 DB 推导复核。事后核验：本案报告（97108e64）无 pub 行无 outbox 行+同 incident（6f99937a）更早 SENT winner 在档——绑案前提成立，本次结论未翻案。
4. phase9 父链断言（时行 181-185）强制父版次=子版次-1——对合法多批流过强（N13）；锚点断言（时行 186-190）INNER JOIN——memory_id NULL/悬空时违规行消失→count=0 假绿（N14）。本案数据（rev1→2→3 连号、锚点实在）恰好不受这两洞影响，结论未翻案。

v2 修复（e2e-scripts/，归档本目录 scripts/ 原件不动）：verify2 退出码 0/1/2/3+result.json+括号配平信封解析+非空 witness（空集=INCONCLUSIVE 不算过）+父序冻结契约；driver v2 phase7 纯 DB 绑案仲裁、phase9 LAG 同作用域父链（允许间隙）+LEFT JOIN NULL-safe 锚点、phase10 verify2 退出码门；retry2 固定 N 有效试验全记录+RUN_ID_RESULT 取案。槽喂入源定谳（ContextAssembler.java:773-795）：四槽仅由回执（counter_evidence_refs/open_gaps）与 FINAL 提案史（hypotheses/ruled_out）供数——直查证据不入槽，非空跨轮 witness 需两批委派（N18 场景=v5 override）。
