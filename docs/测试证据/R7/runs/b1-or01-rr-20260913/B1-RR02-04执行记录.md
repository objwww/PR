# B1 批 OR-01 配套验收：RR02 / RR03 / RR04 执行记录

执行时间：2026-09-12T22:2x UTC（195 CST 2026-09-13 06:2x）
执行机：195（146.56.195.225），全程只读/零接触真实运行面（RR02 注入只改期望面副本、
RR03 用不可达 DOCKER_HOST 独立进程、RR04 纯 SELECT）。
采集器版本：`deploy/audit-runtime.sh` @ bcda248（含 RR03 前置修复：docker 面零容器判
UNKNOWN 不冒充 MATCH；修复前该失效模式会误报 MATCH）。

背景事实（对拍期间发现并定性）：另一执行者 SR/CL/OP 批于 22:18:05Z 部署（jar md5
`fd546d26eae74689d94c37a879e86787`，含 V99~V108 迁移、CL-01 提交围栏、CL-03 分型投影、
RunReconciler；部署窗 22:12–22:22+ 持续验收 run）。本批所有采集不干扰该窗。

## RR02 漂移可见（verdict.json / verdict-baseline.json / verdict-fresh.json）

- 注入：期望面副本加入 `V999__rr02_drift_inject.sql`（模拟"测试环境某 schema/工具版本
  与运行面不一致"的镜像情形，运行面零改动）。
- 结果：`flyway DRIFT 库缺=['999']`、overall=DRIFT、退出码 1（对拍器出口纪律=旧资格
  阻断）。断言 ASSERT-DRIFT-VISIBLE PASS。
- 基线意外漂移与定性：B0 冻结期望件对当前运行面对拍出现 `库多=['108']`——V108
  `sr01 run purpose reconcile` 于 22:18:01 随 SR 部署落库。现场从**当前构建树**重新
  emit 期望面后对拍：81 版本 MATCH、无 DRIFT（verdict-fresh.json）→ 定性为
  **B0 冻结面过期（运行面演进），构建树↔运行时同源一致**，非真实不一致。
- 附带结论（RR01 刷新）：新部署面上 RR01 复验 overall=UNKNOWN 无 DRIFT（诚实 UNKNOWN
  同 B0：镜像无 commit 标签、BUDGET_STEP 未钉）。

## RR03 采集失败不冒充（rr03/）

- 手法：`DOCKER_HOST=tcp://127.0.0.1:1` 独立进程跑采集器（docker/exec/PG 面全失败，
  零接触真实容器）。
- 纯采集：containers=0、flyway query_ok=False、jar_md5=UNKNOWN、
  skill_binding_rows=UNKNOWN、白名单键全 ABSENT。
- 对拍：overall=**UNKNOWN**（flyway/config/container/skill 全 UNKNOWN，无一项 MATCH），
  退出码 1；`ASSERT-OVERALL-NOT-MATCH` PASS。
- 秘密扫描：全部输出件无 KEY/TOKEN/PASSWORD/SECRET 值出现（采集器设计上只采白名单键
  与摘要，无 env dump）——PASS。
- 修复实证：`container_mem_limits` 修复后判 UNKNOWN（修复前零容器会误判 MATCH——
  本批发现并以 bcda248 修复，先修后验）。

## RR04 新旧 Run 双版本身份（rr04/）

- `rca_run_skill_binding` 9 行：每 run 各钉各的 release_digest（9 个互不相同），
  selection_status=NONE 全持久，selector_version 稳定；run26 行原样。
- `rca_run.config_digest` 多版本并存；两个停滞 REPORTING run（18:56/18:59）老身份
  未被 22:18 新部署改写。
- 判定：持久身份面 PASS；"不同 ACTIVE Skill 版本消费"半面 N/A（当前无 ACTIVE skill
  资产，全部绑定 NONE）——如实登记，待 Skill 发布链启用后补用例（关联 CL-09）。

## 遗留与后续

- RR02 的运行面注入变体（真改部署 env/registry digest 后重启看 DRIFT）与 B1-1 FULL
  捕获窗合并排队（需独占重启窗，当前对方窗口未关）。
- 22:18 部署含 CL-03 分型投影（jar 内 projectLogs/projectMetrics 实证、appendTopLevel
  已无）——B1-1 FULL 捕获 run 升级为验证**真实 A13-01 验收**（嵌套证据投影入模）。
