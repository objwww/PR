# 执行记录——RR25/RR26/RR27/RR28 episode 身份链与积压公平性（195 真机·iso 栈）

- 日期：2026-09-13（执行窗约 22:20~23:10 +0800）
- 执行者：主会话（OR-07 入口积压通知 B2 批）
- 环境：195 真机隔离栈 rriso（端口 18091；standing 栈零触碰）；webhook 直投+SQL 逐相位断言
- 执行器：e2e-scripts/rr2528-run.sh（v2）/ rr26-run.sh（v3）；均为 flock 单实例（BA-144①）

## RR25+RR27+RR28 九相位（rr2528-run.sh v2，11/11 断言全 PASS，零模型依赖）

**机理**：alertname=E2RRR25<套件后缀> 不在任何活跃 bundle 白名单内→canary BUCKETED_HOLMES→零 run 铸造（决策行实证）——纯 incident/alert_event 层断言，无模型面噪声。

| 相位 | 注入序列 | 断言 | 结果 |
|------|----------|------|------|
| P1 基线 | firing 首见 | incident 铸 FIRING/gen0；事件=1；run=0（HOLMES 分桶） | PASS |
| P2 重复通知 | 同载荷重投（RR27 响应丢失重试） | received+1/distinct 不变/notification(重复计数)+1；事件仍=1（fingerprint+payloadHash+startsAt 幂等键） | PASS |
| P3 材料变化 | 同身份新 startsAt+新 summary | generation 保持=0（同 episode）；RERUN 尝试被 HOLMES 分桶→WAITING_CAPABILITY | PASS |
| P4 仅 severity | severity 变化 | 新事件（payloadHash 含 severity）但 generation 保持、不触发重查（severity 不在 key labels/annotations） | PASS |
| P5 resolved | status=resolved+endsAt | FIRING→RESOLVED generation 保持=0、resolved_at 落 | PASS |
| P6 重复 resolved | 再发 resolved | 不换态不换代（幂等） | PASS |
| P7 乱序迟到 | firing startsAt<resolvedAt | 不复活不换代——**恢复后旧 firing 不冒充新故障**（RR28） | PASS |
| P8 新 episode | resolved 后新 startsAt firing | RESOLVED→FIRING **generation+1=1**、episodeStartedAt 推进 | PASS |
| P9 幂等键 | 新 episode 载荷×3 物理投递 | 事件=1 条、generation 恒 1（多物理请求一次副作用） | PASS |

**口径登记**：notification_count=重复通知计数（首条非重复=0，仅 duplicate 分支 +1）——v1 轮三处 FAIL 为断言口径误设（误当投递总数），v2 修正后全 PASS（产品计数语义自洽）。

## RR26 积压与公平性（rr26-run.sh v3，4 PASS+AGE 落档）

**机理**：白名单内 11 个 e2rr-* incident，summary 序号化=材料变化→每次 firing 合法 RERUN 铸真 run（11 个）→worker 逐 run 占用；档2 模型指受控 mock /slowfirst（30s 首字节）+8s 测试超时→worker 被拖住制造积压窗口；采样器 5s 记录队列深度/最老年龄/inbox 未处理序列；时延样本 sort+awk 取 p95。

| 断言 | 结果 | 数据 |
|------|------|------|
| T1 基线档 | PASS | 11/11 全 202；p95=0.013s；队列峰值=7；最老峰=50s |
| T2 阶梯档（慢模型） | PASS | 11/11 全 202——worker 被拖住仍持久接收；p95=0.019s |
| Q 队列深度 | PASS | 慢档峰值深度=21 可观测；排空等待 240s 后归零（有界+回收） |
| R RERUN 铸造 | PASS | run 铸造=11（材料序号化→RERUN 通道实证） |
| AGE 最老年龄 | 落档 | 基线峰=50s；慢档峰=225s（有界，未超 run 超时链上限） |

**202 语义核验**：两档全程 202=持久接收承诺成立（AM 只重试 5xx；接收器先落库后断响应）。

## 语法坑登记（dash 可移植性，当轮清账）

1. `$(( $(date +%s) - T0 ))` 嵌套命令替换——dash 算术解析器吞至文件尾（改两步取时 CUR=$(date +%s)）。
2. 词内算术 `cut -d' ' -f$(( ... ))` 同族——改为先赋值 IDX 再引用。
3. 采样 SQL 嵌套聚合 `max(extract(...min...))` 非法——改 `extract(epoch from now()-min(...))`。

## 证据清单（本目录）

| 文件/目录 | 内容 |
|-----------|------|
| rr2528-verdicts.txt / rr2528.out | 九相位 verdict+全日志 |
| final-db-evidence.txt | incident 终态+全事件轨迹（fingerprint/payloadHash/generation/startsAt） |
| body-*.json / resp-*.body | 各相位注入载荷与响应 |
| rr26-verdicts.txt / rr26v3.out | RR26 五断言 verdict+全日志 |
| samples-tier1/2.txt | 队列深度/最老年龄/inbox 未处理 5s 采样序列 |
| lat.txt / codes.txt | 请求时延样本与响应码（p95 计算源） |
| v2-noage/ | RR26 v2 轮（最老年龄 SQL 缺陷轮）superseded 记录 |

## 在栈不变量

standing health=200 / restarts=0 全程不变（两执行器收尾核验行）。
