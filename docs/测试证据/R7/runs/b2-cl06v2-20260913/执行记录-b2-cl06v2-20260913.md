# B2 CL-06 v2 真窗执行记录（2026-09-13）——S0 v2 三件套验收 + witness 定谳

> 执行者：B2 线（S0 owner）。前置：S0 v2 三件套已提交 8d79901（verify2+selftest 8/8+driver v2+retry2+override v5）。
> 环境：195（146.56.195.225），/opt/build/pr 树，qwen 主模型，flagd paymentFailure=100% 故障注入。
> 证据根：本目录（b2cl06v2-pack/，429 文件：runs 18 个 run 目录+三批日志+探针+verify2 结果+发射/复原脚本快照）。

## 一、结论（TL;DR）

1. **S0 v2 三件套真机验收通过**：verify2 退出码契约（0/1/2/3）真窗面成立；driver v2 phase7 纯 DB 绑案、phase9 LAG 父链+NULL-safe 锚点、phase10 verify2 门全数走通；retry2 退出码纪律终批如实（INCONCLUSIVE=3）。
2. **CL-06 结构面 8/8 有效试验全过**（记忆行父链/锚点回环/槽纯度/FULL 捕获对账）。
3. **非空 witness 定谳=结构性不可达（BA-142 立案）**：回执生产者 `NativeInvestigationExecutor.submitDelegationReceipt` 成功路径硬编码 `counterRefs=List.of()`、missing=空；委派子任务=确定性单工具角色（不读主 Agent 任务描述），失败面不可由 prompt 引导 → `counter_evidence_refs`/`open_gaps` 非空跨轮在现行实现无生产路径。**RETRY2-VERDICT: INCONCLUSIVE（exit 3，b3 干净批：3 有效全 verify2=3）**——这是 verify2 契约设计的诚实态，非测试缺陷。
4. **意外收获 BA-141（立案）**：LLM 路由 run 的 PRIMARY_INVESTIGATE 从未被驱动即 DEAD（attempts=0，1.5s，零日志）→ 静默降级 native 未决报告（报告本体诚实：UNRESOLVED/零 claims）→ run=SUCCEEDED。全史 17 例（09-11/12/13）。
5. **retry2 自身两缺陷当窗清账**：分类优先级（V2 已知时应先于 DRV——phase10 门设计上以 exit1 编码 INCONCLUSIVE）+ run 目录提取（grep -l 命中文件而非目录）。

## 二、窗口时间线（5 批全记录）

| 批 | 时间(UTC) | 场景 | 有效/无效 | 结果 | 杀点/根因 |
|---|---|---|---|---|---|
| v5 | 07:10-07:11 | v5：g1 logs 专家+g2 **change 专家** | 3/0 | 3×FAIL@phase5 | 我方 prompt 把子任务引向 change.query（8 工具 allowlist 外）——**场景引导缺陷非产品缺陷**；phase1-4 全过（round=2/batches=2 两批委派序成立）；手动 verify2=3×INCONCLUSIVE（槽恒空） |
| v5.1 | 07:17-07:18 | g2 换 metrics 规则面+委派纪律段 | 3/0 | 3×FAIL@phase9 | 记忆行=1（主 Agent 直查压成一轮，steps=1）；**取证定谳回执双空根因**（见 §三 BA-142）——委派纪律不可能传导（子任务不读任务描述） |
| v5.2-b1 | 07:27-07:28 | 两批间强制独立直查步 | 3/0 | 3×verify2=3（聚合误标 FAIL） | 场景+结构全过；**retry2 缺陷一**（分类优先级）把 INCONCLUSIVE 误判 FAIL |
| v5.2-b2 | 07:32-07:33 | 同上+retry2 v2.1 分类修复 | 3/1 | 2×INCONCLUSIVE+**1×FAIL@phase4** | **BA-141 异常**（PRIMARY DEAD attempts=0→SUCCEEDED）；另暴露**retry2 缺陷二**（dirname），1×INVALID 零委派如实记录 |
| v5.2-b3 | 07:37-07:39 | 同上+dirname 修复 | 3/2 | **3×INCONCLUSIVE，RETRY2-VERDICT: INCONCLUSIVE（exit 3）** | 干净收官批；2×INVALID 零委派如实记录 |

汇总：18 run，有效 12（8×verify2=3 结构全过 + 3×早期杀点迭代 + 1×BA-141），无效 4（零委派采样面），0×PASS（witness 不可达，见 §三）。

## 三、两大产品发现（立案，归对方实现线）

### BA-141：LLM run 的 PRIMARY 未驱动即 DEAD→静默降级 native 未决
- **现场**（b2 attempt3，run=3564fee0）：run 07:33:14.223 建行→07:33:15.773 SUCCEEDED（1.5s）；PRIMARY_INVESTIGATE DEAD/attempt_count=0/零 lease/deadline=infinity；NATIVE_INVESTIGATE DONE；checkpoint final_claims=[]/final_missing=[]；报告 engine=NATIVE「UNRESOLVED（confirmed=0 speculative=0）/NO_CONFIRMED_ROOT_CAUSE」。
- **诚实性边界**：报告本体不造假（零结论零证据如实）；异常在**路由语义**——canary WHITELISTED 把告警定向到 LLM run，LLM 主任务却从未被驱动，用户拿到的是 native 兜底而 run 状态=SUCCEEDED。容器日志该 run 零行（静默面）。
- **历史面**：全史 17 例 PRIMARY DEAD attempts=0（09-11~09-13，含 09-12 单日 8 例；16 例 run=SUCCEEDED）。证据：`b2cl06v2-pack/ba141-*.txt`。
- **根因待查**（ DEAD 迁移写手零日志，rca_event 无任务迁移审计行）；归属：在飞生命周期线（U05~U08 邻域）。

### BA-142：回执生产者硬编码空反证/缺口 → CL-06 非空 witness 结构性不可达
- **供数链定谳**：`ContextAssembler.rebuildMemorySlots`（773-795）counter_evidence_refs/open_gaps=当前轮回执 counter_refs/missing_information 并集；回执生产者 `NativeInvestigationExecutor.submitDelegationReceipt`（~:624）成功路径 `counterRefs=List.of()` 硬编码、missing=空，findings=evidenceIds 机械映射；FAILED 路径 missing=[failureNote] 非空（唯一非空生产路径）。
- **委派子任务模型**：确定性单工具角色（SingleToolRoleRunner 按 roleId 映射固定查询，g2 metrics 角色→固定 prometheus.query），**不读主 Agent 委派任务描述**——v5.1「委派纪律」段证明无效（3 run 回执双空）。
- **推论**：①counter_evidence_refs 非空无生产路径（需回执生产面增强：子任务结论结构化解析）；②open_gaps 非空仅 FAILED 子任务可达且失败面不可由 prompt 引导（不人为制造）；③CL-06 台账「非空反证跨轮可达」维持 IMPLEMENTED_NOT_VERIFIED，升级为**待生产面增强**（N 系规格线）。
- **佐证**：v5.1 三 run 回执 `cref=[]/miss=[]`（probe3/5）；v5 三 run verify2 witness=False（manual-verify2-probe/）。

## 四、S0 v2 三件套验收对账（U01/U02/U03）

- **verify2**：真窗 8 有效试验退出码全=3 与语义一致（结构过/槽恒空）；早期 3+3 run 的杀点面由 driver 承载。离线自测 8/8（195 python3.6 复跑通过）。result.json 含 coverage.nonEmptyWitness/assertions 逐条目。
- **driver v2**：phase7 纯 DB 绑案零 docker logs 依赖；phase9 LAG 父链+LEFT JOIN NULL-safe 锚点+槽纯度（v5.2 起 8 run 全过）；phase4 regex steps∈[1,9]（v5/v5.1 的 steps=1 面通过）；RUN_ID_RESULT trap 18/18 run 正确回传（retry2 零串案）。
- **retry2**：v2.1 两修后分类正确（b3 批 3 INCONCLUSIVE+2 INVALID 如实）；退出码=3 与语义一致。**当窗清账的两缺陷**（分类优先级 DRV>V2 误判、`grep -l` 文件路径当目录用）已修入 e2e-scripts/b2-cl06-retry2.sh 并在本窗 b2/b3 双批实证。

## 五、工程教训（当窗）

1. relaunch 窗口静默 pgrep 会匹配**外层命令行里的脚本名字面量**（`b2-cl06-retr` 命中 sed 参数）——发射命令行不得含被匹配模式串。
2. 日志备份 cp 同名覆盖（v5 批文件面被 v5.1 覆盖；DB/runs 目录未损）——快照必须按批标签分目录（snap.sh 已参数化）。
3. cmd.exe 嵌套引号/`mkdir -p`/`&&` 三坑复现——一律本地 Write→scp→sed CRLF→sh。

## 六、复原（已验证）

override 撤除重建 health=200；容器 env 无 INPUTCAPTURE/MAX_DELEGATION=2/v5 PROMPT（.env 的 MAX_DELEGATION_BATCHES=0 与 PRIMARY_PROMPT=B1 缺省取证配方为既有站定姿态）；flagd paymentFailure=off。195 侧工件：/opt/build/pr-logs/b2cl06-v2/（分批目录）、/opt/build/runs-b2cl06v2/（18 run）、/tmp/b2cl06v2-pack.tgz。

## 七、证据清单

- `b2cl06v2-pack/v2-runs-summary.txt`——18 run×verify2 退出码总表
- `b2cl06v2-pack/logs-batches/v5.1|v5.2|v5.2-b1-pre|v5.2-b2|v5.2-b3/`——各批 t 日志+聚合日志
- `b2cl06v2-pack/runs/*/`——18 run 目录（bundle/审计行/phase 断言产物/verify2-result.json/phase10-verify2.exit）
- `b2cl06v2-pack/ba141-*.txt`——BA-141 三面证据（报告原文/final_missing/17 例历史清单）
- `b2cl06v2-pack/manual-verify2-probe/`——v5 批 3 run 手动 verify2（witness=False 定谳面）
- 脚本快照：e2e-b2-cl06-v2.sh/b2-cl06-verify2.py/b2-verify2-selftest.py/b2-cl06-retry2.sh（v2.1）/override v5.2/relaunch/snap/restore
- 仓库面：e2e-scripts/（同内容已同步提交）
