# RR01 执行记录：运行时事实采集与对拍（OR-01 / B0 事实冻结）

2026-09-13 06:14 +08（采集时间 2026-09-12T22:14:11Z）。执行者：B0 执行窗。
工具：`deploy/audit-runtime.sh`（本批新增，只读、脱敏白名单、无秘密值/无 env dump；
prompt 只记 sha256+长度；模型路由只记主机名）。

## 对拍结论：overall = UNKNOWN（无 DRIFT；两项诚实 UNKNOWN——UNKNOWN 不当 MATCH）

| key | verdict | detail |
|---|---|---|
| flyway | **MATCH** | 80 版本一致（构建树 V1~V107 与运行库 flyway_schema_history 完全对齐，含 CL/OP 批 V99~V102/V103~V107） |
| app_git_commit | **UNKNOWN** | 镜像未嵌 commit 身份（构建树为 tar 快照无 .git，镜像无 revision 标签）——OR-11 发布资格须绑定构建来源 |
| config_whitelist | **UNKNOWN** | APP_ALERT_AM4_BUDGET_STEP 部署层未钉（走应用内默认 8）；其余键显式钉定：AGENT_MODEL=qwen3-max-preview、MAX_DELEGATION_BATCHES=0、BUDGET_TOOL_CALLS=12、PROMETHEUS_SERVICE_ALLOWLIST 四服务、prompt sha256=498fd17f…（848 字符，v13） |
| container_mem_limits | **MATCH** | 6 容器全有内存上限（control-app 768MiB/web 128MiB/pg 512MiB/loki/notify/otelcol 384MiB） |
| skill_binding_rows | **MATCH** | rca_run_skill_binding 9 行（V100 表活着且有数据） |

## 运行面事实摘录（runtime-manifest.json 全文同目录）

- 主机：VM-0-4-centos（CentOS 7 内核 3.10）——**timedatectl 不可用：timezone/ntp_sync=UNKNOWN**（OR-09 时钟关注项登记）。
- 容器：6 运行中（compose 项目 deploy）。
- app_build.jar_md5=8bb553e9f31ddf7b9008caf97e230d4d（本次运行镜像指纹，追溯锚点）。
- release_asset 表 **0 行**（本环境当前无 ACTIVE release 资产——与 A0 e2e 每跑 phase1 发布 bundle 的生命周期对照，登记供 OR-01 后续对拍基线）。

## 采集器行为纪律

- 对拍退出码：MATCH=0，DRIFT/UNKNOWN=1（UNKNOWN 不当 MATCH，§4 纪律落到机器码）。
- 秘密面：不采任何 KEY/TOKEN/PASSWORD；白名单键值仅限非秘密配置；URL 只记主机名。
- `--emit-expect` 从构建树生成期望面（git 身份/迁移文件清单）；期望面如实记录
  `git_commit=NO_GIT`（195 构建树为快照部署，非 git checkout）。

## 关联

- 总方案 §4（OR-01）、§16 RR01、§17 B0 出口门"运行与测试基线可对拍"。
- RR02（故意替换 schema 版本→DRIFT 可见）/RR03（采集权限缺失→UNKNOWN 不泄露）留 B3 批执行面。
