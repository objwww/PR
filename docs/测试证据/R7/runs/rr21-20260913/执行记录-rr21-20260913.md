# 执行记录——RR21 接收器先持久化操作（195 真机·隔离栈 rriso）

- 日期：2026-09-13（执行窗约 09:05~09:35 UTC）
- 执行者：主会话（S0 认领，OR-06 依赖故障有界 B2 批）
- 环境：195 真机隔离栈 rriso（postgres/migrate/control-app 三服务独立卷；端口 127.0.0.1:18091；外联织物裸网 rriso-net 挂 litellm-am3/prometheus-am0/deploy-loki-1，在栈零触碰）
- 注入方法：统一收口技术方案 §6 修订版——`ALTER SYSTEM SET default_transaction_read_only=on` + `pg_reload_conf()` + `pg_terminate_backend()`（应用后端全断，Hikari 重连即只读会话）；基线→注入→发告警→复原→重投 五拍
- 执行器：e2e-scripts/rr2124-rr21.sh（bundle 在可写窗口预激活后才注入，规避"只读期发布面写拒"假阻塞）

## 断言结果（五断言全 PASS，一次过）

| # | 断言 | 结果 | 物理证据 |
|---|------|------|----------|
| 1 | 真表写拒（非临时表面） | PASS | `insert alert_inbox` → `ERROR: cannot execute INSERT in a read-only transaction`（write-reject.txt） |
| 2 | 只读期告警不假 ACK + 零新发送 | PASS | POST → **503** `{"error":"storage unavailable"}`；inbox/run/model_call/tool_inv 四表增量全 0（AlertWebhookController DataAccessException→503 语义，AM 只重试 5xx） |
| 3 | 接收器日志留痕 | PASS | app 日志 `整组无法持久化，返回 503` WARN 行命中=1（app-readonly.log） |
| 4 | 恢复重投 → 单 run | PASS | 复原 off+reload+terminate → 同载荷重投 **202** → WHITELISTED 路由 → run 6670387c **SUCCEEDED**；该告警 run 恰 1 |
| 5 | ACK 丢失重投幂等 | PASS | 同载荷再投 202（inbox 各落一行=投递史），run 恒 1——效果幂等 |

在栈不变量：standing health=200 / restarts=0 全程不变；GUC 复原 off（guc-after.txt）。

## 证据清单

| 文件 | 内容 |
|------|------|
| rr21-verdicts.txt | 五断言 verdict 全文 |
| rr21.out | 执行器全日志（含基线计数 inbox=2 run=2 model_call=10 tool_inv=5） |
| guc-before.txt / guc-after.txt | default_transaction_read_only 注入前/复原后 |
| write-reject.txt | 真表 INSERT 写拒原文 |
| resp-readonly.body / resp-recovered.body / resp-ackloss.body | 503 / 202 / 202 三拍原始响应体 |
| app-readonly.log | 只读期应用日志（无法持久化 WARN） |
| readonly-health.txt / terminated.txt | 只读期读路径 health=200；后端 terminate 计数 |
| alert.body | 注入告警载荷（唯一 fingerprint） |

## 边界与 NOT_RUN

- 幂等去重的落点在 episode/run 面（同 fingerprint+startsAt 重投 run 恒 1），inbox 行为投递史逐次落行——效果幂等与记录幂等分层如实记录。
- RR21 只覆盖"整组无法持久化"面；部分条目拒绝语义（400/413 零落库）归 RR27/28 接收器语义面。
