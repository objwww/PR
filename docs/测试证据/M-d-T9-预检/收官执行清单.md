# M-d T9 收官执行清单（预检 e1fc45d3 已确认合成树 2290 测试全绿；本单供收官轮逐项勾选）

## 〇、开门前置核对

- [ ] `git status` 确认四门已净：RunDetailView.vue / PromptWorkbenchView.vue / AlertAm4Config.java / zh.js
- [ ] 净后按依赖序补三个前端/装配半场（每件单独提交）：
  1. T2：RunDetailView Trace 明细卡（fetch /trace-details，按 invocationId 关联 span.id 展开行，文案入 dict/mdZh.js）
  2. T6：primary v9 写作增量应用（AlertAm4Config 六要素段补把握短语/坏例/禁黑话三条；工作台 diff 验证）
  3. T7：PromptWorkbenchView 草稿 UI（起草/列表/diff/弃稿接 /drafts 四端点，文案入 mdZh.js）
- [ ] 若 zh.js 转净：评估将 mdZh 键收编回 zh.js（可选，mdZh 独立字典长期存在亦合规）

## 一、部署（沿既有 deploy 纪律）

- [ ] 本地 tar（显式 ./ 相对路径）→ scp → 195 /opt/build/pr 解包
- [ ] JAVA_HOME=/opt/jdk-21.0.12.1+1 mvn -q -DskipTests package（control-app）
- [ ] docker compose build && up -d（control-app + alert-web；Flyway V152/V153 随 control-app 启动自动执行）
- [ ] .env 若有变更：先 cp 备份再改，跑后还原核对
- [ ] 迁移验证：`select version from flyway_schema_history order by installed_rank desc limit 2;` → V153/V152

## 二、端点 curl 验证（真 run id 从 `select id from eval_run order by started_at desc limit 1` 取）

- [ ] GET /api/eval/runs/{id}/six-parts → assessed=0 时 rate=null 如实（V152 空表态）
- [ ] GET /api/eval/runs/{id}/process-metrics → 无已结清案例时各率 null
- [ ] GET /api/rca-runs/{id}/trace-details → 空表或行齐（scope/result 截断在场）
- [ ] GET /api/eval/runs/{id}/approval-chain → 零活动如实
- [ ] GET /api/v1/prompt-workbench/drafts → 空列表 200
- [ ] judge：judge 面路由仍 200（rubric v2 四题在 QUESTIONS，欠费期 judged=0 如实）

## 三、页面截图（存 docs/测试证据/M-d-T9-截图/，命名=页面_页签_内容.png）

- [ ] 实验详情页：六要素卡 + 过程面卡（无数据态与有数据态各一张，有数据待充值跑批后回填）
- [ ] 调查详情页：Trace 瀑布展开行（工具明细三要素；待 T2 前端半场合龙后）
- [ ] Prompt 工作台：草稿列表/diff 视图（待 T7 前端半场合龙后）
- [ ] 每张截图在 docs/测试证据/M-d-T9-截图/README.md 标注：页签/数据来源端点/与 SQL 对账结果

## 四、SQL 对账（零错误数据门槛）

- [ ] V152：`select count(*) from eval_case_six_parts;`（0=未跑批如实）
- [ ] V153：`select count(*), status from prompt_draft group by status;`
- [ ] registry v6：装载门 GoldenScenarioRegistryMdSuiteGateTest 全绿（24 场景）
- [ ] 充值后回填项（不阻塞收官，标注待回填）：S26 B 段实跑、judge v2 校准一致率、六要素 rate 真值

## 五、双台账收官

- [ ] 方案 §3 工序表 T2/T6/T7/T8/T9 状态与战果注记终稿
- [ ] docs/告警-PROGRESS.md 追加收官行（时间线只增不删）
- [ ] 收官报告输出（M-d 全量战果+待回填清单+定时任务删除提示）
