# R7 执行日志（2026-09-11，真 LLM 多 Agent 线一单到底）

> 依据：`docs/告警R7-真LLM多Agent技术方案.md` **v2.1**（主 Agent 直接调查+按需委派+有界恢复）+
> `docs/告警-R7-执行卡.md`。执行顺序 X1→X2→X3→X4→X11→R7a-1→R7a-2+X5→X6→A0 门→B 门，
> 按依赖自行推进，中间不询问。本日志逐卡落账：改动文件、测试结果、偏差与决策；断点续接从本日志恢复。
>
> 范围外（不做）：X10 代码工具/R7-A1（增强线编码硬门未解锁，标 NOT_READY）、X7/X8/X9 后期卡、
> release 域 ConfigBundle 面、前端、Redis/MQ、第三方 agent 框架。
> 测试纪律：R7 56 例（§十四 36 + §十七 20）范围内逐例落地或如实标 NOT_IMPLEMENTED/NOT_RUN，
> 禁止 PASS 占位；旧确定性三角色路径行为不变是硬约束，弄红存量测试立即停手。

---

## 0. 断点接手与 v2.0 痕迹重做（本会话开工块）

**接手现场**（会话初勘查 → 用户清理后复核）：上次被取消的 R7 coding agent 按 **v2.0** 方案留下半成品
（V46 迁移、RcaTask.roundId、AgentProfile R7-X3 段、AgentRegistry R7-X3 段、无 PRIMARY 的 AgentPhase、
RoleRuntimeKind）。**用户随后已将 v2.0 痕迹整体清除**（V46 删除、三个既有类回退 AM4 基线，
本会话编辑时实测确认），仅 AgentPhase/RoleRuntimeKind 两文件留存 v2.0 内容。

**裁定与动作**：
- v2.0→v2.1 实质分叉 = "固定三角色流水线 → 主 Agent 直接调查+按需委派"；被取消会话未写到分叉点，
  其铺设的角色身份/绑定/Profile 地基本身是 v2.1 §十一.2 同款机制，但**按用户指令不回收旧代码**，
  全部从 AM4 干净基线按 v2.1 重新落码（下一个空闲迁移号从 V46 重新起用）。
- AgentPhase/RoleRuntimeKind 两文件以 v2.1 语义重写（PRIMARY 阶段补入、DIAGNOSE 降旧版兼容、
  BoundedLlmRoleRunner 定位对齐 §十一.1）。
- 旧确定性三角色路径行为不变是硬约束：兼容构造/兼容路由/兼容提案面全部保留， primary 模式
  走新增面（compilePrimary/startPrimary），不动旧 switch 的存量行为语义（仅外层换新分派路径，
  旧路径经同一路径保语义，逐卡测试钉住）。

---

## 0.1 跨线协调规约（主会话下达，本线遵照执行）

- **Flyway 号段钉死**：R7=V46 起、EN=V60 起、EV/DR=V80 起。本线已落 V46（X1 绑定）、
  V47（X4 主状态机），后续 R7 迁移自 V48 顺延，不越 59；派工单以本条为准，三线不撞号。
- **195 验收窗三线串行**：DR 的 Arena/Flagd 真实故障注入、R7 的 A0/B 门、EN 的真模型验收
  共用 195，演练执行期间其他线真机验证排队。本线 A0/B 门本地不可达本就如实标 NOT_RUN；
  真窗取证（scripts/e2e 断言包）备好后按串行规约约窗执行，不抢窗。

---

## 1. R7-X1 持久角色绑定（v2.1 §十一.2/§十三）——完成

**改动**：
- `V46__r7x1_task_execution_binding.sql`（全新，v2.0 旧件已由用户删除后重占号）：
  rca_task +round_id（default 0，ck≥0），uq_rca_task_key 升 (run_id, round_id, task_key)；
  新表 rca_task_execution_binding（task_id PK→rca_task，role 三元组+release_digest+
  config_epoch+input_refs+expected_output_schema+parent_request_id+required+failure_policy，
  uq(run,round,task_key)，授权 select,insert 给 control_app、revoke publisher_app/public）。
- `TaskExecutionBinding`（domain）：FailurePolicy 枚举、roleDigest 小写 hex64 校验、
  frozenDigest()（LinkedHashMap+InternalCanonicalJsonV1.sha256——Map.of 十对上限教训）。
- 端口 `TaskExecutionBindingRepository` + `PostgresTaskExecutionBindingRepository`
  （JdbcClient+ObjectMapper，`cast(:x as jsonb)` 惯例）；`AlertInMemoryStores.Bindings` 假件
  （uq 冲突抛 DuplicateKeyException）。
- `PostgresRcaTaskRepository`：insert/mapRow 带 round_id。
- `PlanCompiler`：5 参（+bindings）；compile() 同事务逐任务写绑定（role 取自
  agents.require 的 Profile、digest 冻结、outputSchema 冻结件、required=true、DEAD_ON_FAILURE）。
- `AlertAm4Config`/`PersistenceConfig` 装配。

**测试**：PlanCompilerTest 构造点更新；RX07（同 taskKey 跨轮独立/同轮撞唯一键显式拒）
落 `R7PrimaryStateTest`（X4 卡内一并）；全量 `mvn -pl control-app test` 绿。

---

## 2. R7-X3 Profile 完整性（v2.1 §十一.2）——完成

**改动**：`AgentProfile` 重写为 13 件套（+inputSchema/phase/runtimeKind/
requiredCapabilities/maxSteps/terminationPolicy）；7 参兼容构造保留（默认 INVESTIGATE/
deterministic-single-tool/单步/single-pass）——旧三角色装配与存量测试零改动（硬约束）；
紧凑构造器拒 DIAGNOSE（v2.1 已无独立诊断阶段）；outputSchema/inputSchema **递归深冻结**
（修 Map.copyOf 顶层复制缺口，叶子域 String/Number/Boolean/null=可 canonicalize 域）；
digest() 全字段进哈希；promptDigest/inputSchemaDigest/outputSchemaDigest manifest 面。
`AgentPhase` 重写（PRIMARY 首位，DIAGNOSE @Deprecated 编译准入拒绝）；`RoleRuntimeKind`
重写（DETERMINISTIC_SINGLE_TOOL/BOUNDED_LLM）；`AgentRegistry` 补 forRelease/requireExact
（digest 漂移=冒名顶替拒绝）/releaseDigest。

**测试**：RX05（构造后改嵌套 schema 旧 digest 不变）、同名多版本不串用（requireByName
歧义拒绝+requireExact 精确解析）随 X4 批测试补齐后收口；深冻结单测落 AgentProfileTest。

---

## 3. R7-X4 主任务状态机 + R7-X11 主 Agent 决策（v2.1 §三/§四）——完成（检查点/裁决/唤醒面）

**改动**：
- `V47__r7x4_primary_state.sql`：rca_primary_checkpoint（task_id PK，phase∈{PRIMARY_READY,
  WAITING_CHILDREN}，decision_seq/steps_used/batches_used/round_id，input_snapshot_digest，
  final_claims/final_missing_information jsonb——FINAL 先落检查点再进报告相位）；
  rca_delegation_decision（id PK，uq(run,gap)=缺口全 run 只裁一次，uq(run,primary,round,seq)，
  status∈{APPROVED,REJECTED}，reject_reason 封闭码，child_task_id 回填）；授权同 V46 惯例
  （检查点+update，台账+update 限回填）。
- 领域件：`PrimaryCheckpoint`（Phase 枚举、initial/withPhase/withStepAdvanced/withFinal）、
  `DelegationDecision`（APPROVED 无因/REJECTED 必因、withChild 单一变更）；两端口+PG 实现
  （checkpoint=ON CONFLICT upsert+相位 CAS 单语句）+假件（Checkpoints/DelegationDecisions）。
- `PrimaryDecision`（X11）：互斥三分支 TOOL_CALL{tool_id,args}/DELEGATE{requests:[…]}/
  FINAL{claims,missing_information}；**恰好一分支**结构性保证；未声明字段显式拒绝
  （PlanProposal 同纪律）；DELEGATE 请求批形状（gap_id/role_id/question/input_refs/scope/
  requested_budget）解析面钉死。
- `DeterministicSupervisor` 扩三原语（构造 10 参，装配同步）：
  - `startPrimary`（幂等）：compilePrimary 只建主节点+检查点初始 upsert（重驱动不重置计数）；
  - `adjudicateDelegation`（唯一建子任务路径，单事务）：批形状≤2 → 委派批预算≤2 → 逐请求
    去重（批内/repo 台账）→ 角色目录（requireByName 钉唯一版本，歧义=拒绝）→ run 任务上限
    ≤8；APPROVED 原子建 **READY** 子任务（父子归属不是完成依赖边，零边）+冻结绑定
    （parent_request_id=裁决行 id）+台账行；≥1 获批才推进检查点（round+1/batches+1/WAITING_
    CHILDREN），全拒状态不动；拒绝码封闭（BATCH_SHAPE/DELEGATION_BUDGET_EXHAUSTED/
    GAP_ALREADY_ADJUDICATED/DUPLICATE_GAP_IN_BATCH/ROLE_UNKNOWN/RUN_TASK_CAP）；
    同事务内同 gap 只落一行（批内重复撞 uq 会中止 PG 事务——先查本调用已落集，跨事务
    抢先 DuplicateKeyException 兜底以先到者为准）；
  - `wakePrimary`（幂等 CAS）：**按子任务自身 round==检查点当前轮**复判全终态（裁决行记
    请求轮、子任务落新轮——首版按裁决行过滤是真 PG 前假件测试先抓出的缺陷），死亡也是
    终态（DEAD_ON_FAILURE 由主 Agent 下一步看见，不复活）。
- `PlanCompiler.compilePrimary`：PRIMARY 阶段 Profile 校验，单 BLOCKED 主任务+绑定一事务，
  digest=单任务 PlanProposal 摘要（相同主 Profile 相同摘要）。

**测试**（`R7PrimaryStateTest` 18 案全绿）：互斥/未声明字段/批形状解析 4 案；startPrimary
只建主节点+检查点就绪/重驱动计数不重置/非 PRIMARY fail-closed 3 案；裁决获批原子建+
RD04 批内跨决策去重+RD06 role 未注册+RD13 第三批拒不新建不重置+RD28 面任务上限+相位守卫
6 案；唤醒终态含死亡/轮次隔离 2 案；RX07 轮次身份 1 案；存量测试构造点同步（7 文件），
**全量 `mvn -pl control-app test` 绿（存量零弄红）**。

**偏差/注记**：
1. RD13 的"Run 预算不重置"面归 R7a-1/RD14（TOKEN 预算维落地后一并）；本卡钉住
   batches/steps 不重置。
2. RD08（裁决事务提交前后硬杀）是 PG 原子性证据，假件面不可证——登记待 195 具名 IT。
3. wakePrimary 轮次过滤首版按裁决行 round（=请求轮）实现，假件测试红后改按子任务
   round——如实在案：这类问题正是"假件先红绿、真 PG 补证据"两段式的价值面。

---

## 4. R7a-1 模型网关 RCA 适配（v2.1 §二）——完成

**改动**：
- `V48__r7a1_rca_model_call.sql`：rca_model_call（PENDING/SUCCESS/FAILED/UNKNOWN 四态、
  uq(run,task,attempt,action_seq,physical_seq)、usage jsonb+usage_missing+cost 三列、
  invocation_id=平台账本跨账对账锚、budget_reservation_id=ActionGuard 预留锚、
  ck usage_missing→cost 不猜零；grant select,insert,update）。
- `RcaModelCallContext`（§二.2 十六字段；leaseHeartbeat 为实施增补——网关退避活性
  切片需要租约探针，平台上下文 v1.4 同款偏差留痕）；`effectiveDeadline()=min(run,task)`。
- 端口 `RcaModelCallLedger`（open/succeed/fail/markUnknown/findUnsettledByRun）+
  `PostgresRcaModelCallLedger`（PENDING 先行+终态 CAS）+假件 `ModelCalls`。
- `RcaModelGateway` 适配器（复用既有 ModelGateway 路由/重试/熔断面，§二.1 不新抽路由器）：
  **open 失败=零触网**；成功 SUCCESS 带 usage/cost；usage 缺失 SUCCESS+usageMissing
  不猜零（RX19）；DEFERRED/BUDGET/终态失败→FAILED+原因码；平台 LEDGER_WRITE_FAILED→
  UNKNOWN（保守占预算，findUnsettledByRun 供恢复对账不盲重发）。
- `RcaModelEventSink`：平台 MODEL_* 决策事件 → rca_event（GATEWAY_ 前缀）——
  execution_event.pr_revision_id 是 NOT NULL FK，RCA run 无 PR 身份不能直写平台事件表；
  RCA 侧以本汇构造专用 ModelGateway 实例（装配归 X6）。
- `PersistenceConfig`：rcaModelCallLedger/rcaModelEventSink 两 bean；executionLedger
  注入点加 @Qualifier（EventRepository 出现第二实现后按型注入歧义）。

**测试**（`R7ModelGatewayTest` 8 案绿，脚本化 RouteClientPort 零真网）：账本 open 撞键
零触网（client.calls==0+平台账本零行）；成功账实两落+outcome 对账锚；usage 缺失费用
未决；终态失败原因码；平台账本写失败→UNKNOWN+未结算读可见；配额长等待 DEFERRED；
预算预检拒绝+GATEWAY_MODEL_BUDGET_REJECTED 落 rca_event 假件；deadline 已耗尽未发送。
`mvn -pl control-app -am -DskipTests package` + 全量 `mvn -pl control-app test` 绿。

**偏差/注记**：
1. 状态枚举原文 "STARTED" → 落库为 **PENDING**（V15 工具账本四态惯例，§二.3 四态语义
   一一对应，仅首态命名随库惯例）。
2. 原文列名 agent_id/agent_version → 落列 role_id/role_version（§二.2 身份映射裁定，
   不维护两套语义）。
3. physical_seq 首期恒 1：平台网关内联物理重试已在平台账本逐行记账并受
   maxPhysicalCallsPerStep 约束，RCA 账本每逻辑动作一行经 invocation_id 关联；
   "每个物理重试单独预留单独计费"的经济面由平台账+usage 实扣保证，RCA 侧预留/
   结算按逻辑动作走（ActionGuard 接线时复核）。
4. provider_request_id 由平台账本持有（RoutedModelResult 不透出该字段），RCA 账本
   经 invocation_id 关联取得，不重复落不伪造。

---

## 5. R7a-2 动作守卫 + R7-X5 Claim 准入（v2.1 §六/§五）——完成

**改动**：
- `RcaActionGuard`（application.agent）：§六固定顺序组装件——①Run 活跃 ②未取消
  （isActive 覆盖终态族）→ ③generation 栅栏 → ④leaseEpoch 栅栏 → ⑤deadline
  （task.deadlineAt=Instant.MAX 视为无限，与动作 deadline 取 min；过线即拒，不落
  调用记录）→ ⑥角色版本获准（AgentRegistry.requireExact 精确三元组解析，digest
  漂移=冒名顶替拒绝）→ ⑦⑧RunBudgetGate TOKEN 维原子预留 + RcaModelGateway 账本
  先行取得发送资格 → ⑨执行 ⑩有界读取 ⑪结算（usage 实扣 commit/确证未发出
  release/其余 provisional 保守占用）。拒绝以封闭码 `RcaActionGuardReject`
  （RUN_NOT_ACTIVE/GENERATION_FENCE/LEASE_FENCE/DEADLINE_EXCEEDED/
  ROLE_NOT_ADMITTED）；`ModelAction` record 由持久任务/绑定/租约面装配，禁
  ThreadLocal。工具路径不走本类（既有 SingleToolEvidenceAgent 入口即固定顺序，
  角色新增不产生第二条执行入口）。
- `RcaModelCallException` 增补 `zeroNetwork` 标志：仅"我方账本 open 失败"与
  "发送前 deadline 耗尽"为 true（确证未发出），作为 RunBudgetGate.releaseOn 判别。
- `PrimaryClaimAdmission`（X5，纯代码规则）：evidence_refs 只认本 run 已准入工件集，
  越界剥离留痕（NOTE_REFS_STRIPPED）；ROOT_CAUSE 剥离后零有效引用 → 降级 HYPOTHESIS
  （NOTE_DOWNGRADED_NO_EVIDENCE，RD05）；同 claim 内同一来源去重算一份（LinkedHashSet，
  RX20——来源身份=证据行 id，跨角色复述不构成第二份独立来源）。

**测试**（`R7ActionGuardAdmissionTest` 11 案绿）：守卫顺序逐项拒绝 5 案（run 取消/
generation 漂移/lease 漂移/deadline 过线零调用记录/角色 digest 漂移，全部零触网零
预留）；预算三段结算 4 案（成功 TOKEN 实扣 30+账本 COMMITTED+RCA 账本 SUCCESS；
我方账本 open 撞键→release 全额退款；平台账本不可写→UNKNOWN+provisional 保守占用
50；发送后 RequestInvalid 失败→provisional 保守占用+RCA 账本 FAILED）；Claim 准入
2 案（RD05 降级+越界剥离 strippedRefs=2；本 run 引用保留+同源去重算一份）。
门禁：`mvn -pl control-app -am -DskipTests package` 绿 + 全量 `mvn -pl control-app
test` 1154 案 0 失败 0 错误（21 skip 为既有 PG-profile IT 无 PG 跳过，基线同）。

**偏差/注记**：
1. §六原文"记录写失败=确证未发出→release"按账本归属拆两面：**我方** rca_model_call
   open 失败=发送资格未取得=release；**平台**账本 insertStarted 失败（D5 闸）→
   RCA 侧行 UNKNOWN+预算 provisional（保守）。平台 D5 闸同样保证未触网，release
   亦可通过论证，但 RCA 账本 PENDING 行已铸、发送资格已登记，统一保守面
   （UNKNOWN+对账不盲重发）不变式更强——裁定从保守。
2. 拒绝封闭码为守卫语义命名，非 §六原文逐字——映射表见本节改动块。
3. Claim 准入的 validRefs（本 run 已准入工件集）由执行器装配传入；本卡以注入面
   单测，X2 执行器接线时闭环（工件集冻结快照面）。
4. R7ModelGatewayTest 交稿时 import 面残留两处陈旧引用（domain.ai.ModelGateway/
   shared.TokenUsage——平台网关实际居 application 包），本卡修正后 8 案复绿。

---

## 6. R7-X2 通用运行器（v2.1 §三 执行体面）——完成

**改动**：
- 新建 `RoleRunner`（应用层契约）：`runtimeKind()` + `drive(RoleDriveRequest)`；结果 `RoleDriveResult(outcome, evidenceIds, reason)`，六态 EVIDENCE_PRODUCED/NO_DATA/FAILED/WAITING_CHILDREN/FINAL_READY/DELEGATE_REJECTED。
- 新建 `RunnerDirectory`：按 `AgentProfile.runtimeKind` 解析运行器；未部署 kind 抛 `CapabilityUnavailableException`（CAPABILITY_UNAVAILABLE 显式拒绝，准入期而非驱动期）；重复注册 fail-fast；`deployedKinds()` 观察面。
- 新建 `SingleToolRoleRunner`（DETERMINISTIC_SINGLE_TOOL）：旧三确定性执行器兼容适配——role→RoleQueryHandler 映射由装配注入，未知 role 显式拒绝；AgentResult 三态映射。
- 新建 `BoundedLlmRoleRunner`（BOUNDED_LLM）：主 Agent drive 循环——checkpoint 装载 / WAITING_CHILDREN 唤醒（STILL_WAITING 零模型回弹）/ 步数耗尽零模型确定性 FINAL（missing_information=STEPS_EXHAUSTED）/ 守卫内模型调用 / PrimaryDecision 解析（不可解析步进不崩）/ TOOL_CALL 白名单外 TOOL_NOT_ALLOWED / DELEGATE 裁决（获批 WAITING_CHILDREN、拒发 DELEGATE_REJECTED 带封闭码）/ FINAL 经 PrimaryClaimAdmission 准入后 withFinal 落检查点。`PrimaryToolPort` 为函数端口，生产实现随 X6 接线。
- `NativeInvestigationExecutor.drive` 拆固定 switch：按持久化 binding.roleId 经 `AgentRegistry.requireExact`（版本+digest 精确）→ `RunnerDirectory.requireFor` 分发；绑定缺席 / 未知 kind / 未知 role → DEAD+WARN 显式拒绝（不猜角色）；构造面收编（三 agent 参数+metricsExpr 移除）。
- `AlertFlowConfig`：compat handlers（metrics/logs/change，metrics 闭包含 oa_duplicate_orders_current 查询构造）装配 `SingleToolRoleRunner`+`RunnerDirectory` 注入执行器。

**测试**：
- `R7RoleRunnerTest` 9 案全绿：**RX01 第四同运行器角色纯配置证明**（code-searcher/BOUNDED_LLM 纯种子即驱动 TOOL_CALL 产出证据，代码零改动）、RX03 未知 runtime_kind 零触网显式拒绝+deployedKinds 两 kind 在册、未知 role 拒绝、WAITING_CHILDREN 零模型、唤醒后 TOOL_CALL 账本 SUCCESS、白名单外 TOOL_NOT_ALLOWED、FINAL 准入（ROOT_CAUSE 零引用→HYPOTHESIS 降级+NOTE、跨 run 引用剥离）、步数耗尽零模型 FINAL_READY、DECISION_UNPARSEABLE 步进。
- 三处存量构造点同步（NativeInvestigationExecutorTest compatRunners helper、Am6NativeFullChainIT、ExA3DriverRecoveryIT——真 PG 面传 PG 绑定仓库+registry+目录）。
- 全量 `mvn -pl control-app test`：**1163 例 0 失败 0 错误 21 跳过 BUILD SUCCESS**（r7-x2-fulltest.log；+9=R7RoleRunnerTest，存量零弄红——旧三角色路径经兼容适配行为不变）。
- package 门绿（r7-x2-package.log 静默 + exec.jar 03:15 落盘）。

**偏差/注记**：
1. BoundedLlmRoleRunner 的生产 PrimaryToolPort（ToolGateway+budget TOOL_CALL 消费）与不动点驱动循环接线归 X6 装配卡——本卡交付契约/目录/两实现与执行器分发收编，测试用 stub 端口。
2. 执行器构造签名收编属行为中性重构，但触及 3 处存量构造点——如实在案；驱动语义（DEAD/结果映射）逐案不变，NativeInvestigationExecutorTest 8/8 绿即证据。
3. taskKey 不再承载角色身份：分发只看 binding.roleId（X1 持久绑定面）；PLAN 编译面 taskKey 语义不动。

---


## 7. R7-X6 装配与版本（v2.1 §九/§十一）——完成

**改动**：
- `AlertAm4Config`（infrastructure.config）：主模式总装——①`am4AgentRegistry` 增主
  Profile 感知：`app.alert.r7.primary.enabled=true` 且 primary Profile bean 在场时
  `AgentRegistry.forRelease(release-digest, [metrics, logs, change, primary])`
  （release-digest 空白显式 IllegalStateException——版本身份面不猜）；关闭时旧三角色
  `new AgentRegistry([...])` 逐字节旧行为。②`am4PrimaryProfile`（enabled=false 返
  null=零行为变化）：name=primary / phase=PRIMARY / runtimeKind=BOUNDED_LLM /
  allowlist={prometheus.query, logs.query} / 预算 {STEP: max-steps, TOOL_CALL:
  tool-call-budget, TOKEN: budget-tokens} / termination=deterministic-final-on-exhaustion；
  旋钮 `app.alert.r7.primary.{enabled, release-digest, max-steps(8), tool-allowlist,
  budget-tokens(60000), tool-call-budget, prompt, prompt-version}`。③`am4RcaModelGateway`：
  M3ModelGatewayConfig 工厂出平台网关（EventSink=RcaModelEventSink→rca_event，无
  pr_revision FK）+ RcaModelCallLedger 包装成 RcaModelGateway——模型路由/客户端构造
  零复制（M3 buildModelGateway 工厂方法抽取，原 @Bean 方法逐参透传委托）。④
  `am4PrimaryToolPort`：三工具 Agent 组成 delegates 表 + 调用账本 →
  PrimaryGatewayToolPort。⑤`am4BoundedLlmRoleRunner`：RcaActionGuard(runs, tasks,
  registry, budgetGate, rcaGateway)+BoundedLlmRoleRunner(guard, supervisor,
  checkpoints, evidence, port, mapper)；enabled 时 gateway/port 缺件显式
  requireNonNull 拒绝启动。
- `PrimaryGatewayToolPort`（application.agent，生产受控口）：delegates 查表（未知
  工具=ToolControlPlaneException UNKNOWN_TOOL）；callSeq=工具账本该任务恢复行
  max+1 跨步单调；EVIDENCE_PRODUCED→evidenceId / NO_DATA、FAILED→模型可见异常上抛
  （运行器计步面消费）。
- `PrimaryFinalClaimProjector`（application.agent）：检查点 final_claims →
  ClaimStore 确定性投影——ROOT_CAUSE→TRUE（sources≥2=MULTI_SOURCE_CONSISTENT，
  单源=SINGLE_SOURCE）/EXCLUSION→FALSE/其余 UNKNOWN；kind 解析失败缺省 HYPOTHESIS；
  零有效引用行跳过 WARN（ClaimVerdict 构造禁空 refs）；scope/time_range/policy_version
  (r7-primary-v1)/snapshotDigest 随行。
- `NativeInvestigationExecutor`（X6 接线）：构造增 checkpoints+primaryProfile 两参
  （null=主模式关闭，旧兼容路由零变化）；②主模式 startPrimary（不读提案段——主
  Profile 即编译源）否则旧 proposalOf fail-closed；⑥主模式检查点提案投影替代
  NativeRcaAgent 证据推导面（报告/适配/验证链共用）；③④驱动改不动点 sweep：
  drivableTasks 每 sweep 重读（委派批中途生长 READY 子任务）、MAX_DRIVE_SWEEPS=8
  防御上限、callSeq 跨 sweep 单调；BOUNDED_LLM 分派 drivePrimary 循环——
  FINAL_READY→DONE / WAITING_CHILDREN→返回 true（子任务行已生长=行迁移进展，
  sweep 转驱子任务后下轮唤醒续走）/ 可重试失败封闭集（DECISION_UNPARSEABLE/
  TOOL_NOT_ALLOWED/TOOL_RETRYABLE:*）继续循环 / 未知 FAILED→DEAD / 迭代上限
  （maxSteps*2+2+4）→DEAD（DELEGATE 空转防御，不静默无限烧模型）。
- `PrimaryCheckpoint`（X6 缺陷修复，决策序语义收紧）：withStepAdvanced 改为
  decisionSeq+1 **且** stepsUsed+1（原只步进）——decision_seq=已出决策序（每步模型
  动作占一序），保证 guard TOKEN 预留键与 rca_model_call.action_seq 跨步单调不重复
  （重复键=InMemoryRunBudgetLedger.reserve 幂等假成功不入新账+结算踩已结算条目，
  真 PG 面=唯一键拒杀）；新增 withDecisionAdvanced（DELEGATE 批全拒：决策已出但
  步不动，动作序仍推进——空转防御的同律）。
- `DeterministicSupervisor.adjudicateDelegation`：委派目标守卫——目标 Profile
  phase≠INVESTIGATE（含 primary 自身/未来规划角色）按 REJ_ROLE_UNKNOWN 拒绝，
  不建子任务（主 Agent 不递归自委派）。
- `AlertFlowConfig`：nativeInvestigationExecutor 装配点同步（checkpoints/bounded
  runner/primaryProfile 三 ObjectProvider 注入；runner 目录=compat 单工具+bounded
  双注册，primary 关闭时单注册）。
- 存量测试构造点 3 处同步（NativeInvestigationExecutorTest×2、Am6NativeFullChainIT、
  ExA3DriverRecoveryIT——PG 面传 PostgresPrimaryCheckpointRepository+null primary）。

**测试**（新增 `R7PrimaryModeExecutorTest` 6 案绿——A0 门 CI 侧 harness 种子，脚本化
stub 模型零触网；FINAL 脚本经取证钩子懒入队，证据 id 就绪后才有引用可写，不预灌）：
①主模式全链（零提案段→startPrimary 单节点→TOOL_CALL 直查→FINAL→投影→带引用报告
→finishTask SUCCEEDED，bundles 全程空=编译源为主 Profile）；②委派批中途生长子任务
（sweep 转驱结清→唤醒续走 FINAL，batchesUsed=1/roundId=1，claim 来源=子任务证据行
loki）；③委派目标=主 Agent 自身→REJ_ROLE_UNKNOWN 显式拒绝零子任务；④三步模型链
动作序单调（TOKEN 实扣 90=3×usage 30、rca_model_call.action_seq 三行不重复——
decisionSeq 收紧的红绿锚，重复键在假件即撞唯一键/账面异常）；⑤主模式关闭回归旧
提案路由（PROPOSAL_MISSING fail-closed）；⑥投影映射四规则（双源 TRUE 多源一致/
EXCLUSION FALSE 单源/零引用跳过/未知 kind→HYPOTHESIS UNKNOWN）。
- 回归门禁：定向 41 案绿（新 6+R7RoleRunner 9+R7PrimaryState 18+存量执行器 8，
  r7-x6-run1.log）；全量 `mvn -pl control-app test` **1169 例 0 失败 0 错误 21 跳过
  BUILD SUCCESS**（r7-x6-full1.log，+6=新执行器测试，存量零弄红）；package 门绿
  （r7-x6-pkg.log 静默 + exec.jar 04:08 落盘）。

**偏差/注记**：
1. **sweep 进展语义裁定（X6 实现修正）**：drivePrimary 于 WAITING_CHILDREN 返回
   true（子任务行已原子生长=任务行迁移进展）——若返回 false，sweep 首轮即无进展
   退出，委派子任务永不被驱动（for 迭代清单每 sweep 快照）。STILL_WAITING 重驱计
   一次进展属保守放行：子任务当轮/次轮必结清（兼容运行器只有 DONE/DEAD 两终态），
   不动点收敛面不变；异常情形（子任务悬死）由 MAX_DRIVE_SWEEPS=8 上限诚实终态失败。
2. decision_seq 语义收紧属 X4 建卡时的语义空洞（当时无动作身份消费者），本卡接线
   guard 预留键/账本 action_seq 时补齐；R7PrimaryStateTest 18 案零改动全绿（无案
   钉死旧双计数耦合）。
3. 检查点→ClaimStore 投影映射（ROOT_CAUSE→TRUE 单源即 TRUE/SINGLE_SOURCE）为装配
   期裁定，R7c P1-01 若落"单源 ROOT_CAUSE 需复核"规则则在 Reducer/投影层收紧，
   接口不变。
4. M3ModelGatewayConfig 出工厂方法 buildModelGateway（原 @Bean 方法体平移）——
   RCA 侧网关须以 RcaModelEventSink 为账面（rca_event 无 pr_revision FK），平台侧
   构造不可原样复用；工厂逐参透传，平台 @Bean 行为不变（ModelGatewayTest 全绿）。
5. `app.alert.r7.primary.*` 模型路由键与平台 `app.model.*` 键面各自独立读取
   （AlertAm4Config @Value 透传给 M3 工厂）——配置面有键名重复，收敛归后续装配
   债（登记偏差清单；不阻塞主模式门禁）。
6. 195 真模型验收（A0 真供应商回执关联链/B 门放量窗）按 §0.1 串行规约排队，
   本卡只交 CI 面 harness 与 NOT_RUN 如实登记（见 §8）。

---

## 8. R7-A0 门 / B 门取证（CI 面已落，195 真窗如实 NOT_RUN）

**A0 门**（真实 checkout 现场→主 Agent 受限指标查询→真实执行→带引用报告；零子 Agent；
供应商回执关联完整）：
- **CI 面 harness 具名绿**（`R7PrimaryModeExecutorTest.primaryModeFullChainProducesReferencedReport`，
  §7 测试①）：checkout 告警场景→主 Profile 单节点编译（零提案段）→脚本化 stub 模型决策
  prometheus.query（allowlist 内）→受控口取证落证据行→FINAL 提案→PrimaryClaimAdmission
  准入→检查点投影 ClaimStore（引用=本 run 证据行、来源=prometheus、快照摘要=冻结件）→
  报告 STRUCTURE_VALIDATED→finishTask SUCCEEDED。零子 Agent 全程断言（单模型调用链 2 次）；
  供应商回执关联面（rca_model_call.action_seq 单调+invocation_id 对账锚+usage 实扣）由
  §7 测试④与 R7ActionGuardAdmissionTest 预算三段案在假件面具名。
- **195 真模型窗：NOT_RUN**（§0.1 三线串行规约排队；真实 checkout 现场、真供应商回执、
  真实取证数据不在本地可达面）。scripts/e2e 断言包未备——登记遗留（§11）。

**B 门**（§七四场景 + 三臂同预算盲评）：
- 四场景 CI 面各自有具名绿：零委派=§7 测试①；必要委派=§7 测试②；双专家=批形状双请求
  获批原子建（R7PrimaryStateTest）；有界收敛=步数耗尽确定性兜底 FINAL 零模型
  （R7RoleRunnerTest）+迭代上限 DEAD（执行器防御面）。
- **三臂同预算盲评：NOT_RUN**（真模型+冻结数据快照+人工标注，仅 195 窗可执行）。
  R7-A1（增强线硬门未解锁）按卡面裁定 **NOT_READY**，A0 冒烟不冒充日志+代码闭环。

---

## 9. 56 例状态表（RX01-36 + RD01-20，逐例如实标注）

状态语义：**PASS(CI)**=具名测试在假件/脚本化 stub 面绿（类名可查）；**NOT_RUN**=需 195
真窗（真模型/真 PG 原子性/真竞争现场）；**NOT_IMPLEMENTED**=面未建（范围外钉死或后期卡）。
PASS(CI)+NOT_RUN 并列=执行约束面已证、现场面待窗——按测试纪律不做 PASS 占位。

| 用例 | 状态 | 证据/缺口 |
|---|---|---|
| RX01 E | PASS(CI) | R7RoleRunnerTest rx01 第四角色纯配置即分派执行（目录解析+TOOL_CALL 产出） |
| RX02 E | PASS(CI)+NOT_RUN | 绑定只读恢复+恢复分诊（NativeInvestigationExecutorTest EX-A3 面）；真 worker 重启窗待 195 |
| RX03 E | PASS(CI) | R7RoleRunnerTest rx03 零触网显式拒绝+执行器绑定缺席 DEAD |
| RX04 E | PASS(CI)+NOT_RUN | requireExact 精确三元组/digest 漂移拒绝（AgentRegistryTest+R7ActionGuardAdmissionTest）；双 Run 并行现场待窗 |
| RX05 E | PASS(CI) | AgentProfileTest 嵌套 schema 深冻结 digest 不变 |
| RX06 E | PASS(CI) | R7ActionGuardAdmissionTest 跨 run 引用越界剥离 |
| RX07 E | PASS(CI) | R7PrimaryStateTest RX07 轮次身份/唯一键 |
| RX08 E | PASS(CI)+NOT_RUN | 缺源降级 DEAD/UNKNOWN_TOOL 计步/兜底 FINAL 缺口说明具名；报告 PARTIAL 断言未单列（待窗） |
| RX09 E | NOT_RUN | 部分可用现场（change 续取证+PARTIAL）待 195；兼容面回放 MISS 降级具名同律 |
| RX10 E | PASS(CI) | R7RoleRunnerTest 越权 TOOL_NOT_ALLOWED 计步+委派目标守卫 REJ_ROLE_UNKNOWN |
| RX11 E | PASS(CI) | RunBudgetGateTest 单一预算所有者+守卫预算三段案（失败者零双扣） |
| RX12 E | PASS(CI) | R7PrimaryStateTest RD13 面第三批拒/计数不重置 |
| RX13 E | NOT_RUN | 裁决事务原子性=真 PG 证据（§3 偏差 2 已登记具名 IT 待窗） |
| RX14 E | PASS(CI) | 步数耗尽兜底 FINAL 零模型+DECISION_UNPARSEABLE 计步+未知 FAILED→DEAD |
| RX15 E | PASS(CI) | R7PrimaryStateTest RD04 批内+台账 gap 去重 |
| RX16 E | NOT_IMPLEMENTED | 隐藏上游无依赖边校验面未建（首期零边设计），如实标注 |
| RX17 E | PASS(CI)+NOT_IMPLEMENTED | required=DEAD_ON_FAILURE 冻结面具名；optional 策略分支无写入方，面未建 |
| RX18 E | PASS(CI)+NOT_RUN | 守卫 RUN_NOT_ACTIVE/LEASE_FENCE 具名；真 Cancel/失租竞争窗待 195 |
| RX19 E | PASS(CI) | R7ModelGatewayTest usage 缺失费用未决不猜零 |
| RX20 E+B | PASS(CI) | R7ActionGuardAdmissionTest RX20 同源去重复述不算第二源 |
| RX21 E | NOT_IMPLEMENTED | findings 不可信注入专项面未建 |
| RX22 E+L | NOT_RUN | 真模型+trace 现场（L 模式）待窗 |
| RX23 E+B | NOT_IMPLEMENTED | trace 采样面（trace 专家=X7 后期卡）未建 |
| RX24 E+L | NOT_RUN | 变更反证保留（真模型窗） |
| RX25 E+B | NOT_IMPLEMENTED | Reviewer 反证复核=X8 后期卡 |
| RX26 E+B | NOT_IMPLEMENTED | 同上（X8） |
| RX27 E | NOT_IMPLEMENTED | Reviewer 超时/预算有界（X8） |
| RX28 E | PASS(CI) | R7PrimaryStateTest RUN_TASK_CAP run 任务上限案 |
| RX29 E | PASS(CI)+NOT_RUN | forRelease 集合随 Run 冻结（旧 Run requireExact 锚不漂移）+装配切换回归案；现场并行待窗 |
| RX30 E | PASS(CI) | in-flight 热迁移首期拒绝（绑定冻结+digest 漂移拒绝同律具名） |
| RX31 E | PASS(CI) | 撤销 role 后 requireExact 拒（冒名顶替拒绝具名）；重启现场待窗 |
| RX32 E | NOT_IMPLEMENTED | 前端（范围外钉死） |
| RX33 E | NOT_IMPLEMENTED | 处置建议=报告后置面未建 |
| RX34 E | NOT_IMPLEMENTED | SkillCurator=报告后异步面未建 |
| RX35 B | NOT_RUN | 三臂盲评=B 门本体待窗 |
| RX36 B+L | NOT_RUN | 触发选择真模型验证待窗 |
| RD01 E+L | PASS(CI)+NOT_RUN | R7PrimaryModeExecutorTest ①全链（零子任务/引用有效）；真模型窗待 |
| RD02 E+L | PASS(CI)+NOT_RUN | §7 测试②委派闭环（专家结清→主续走）；真 LLM 循环窗待 |
| RD03 E+L | PASS(CI)+NOT_RUN | R7PrimaryStateTest 批形状双请求获批原子建；串行/并发调度窗待 |
| RD04 E+B | PASS(CI) | R7PrimaryStateTest RD04 超额/重复请求拒绝+预算受控 |
| RD05 E+B | PASS(CI) | R7ActionGuardAdmissionTest RD05 零证据 ROOT_CAUSE 降级未决 |
| RD06 E | PASS(CI) | R7PrimaryStateTest RD06 role 未注册结构化拒绝+§7 测试③自委派拒 |
| RD07 E | PASS(CI) | 子任务绑 INVESTIGATE（目标守卫拒 primary 具名）+专家=单工具单驱无决策面（结构性无孙任务） |
| RD08 E | NOT_RUN | 批次事务提交前/后硬杀=真 PG 原子性窗（§3 偏差 2） |
| RD09 E | PASS(CI)+NOT_RUN | 检查点持久+领养恢复+唤醒幂等具名；真 worker 重启窗待 |
| RD10 E | PASS(CI)+NOT_RUN | R7PrimaryModeExecutorTest ④ decision_seq/action_seq 单调不重复+wakePrimary 幂等 CAS；两 driver 竞争窗待 |
| RD11 E | PASS(CI)+NOT_RUN | 守卫 RUN_NOT_ACTIVE/LEASE_FENCE 具名；等待期取消竞争窗待 |
| RD12 E | PASS(CI)+NOT_RUN | 唤醒终态含死亡（R7PrimaryStateTest）+子 DEAD 主带缺口继续/兜底；真 PARTIAL 窗待 |
| RD13 E | PASS(CI) | R7PrimaryStateTest RD13 第三批拒不新建不重置 |
| RD14 E | PASS(CI)+NOT_RUN | 共享账本原子预留+TOKEN 维实扣（守卫三段案+§7 测试④）；多专家边界窗待 |
| RD15 E | NOT_IMPLEMENTED | 代码工具=X10 范围外（R7-A1 同律 NOT_READY） |
| RD16 E+B | NOT_IMPLEMENTED | 同上 |
| RD17 E | NOT_IMPLEMENTED | 同上 |
| RD18 E | NOT_IMPLEMENTED | Prompt 热切换=增强线安全点协议（R7-A1 NOT_READY）；in-flight 拒绝面已钉（RX30） |
| RD19 E | PASS(CI)+NOT_IMPLEMENTED | forRelease 旧 Run 锁旧集合/新 Run 新集合（装配+requireExact 锚具名）；前端显示面范围外 |
| RD20 B+L | NOT_RUN | 三臂联合报告=B 门本体待窗 |

计数：PASS(CI)（含与 NOT_RUN/NOT_IMPLEMENTED 混合行）35 例；NOT_RUN 主责 8 例
（RX09/RX13/RX22/RX24/RX35/RX36/RD08/RD20）；NOT_IMPLEMENTED 主责 13 例（范围外
钉死 4：RX32/RD15/RD16/RD17——RD19 前端显示面为混合行；后期卡 5：RX23/RX25/RX26/
RX27/RX34；面未建 3：RX16/RX21/RX33；NOT_READY 同律 1：RD18——R7-A1 硬门未解锁）。
无一例填 PASS 占位——每 PASS 行均有具名测试类可查，每 NOT 行均有归因。

---

## 10. 迁移清单与 DDL（R7 号段 V46-V59 内，本线实占 V46/V47/V48）

| 版本 | 文件 | 内容 |
|---|---|---|
| V46 | `control-app/src/main/resources/db/migration/V46__r7x1_task_execution_binding.sql` | rca_task +round_id（default 0，ck≥0）；uq_rca_task_key 升 (run_id, round_id, task_key)；新表 rca_task_execution_binding（task_id PK→rca_task、role_id/role_version/role_digest、release_digest、config_epoch、input_refs jsonb、expected_output_schema jsonb、parent_request_id、required、failure_policy；uq(run,round,task_key)） |
| V47 | `control-app/src/main/resources/db/migration/V47__r7x4_primary_state.sql` | 新表 rca_primary_checkpoint（task_id PK、phase∈{PRIMARY_READY,WAITING_CHILDREN}、decision_seq/steps_used/batches_used/round_id、input_snapshot_digest、final_claims/final_missing_information jsonb）；新表 rca_delegation_decision（id PK、uq(run,gap)、uq(run,primary,round,seq)、status∈{APPROVED,REJECTED}、reject_reason、child_task_id） |
| V48 | `control-app/src/main/resources/db/migration/V48__r7a1_rca_model_call.sql` | 新表 rca_model_call（四态 PENDING/SUCCESS/FAILED/UNKNOWN、uq(run,task,attempt,action_seq,physical_seq)、usage jsonb+usage_missing+cost 三列、invocation_id、budget_reservation_id、ck usage_missing→cost null） |

授权惯例三表一致（control_app select/insert[/update]，revoke publisher_app/public）；完整
DDL 以迁移文件为准（本表为清单摘要）。X6 装配卡零新迁移。V49-V59 留 R7 后续卡。

---

## 11. 偏差与遗留清单（评审入口，逐卡详情见各节偏差块）

**偏差（已裁定，附理由）**：
1. sweep 进展语义：WAITING_CHILDREN 返回 true（子任务行生长=进展）——防首 sweep 假收敛
   退出（§7 偏差 1）。
2. decision_seq 语义收紧：每步模型动作占一序（X6 缺陷修复，动作身份单调面）（§7）。
3. 检查点→Claim 投影映射为装配期裁定（单源 ROOT_CAUSE 即 TRUE/SINGLE_SOURCE），R7c 可收紧（§7）。
4. M3 网关工厂抽取（RCA 侧 EventSink 面不可复用平台构造）；RCA 模型路由键与平台键面
   独立读取，配置键名重复登记为装配债（§7 偏差 5）。
5. rca_model_call 状态名 STARTED→PENDING；列名 agent_*→role_*；physical_seq 首期恒 1；
   provider_request_id 经 invocation_id 关联不重复落（§4 偏差 1-4）。
6. 平台账本写失败→UNKNOWN+provisional 保守占用（§5 偏差 1，从保守裁定）。
7. wakePrimary 轮次过滤首版实现缺陷假件先红（§3 偏差 3，两段式价值面如实留痕）。
8. RX13/RD08 类真 PG 原子性用例本地不可证——具名 IT 待 195 窗（§3 偏差 2）。

**遗留（待窗/待卡）**：
1. 195 验收窗（§0.1 串行）：A0 真供应商回执关联链、B 门三臂盲评、RX13/RD08 真 PG 具名
   IT、RX02/RD09 真 worker 重启面、全部 E+L/B+L 例。scripts/e2e 真窗断言包未备——
   备好后按串行规约约窗，不抢窗。
2. R7-A1/X10：NOT_READY（增强线编码硬门未解锁），RD15/16/17 与 RD18 相应面未建。
3. X7/X8/X9 后期卡与 RX16/RX21/RX33 面：本期范围外，面未建如实标注。
4. 主模式生产放量旋钮 `app.alert.r7.primary.enabled` 默认 false——放量由 B 门数据与
   用户裁定驱动，装配面只保证开关语义（关闭=逐字节旧行为，§7 测试⑤）。

---

## 12. 收官声明（本线交付态）

- 交付序：X1→X3→X4+X11→R7a-1→R7a-2+X5→X2→X6 全部落账（§1-§7），A0/B 门 CI 面已落、
  真窗 NOT_RUN 如实登记（§8），56 例逐例状态表（§9），迁移清单（§10），偏差与遗留（§11）。
- 最终门禁：全量 `mvn -pl control-app test` **1169 例 0 失败 0 错误 21 跳过 BUILD SUCCESS**
  （r7-x6-full1.log，21 skip=既有 PG-profile IT 无 PG，基线同）；
  `mvn -pl control-app -am -DskipTests package` 绿（r7-x6-pkg-am.log 静默，
  control-app-0.0.1-SNAPSHOT-exec.jar 04:14 落盘）。
- 存量零弄红：旧确定性三角色路径与平台网关行为逐卡具名回归（NativeInvestigationExecutorTest
  8/8、ModelGatewayTest、PlanCompilerTest、DeterministicSupervisorTest 等全绿）。
- 本线收口待主会话独立评审核验（执行卡 §最终交付）。


## §13 真窗 E2E 断言包备件（§11 遗留① 清偿）——2026-09-11 备好未执行

用户指令："把遗留里登记的 scripts/e2e 真窗断言包备出来（A0 真供应商回执链、B 门三臂盲评、
RX13/RD08 真 PG 原子性、真 worker 重启），备好放着不抢窗，等合并窗统一约 195。"

**交付**（`docs/测试证据/R7/e2e-脚本/`，六件，沿 AM6 e2e-am6-common.sh house style）：
- `e2e-r7-common.sh` ——公共库：r7_ 前缀脱敏/轮询/只读 SQL/HTTP/终态轮询（finished_at 面，
  V12 活跃集）/绑定快照/容器 SIGKILL+拉起助手（R7_ALLOW_DESTRUCTIVE=1 显式闸）；
- `e2e-r7-a0-provider-receipt-chain.sh` ——A0 门（执行卡 #9）：真实 checkout 现场→主 Agent
  受限直查→带引用报告；断言零子 Agent（DELEGATE-% 恒零）/绑定三元组/检查点 final_claims/
  allowlist 工具面/evidence_refs 全可解析（不预灌答案）/rca_model_call 全 SUCCESS usage-cost
  结清/invocation_id↔model_call_ledger 跨账对账+物理合计 tokens≥逻辑行/report→publication→
  outbox 生产链/供应商密钥词形零回显；
- `e2e-r7-b-gate-three-arm.sh` ——B 门（执行卡 #10）三步式 armB|armC|joint：姿态行为面核验
  （不信任部署证词）；joint 汇总两臂 claims/tokens/latency/委派数 + 盲评封存（blind-cases.json
  无臂标签交评分者；arm-map.json 600 操作员保管）+ 臂A BLOCKED_EXTERNAL 入账；
- `e2e-r7-pg-atomicity-rx13-rd08.sh` ——RX13/RD08：委派诱导案在飞时 docker kill（SIGKILL，
  非 compose stop）×N 轮（延迟档 0/1.5s/4s 循环）→拉起→终态+不变式 I1~I6 全扫（无半套图/
  不重复子任务/不重复派发/批次计数一致/任务↔绑定双射/零重复物理调用/同 incident 恰 1 run）；
- `e2e-r7-worker-restart-rx02-rd09.sh` ——RX02/RD09：等 WAITING_CHILDREN+委派批在场→杀前
  绑定/检查点/裁决三快照→SIGKILL+拉起→断言绑定快照逐行恒等（恢复只读不漂移）/模型调用
  身份全落绑定三元组内（不猜 latest）/任务零 DEAD/检查点计数单调（awk 前后比对）/零重复
  执行计费/重启时刻证据；
- `e2e-r7-RUNBOOK.md` ——前置（env 锚点/两态部署/破坏闸/服务面）+ 七步串行顺序 + 通过判据 +
  风险登记 + 证据布局 + 备好未执行诚实声明。

**断言面静态核对锚**：V46（绑定表列）/V47（检查点+裁决台账列与 uq）/V48（rca_model_call
四态与 uq/ck）/V15（工具账本）/V16（证据）/V17（claim）/V5（model_call_ledger）/V12
（rca_run 终态域）+ canary_route_decision 路由面（AM6 已验 SQL 形照抄）；bundle 内容形态沿
e2e-am6-07 已验 JSON（percent=0+白名单外科式，native proposal 保形）。

**关键裁定**：① A0/B 门 service 用真实服务 checkout（合成服务名查空证据违反 A0 语义），
场景身份在 alertname 侧 stickiness 白名单；② 硬杀档位=观测归档（PRE_COMMIT/POST_COMMIT/
POST_TERMINAL 由杀时 finished_at 快照+裁决行存在性判定），非精确注入——真模型时序不可控，
诚实面代替假精度；③ 臂A（零委派）=BLOCKED_EXTERNAL：max_delegation_batches=2 是
DeterministicSupervisor 编译常量、无运行时旋钮——旋钮化登记为 B 门执行前置债；④ 臂B=legacy
确定性 handler 基线（SingleToolRoleRunner 零模型调用零 token），非方案原文"LLM 三角色"——
joint-report 带 armB_semantics_note，盲评结论必须携带该口径；⑤ 四案=同真实服务四独立
episode，场景意图经 annotations.summary 差异注入，语义打分归独立评分者。

**⚠ 新风险登记（本节首次入账，此前任何台账未载）**：RCA→平台账本跨域 FK 面——
RcaModelGateway.java:87 以 (ctx.runId(), ctx.runId(), ctx.taskId(), ctx.attemptId()) 构造平台
上下文，ModelGateway.java:221 照抄为 model_call_ledger.review_run_id/run_step_id/attempt_id
（V5 三列 FK 指向 PR 域 review_run/run_step/step_attempt）——rca id 在三表无行，静态推演
23503 面必然违反。历史零实证原因：legacy 三角色路径零模型调用（AM6 真窗全链绿不覆盖本面），
R7 全部测试跑 fake/in-memory 账本。预期失败签名：rca_model_call 全 UNKNOWN+error_code=
LEDGER_WRITE_FAILED+control-app 日志「账本 STARTED 写失败，零触网（D5）」≥1（A0 phase8
已落证该计数）。若实证成立=R7 线真缺陷（开 BUGLOG 新卡，修复候选：rca 侧独立账本分表或
FK 面裁定豁免），修复后重跑 A0；断言包不改口径放行。

**校验方式（诚实）**：本机无 POSIX shell 解释器（bash/wsl/docker/busybox 全无）——
Python shlex 校验（先按子壳引号模型掩码 $(…) 跨度再线性配对）五件全过；块关键字
（if/fi、循环 done、case/esac）与括号平衡静态检查的若干标记逐处人工读定为检查器伪影
（case 竖线模式、's/'"'"'//g' 惯语、$() 内嵌多行 SQL、$() 内行续符均合法 POSIX）。
最终语法门=RUNBOOK 步骤 0：195 侧 `sh -n e2e-r7-*.sh` 全过才进场景执行。

**约窗纪律（不抢窗）**：全部脚本未在 195 执行、零运行证据；合并窗按 RUNBOOK §三串行——
语法门 → A0（无破坏）→ RX13/RD08（R7_ALLOW_DESTRUCTIVE=1，窗内 195 独占）→ RX02/RD09 →
armC → 操作员切姿态 → armB → joint → 窗后回填本日志 §8/§9 状态表（A0/B 门取证行与
RX02/RX13/RD08 NOT_RUN 行改写）+ 新缺陷当场开 BUGLOG。通过判据：执行过的场景
scenario-results.json 全 PASS，BLOCKED_EXTERNAL 仅允许臂A 一行。

## §14 真窗执行（2026-09-11 合并窗，195 实证与连环修复）

**窗序**：RUNBOOK 步骤 0 语法终门 195 侧 `sh -n` 五件全过 → A0 三跑两雷连环截获 → 修复部署 → 三跑复核。

**部署姿态面（新增，此前台账未载）**：compose control-app 增 R7 主模式四键映射
（`APP_ALERT_R7_PRIMARY_ENABLED/RELEASE_DIGEST/AGENT_MODEL/OPENAI_COMPAT_BASE_URL`，
缺省 OFF/代码内置值=旧行为不变）；195 .env 置 ON：release-digest=激活 bundle
99f1e6cf…（revision 22）、AGENT_MODEL=glm-5、BASE_URL=http://litellm-am3:4000/v1
（alert-net 内网共享，litellm 无 master_key 内网开放，模型名 glm-5 上游 dashscope）。

**BA-108（A0 首跑 FAIL 实证）**：run_budget_state 缺 TOKEN 行 → PG fail-closed →
全量 BudgetExhaustedException（余额=0 本次=1500），run 终态 FAILED。
根因双重：executor openRun 只传旧四维（TOKEN 仅在 primaryProfile 从未并入）+
CI 盲区（夹具 generousLimits 自带 TOKEN、InMemory 缺行放行 vs PG 缺行拒绝分叉）。
修复：openRun 限额并集 putIfAbsent；回归案生产形四维驱动（1334 绿）。证据
runs/a0-20260911T042332Z（phase3 FAIL）。

**BA-109（A0 二跑 FAIL 实证，RUNBOOK §四.1 预登记风险面兑现）**：过预算门后
rca_model_call 全 UNKNOWN(TRANSPORT_UNKNOWN)+「账本 STARTED 写失败，零触网（D5）:
DataIntegrityViolationException」——静态推演 23503 面实证。**裁定=候选形态①RCA 侧
独立账本山**：NoOpModelCallLedgerRepository 装配旁路平台写面（放松 FK 裁定为负）；
rca_model_call（V48）唯一账本山，D5 等价门在 RcaModelGateway.open。回归面：
执行器测试夹具改 NoOp 镜像生产（35/35+1334 全绿）；A0 脚本 phase6 断言翻面=
平台账本对 RCA 恒零行（跨域污染防栅，设计修正非口径放行——预登记候选形态）。
证据 runs/a0-20260911T043734Z。

**偏差登记**：①provider_request_id 列暂缺（RoutedModelResult 不透传该字段，
invocation_id 为审计锚，后续如需供应商回执对账再开透传卡）；②InMemory/PG 缺行
语义分叉（InMemory 放行 vs PG fail-closed）对齐=后续债（全量用例种子限额改造，
另立卡）；③ControlArchitectureTest AFT-26 封闭集拦获 javadoc 字面量 3 处
（已改述，红线守住=恰 2 文件）。

**窗内提交**：86788f0（BA-108 修复+compose 四键）→ BA-109 修复批（NoOp+装配+
测试+A0 脚本翻面+RUNBOOK/BUGLOG，提交号见 git log）。

**A0 第八跑（用户指定的执行者接续，suite a0-20260911T063304Z，run=87572b9d）主会话只读诊断**：
phase1~4 全 PASS——**BA-113 修复生效，真模型全链决策循环首次跑通**（rca_tool_invocation 7 笔：
prometheus.query SUCCESS×3、logs.query FAILED INVALID_INPUT×4 重驱不再 DEAD、证据 cd6e80ea
落库被引用）。phase5 FAIL 判读：**ClaimValidator 按设计工作**——模型诚实退出（"缺乏具体故障
现象描述或查询目标，无法进行针对性取证"）被分类 UNKNOWN 而非注水 TRUE（rca_claim c1=
SYMPTOM/ACTIVE/UNKNOWN，引用可解析），phase5 门正确拦下空洞调查。结论：执行链零缺陷，
缺口=模型能力+盲重驱无错误反馈（logs.query 4 连 INVALID_INPUT 模型拿不到修正依据）。
**"信封 last_error 反馈环"后续卡升级为 A0 phase5 的关键解锁项**；备选路径=评测场景注入
更具体的故障信号使通用查询可见异常。另：执行者首跑撞部署 recreate 竞态（旧容器答 health 后
被 compose up 杀掉）已自行重发处理，记录在 a0-console-8.log。

**A0 第八跑执行者补充诊断（同 run 只读复算，两新雷 BA-115/116）**：主会话 §14 只载到
phase5 判读；执行者按脚本 phase6/7 断言原文逐条只读复算 run=87572b9d，发现两面上游未载——
①**phase6 必红（BA-115）**：`_nosettled` 精确复算=8——8 笔 rca_model_call 全 SUCCESS/
usage 在场/usage_missing=false，但 cost_micros/pricing_version/currency 三列全 NULL
（PricingService 对 glm-5 无定价项，空 cost 落 NULL 与 ck_no_fake_cost 不冲突但结清语义
要求 cost 必结）；BA-109 翻面断言（平台账本对 RCA 恒零行）则复算为绿（invocation join=0，
平台总 189 行全 PR 域）。②**phase7 必红（BA-116，产品无罪）**：A0 套件 8 次注入全 firing
无 resolved → incident 单 episode generation 恒 0；run4(05:01) 首个 SUCCEEDED 报告已发布
（winner 行+publication+outbox×2 全链在场），run5/6/8 报告皆「输给 generation 发布赢家，
落档不发布」——发布赢家 (incident,generation) 一次语义正确，phase7「本 run 必发布」断言
与 fixture 恒同代际错位，复跑恒红。两雷均只立案待主会话裁定修法（BA-115：定价数据面补
glm-5 vs 缺项显式语义；BA-116：套件唯一 alertname vs resolved 闭环 vs 断言翻面），禁不裁
定改口径。证据包已回拉 `docs/测试证据/R7/runs/a0-run8-20260911/`（console+两套件目录+只读
复算 77 行）。

**换模事件（用户指示，glm-5 额度告罄）**：litellm-am3 原只注册 glm-5 → 执行者在 litellm 配置
（deploy/alert/litellm/config.yaml，备份 .bak-modelswap-*）纯加法增补两候选条目（同
UPSTREAM_BASE/UPSTREAM_KEY 上游端点）：deepseek-v4-flash-0731 + qwen3-max-preview，容器重启
约 90s（含 prisma migrate）后三模型在场。迷你协议实测（严格 JSON tool_call + logs.query
since/until 形状=glm-5 真窗卡点）：两候选全 PASS 且零围栏零废话；选 **qwen3-max-preview**
（token 效率约为 deepseek 40%：164+63 vs 232+153），deepseek 留注册备选（换模只需改
AGENT_MODEL）。.env 备份 .env.bak-modelswap-20260911T082848Z → AGENT_MODEL=qwen3-max-preview
→ control-app 重建 health 200（08:28:52Z）。行为观察：qwen 下 atomicity 单轮恢复 ~11min
（glm-5 为 15m51s，重驱更快）；litellm config 内 cost 仍为占位值（原 TODO 同族），应用侧
PricingService 定价缺口（BA-115）不随换模消失。

**RX13/RD08 真 PG 原子性六 attempt 史（执行者）**：①attempt-1 白名单小写化雷复发（b-gate
同律第三处：白名单匹配对 incident_key 原样大小写敏感，atomicity 脚本 tr 小写化→
BUCKETED_HOLMES 不铸 run→轮询超时）；②attempt-2 终态等待 900s 恰截断——kill 后 run 保持
活跃集 15m51s 才 SUCCEEDED（恢复解剖：kill 时在飞模型调用行 PENDING 诚实保留→重启后同行
结清 SUCCESS、lease_epoch 1→2、全链重驱后终态、无孤儿无半账——**恢复机制本身产品正确**，
是场景等待预算没算租约超时 ~10min+全重驱）；cap 900→1800；③attempt-3 不变式两断言缺陷
（run_cnt 谓词漏 alertname=/service= 标签恒零；no_binding 过宽——NATIVE_INVESTIGATE 确定性
任务本无 LLM 角色绑定，收窄到 PRIMARY/DELEGATE-% 任务）；④attempt-4 三轮全过但 phase3
姿态统计同族谓词误报"零主模式"（round2 时逢换模中断）；⑤attempt-5 新模型三轮全过、phase3
同样误报；⑥谓词修复后 attempt-6 **SUITE PASS**：3 轮（0/1500/4000ms 档）全 SUCCEEDED+
PRE_COMMIT 零残留面+不变式 I1~I6 全绿+在飞命中 3/3——RX13 半套图禁令与 RD08 账本原子性
在真 PG 真硬杀下成立。证据回拉 `docs/测试证据/R7/runs/atomicity-20260911/`（console
4/5/6+套件目录 rounds.txt）。

**RX02/RD09 重启场景四 attempt 史 + 三新雷立案（执行者，BA-117/118/119）**：①attempt-1
qwen 缺省 prompt 注入后零委派（22 步直查收敛，phase2 WAITING_CHILDREN 300s 超时）——
委派是「确需专业能力」的模型自主决策，简案面不触发；fixture 换复杂多域 summary +
prompt 旋钮（APP_ALERT_R7_PRIMARY_PROMPT 透传，compose 增映射，.env 双引号防 bash
source 断行——含空格未引号值会让运行脚本 source 即炸）二次尝试。②attempt-3 qwen +
策略 prompt **委派首次发生**（g1→change APPROVED，决策行+DELEGATE-g1 子任务创建），
但子任务 71ms 即 DEAD（BA-117 雷面首曝），主任务被唤醒后盲重提同 gap 15 次被台账拒
（「委派裁决撞既成台账」不回喂模型）至驱动上限 22 → PRIMARY DEAD（BA-119 雷面首曝）；
phase2 等的 WAITING_CHILDREN 窗口仅百毫秒级不可捕获，300s 超时。③换模 deepseek-v4-
flash-0731（.env.bak-modelswap2-* 备份，AGENT_MODEL 一键切换）attempt-4：phase1 PASS、
phase2 **首咬成功**（deepseek 面子任务存活到轮询捕获 WAITING_CHILDREN+子任务在场，
SIGKILL 打进委派等待窗）、「容器已拉起」恢复面走完、run SUCCEEDED——**套件机制全通**，
phase3 仅因 2 个 DELEGATE 子任务 DEAD 断言红。④恢复面解剖出 BA-118：主任务重启后被
阶段③ triage「result_ref 幂等收尾（零触网）」直接 RUNNING→DONE，checkpoint 冻结
WAITING_CHILDREN（steps_used=1/batches_used=2/final_claims=[]）与任务 DONE 三者并存——
多步 LLM 主任务被单工具语义的幂等收尾绕过了决策环。⑤deepseek 同样撞台账空转 11 次
（10:38:54→10:39:55）后换新 gap 逃逸——BA-119 非模型特异。**场景裁定**：RX02/RD09
套件机制面已验证可用（捕获/击杀/恢复/终态全链走通），product 面（RD09 恢复语义）被
BA-117/118 挡住——修复+部署后重跑即可收口；**armC 动态委派臂同被 BA-117 阻塞**（委派
子任务全灭使「子任务 DONE+证据回流」断言不可达），joint（依赖 armC+armB）顺延。证据
回拉 `docs/测试证据/R7/runs/restart-20260911/`（attempt-3/4 套件目录+diag-ba117-
evidence.txt 五面同框）。诚实记录：本轮 4 次 launcher 预检失败（R7_ALLOW_DESTRUCTIVE/
R7_RELEASE_BEARER/R7_WEBHOOK_BEARER/R7_PG_URL 逐个补齐——195 侧无集中 env 文件，运行
脚本要求的注入面以 RUNBOOK 清单为准）；attempt-4 运行目录落 /root/runs（ssh bash -s
的 CWD 与先前 cd /opt/r7-e2e 不同），证据已归并回拉。背景运行 618d8262（10:34 起，
change_attribution 子任务同面秒死+「route=primary 返回空 content」+主任务 FAILED 回执
孤儿 → DEAD）作旁证归档。

**B 门 armB（固定三角色臂）attempt-2 SUITE PASS（执行者）**：①姿态切换 OFF=操作员步骤
（.env.bak-posture-off-20260911T110337Z 备份 → APP_ALERT_R7_PRIMARY_ENABLED=false →
recreate → health 200 → 容器 env 面核验）；②attempt-1 四案全 SUCCEEDED 后聚合面崩——
脚本 `_runlist` 拼 IN () 时 UUID 未加引号（PG「trailing junk after numeric literal」，
e2e 脚本缺陷非产品面），修一行（每 id 包单引号）双侧 md5 同步后重跑；③attempt-2
**SUITE PASS**：四案 run id 与 attempt-1 全同（同 alertname+summary 的复注被产品调查
去重面吸收=BA-111 语义的诚实实证，零新 run 消耗）+ 姿态行为面 PASS（零 PRIMARY 任务 +
NATIVE_INVESTIGATE DONE×4 = legacy 三角色形态实证）+ scenario-results.json 官方 PASS 章
（deterministic-handler@195，11:10:14Z）；④姿态恢复 ON（.env.bak-posture-on-* 备份，
enabled=true、AGENT_MODEL=deepseek-v4-flash-0731、release digest 99f1e6cf002c 与 M6
收官锚一致、health 200）。诚实边界：armB 对比面是「确定性工程基线」（legacy handler
零模型调用），非 LLM 三角色——语义偏差按脚本头登记入账；armC（主模式动态委派）被
BA-117 阻塞（委派子任务必死使「子任务在场+委派>0」姿态断言不可达），joint（依赖
armC+armB 双臂盲评封存）顺延至修复合并窗。证据回拉 `docs/测试证据/R7/runs/
r7bgate-20260911/`（两轮 console+runs.txt+scenario-results.json+资源日志）。

## §15 V88 反馈环实施（信封 last_error，A0 八跑解锁项）——2026-09-11 傍晚

**动因**：§14 八跑判读——盲重驱=模型连猜同错（logs.query INVALID_INPUT×4 无修正依据），
phase5 被 ClaimValidator 正确拦下。本卡把上一步可重试失败的原因与修正指引持久化到检查点，
随下一步信封 `last_error` 面回喂模型，给重驱一个明确的修正依据。

**改动（7 文件 + 1 迁移，全部本卡路径）**：
- `PrimaryCheckpoint`：record 加第 12 组件 `lastError`；`withStepAdvanced` 签名扩为
  `(snapshotDigest, lastError, now)`；withPhase/withDecisionAdvanced/withFinal 传播。
- `BoundedLlmRoleRunner`：`advanceStep` 四参化；四分支各带中文修正指引——
  DECISION_UNPARSEABLE（严格按协议三形状恰选一纯 JSON）、TOOL_NOT_ALLOWED（不在
  tool_allowlist，按 tool_schemas 重发或走 final）、模型可见族 TOOL_FAILED <reason>
  （修正查询/换工具/走 final）、INVALID_ARGS（严格对照 tool_schemas 的 JSON Schema 修
  args）；成功步传 null 清空。`promptOf` 信封在 valid_artifact_refs 后按检查点非空条件
  注入 `last_error` 键（成功步该面缺席）。
- `PostgresPrimaryCheckpointRepository`：upsert 三处（INSERT 列/VALUES/ON CONFLICT SET）
  + mapRow 加 last_error。
- **V88__r7_checkpoint_last_error.sql**：`rca_primary_checkpoint` 加 `last_error text`（可空，
  存量行零迁移成本）。号段裁定：V87 已被 EV-08（ev/eval-center 分支）占用，本卡取 V88。
- `DeterministicSupervisor:301`：WAITING_CHILDREN 重建检查点构造点传播 lastError。
- 测试适配三处构造点（R7PrimaryStateTest:189 / R7PrimaryModeExecutorTest:378 /
  R7RoleRunnerTest:280）+ R7RoleRunnerTest ba112 用例新增断言：INVALID_ARGS 计步后
  `lastError()` 非空、含 "INVALID_ARGS" 与 "tool_schemas"（反馈环落库实证）。

**测试**：定向 38 例（R7RoleRunnerTest 13 + R7PrimaryStateTest 18 + R7PrimaryModeExecutorTest 7）
全绿；全量 `mvn -pl control-app test` **1338 例 0 失败 0 错误 21 跳过**（PG-profile IT 无 PG
跳过，与基线同），存量零弄红。证据：control-app/target/surefire-reports 汇总
（total=1338 failures=0 errors=0 skipped=21，186 个报告文件）。

**诚实边界**：①反馈环只解决"重驱无修正依据"，不解决模型能力上限——glm-5 能否据此修对
args 仍需 195 真窗第九跑实证（NOT_RUN）；②last_error 为单步浅记忆（只载上一步），连续
多步同错的趋势面未做（后续卡候选）；③V88 迁移未在真 PG 执行（本地无 Docker，
NOT_RUN，并入下次 195 部署窗）。RV08 红线守住：全 diff 无禁词字面量。

**部署裁定**：195 正由用户指定的执行者跑 A0 窗（基线镜像 BA-113@7fd5705），本卡部署
会动其基线——**不主动部署，留待用户裁定合并窗**。
