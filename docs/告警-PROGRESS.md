# 告警 Agent 线 —— 进度总账（断点续做入口）

> 本文件是**告警 Agent 项目**的唯一进度入口。2026-09-04 起文档体系彻底分家：旧 PR-Agent 线全部文档压缩归档至 `docs/archive/pr-agent-line-20260904.tar.gz`（不再使用；git 历史 + m4-wip 分支另存）。
> **文档体系（2026-09-04 晚更新）**：架构基线双文档 = `docs/架构设计-告警Agent-v1.md`（AA-1~26 既有基线，内容版本 v2.2）+ `docs/架构设计-告警Agent-v1.2.md`（FUT-01~55 目标增量，**设计 PASS 待落码**，含 AA↔FUT 追溯矩阵，两文档互补不互替）；执行计划 = `docs/告警Agent-增量实现任务拆解-v1.md`（96 任务，**当前只释放 G0**）；评审裁定 = `docs/架构评审-*.md` 三份；方案系列 = `docs/告警AMx-技术方案.md`；本线账本 = 本文件 + `docs/告警-OSS-证据清单.md` + `docs/告警-BUGLOG.md`。
> 规则：每完成一个任务/工序立即更新；时间线只增不删。

## 项目定位

**交易域告警 RCA Agent**：开源拼装告警平台（Alertmanager + Keep）+ RCA 执行引擎（HolmesGPT 先行，后续 Java 逐步替换）+ 现有仓库 control-app 改造为告警控制面。五层（接口/调度/执行/数据/安全观测）每层抄开源已验证设计，证据入 `docs/告警-OSS-证据清单.md`（待建）。

**已定选型**（2026-09-03 用户确认）：
- 接口层：Alertmanager + Keep 都部署
- 执行层：HolmesGPT 先部署，逐步替换为 Java 实现
- 告警源第一期：OTel Demo（Astronomy Shop）靶子，内置故障开关 + Pumba 注入
- 总原则：能抄开源就不自研；LLM 只提建议，决策权在确定性组件

**里程碑序列**：AM0 平台拼装 → AM1 接入控制面（control-app 改造）→ AM2 交易域化（订单靶场 + 业务故障注入）→ AM3 评测 + 出口 → AM4+ 压力点驱动。

## 当前状态（2026-09-06）

**架构状态**：**已定格**（用户确认）。AA 系列封版于 v2.3；FUT 系列以 v1.2 为准；G0 已收口，冻结纪律继续：所有工作以代码与证据为中心。

**当前阶段**：**AM0~AM3 全部收官；AM4 G1 已通过（2026-09-06 用户批准 v1.3），正式开工（工序 3 编码）**。AM1 G2 ✅ 2026-09-05；AM2 G2 ✅ 2026-09-05；AM3 G2 ✅ 2026-09-06（推送 `574c01f` 先于 G2，纪律满足）。执行依据 = `docs/告警AM4-落码技术方案.md` **v1.3**（M4-01~38 全部解锁按序执行；迁移 V12~V18）；任务树总表 = `docs/告警Agent-增量实现任务拆解-v1.md`。

**移交 AM4 的输入项（AM3 G2 切换清单）**：① eval 47 案例真实命中率 0（模型/词典校准面，Native Agent 质量的起点信号）；② 5 个 eval_run 滞留 RUNNING（被杀批件无 reap——M4-37 Reconciler 预算统一件的现实用例）；③ notify-app compose 无 healthcheck（部署面补强）；④ LITELLM_MASTER_KEY 轮换建议仍开放（安全事件遗留）；⑤ BA-20/21 开放观察项继续移交。

**验证终局（G1~G7）**：
- G1 镜像供应链 ✅（20/20，ghcr 直拉停滞 → 本机 crane 摆渡，记 INC-3）
- G2 中台 ⏭ **双出局降级**：HertzBeat RSS 1.126GiB 超 800M 线；Alerta 一次性 PG 起不来（细节待从证据目录复盘）；链路降级为 **Alertmanager 直打 echo-receiver**，汇聚层空缺记录在案
- G3 靶场 ✅（20 容器全 healthy，flagd 故障开关实测可用）
- G4 告警自动产生 ✅（paymentFailure 注入后 **3 分 17 秒** Sloth burn-rate 自动 firing，零手写 PromQL）
- G5 全链路 ✅（firing → AM 30s 聚合 → echo webhook 自动送达，含 resolved 回执实证）
- G6 RCA ✅（**qwen-plus 在专属 MaaS 实例不可用** → 按降级路径换 `openai/deepseek-v3` 成功；端点坑：MaaS 专属端点需补 `/v1` 后缀）
- G7 内存 ✅（全栈+存量同跑 available 5140M）

**下一步动作**：
1. AM4 工序 3 编码中：按 `docs/告警AM4-落码技术方案.md` v1.3 从 M4-01 顺序执行（迁移 V12~V18，INV-AM4-8/9 红绿）
2. M4-31~38（Holmes 对照/Replay/Shadow/Reconciler 族/AM4 G2）已随 AM3 G2 解锁，按序衔接
3. 安全遗留：195 deploy/alert/.env 的 LITELLM_MASTER_KEY 轮换（建议择机执行，litellm-bootstrap 与 holmesgpt env 同步换）

## 六道工序状态（AM1）

| 工序 | 状态 | 产物 |
|---|---|---|
| 1 任务拆解+技术方案 | ✅ v2.1（用户批准执行，E2E 全流程截图要求并入） | `docs/告警AM1-技术方案.md` v2.1 |
| 2 G1 方案门 | ✅ 用户批准（2026-09-03 会话，含增补计划） | — |
| 3 编码 | ✅ 完成（T00~T10 全绿） | 见下方 AM1 编码明细 |
| 4 部署验证 | ✅ 2026-09-04 | T10 + G0-09 配置回收/G0-10 E2E 部署面 |
| 5 测试验证 | ✅ 2026-09-04 | G0-10 单链路 E2E + 截图证据；测试记录 `docs/告警-测试记录.md`；证据 `docs/测试证据/G0/` 与 `docs/测试证据/AM1/` |
| 6 G2 阶段门 | 🔶 复核材料齐备（G0-11），待用户终审 | 本 commit 即复核材料 |

### AM1 编码明细（工序 3）

| 任务 | 状态 | 验收证据 |
|---|---|---|
| T00 死代码清除 | ✅ 2026-09-03 | 全 reactor `mvn clean test` 绿（100 tests/0 fail/2 skip=需 docker）；残留扫描仅注释性提及；`docs/测试证据/AM1/T00-死代码清除/`（完整日志+BUILD SUCCESS 截图） |
| T01 V7 迁移 9 表 | ✅ 2026-09-03 | `M7MigrationContractTest` 7/7 绿（本地静态门）；CT-A01 真 PG 归 195；`docs/测试证据/AM1/T01-V7迁移/` |
| T02 告警域 domain | ✅ 2026-09-03 | 枚举 10 + 实体 9 + 状态机 4（TransitionTable 通用件）+ 纯函数 4（Identity/Sla/Deferred/EvidenceValidator）+ 端口 9 + HolmesErrorClassifier；UT-A01~A09 全绿（141 tests 总量，0 fail）；AFT-A01 零框架依赖规则入 ControlArchitectureTest；`docs/测试证据/AM1/T02-告警域domain/` |
| T03 Postgres 仓储×9 | ✅ 2026-09-03 | PostgresAlertInbox/Event/Incident/RcaRun/RcaTask/RcaAttempt/RcaReport/ExternalInvocation/SchedulerSlot 九仓储 + PersistenceConfig 装配（docker profile）+ AlertInMemoryStores 约束模拟（唯一约束 23505 模拟/epoch 栅栏/slot 原子/SLA 领取排序）；146 tests 绿；PostgresITBase 清场清单增 V7 九表；CT-A02~A08 归 195；`docs/测试证据/AM1/T03-Postgres仓储/` |
| T04 告警入口 | ✅ 2026-09-03 | `AlertWebhookController`（`@Profile("docker")` POST /webhooks/alertmanager）+ `AlertIntakeService`（gunzip 边解压边限流/Jackson 深度限制/空组落 IGNORED）+ `AlertIntakeLimits`/`IntakeRejectedException` + `AlertFlowConfig` 装配；`AlertWebhookControllerTest` 11/11 绿（202 语义/401 常量时间验签/400 五变体/413 gzip 炸弹 64KB/503 DB 故障/空组 IGNORED）；全量 157 tests 0 fail；EX-A01~A03/A09/A10 本地收口；`docs/测试证据/AM1/T04-告警入口/`（日志+2 截图） |
| T05 投影与消费 | ✅ 2026-09-03 | `AlertPayloadParser`（拆组/腐坏死信）+ `ParsedAlert` + `IncidentProjector`（§6.7 单事务：逐 alert 软背压重读 backlog → event 幂等 existsByDedup 预判+merge 后追加 → incident upsert（episode 水印乱序收敛/三计数/首见 INITIAL/再现 generation+1/调查中材料变化记 pending/完成后材料变化铸 RERUN）→ run+task 铸造 SLA 列）+ `AlertInboxProcessor`（六态消费循环/虚拟线程/DEFERRED→RETRY_WAIT 决策列/DEAD_LETTER/崩溃租约回收）+ `AlertClock` 统一时钟 + AlertFlowConfig 装配；端口增 scheduleRetry(decision) 与 existsByDedup；ST-A01~A05/A07 + 幂等/升级不裂单/新 episode/RERUN/死信 15 用例绿，全量 172 tests 0 fail；**BA-05/06/07 三条 bug 记录与修复**（Timestamp.from(Instant.MAX) 溢出环绕 → pgjdbc infinity 特判）；ST-A06/A08 与 CT 归 T06/195；`docs/测试证据/AM1/T05-投影与消费/`（日志+截图） |
| T06 Worker 与收尾 | ✅ 2026-09-04 | `RcaTaskExecutor` 执行抽象（ReportContent 内容子集，归属由收尾铸造）+ `RcaRunOrchestrator.finishTask`（§6.7 四分支固定算法：epoch 栅栏拒写/attempt 终态/task 终态+slot 归还/RESOLVED 短路/材料变化铸 RERUN/材料锚定；失败退避 RETRY_WAIT 耗尽 DEAD+run FAILED）+ `RcaWorker`（恢复扫描双回收+悬挂账本 STARTED→UNKNOWN/slot+task 同事务领取/心跳续租/虚拟线程循环）；**V7 增 rca_run.investigation_hash 材料快照列**（finishTask rerun 判定锚，未部署可改）+ RcaRun 实体/PG 映射/契约块级断言；SchedulerSlotRepository.tryAcquire 返回 AcquiredSlot(slotNo, epoch)+heartbeat 端口；ST-A06 四分支+ST-A08 旧 epoch 拒写+SLA 排序+槽饱和+崩溃恢复 9 用例绿，全量 181 tests 0 fail；CT-A02~A05/A07 真 PG 并发归 195；`docs/测试证据/AM1/T06-Worker与收尾/` |
| T07 Holmes 执行器 | ✅ 2026-09-04 | `HolmesClient`（HTTP 包装，未使用）+ `HolmesErrorClassifier`（LLM 错误五分类：TRANSIENT/RATE_LIMIT/AUTH/INVALID_REQUEST/UNKNOWN）+ `HolmesInvestigationExecutor`（CLI 执行模式：docker exec holmes investigate，stdout 捕获 + 结构化解析 fallback plain）+ `ExternalInvocation` 实体（AM1 不存储调用历史，仅领域模型占位）+ `ExternalInvocationRepository` 端口；`HolmesErrorClassifierTest` 28 tests 绿（五分类覆盖）+ `HolmesInvestigationExecutorWireMockTest` WireMock 模拟（未覆盖 CLI 路径，真实调用归 T10）；全量 209 tests 0 fail；`docs/测试证据/AM1/T07-Holmes执行器/` |
| T08 Holmes 常驻容器 | ✅ 2026-09-04 | 195 部署 `holmesgpt-am1` 容器（镜像 `local/holmesgpt:am0`，CLI 守护模式 `/bin/sh -c "while true; do sleep 3600; done"`）；网络 `alert-net`（与 Alertmanager/Prometheus 互通）；安全硬化：`security_opt: no-new-privileges`、`cap_drop: ALL`、tmpfs `/tmp` 512M、内存限制 1536m；环境变量 `OPENAI_API_BASE`/`OPENAI_API_KEY`/`HOME=/tmp`；**部署策略确认**：HolmesGPT 官方无 HTTP server 模式（仅 CLI），采用 **CLI 执行模式**（control-app 通过 `docker exec holmesgpt-am1 holmes investigate` 调用，stdout 捕获）；容器状态 Up 运行中，CLI 可用（`holmes --help` 验证通过）；195 `smoke-evidence/t08-holmes/` |
| T09 启动自检 | ✅ 2026-09-04 | `AlertSelfCheck`（告警域自检：webhook bearer token 存在性 + Holmes 凭证存在性（holmesEnabled 控制）+ V7 九表权限（alert_inbox/alert_event/incident/rca_run/rca_task/rca_attempt/rca_report/external_invocation_ledger/scheduler_slot，其中 alert_event/external_invocation_ledger 只需 INSERT+SELECT，其余需 CRUD））+ `AlertSelfCheckTest` 6 tests 绿（缺失 token/缺失 Holmes key/Holmes disabled/V7 权限缺失）；`ControlSelfCheck.violations()` 新增 `holmesEnabled` 参数集成告警域自检，向后兼容重载保留；全量 215 tests 0 fail；`docs/测试证据/AM1/T09-启动自检/` |
| T10 部署验证 | ✅ 2026-09-04 | 195 端到端验收：(1) 容器状态检查（holmesgpt-am1/control-app/postgres/alertmanager/prometheus 全 Up）；(2) HolmesGPT CLI 可用性（`holmes version` 通过）；(3) Postgres 连接性（`alert_inbox` 表可查）；(4) 故障注入（flagd `paymentServiceFailure=ON`）；(5) **告警触发验证**（Alertmanager 活跃告警：`checkout` SLO 慢烧损，severity=ticket，state=active，fingerprint=283f38d9cb61c6aa，startsAt=2026-09-03T12:06:02.984Z）；证据文件：`smoke-evidence/t10-e2e/e2e-result-*.log`（完整告警 JSON + flagd 配置）、`t10-summary.txt`（验收总结）；**待办**：control-app webhook 接收（需配置 Alertmanager webhook_configs）、Grafana 告警面板截图；`docs/测试证据/AM1/T10-部署验证/` |

## G0 收口执行记录（2026-09-04，执行依据 `docs/告警G0-收口技术方案.md` v1.0）

| 任务 | 内容 | 结果 |
|---|---|---|
| G0-01 基线 manifest | commit 基线+测试计数+迁移清单固化 | ✅ `docs/测试证据/G0/baseline-manifest.md` |
| G0-02 webhook 配置键三方统一 | compose/yml/.env.example 三方对齐 `ALERTMANAGER_WEBHOOK_BEARER_TOKEN` | ✅ 关闭 BA-09① |
| G0-03 Holmes key 桥接 | yml 显式占位 `${HOLMES_API_KEY:}` | ✅ 关闭 BA-10① |
| G0-04 rca_report 自检权限修正 | alert_event/external_invocation_ledger/rca_report 三只增表改查 INSERT+SELECT | ✅ 关闭 BA-10② |
| G0-05~07 状态机三线接线+episode 乱序修复 | Inbox/Run/Task 三台 requireTransition 全接线；resolved 后迟到 firing（startsAt<resolvedAt）只计数不复活 | ✅ 关闭 BA-11①/BA-12① |
| G0-08 告警仓储 PG IT | AlertV7MigrationContractIT/PostgresRcaTaskRepositoryIT 195 真 PG 真跑；迁移增量裁定移交（rca_task_edge 等归 AM4，V8 编号归还 AM3） | ✅ 关闭 BA-09③ |
| G0-09 AM0 配置回收+Holmes 运行时修复 | AM webhook 配置入 git；响应限读/UNKNOWN 语义/Hikari 显式预算 | ✅ 关闭 BA-12②③④；`docs/测试证据/G0/g0-09-配置回收与Holmes三修复.md` |
| G0-10 单链路 E2E（195 真栈） | 全链打通+SIGKILL 崩溃演练+resolved 归并+全流程截图；BA-14/15 双修复出自本轮实证 | ✅ 见下"G0-10 E2E 实录" |
| G0-11 AM1 G2 复核 | 构建绿+审查逐条对照+IT 计数非零+E2E 证据四件套齐备 | 🔶 材料即本 commit，待用户终审 |
| G0-12 BA-13 四条优化 | startedAt 绑定参数/退避宽限配置化/组首 count 上界/首见并发重读合并 | ✅ 关闭 BA-13 |

### G0-10 E2E 实录（2026-09-04，195 真栈）

- **链路**：flagd `paymentFailure` 注入 → Prometheus Sloth 烧损率 firing（page/ticket 双档）→ Alertmanager 分组投递 → control-app webhook 202 → alert_inbox → alert_event → incident → rca_run/rca_task → HolmesGPT（DashScope deepseek-v3 真 LLM 调查）→ rca_report **STRUCTURE_VALIDATED**
- **过程修复**：BA-14（response_format 被兼容端点忽略→全 REJECTED_MALFORMED）与 BA-15（bash 工具诱导 kubectl+prometheus 工具集静默禁用）——双修复后报告引用 prometheus:// 真实证据、根因指向真实烧损
- **SIGKILL 崩溃演练（DP-B05）**：kill 时 task LEASED/账本 STARTED → 租约到期回收 RETRY_WAIT → 重领 attempt+1 → SUCCEEDED，报告唯一无重复；悬挂账本超宽限 → UNKNOWN（诚实对账）；195 旧 docker SIGKILL 后不自动拉起记 BA-17
- **resolved 归并**：停注入 → 1h 窗口自然稀释（程序化成功流量造不出记 BA-20）→ 烧损率回落 → Alertmanager 真实 resolved 通知 → incident **RESOLVED**
- **DB 终态**：alert_inbox 11 行、incident 2 单全 RESOLVED、rca_run 7（1 FAILED 修复前+6 SUCCEEDED）、rca_task 7（1 DEAD+5 DONE@1+1 DONE@2）、外部调用账本 10（9 SUCCEEDED+1 UNKNOWN）、报告 6 全 STRUCTURE_VALIDATED 无重复
- **证据**：`docs/测试证据/G0/e2e/`（5 张全流程截图+195 终态快照五件套+manifest.sha256）；测试记录 `docs/告警-测试记录.md`

## 六道工序状态（AM0）

| 工序 | 状态 | 产物 |
|---|---|---|
| 1 任务拆解+技术方案 | ✅ 起草完成，待 G1 | `docs/告警AM0-技术方案.md` v1.0 + `docs/告警-OSS-证据清单.md` |
| 2 G1 方案门 | 🔨 等用户评审 | — |
| 3 环境拼装 | ✅ done（验证形式） | 195 实测全链路（执行者） |
| 4 部署验证 | ✅ done | G1~G7 判定表 `smoke-evidence/s9-verdict-G1-G7-20260903-203846.md`（195） |
| 5 测试验证 | ✅ done（演练形式） | 演练 1/2 取证 26 文件，echo 载荷解码留证 |
| 6 G2 阶段门 | pending | 待用户确认"AM0 完成" |

## 环境锚点

- **195 服务器**（146.56.195.225）：CentOS 7 内核 3.10.0、docker 26.1.4、**内存 7.5G（可用约 5.7G）、磁盘余 24G**、公网 IP（端口只绑内网/localhost，访问走 SSH 隧道）
- 现状容器：postgres（healthy，PG 16）、control-app、publisher-app、github-stub（**unhealthy**）；AM0 计划停 publisher-app + github-stub 腾约 1G 内存（可逆，`compose up` 即恢复）
- SSH：`ssh -i ~/.ssh/id_ed25519 root@146.56.195.225`；构建现场 `/opt/build/pr`，持久态 `/opt/projects/pr_agent`
- 本机 Windows **无 docker**——所有容器验证只能在 195 上跑
- 模型：阿里云百炼 OpenAI 兼容端点，key 经环境变量 `AGENT_MODEL_API_KEY` 注入（纪律：密钥永不写入文档/代码/日志）
- **旧方向封存**：旧线全部文档已归档 `docs/archive/pr-agent-line-20260904.tar.gz`；M4 未完结工作区在 **m4-wip 分支**（commit `ab32b79`）；main 停在 M4-A 锚 `5686d28`。旧线复启：解归档 + 读其中 PROGRESS.md。

## 关键风险（勘察与调研已识别）

0. **Coroot 路线已判死刑（2026-09-03 实测+官方文档）**：用户并行调研曾推 Coroot（自动 inspection/默认 SLO/免写告警规则 + MCP Server 可当 Agent 证据源），但 Coroot 硬依赖 eBPF，官方要求内核 ≥ 5.1；195 实测内核 3.10.0，不可行。备选路线（待 G1 裁定）：A=原计划 Alertmanager+Keep+HolmesGPT + 现成规则库导入；B=OpenObserve 单容器替换 Prom+AM；C=Coroot 迁往内核 ≥5.1 的机器（本机 Docker Desktop/WSL2 或待确认的 117）

1. **内存超卖风险**：OTel Demo（最小模式约 3G）+ Keep 全家（约 1.5G）+ HolmesGPT（约 1G）+ Prometheus/Alertmanager（约 0.6G）+ 存量（约 1.5G）≈ 7.6G > 7.5G 上限——AM0 按"风险优先"顺序验证，资源扛不住时告警源降级为模拟脚本直打 Alertmanager（链路其余不变）
2. **ghcr.io 国内可达性**：OTel Demo 镜像在 ghcr.io，195 是国内机器，拉取可行性是一票否决项，AM0 第一个验证
3. **HolmesGPT 接百炼**：OpenAI 兼容端点配置（OPENAI_API_BASE + openai/<model>）未经实测，第二个验证
4. **HolmesGPT 账本盲区**：其模型调用不经过我们 ModelGateway 账本，Token 消耗无记录（AM3 前经 LiteLLM proxy 收口）

## 时间线

- 2026-09-04：用户确认架构核心冻结后进入“逐渐模块堆积实现整个任务”阶段 → 新增 `docs/告警Agent-增量实现任务拆解-v1.md`。任务树按 G0→AM2→AM3→AM4→AM5→AM6 切为可独立提交和验证的小模块；每项区分 V0 静态、V1 单元、V2 真实组件、V3 真栈 E2E。现场复跑 `mvn -q test`：215 tests、0 failure、0 error、2 skipped；本机 Docker/Testcontainers 不可用，因此仅记普通测试基线，不把跳过的 PG IT 计为 G2 证据。首批只释放 G0-01~11，先关闭配置三名漂移、报告权限自检、三台状态机生产接线、告警 PG IT 和 AM1 E2E，再启动 AM2。

- 2026-09-03：方向切换（用户裁定 PR 线暂停，转告警 Agent）；三项选型确认；M4 打锚 m4-wip；195/本机资源勘察完成；旧 PROGRESS.md 保持不动，本文件开账；后台调研启动（快速部署最短路径）。
- 2026-09-03：195 服务器备份数据清除（用户裁定"清除不保留"，仅限服务器侧）：/opt/backups、/opt/backup、/opt/projects/mall_R_new*、/opt/build 与 /root 下各备份项全部删除，磁盘余量 24G→**29G**；保留 maven 安装包、hotel 源码包、备份脚本（非备份数据）。117 服务器 SSH 公钥认证失败，其备份未清（待用户提供访问方式）。本地 `backups/`（4.7G）保留未动。
- 2026-09-03：部署调研因会话重启断联，已重启并追加调研题——**一体化告警平台核查**（Keep 一体化程度/Robusta+HolmesGPT 捆绑/夜莺/SigNoz/Grafana 等 vs 拼装方案的妥协对比）。
- 2026-09-03：调研 v1 产出（`docs/告警-调研-快速部署-v1.md`）；主会话 195 注册表实测：ghcr/quay/docker.io 通、**GAR 不通**（Keep 出局、HolmesGPT 转自建镜像）；Coroot 内核门槛判死（官方要求 ≥5.1，195=3.10）；用户两轮外部调研收敛 Sloth 路线 + HertzBeat 主候选；**用户 G1 前拍板路线 D（HertzBeat，A/B 先行）**；AM0 技术方案 v1.0 + 告警线 OSS 证据清单完成起草，待 G1。
- 2026-09-03：用户指示"先别写技术方案，先出部署验证设计给执行者验证路线"→ `docs/告警AM0-部署验证设计.md` v1.0 产出；应用户要求补背景简报/范围边界（含禁区）/验证证据规范，升 v1.1。交叉调研 `告警-调研-Keep替代-v1.md` 回收：推荐 Alerta，推翻"夜莺可收 AM 告警"——主会话复核属实，A/B 更正为 **Alerta vs HertzBeat**，证据清单留处置痕迹。当前等用户转交执行者。
- 2026-09-03 晚：**执行者完成 AM0 部署验证，路线 Go**（G1~G7 判定表在 195 `smoke-evidence/s9-verdict-G1-G7-20260903-203846.md`）。关键事实：中台双出局（HertzBeat RSS 1.126GiB 超标 / Alerta 一次性 PG 未起来）→ AM 直打 echo 降级链路验证通过；Sloth 零手写规则 3 分 17 秒自动 firing；HolmesGPT 经百炼出 RCA（qwen-plus 专属实例不可用 → deepseek-v3 降级成功；MaaS 端点需补 `/v1`）；INC-3 ghcr 直拉停滞 → crane 摆渡；全栈+存量同跑内存 available 5140M。主会话收尾：`github-app-key.pem` 从 `/opt/projects/pr_agent/keys/` 恢复，publisher-app 与 github-stub 还原运行。
- 2026-09-04：AM1 方案 v1.0/v1.1 → 用户 G1 评审有条件退回（8 条必须修正 + 账本/结构验证/安全三项提前）→ 主会话对评审承重声明做一手核查（AM webhook.go + Holmes http-api.md，全部属实，无驳回）→ **v2.0 修订完成重新送审**；核查证据入 E-12。
- 2026-09-04：用户指示"无用的类删除不留技术债"→ AM1 方案升 v2.1（新增 T00 死代码清除，依赖链 T00 起手）；用户指示"旧架构文档压缩归档、新线另起一套"→ **旧线文档整体归档** `docs/archive/pr-agent-line-20260904.tar.gz`（15M，5036 项，含 M0~M5 方案/PROGRESS/BUGLOG/冻结文档/ADR/测试文档/agent-learning），docs/ 只剩告警线；新建 `docs/架构设计-告警Agent-v1.md`（AA-1~13 冻结决策 + 组件锚点 + 演进阶梯 + 工作流规范）、`docs/告警-BUGLOG.md`（BA-01~03 补录 AM0 期三个缺陷）。
- 2026-09-04：AM1 编码方自称"编码测试完成"→ 主会话双轴审查（Standards+Spec 子代理）+ 实测 `mvn clean verify`：**BUILD FAILURE**（ControlSelfCheckTest 4 错误）+ Standards 5 条硬违规（配置三名漂移/自检与 V7 授权互斥/slot scope 错位/状态机零调用/双时钟混用）+ Spec 3 条运行时缺陷（gzip 载荷必死信/重试 deadline 不重算/T00 残留 ExecutionLedger 等）——**回流修复，AM1 未过 G2，不进 AM2**。
- 2026-09-04：用户指示提前起草 AM2/AM3 方案并要求详细版（每设计点带参照/思路/弊端/注意）→ `docs/告警AM2-技术方案.md` v2.0（订单靶场：两步创单/三单/废单/三类故障注入/ground truth）与 `docs/告警AM3-技术方案.md` v2.0（确定性评测 + notify-app 出口 + LiteLLM proxy 收口）完成，均标注"待前序 G2 后正式送 G1"。
- 2026-09-04：用户转来两批外部架构调研，指示先调研找差距再采纳 → 主会话一手核查发现三个差距（①Spring AI ToolCallingAdvisor 为 2.0 特性绑 Boot 4，1.1 才兼容本项目 Boot 3.4；②kagent/K8sGPT/Kyverno/Helm 系 K8s 环境假设，docker 靶场不适用，降为迁移储备；③agentgateway CEL 授权空规则全放行）→ 其余声明属实采信。**架构基线升 v2**（AA-14~26 + §6~§12 新增章节）；**AM1 方案升 v2.2**（V7 增 rca_task_edge 变 10 表 + task 预留列 + 四契约版本化 + 报告状态链预留 + §15 线程池预算 + DP-B 接 E2E 证据契约）；证据入 E-13。
- 2026-09-04：用户问"无 K8s 替代方案"→ 专项调研 `docs/告警-调研-无K8s替代-v1.md` 产出并固化：**架构基线升 v2.1**（§8 安全增补 conftest 静态门 + docker-bench-security 运行时门 = "穷人版 Kyverno"；gVisor 内核 3.10 确认不可用；§12 工具层基线修正为 Holmes 自带只读 toolset + 可选 prometheus-mcp 官方组织版/DomainProbe 只读 API；预诊断走自研 DomainProbe + docker inspect/events）。证据入 E-14。新增常驻内存 <150MB。
- 2026-09-04：编码方报"AM1 都修复了"→ 主会话复核：构建转绿 + 6 条修复属实（gzip/deadline 重算/自检授权/slot scope/状态机接线/构建），**3 条未收口**（compose 环境变量名仍与 yml 漂移、publisher/sandbox 目录与 ExecutionLedger 残留、IT 套件为空 "No tests to run"）→ 记 BA-09 开放，AM1 仍未过 G2。同日：用户指出架构设计缺多 Agent 全案与 HolmesGPT 借鉴地图 → **架构基线升 v2.2**（新增 §14：分层定案对照表/Supervisor=状态机/HolmesGPT 直接用-抄机制-逐块替换三分法）。
- 2026-09-04：用户转来对照真实代码基线的 AM2/AM3 评审（7 阻断项 + 代码前置问题 + M2/M3 补充）→ 主会话抽查关键声明（EvidencePackageValidator 自由文本/RcaReport 非空约束/HolmesClient 只取 body/compose 服务清单）全部属实 → **AM2 方案升 v2.1**（§15：chaos 开关 DB 化 oa_chaos_session、eval-mgmt 私有管理网、Gauge/Counter 双族 + 探测去重、补偿改 PG outbox、幂等记录完整字段、scenario→report 映射表、GT 权限隔离、F3 持久化状态机、线程预算）；**AM3 方案升 v2.1**（§15：EvidencePackage v2 结构化契约、失败调查落档、通知降级 at-least-once、tool_calls 提取 + LiteLLM 一对多对账、报告不可变 + report_publication、通知白名单渲染、eval-runner 独立身份、5 场景枚举、评分公式写死）；新冲突记 BA-10（Holmes key 桥接缺失 + rca_report 自检/授权矛盾）。AM2/AM3 动工仍待 AM1 G2。
- 2026-09-04：用户批评"先调研核对现有代码再修正"→ 主会话派 agent-64 全量盘点当前工作区，**纠正两处前判**：① 主会话曾误判"状态机已接线"（实际仅 1 处生产调用，三台状态机零调用，记 BA-11① 认账）；② 评审"EvidencePackageValidator 全自由文本"已过时（实际为六段式结构 + schema_version 路由，缺的是类型化 root_cause/claims）→ AM3 §15.1 措辞校正。另确认：publisher/sandbox 仅剩 target 残留；ExecutionLedger 链被 ModelGateway 依赖不宜蛮删（T00 保留清单修订，AM1 方案 v2.3）；V7 落码为 9 表，v2.2 的 rca_task_edge+预留列属未实施增量；AM1 告警域 IT 为空（BA-09③）。
- 2026-09-04：评审第二轮（§15 补丁与正文双轨不可接受 + 5 阻断项）→ **AM2/AM3 规范归一重写为 v3.0**：补丁全部合入正文（任务表/类设计/时序图/不变量/测试/DoD 单轨语义）；五项阻断落地——① 正文归一 ② 部署形态冻结（新建 deploy/alert/docker-compose.yml 为告警栈唯一事实源 + AM0 手工配置回收进 git + deploy/docker-compose.yml 分工） ③ DB 角色与迁移所有权矩阵（arena_app/eval_app/notify_app + 01-roles.sh 扩展 + GT 对 control/Holmes 禁读） ④ InvestigationResult/rca_tool_call 冻结 DDL（V8，每次 attempt 必落档） ⑤ 三指标评分公式（coverage/conditional_accuracy/end_to_end_hit_rate + UNRESOLVED 单列）；另冻结 chaos 激活事务四 DB 约束、通知触发点（结构验证+候选标记）、AM1 上游能力以代码盘点为准（M2/M3 不假设 v2.2 增量已落码）。
- 2026-09-04 晚：磁盘新增五份文档（另一会话产出）——`架构设计-告警Agent-v1.2.md`（FUT-01~55 目标架构，自述"不覆盖 v1、设计 PASS 待落码"，含双机 195+2C4G 部署、六维评测、Quality Gate、AM2~AM6 路线）、三份 `架构评审-*.md` 裁定记录（采纳项已写入 v1.2，声明不改 AM2/AM3 冻结语义）、`告警Agent-增量实现任务拆解-v1.md`（96 任务执行计划，当前只释放 G0）。主会话全部消化核对：与现有文档无致命冲突（报告两轴状态/AM3 只产 Top-1 等与 AM3 v3.0 一致）；**G0 任务清单与 BA-09/BA-10/BA-11 完全同构**——缺口清单两侧独立互证。
- 2026-09-04 晚：主会话代码审计（agent-67）：P0×3（BA-09/10 未修确认）+ P1×4 新发现（episode 乱序漏洞/Holmes 响应限读/UNKNOWN 语义无触发点/Hikari 无显式配置，记 BA-12）+ P2×4 优化（记 BA-13）；核实无缺口六项（限长拒绝不截断/202=已持久化/429 不落队列/外部调用零事务/悬挂回收路径/claim 索引齐全）。用户问"方案有没有修正或不足"→ 主会话自纠六条（文档跑在代码前/任务编号双轨/Holmes 未验证假设/内存未实测/决策治理成本/评测统计意义），用户裁定两条立即固化。
- 2026-09-04 晚：**用户确认"架构基本定了"**——架构定格（v2.3）：① G0 收口前不再新增设计文档；② `告警Agent-增量实现任务拆解-v1.md` 为唯一执行任务表，AM1/2/3 方案降为设计依据文档；③ AA 系列封版，演进走 v1.2 FUT 系列。后台 harness 设计调研启动（服务 AM4 Native 内核）。
- 2026-09-04 晚：用户指示"给出下一步技术方案让执行者开始编码"→ 主会话出具 `docs/告警G0-收口技术方案.md` v1.0（G0-01~12：基线 manifest/配置键统一/Holmes key 桥接/rca_report 自检/状态机三线接线 + episode 乱序修复/V8 迁移增量 + PG IT/AM0 配置回收 + Holmes 运行时三修复/195 单链路 E2E + 崩溃演练/G2 复核/BA-13 顺带优化），可直接转执行者。**注意迁移编号裁定**：G0 增量用 V8，AM3 eval/notify 顺延 V9（AM3 v3.0 §6.1 引用待编码时同步）。
- 2026-09-04 晚：harness 设计调研回收 `docs/告警-调研-Harness设计-v1.md`（35KB）——最值得抄 5 条（Claude Code 权限 harness 强制执行/Codex safety.rs 三态判定/MCP 治理三件套/Holmes spill-to-disk 上下文预算/工具宁少勿多按工作流聚合）+ 拒绝 3 条（Pi YOLO 无权限/auto 模式 LLM 代审/OpenHands 平台化架构）；MCP 接入施工图齐备（命名空间/白黑名单/延迟加载）。证据入 E-15，全部作 AM4 设计素材，不动已定格基线。
- 2026-09-04 晚：用户指示开始 G1 技术方案 → 主会话出具 `docs/告警AM2-落码技术方案.md` v1.0（AM2 订单靶场施工图，对齐任务树 M2-01~28 四阶段：工程底座/正常订单切片/故障叠加/三场景 E2E+G2，每任务文件级改动点 + 验收命令；设计依据 AM2 v3.0；动工硬前提 = AM1 G0 过 G2），可转执行者备料。
- 2026-09-04 深夜：**G0 收口执行完毕**（执行依据 `告警G0-收口技术方案.md` v1.0）：G0-01~10、G0-12 全部单项验收通过；G0-10 单链路 E2E 195 真栈全绿（flagd 注入→Sloth firing→AM 投递→webhook→投影→Holmes 真 LLM→结构验证报告→SIGKILL 崩溃演练→真实 resolved 归并，全流程 5 截图+DB 终态快照）；**BA-14（response_format 被忽略→散文拒收）/BA-15（bash 工具诱导 kubectl+prometheus 工具集静默禁用）双修复出自 E2E 实证**；G0-12 四条优化带单测并部署 195。缺陷账：BA-09~17 关闭、BA-18 纪律固化、BA-20/21 开放观察项移交 AM2/AM3。本地全量 215 tests 0 fail（2 skip=需 docker 的 PG IT），195 真 PG IT 真跑。全部成果**单个 commit 落账（不 push）**；G0-11 G2 复核材料即该 commit，待用户终审后进 AM2。
- 2026-09-05：**主会话 G0-11 复审（四件套）全过**：① 构建绿（BUILD SUCCESS，215 tests 0 failures 2 skipped）；② 逐项抽查：配置键统一/Holmes key 桥接/rca_report 自检挪组/三台状态机接线/episode 乱序修复/Holmes 响应限读/UNKNOWN 账本/Hikari 显式配置全部落实；③ IT 非零（failsafe 含 AlertV7MigrationContractIT + PostgresRcaTaskRepositoryIT）；④ E2E 证据完整且含诚实声明（BA-16）。遗留：BA-13 未逐条核验（非阻塞）；BA-20/21 移交 AM2/AM3。
- 2026-09-05：**AM1 G2 用户终审通过（"am1确认"）+ 推送完成**：commit `4acebce`（G0 收口）+ `93d6445`（G2 台账）已推送 origin/main（5686d28..93d6445）。注：本机直连 GitHub 不通，推送经本地代理 127.0.0.1:7890（git -c http.proxy 一次性指定，未改 git 全局配置——后续推送如遇同样问题照此办理）。**AM1 阶段正式收官，AM2 订单靶场解锁**，执行依据 `docs/告警AM2-落码技术方案.md` v2.0r1（M2-01~28）。
- 2026-09-05：用户指示开启下一个技术方案 → 主会话**先亲自读任务拆分 M3 原文（173~220 行）再动笔**（纪律修正后首次执行），出具 `docs/告警AM3-落码技术方案.md` v1.0（M3-01~30 三阶段施工图：契约与持久化/评分闭环/通知成本观测；含 V8 迁移四表、EvidencePackage v2、三指标纯函数、eval-runner 独立身份、LiteLLM 一对多对账、Holmes 预算 spike；动工硬前提 = AM2 G2）。AM2 方案自查补 M2-01 Dockerfile/512MiB + C-1 术语说明（v2.0r1）。
- 2026-09-05：评审校准 AM2/AM3 边界（AM2 只做故障/真值/映射/恢复，不越界建评分器与批量评测）→ 落码方案修正：M2-03 不创建 notify_app（归 AM3 M3-03/V8）、M2-24 增 ScenarioMapExportV1 交接契约及测试、M2-25~27 明确每场景一次；AM3 v3.0 §6.1 角色创建分工纠正 + T 编号声明为设计工作包。七项并行备料任务由**用户自行派送**第三方 agent，派送单 = `docs/告警-并行任务-七项备料.md`（P1 Holmes 预算 spike / P2 bench 基线 / P3 conftest 策略 / P4 模型输出稳定性 / P5 同义词+场景清单 / P6 prometheus-mcp 验证 / P7 195 内存水位）。
- 2026-09-05：评审第三轮（AM3 数据契约 8 P0 + P1）全部采纳 → `docs/告警AM3-落码技术方案.md` 升 v1.1（迁移拆 V8/V9、generation 栅栏列、attempt 全程落档 STARTED 先行、统一 ScenarioDriver 三实现、S2 换 flagd paymentUnreachable、GT 延迟授权、评分对象选择规则、对账降级链、通知三档、Holmes 服务端日志脱敏、可复现元数据扩展、评测时间参数显式化）；AM3 v3.0 同步升 v3.0r1。评审第四轮（E2E 太粗）→ §12 新增 E2E-M3-01~08 八链矩阵（v3.0r2 + 落码方案 v1.2），M3-30 升为八链最终部署门。
- 2026-09-05 凌晨：七项备料改派 ZCode MCP（用户指示）——派发发现桥 bug：agent-start 客户端校验报错 `data/threadId must be string`，但服务端实际可能已建会话（P1 即如此）；用户侧修复 `~/.zcode/cli/config.json`（model.main/provider 缺失致冷启动失败，GLM-5.3-Flash 已配）。工作区独占租约导致同时只能跑一个 → 改串行派发 + 监控 cron 续派。P1（Holmes 预算 spike）运行中。
- 2026-09-05 凌晨：P1（Holmes 预算 spike）主体完成——结论：holmes 原生只能 BEST_EFFORT（max_steps/max_tokens 实测硬拦，无 token/成本预算参数），metadata 透传双通道实证可达（EXTRA_HEADERS + extra_body spend_logs_metadata 正中 LiteLLM SpendLogs 白名单）；**重要发现：LiteLLM proxy 在 holmes 镜像内因缺 Prisma binaries 起不来（M3-24 部署路径须改官方镜像）**；空载 prompt 底座实测 4545 token（预算校准基线）。run 超时终止，差 Exp5 proxy 硬拦证据与清单回填；恢复 run 卡资源等待（session is not ready），新会话派发双侧失败——桥故障升级，监控 cron 已换修订版（每 17 分钟，桥恢复才续派 P2~P7，不空转）。证据：docs/测试证据/AM3/spike-holmes-budget/
- 2026-09-05 午：**七项备料全部完成**（P1~P7 ✅，完成清单全部回填）：P1 Holmes 预算 spike（原生 BEST_EFFORT + metadata 双通道可达 + LiteLLM proxy 在 holmes 镜像缺 Prisma 起不来）；P2 bench 基线（117 项，整改清单：pids_limit 全缺/健康检查缺/CPU 限额缺等）；P3 conftest 7 deny+1 warn（25/25 正反样本绿 + 195 实栈 18 违规清单）；P4 模型输出稳定性 180 调用（文字硬指令是唯一有效载体、response_format 在此端点零约束、qwen-plus 裸 API 可用澄清 AM0 结论）；P5 同义词词典+场景清单（含四组 ExportV1 映射对齐 + F2 指标名分歧上报）；P6 prometheus-mcp 验证（主会话接管停滞会话直接执行：crane 摆渡 + 只读白名单 + query 闭环，RSS 62.6M）；P7 195 内存水位（ZCode 侧精测修正主会话初稿：available 4.25G、AM3 最坏 +800M 可容纳、node_exporter 缺失致三级闸不可评估、磁盘 74% 先到瓶颈）。桥故障期间 ZCode 侧自治推进 P2~P5/P7，主会话接管 P6 与 P1 回填；两个监控 cron（含明 13:00 收取）已删。
- 2026-09-05：**AM2 阶段一~三落码完成**（执行依据 `告警AM2-落码技术方案.md` v2.0r1，M2-01~28 顺序执行）：阶段一工程底座（order-arena/arena-chaos-admin 双模块 + V1~V3 迁移 + docker compose + CI 门）；阶段二正常订单纵向切片（两步创单/幂等/资源台账/补偿出箱/流量发生器）；阶段三故障叠加（ChaosSwitchboard/三故障 F1/F2/F3 注入与恢复/DomainProbe 三台账指标/场景图 oa_scenario_map/管理面 API）；每阶段本地门 `mvn test` 全绿（order-arena 43 UT）。
- 2026-09-05 深夜：**AM2 阶段四部署门完成**：195 真栈重建（deploy/alert/docker-compose.yml，双 arena 容器 healthy——Ubuntu 26.04 bash 5.3 禁 /dev/tcp 改 wget 健康检查）；order-arena 11 IT + arena-chaos-admin IT 195 真 PG 真跑；promtool 双检 5 用例全过（time() 是相对评估时刻的坑实测修正）；INV-AM2-1~7 逐条落码实证。三场景 E2E 驱动/编排器/侦听器落 `var/e2e/`（arena-e2e-cli 容器 eval-mgmt+alert-net 双网，slot=1 串行，run tag 派生全部场景名防 uq_chaos_scenario 烧名）。
- 2026-09-05 深夜：**E2E attempt 1~7 排障七轮**（详单）：①驱动缺 sku 参数/②V5 uq_scenario_map_fp 全局唯一过严激活 409 → V6 迁移 drop（BA-23）/③switchboard 2s 可丢缓存激活竞态（激活后等 3s）/④**C-6 指纹算法 ground-truth 修正**——AM 实测 0d7404ae811ae84a vs 冻结 0815624c44921472，17 变体枚举锁定"每对尾随 0xff 含末对"形态，Java/测试/驱动三端改齐（BA-22）/⑤侦听器依赖 incident.current_rca_run_id（run 完成即被清除）→ v2 三独立查询（BA-26）/⑥驱动跨进程订单号不传递 + TTL 断言元组笔误 + dpc02 漏 sku + 编排器只 grep FAIL 漏 Traceback 假绿/⑦幂等重放契约实测 200 replayed=true 返回原单（非 409），断言改按真实契约。期间 ssh 断连一次，远端脚本因 tee 持句柄存活自跑完成（F2/F3 下游链意外先证全通）。
- 2026-09-05：**E2E attempt 8 真全绿（run tag 0905040208）**：preflight/F1/F2/F3 phase1+phase2/TTL/DP-C02 八阶段退出码全 0，RESULT: ALL PASS（判定已含退出码+Traceback 双查）。三场景指纹三重一致（F1=0d7404ae811ae84a/F2=653693464eea7e9b/F3=f95e79c26f0e7b4c）；恢复语义全实证（F1 canonical 保留+重复废单、F2 事实驱动回跳+审计一一配对、F3 拒绝废单+迟到补 ENABLE）；TTL 30s 自愈 CLOSED+RECOVERED；DP-C02 流量/幂等重放/探针全过；落库终态 4 会话 CLOSED、scenario_map v1/v2 指纹全对、incident 全 RESOLVED、run 全 SUCCEEDED、报告全 STRUCTURE_VALIDATED。manifest 四件套关联实 ID 记录于证据包 README。证据：`docs/测试证据/AM2/`。
- 2026-09-05：**DoD 全门 mvn clean verify 揪出指纹残留并收口**：ChaosActivationLifecycleIT 两处硬编码旧指纹 46bc3a8af2447e6c（C-6 修正时只改了单测向量漏了 IT）→ 改为冻结值 f95e79c26f0e7b4c；ChaosAdminControllerTest 桩值同批对齐。**全门终验（195，docker 守护态）：ExitCode=0，failsafe 71 IT 全绿**（order-arena 9 类 54 + arena-chaos-admin 8 + control-app 回归 9，全 0 失败 0 错误）；教训入账：`-q` 静默模式全绿时无任何汇总输出，勿把安静当挂死，守护态容器 ExitCode 才是判定源。文档三件套同步（测试记录 AM2 章节 + BUGLOG BA-22~26）；证据归档 `docs/测试证据/AM2/`（E2E 全绿日志 + 全门日志 + 五脚本 + README 含 DP-C01~07 实例化、manifest 实 ID、诚实声明）。
- 2026-09-05 午：用户指示提前启动 AM4（方案+落码）→ 主会话亲自读任务拆分 M4 原文（§7，M4-01~38）后出具 `docs/告警AM4-技术方案.md` v1.0（分层铁律 R1~R5 为头号约束、DAG/工具/证据三底盘、迁移自 V10 起、M4-31~38 待 AM3）；随后派 coder 落码**纯 domain 第一批**（M4-05/06/08/15/21/22 + ArchUnit 守卫）：14 新文件 + 52 穷举单测，268 tests 全绿。诚实接缝记录：EvidencePackageValidator 的 Jackson 遗留（待迁移 CanonicalJson 后扩 ArchUnit 覆盖面）、DagTaskState↔RcaTaskState 对齐留 M4-01/02 批次。AM2 主线（order-arena）未触碰。
- 2026-09-05 午：AM4 落码施工图出具 `docs/告警AM4-落码技术方案.md` v1.0（M4-01~38 全表，第一批已落码行标注；迁移编号 V10~V12；M4-31~38 待 AM3；BA-22 Jackson 遗留纳入 M4-01~03 批次处理）。
- 2026-09-05 午后：用户报 AM2 完成 → 主会话 G2 复审四件套全过：① 四模块 BUILD SUCCESS（shared-kernel/control-app/order-arena/arena-chaos-admin），单测 327/0/0（control 268 + arena 30 + admin 14 + shared 15）；② IT 非零（order-arena 10 个 + arena-chaos-admin 2 个 failsafe IT 类，含权限矩阵/补偿恢复/Probe episode/F3 reconcile/幂等 23505）；③ 抽查：六迁移（V1~V6 arena 域独立）/ChaosSession DB 开关/双族指标/C-3 双网络拓扑 compose 注释/ScenarioMap；④ E2E 证据包（docs/测试证据/AM2/）：三场景 GT/Alert/Incident/Run/Report 一一关联 manifest + 指纹三重一致 + TTL 自愈 + DP-C01~07 逐条 log 行证据 + 诚实声明（F2/F3 run 复用早轮去重、预热轮自伤修复记录）。**AM2 G2 待用户终审确认**。
- 2026-09-05 午后：**AM2 G2 用户通过 + 推送完成**：origin/main 更新至 `a42acf3`（含 AM2 收口 `0e54c21` + AM4 第一批/七项备料/方案校准 `a42acf3`）。推送前工作区清理：ZCode 侧的根目录临时探针文件（_audit*/_exp*/_holmes* 等，删除被用户拒）保持未跟踪未提交，仅选择性提交合法资产。P6 目录另含 ZCode 第三方复核文档（与主会话结论互补）。
- 2026-09-05 午后：AM4 评审（7 P0 + 第一批代码 4 问题）——主会话核实仓库现状属实（V8__am1_dag_reserve.sql 已存在且含 rca_task_edge，此前'G0 不占迁移号'裁定与实际落码不一致，以实际为准）→ 全部采纳：**AM4 技术方案升 v1.1**（提前实施降级为预研/备料不计任务完成；迁移重排 V9/V10=AM3、V11~V16=AM4 一迁移一任务；M4-04 复用补强不重建 + 同 run 连边约束；状态全集对齐冻结表 WAITING_APPROVAL 移出 AM4；DoD 移除 Replay/Shadow/Holmes 隔离；Logs/Change 无实时数据源只许 replay fixture；E2E-M4-00~09 套件入 §12）+ **落码方案升 v1.1**（新增 GX-1~5 修正批次）+ 第一批代码四项修正已派 agent-69 后台执行（ClaimReducer 分组键+平局裁决/Claim 校验/RunBudget 补强/ActionDigest envelope+CanonicalJson 更名 InternalCanonicalJsonV1/DagPromoter 终局收敛+冲突边拒绝）。
- 2026-09-05 午后：根目录 ZCode 临时探针文件删除（用户明确授权，含 _spike_env.raw 等敏感残留；下次此类残留主会话会先说清内容再请裁定）；P1 Exp5 由 ZCode 侧后续补完（proxy 虚拟 key max_budget 硬拦 429 实证 + spendlogs 对账 + 195 内核 seccomp EPERM 坑记录），P1 清单回填更新为完整。七项备料至此全部闭环。
- 2026-09-05 午后：AM4 GX-1~5 修正复核通过（292 tests 0 败，主会话独立构建验证）+ 推送完成：origin/main = `3be79d5`（`32ec891` 代码修正 + `3be79d5` 文档修正）。AM4 备料批（纯 domain）至此完整入库。
- 2026-09-05 午后：**AM3 评测闭环（M3-11~18）落码完成**（执行依据 `告警AM3-落码技术方案.md` v1.1，编号顺序执行；`d680202`=M3-11~13、`5df324e`=M3-14、本批=M3-15~18）：M3-11~13 词典/三指标/快照纯函数（公式冻结 §6.4，UNRESOLVED 不进 conditional 分母，零分母=0.0）；M3-14 V10 迁移 eval_run/eval_case_result + 十项可复现元数据 configDigest + 主键禁重生/finalizeOnce CAS/UNIQUE 禁覆盖三层 + 6 IT；M3-15 **V11 新增迁移**（eval_app 只读授权 rca_run/rca_attempt/rca_report/incident——落码方案原文 V9 编号过时顺延）+ EvalRunnerConfig `@Profile("eval")` 与生产装配互斥（ApplicationContextRunner：无 profile 全缺席/eval profile 全 bean 离线在场，桩数据源不建连）+ EvalRunnerMain 跑批退出；M3-16 评分三件套——FinalReportSelector（selection_policy=`final-validated-report-v1`：created_at 最晚 STRUCTURE_VALIDATED，同刻 attempt_id 定序，禁挑最优）+ ScenarioEvaluator（词典三维精确等值命中、白名单外 NO_MATCH 不抛、reason_code 从属校验、TP/FP/FN 规范化、silence_penalty）+ SingleCaseScorer（verdict 分派冻结：REJECTED→STRUCTURE_REJECTED、否则缺席→TIMEOUT_OR_ABSENT）；M3-17 ScenarioDriver 三实现（Flagd/ArenaChaos/Infrastructure 预留 fail-fast）+ AlertProbe（Prometheus 按注册表期望 alertname 逐码判定，不数生产告警）+ RcaRunResolver（靶场走 arena.oa_scenario_map 回填行、flagd 走 incident_key 匹配）+ EvalBatchRunner（next_round_gate criteriaMet+alertsResolved 双真、gate_blocked 不注入但案例行照常落档、activate 失败落档不中断、finally 必解除注入、评分链异常→FAILED 终态 CAS 后上抛、EvalClock 拨钟测试面）；M3-18 BaselineReportGenerator（canonical JSON 固定字段序 + digest 重跑一致/逐项敏感 + raw_counts 分子分母快照 + 失败样本原文）。测试 348→**372 全绿**（+24：Scorer 5/Selector 3/Evaluator 5/Runner 4/Generator 5/Profile 2）。**落码新定待主会话确认**：① UNRESOLVED 哨兵=报告 root_cause.component 规范化=="unresolved"（v2 结构 root_cause 必填，拒答只能经哨兵表达）；② V11 编号新增（V12 起归还 AM4，AM4 方案 v1.1 迁移表需再顺延一格）；③ FlagAdminClient HTTP 契约 POST /flags {flag,variant}→{applied}（195 flag-setter 落实归 E2E 门）；④ flagd 场景 RCA run 归属用 incident_key LIKE 匹配（V11 补 incident select 的依据）。
- 2026-09-05 深夜：**AM3 通知出口（M3-19~23）落码完成**（新增 `notify-app` Maven 模块 `com.objwww.pr.notify.*`，根 pom 注册；M3-19 outbox 生产者已于 M3-08 随 ReportCompletedNotifier 落地）：M3-20 NotifyOutboxClaimer（SKIP LOCKED+短事务租约+epoch 栅栏，单 worker 虚拟线程 §6.8 预算；**崩溃回收折叠进领取查询**——租约过期 CLAIMED 直接可重领，无独立 scanner）；M3-21 NotificationRenderer 白名单渲染（两层防线第二层：控制字符剥离/@all/@everyone 中和/Markdown 链接仅放行 http(s) scheme/单字段+总文本双截断/秘密值 [REDACTED] 遮蔽；缺 operation_id 或不可解析→确定性拒绝）；M3-22 三档渠道（VALIDATE_ONLY 零触网 / TEST_CHANNEL / LIVE 共用 WebhookChannel：钉钉 HmacSHA256 加签+企微 markdown body；webhook/secret 仅 env 注入 INV-AM3-3，缺配置装配期 fail-fast；JdkWebhookTransport 窄端口+纯函数分类器）；M3-23 FencedNotifyExecutor 落账语义（2xx→SENT；**429→RETRY_WAIT 读 Retry-After 持久化退避且不耗 attempt 预算不占 worker 槽**；5xx/连接失败→耗预算指数退避、耗尽→DEAD；4xx→DEAD；**结果未知→DEAD 不自动重发**；渲染失败/渠道未配置→DEAD；epoch 栅栏未中→DEFERRED 不覆盖新租约）+ publication 聚合同步（任一渠道行 SENT→publication SENT；全终态无一 SENT→DEAD，只在冻结状态机出边内迁移，E2E-M3-05 "终入 SENT" 可达）。notify-app 19 UT 全绿（Renderer 5/WebhookChannel 6/Executor 6/Claimer 2）+ 2 IT 类 10 用例（NotifyOutboxStoreIT 7：领取互斥/SKIP LOCKED/租约回收/epoch 栅栏/预算语义/唯一键/聚合同步；NotifyPermissionIT 3：报告不可改/不产 outbox/列级白名单——本机无 docker，195 verify 真跑；IT 基座 filesystem 引用 control-app 迁移单一事实源）。根全模块 `mvn test` 绿（control-app 372 + notify-app 19）。**落码自决待主会话确认**：① 429 不耗 attempt 预算（限流非投递失败，防限流风暴把行打 DEAD）；② UNKNOWN→DEAD 终态（状态机无 MANUAL 态，"不自动重发"的唯一落点，last_error 留 reason=outcome_unknown 供人工复核）；③ VALIDATE_ONLY→SUPPRESSED（不冒充 SENT，诚实"按策略未投递"）；④ 全 SUPPRESSED 批次 publication 落 DEAD（READY→SUPPRESSED 不在 M3-09 冻结出边内）；⑤ NotifySender 抽顶层接口（嵌套在实现类内 implements 自身嵌套类型=循环继承，javac 拒绝——技术发现留档）。
- 2026-09-05 深夜：**AM3 成本观测（M3-24~26）落码完成**（LiteLLM proxy 收口 + usage 对账降级链）：M3-24 部署收口（deploy/alert）——`litellm` 容器（官方 `litellm/litellm:1.89.0`，**按 spike 结论修正方案原文"ghcr+crane BA-01"**：docker.io 官方镜像 195 加速可拉且与 holmes 内 litellm 客户端同版；holmes 镜像内 pip 装 litellm[proxy] 缺 Prisma binaries 起不来的 take-1 负结论留档）+ `litellm-bootstrap` 幂等自举（litellm 角色/库，只 psql 不 initdb 免 INC-09 seccomp 坑；SpendLogs/虚拟 key 表由 Prisma 自建）+ holmesgpt 模型面收口（`OPENAI_API_BASE=http://litellm:4000/v1`，**proxy 不可达即调查失败、无直连百炼回退** = E2E-M3-06 断言面；`OPENAI_API_KEY` 换 `LITELLM_RUN_KEY` per-EvalRun 虚拟 key，master key 不进 holmes）+ master key/上游 key 仅 env 注入（INV-AM3-3）+ `litellm-logs` 日志卷 600 纪律（禁 verbose 请求体日志）+ healthcheck `/health/liveliness`（start_period 120s 兜首启 Prisma migrate）+ 512M（§6.8 冻结）+ 127.0.0.1:4000 仅 loopback 供铸 key；deploy/alert/README 补 per-EvalRun key 流程（curl /key/generate → .env → force-recreate holmesgpt）。M3-25 usage 适配器 + **对账降级链（P0-8）**（control-app eval）：`SpendRecord`（litellm 1.89.0 SpendLogs 列投影，防御解析——未知字段忽略、缺失落 null 不猜默认值）+ `UsageLedgerReconciler`（一对多聚合三态 MATCHED/PARTIAL/UNMATCHED，只比 sum(prompt/completion) 不比行数，§6.6 冻结；降级链①metadata attempt 级 1:N 归因（残行 taint：无法证伪即不判 MATCHED）②virtual key run 级聚合③NONE+bestEffort；**结构防时间窗**——reconcile 签名无任何时间参数 + 源码断言测试钉死；`budgetEnforcement` 字段账面化 spike 结论 PROXY_VIRTUAL_KEY/BEST_EFFORT）+ `LiteLlmAdminPort`/`HttpLiteLlmAdminClient`（RestClient 家规；/key/generate、/spend/logs；非 2xx 只出状态码不回显错误体——master key 域）+ `UsageLedgerService`（EvalRun 级出账；未配置/查询失败降级 UNMATCHED 不阻断批件终态；usage_json 残缺按 usage_missing 落账；wait 兜 spend 异步 flush ~16s）+ EvalRunnerMain 对账出账日志 + eval profile 四配置键（litellm.base-url/master-key/run-key-alias/reconcile-wait-seconds）。M3-26 Holmes 预算 spike **已由备料 P1 提前完成**（docs/测试证据/AM3/spike-holmes-budget/ 全档案：holmes 原生无 token/成本预算参数，仅 max_steps/max_tokens 真拦截=BEST_EFFORT；proxy 虚拟 key max_budget 硬拦实证 429/16ms auth 层拒绝、上游零流量；metadata 双通道可达但均实例级静态；空载底座 4545 token），本批只做结论账面化。测试 391→**412 全绿**（Reconciler 12/Adapter 4/Service 5；根全模块 mvn test 绿）。**落码自决待主会话确认**：① 对账主体=**EvalRun 级**——holmes 常驻单容器，metadata/key 都是实例级静态注入（spike §二.3），attempt 级归因结构上不可得；链① attempt 级 1:N 实现保留（供 proxy 侧自定义回调转写头的未来形态），当前出账为 run 级聚合；② `/spend/logs` 全量拉取 + 域侧按 alias/metadata 筛拣（不用时间窗过滤参数；1.89.0 无独立 api_key_alias 列，alias 在 metadata JSON 内）；③ 虚拟 key 明文不落库不入 eval_run（部署侧 .env `LITELLM_RUN_KEY` 交付 holmes，eval-runner 只配 alias）；④ 铸 key 的运行时调用点归 E2E-M3-02/06 编排（eval-runner 无 docker socket、不重启 holmes），适配器方法已备好；⑤ 单 run 预算（max_budget）由部署侧按 ~4.5k token/调用底座 × max_steps 校准（spike Exp4 基线）。
- 2026-09-05 深夜：**AM3 观测面（M3-27~29）落码完成**（结构化 Logging / Micrometer 最小指标 / OTel Context 贯穿最小链路，新包 `control.infrastructure.observability`）：M3-27 双侧收口——`StructuredLog.event`（单行 JSON，event/ts 打头 canonical 字段序；序列化失败降级为事件名日志不上抛）+ 控制侧两个结构化事件（`HolmesInvestigationExecutor` 终态 `rca_attempt_finished`{run/task/attempt/validation_status/latency_ms/usage_missing}、`RcaRunOrchestrator.finishTask` 决策 `rca_task_decision`{run/task/attempt/decision/latency_ms}，字段全为标识符/状态/时长）+ `HolmesClient.chat` 增 (runId, attemptId) 重载透传 `X-Run-Id`/`X-Attempt-Id` 关联头 + **server.py `_safe_req_info` 收口**（违例点原 line 562 `ask={chat_request.ask}` 替换为 ask 字节数 + sha256 前 16 hex + 关联头；Received/Completed/stream cleanup 复用 req_info 全数安全；trace_span.log(input=ask) 为 tracing 面不改——no-op 后端无落盘，待 collector 上线时评估，**记台账**）+ AlertFlowConfig 构造点注入 AlertMetrics + 4 处测试夹具补 NOOP。M3-28 `AlertMetrics` 三指标（`rca_task_decision_total`{decision}/`rca_attempt_finished_total`{validation}/`rca_attempt_latency`）——`registry.counter` 按标签分桶、null/blank 归 unknown、值截断 64 防意外高基数；label allowlist 测试钉死全 registry tag 键 ⊆ {decision,validation} 且值无 UUID；actuator 暴露面收口 health,info,metrics + show-details never（application.yml）。M3-29 `TracedTasks.wrap`（提交点捕获 currentSpan、执行线程 `withSpan` SpanInScope try-with-resources 恢复；tracer null 直通）+ brave 桥跨线程 traceId 相等测试钉死机制；**生产接线点（inbox/worker 循环）待 OTel collector 上线后接**——无 exporter 时 span 仅内存态、接线零风险，**记台账**。测试 +15：StructuredLogTest 4（单行/转义/null 诚实落 JSON/不可序列化不抛）+ AlertMetricsLabelAllowlistTest 3 + TracedTasksTest 3（跨线程 traceId 相等/无 currentSpan 不抛/null 直通）+ WireMockTest 日志卫生 capture 1（ListAppender 断言执行器日志无 ask 正文/渠道 secret、结构化事件在位）+ `deploy/alert/holmesgpt/test_server_logging.py 4`（stdlib unittest 源码守卫：违例插值模式消失/_safe_req_info 在位且接线/ask 只以 size+短摘要出现/关联头被读；不 import server，动态半边归 195 E2E-M3-03）。编译期当场修正三处：`SpanInScope` 无 makeCurrent（只有 close，try-with-resources 即正确姿势）、Micrometer `Counter` 无 `increment(Tags)`（改 registry.counter 分桶惯用法）、brave 6.0.3 的 ThreadLocalCurrentTraceContext 在 `brave.propagation` 包（micrometer-tracing 1.4.5 `BraveTracer(Tracer,CurrentTraceContext,BaggageManager)` 构造器 javap 实证）。control-app `mvn test` 全绿（聚合 404，本批 +11 Java）；根全模块 `mvn test` 绿（-q 安静模式无计数输出，notify-app 19 等不受影响）。
- 2026-09-06：**M3-30 最终部署门执行（第一批：M3-30e/f 校准与 M3-01/02/03/07 四链）**：① flagd 管理面权限 0600 修复（P3 conftest 违规清单收口）+ E2E-M3-07 权限九断言绿；② eval 场景 firing 等待窗校准（1500）与 S5 工单慢烧窗 vs next_round_gate 时序冲突校准（eval-scenarios*.yml）；③ SingleCaseScorer 判据修正（五件修改面 server.py/SingleCaseScorer/单测/双场景 yml 本地↔195 哈希一致）；④ E2E-M3-01 黄金链全绿归档；⑤ M3-02 批量链 c 批（5 场景）17/17、d 批（S345 子集）17/17——判定面如实：S3r1/S5r1 DECIDABLE 但 hit=false（模型答 Prometheus/TOOL_RESTRICTION vs GT CREATED_STATE_DELAY，词典/模型质量校准面非门禁）、S4r1 STRUCTURE_REJECTED（自然坏模型面：模型中文散文拒答）、r2 轮 run_not_found（根因见下条）；c09 spend 断言改锚定批件自身 started_at/finished_at 窗（滚动 now()-240min 窗在批后 8h 断言必失效）+ 跨库子查询（litellm 库引用 pr_agent 表）postgres 不支持改 shell 变量传字面量；⑥ E2E-M3-03 结构失败链 13/13（badmodel stub + litellm 路由 + 无条件还原全链；r2=REJECTED_MALFORMED 全链：validationErrors 引用 stub 原文、调查记录同权落档、零报告零通知、STRUCTURE_REJECTED 进分母不入超时列；r1=AM 抑制诚实缺席）。三次误启动教训留档：flagd-admin 镜像 ENTRYPOINT 吞 cmd（`--entrypoint python` 覆盖）、LITELLM_RUN_KEY 在 deploy/alert/.env 非 deploy/.env（读错文件→空 key→holmes 401→run e7824f91 FAILED）、MODEL 必须带 openai/ 前缀（裸名 litellm SDK 本地 BadRequest，生产同构 openai/deepseek-v3）。
- 2026-09-06：**AM 收敛机制 vs 多轮评测批冲突实证与破解（本轮核心新发现，台账必读）**：两级产品收敛叠加会锁死"同指纹多轮"评测批——① Alertmanager **nflog 持久化**：`/alertmanager/nflog`（volume）存"已通知"记录，repeat_interval=4h **跨 docker restart 依然有效**（组态清除/重启均无效）；真清除 = 运行中 `rm nflog` + `docker kill`（SIGKILL 防优雅停机把内存 nflog 重写回盘）+ start。② control-app **同指纹同 generation 幂等去重**：resolve 未投递则 incident 保持 FIRING@gen，同代际已有 run 不再开新 run（05:17:28 投递的 firing 带 gen8=04:55:13 同值，被正确去重——产品行为正确，是评测时序与收敛机制的冲突）。M3-06 批1（am3-e2e06-0905）两轮 run_not_found 诚实缺席面即此因（证据 `docs/测试证据/AM3/e2e-m3-06-故障链断言-批1-诚实缺席.log`）。破解三件套（harness 侧，均事后还原）：批间等 resolve 投递使 incident RESOLVED 隔轮（deactivate 的 alertsResolved 校验保证 Prometheus 先见 gauge=0）+ AM group_interval 5m→10s 临时放宽（drill 后 sed 还原+restart 实证 runtime 5m）+ 起批前清 nflog。**M3-06 batch-b（am3-e2e06b-0906）16/16 PASS**：两轮 TIMEOUT_OR_ABSENT/reason=no_validated_report（**run 存在** gen9/10 均 FAILED=litellm 停机打挂调查，区别于批1 的 run_not_found）、两轮均锚定调查 run、零报告/零通知/零成功调查、alert_event 全循环 firing9/resolved9/firing10/resolved10、**usage 对账降级 UNMATCHED 落台账**（6 attempt 全 UNMATCHED、basis=NONE、best_effort=true、无伪造 MATCHED）、control-app 零崩溃循环、chaos 会话 CLOSED、批件 SUCCEEDED 不被对账阻断；无条件恢复 litellm healthy。两批证据合起来覆盖 §12 E2E-M3-06 的"缺席面+失败面"。
- 2026-09-06：安全事件记录：本轮一次 `docker exec env` dump 曾把 SPRING_APPLICATION_JSON 内 litellm master-key 明文带入会话记录（未落任何文档/提交/日志文件）→ **建议 M3-30 收口后轮换 195 deploy/alert/.env 的 LITELLM_MASTER_KEY**（litellm-bootstrap 与 holmesgpt env 同步换）。另：BA-18/BA-20 再次验证（PowerShell 触碰含中文 sh/ps1 必炸；本地 cmd 无 cp/tail/grep——全部走 Write 工具写脚本 scp 远端执行）。
- 2026-09-06：**E2E-M3-04 崩溃恢复链 14/14 PASS**（drill v4，新 tag am3-e2e04b-0906；v3 误启动根因=复用旧 tag 使 RR 发现面命中 v2 遗留 run，新 tag 根治并留档）：KILL1 命中（STARTED 1s 后杀 control-app）→ attempt 被杀回收 UNKNOWN/NOT_VALIDATED ×2 → **docker kill 不触发 unless-stopped restart 策略**（RestartCount=0 全链冻结躺尸 33 分钟——编排器语义必须"kill 后显式 start"建模）→ 拉起后 gen12 重投递触发处理器复用重驱 pending task（不开新 run）→ attempt3 SUCCEEDED/STRUCTURE_VALIDATED → 报告唯一 → outbox/publication SENT → run SUCCEEDED → 两轮诚实缺席 CLOSED。KILL2（outbox 投递亚秒窗）MISSED 如实记录，重投递面移交 M3-05；resolver 会话作用域实证：崩溃重收敛 run 属旧会话，新批 resolver 不认领 → 诚实缺席 run_not_found。证据四件（drill-v4/kill 不自启证据/断言/批量日志）。
- 2026-09-06：**E2E-M3-05 崩溃重驱与冻结语义 13/13 PASS + outbox DEAD 悬案定性（结论：M3-23 冻结语义正确执行，非缺陷）**：r1 牺牲局（gen12 事件级去重不开新 run，判 TIMEOUT_OR_ABSENT）→ group_interval 10s 配方下 resolve 抢先落投 → gen13 开 run（2ea33041，18s SUCCEEDED）→ 报告 13fbc752 → outbox 54ce2bdb claim#1（epoch 1）→ **KILL5 杀 notify-app 于 20s 延迟投递第 1s** → 60s 租约过期 claim 折叠回收重领（epoch 2，claim 不计 attempt）→ 重发撞仍在位 stub → JdkWebhookTransport 10s 请求超时 → **"结果未知→DEAD 不自动重发"**：last_error= outcome_unknown 诚实话术、attempt_count=0 系 DEAD 路径不耗预算计数（两次线上投递由 journal 22→25 + lease_epoch=2 双证）、publication 6ms 聚合 DEAD、**批件 SUCCEEDED 不被通知终态阻断**；r2 DECIDABLE、alert 四拍全循环（firing12/resolved12/firing13/resolved13）、incident RESOLVED@13、两会话 CLOSED；AM group_interval 10s→5m 还原 + restart 后 runtime API 确认。证据四件（drill-v2/批量监视与批结果/断言 v3/取证笔记）。
- 2026-09-06：**M3-08 共存终快照 + M3-30 八链最终部署门收官**：during/after 双拍 RSS 合账（during ≈3794.7 MiB@32 容器含 eval-runner 228 / after 3688.1 MiB@31 容器，关键容器批中→批后稳定：control-app 204.4→204.1、notify-app 167.8→166.3、litellm 501→428.6 回落、postgres 367.1→446 正常增长；during TOTAL 行系旧版脚本 awk bug 显示 0.0，以逐容器手工合账为准）；关键容器零重启零 OOM（notify-app restarts=0 系 kill 不计 RestartCount，M3-04 已证）；frontend live 200；chaos/live 流量零串扰（0 violations）；故障 gauge 三项归零。**八链 E2E M3-01~08 全部闭环** + 最终 mvn verify 全门 6 模块 **539 tests 全绿**（V10/V11 契约 IT 含）已归档。AM3 执行面至此收口，待 git 提交与用户验收。
- 2026-09-06：**Harness+M4 机制源码级全量重调完成**（用户批评上轮"只看 README"，11 路并行 agent 浅克隆逐仓读源码）：产出 `docs/告警-调研-Harness设计-v3.md`（13 对象含新增 Hermes Agent——NousResearch，实查 242k★/2025-07 创建，推翻传闻；360 行，含对 v1/v2 的 43 条印证/推翻总表）与 `docs/告警-调研-M4机制调研-v1.md`（工具/证据/裁决/预算四册 50+ 项目，M4-13~23 与 M4-08/09/37 逐条裁定；221 行）。关键修正：LiteLLM 已演进事前预留制；三家框架超限后再调一次 LLM 收尾（M4-08 须断言耗尽路径零 LLM 调用）；重名静默覆盖×3 → M4-14 fail-fast；default-allow 陷阱×3 → M4-16 空策略拒绝启动。网络降级声明：GitHub 直连与本地代理均不通，经 gh-proxy/codeload 镜像取证、commit 逐仓核实。证据登记 E-16；分册留档 `var/m4-research/`（harness-src/h01~07 + src-part1~4，克隆仓约 4GB 未提交 git）。
- 2026-09-06：**主会话 M3 独立抽检通过 + 推送完成**：不信测试报告重验四件套——① 本机 `mvn clean verify` 6 模块 BUILD SUCCESS、592 用例 0 失败（111 IT 本机无 docker 跳过，195 真跑过）；② 逐行读码抽检全过（ScenarioMetrics §6.4 公式/FinalReportSelector 禁挑最优/SingleCaseScorer verdict 分派/FencedNotifyExecutor 五分支/UsageLedgerReconciler 结构禁时间窗/NotificationRenderer 五处理面/V9~V11 迁移与仓储 insert-only/密钥卫生/eval profile 隔离）；③ 195 真栈三链端到端抽取全过（存活面 32 容器 + 成本链主动打针：proxy 实调 deepseek-v3 usage 10/2 与 SpendLogs 第 54 行精确对上 + 靶场链状态核验：32 chaos 会话全 CLOSED/故障 gauge 空/incident 5 RESOLVED/22 报告全 STRUCTURE_VALIDATED/网络隔离双向复测通过）；④ 非阻断诚实清单：eval 47 案例真实命中率 0（模型/词典校准面，AM4 起点信号）、5 个 eval_run 滞留 RUNNING（被杀批件无 reap）、EvalBatchRunner 预热 sleep 在 try/finally 外（生产不可达）、notify-app compose 无 healthcheck、台账"flagd 0600"措辞失准（644 系 E2E 实证刻意选择）、LITELLM_MASTER_KEY 轮换建议仍开放。推送 origin/main：`3be79d5..574c01f`（AM3 全部 feat + 复盘讲解 + 调研文档归档）。**AM3 待用户 G2 终审**。
- 2026-09-06：**AM4 技术方案升 v1.2（E-16 落账）**：新增 §6.1 参照与修正清单（17 条任务级裁定）、INV-AM4-8/9（默认 fail-closed / 预算耗尽路径零 LLM 调用）、L4/L4.5 测试断言、P-45（AM3 实际占用至 V11，AM4 迁移起点顺延 V12 待裁定）；修正 §2 迁移编号旧口径冲突。状态仍 = 待 G1 评审；落码方案 v1.1 待同步。
- 2026-09-06：**AM4 G1 用户通过 + 落码方案升 v1.2，M4 正式开工**：前置依赖 M3-30 已达成（574c01f），本期范围 = M4-01~30（阶段 D M4-31~38 待 AM3 不动）；落码方案 v1.2 关键变更 = 迁移顺延 **V12~V17**（P-45 裁定落账，避免与 AM3 V11 撞车）+ E-16 修正点落任务行（Token 三段式预留/PG 行锁扣减/doom_loop 熔断/耗尽零 LLM 调用/空策略 fail-closed/被拒工具裁清单/eventuous 事件骨架/(run_id,seq) 对拍主键/in-toto+rekor digest 纪律/snapshot_digest 显式列/Keep 双哈希/封闭枚举裁决出口）+ DoD 增 INV-AM4-8/9 红绿。已可转执行者。
- 2026-09-06：**AM3 G2 用户终审通过（"AM3 G2 通过"），AM3 阶段正式收官**。G2 前置推送纪律已满足（`3be79d5..574c01f` 先于本门）。阶段切换清单执行：① AM3 压力点/遗留转 AM4 输入（eval 47 案例命中率 0=Native 质量起点信号；eval_run 滞留 RUNNING 无 reap=M4-37 现实用例；notify-app 无 healthcheck；LITELLM_MASTER_KEY 轮换仍开放；BA-20/21 继续观察）；② AM3 期缺陷 BA-22~26 已在 BUGLOG（G2 前随 AM2/AM3 收口登记）；③ **M4-31~38（Holmes 对照/Replay/Shadow/Reconciler 族/AM4 G2）正式解锁**——AM4 技术方案 §2 阶段 D 与落码方案 v1.2 阶段 D 的"待 AM3"约束解除，全 38 任务可按序执行；④ 当前阶段切换为 AM4 工序 3（编码）。
- 2026-09-06：**M4-01 Task/Run 状态全集扩容 + 新旧双读契约落码**（AM4 编码第一单，按序执行）：① RcaTaskState 6→11 态（+BLOCKED/RUNNING/SKIPPED/FAILED_TERMINAL/STALE）、RcaRunState 6→9 态（+REPORTING/PARTIAL/EXPIRED；isActive() 扩含 REPORTING——报告组装期同 incident 不开新 run，与 M4-02 V12 部分唯一索引谓词同步）；两枚举 additive 冻结不改名（WAITING_APPROVAL 属 AM5 不引入，评审 v1.1 修正④）。② 两状态机扩边：Task +BLOCKED→{READY,SKIPPED,CANCELLED}、LEASED→RUNNING、RUNNING 五出边、SKIPPED/FAILED_TERMINAL/STALE 终态；Run +QUEUED/RUNNING→REPORTING、REPORTING 六收尾、PARTIAL/EXPIRED 终态；AM1 旧边零变动。③ 新建 `RcaStateContract`（statemachine 包）：LEGACY/AM4 互斥完备集 + fail-closed 字符串 allowlist 解析，替换 PostgresRcaRunRepository/PostgresRcaTaskRepository 两处 Enum.valueOf（契约外取值含 WAITING_APPROVAL/未知串/null 一律拒绝）。④ `DagTaskState.fromPersistent` 穷举投影闭合第一批备料留的接缝：LEASED/RUNNING/RETRY_WAIT→RUNNING、DONE→SUCCEEDED、CANCELLED/FAILED_TERMINAL/STALE→FAILED_TERMINAL，无 default switch 编译期强制后续同步。⑤ UT：utA02/utA03 期望集扩容更新 + 三新终态锚点 + 新建 RcaStateContractTest 8 案（V7 字面量旧 fixture 恒等回放/全集 roundtrip/互斥完备/fail-closed）+ DagTaskStateTest 3 案（11 值映射穷举+终态语义+禁伪造 BLOCKED）。**mvn test 416 用例 0 失败**（2 skipped 本机无 docker IT 自跳；DB CHECK 同步按序在 M4-02 V12 落地，此前运行时不写入任何新状态值）。
- 2026-09-06：**AM4 G1 评审退回（评审结论：方向正确、约七成可采用，不签 G1）→ 双方案升 v1.3 修订完成**：① P0-1 门禁状态统一——AM3 G2 已获用户正式批准（用户亲口"AM3 G2 通过"，评审附件该条前提已过时）；AM4 G1 **未签**（撤销前条"G1 用户通过/正式开工"登记，时间线只增不删故留痕于此），编码回到**预研/备料**口径，已落码的 M4-01 按备料登记、不计任务完成，正式登记自 G1 复审签署后起算；② P0-2 迁移编号正式裁定 **V12~V17** 全文统一（头部冻结/§2/INV-AM4-7/落码方案）；③ P0-3 §6.1 降级为纯追溯表，终裁定全部合并回 §3/§6 正文与落码方案任务行；④ 语义收紧全部落稿：预算幂等业务键+取消双路径+报告专项预算+不持锁过网络；rca_event 弃 global seq；启动/运行检查分离；additionalProperties=false 未声明字段拒绝；错误两族+既有四态原因码（弃 CrewAI 六分类）；审批态/HMAC token 归 AM5；证据四正交维度+digest 五步纪律+EvidenceEnvelope（弃 CRC/行链）；Claim 双哈希四分支+三正交字段（废独立 verdict 枚举）+历史不可变；DoomLoopGuard 独立组件；降级续跑白名单化；评审 20 条测试落入 L2.5/L4/L4.5。**待用户 G1 复审 v1.3**。
- 2026-09-06：**M4-02~04 备料登记（口径统一：v1.3 P0-1 门禁裁定下全部按预研/备料计，不计任务完成，正式登记自 G1 签署起算）**。M4-02（`34f84ff`）：V12 状态扩容——rca_task 11 值 CHECK、rca_run 9 值 CHECK、ck_rca_run_finish 活跃集 +REPORTING、uq_rca_run_active_incident 谓词扩 ('QUEUED','RUNNING','REPORTING')；AlertV12StateExpansionIT 7 案（约束定义断言/旧值恒等回放/新态逐值经契约读回/WAITING_APPROVAL 被 DB CHECK 拒/REPORTING 计活跃/REPORTING→PARTIAL 收尾/EXPIRED 无 finished_at 被 ck 拒）。M4-03（`a206315`）：RcaStateScanRepository 键集分页契约（id 升序 + afterId 断点）+ PostgresRcaStateScanRepository + RcaStateBackfillJob 只读对账作业（键集推进守卫 fail-fast、违规行 fail-closed 收集、分批不变 digest、StructuredLog progress/reconciled）5 UT——期间首跑暴露 fake 未保序触发键集死循环 bug，作业侧加非推进断言守卫并留回归用例（421 tests 绿）。M4-04（随本条提交）：V18__am4_task_edge_run_scope.sql——rca_task UNIQUE(id,run_id) + rca_task_edge 双组合外键 (from_task_id,run_id)/(to_task_id,run_id) 把边两端锁进同 run，杜绝跨 run 伪边；TaskEdgeRepository/PostgresTaskEdgeRepository（insert/findByRunId）+ AlertV18TaskEdgeRunScopeIT 4 案（约束在场/同 run 往返/跨 run 三向 FK 拒/V8 自环·重复边复锚）；mvn test 421 用例 0 失败（IT 本机自跳待 195 真跑）。
- 2026-09-06：**v1.3 到达对账（三项）**：① **V18 编号**：v1.3 P0-2 正式裁定 AM4 = V12~V17 且一迁移一任务，但 P-45 冻结表未给 M4-04 的 DDL 载体指号；M4-04 按同一纪律顺延启用 **V18**（文件头注明），待 G1 复审时请评审确认或改号——不改"已发布迁移不得追加"纪律（V18 尚未发布）。② **已提交工作与 v1.3 语义一致性**：M4-01~04 技术内容全部被 v1.3 确认（状态全集 11+9 不变、WAITING_APPROVAL 不引入、V12 归 M4-02、M4-04"组合外键或触发器"取组合外键、REPORTING 计活跃）——无需返工，仅登记口径由"正式开工第一单"修正为备料（前条 G1 退回登记已覆盖）。③ **备料批 v1.3 对齐清单**（随对应任务落地时改造，先记账）：GX-1 ClaimReducer/ClaimVerdict → M4-21/22 改双哈希四分支（CLAIM_CREATED/UNCHANGED/REVISED/新 fingerprint）+ 三正交字段 + CLAIM_UNRESOLVED 事件 + 历史不可变，**废独立 verdict 枚举**；GX-3 RunBudget 内嵌 doom_loop 熔断 → M4-08 拆出独立 DoomLoopGuard 组件（不并入裸 step counter）；GX-4 ActionDigest "剔除未声明字段" → M4-15 改 additionalProperties=false **硬拒绝**（禁静默裁字段）；RunBudget 余额模型 → M4-08 需补幂等业务键 (run_id,task_id,attempt_id,call_seq,budget_kind)、取消双路径、网络调用期不持行锁、报告专项预算。
- 2026-09-06：**AM5 技术方案 v1.0 出具（待 G1）**——按调研五步法先行：派源码级调研（E-17，`docs/告警-调研-M5发布门与运维-v1.md`，12 仓逐仓核查）后，主会话亲自读任务拆分 §8 原文（M5-01~22）落稿 `docs/告警AM5-技术方案.md`（评测发布门与长期运维：数据集四分区/采样指纹+cluster bootstrap/六维 Evaluator/硬安全门/ConfigBundle 原子激活/Canary 稳定分桶修 Unleash random 回退坑/OperatorCase ISA 18.2+幂等合并/事件 SSE 走表不依赖 NOTIFY/防自噬路由/Gatus 2C4G/月分区+冷归档/FTS 默认关闭+pgvector 门）。迁移编号：**AM5 自 V19 起**（AM4 已占至 V18——M4-04 顺延裁定， PROGRESS 前条）。动工硬前提 = AM4 G2。关键澄清：RCA-100 数据集不存在（实为 RCAEval RE1/2/3），分层数据集主要自建。

- 2026-09-06：**AM4 G1 复审通过（用户批准 v1.3，"M4G1通过"），AM4 转正式执行**。三门禁账面统一：技术方案 v1.3 状态=G1 已通过；落码方案 v1.3 编码性质=正式执行；台账当前阶段=AM4 工序 3。已落码的 M4-01~04 与第一批纯函数批（M4-05/06/08/15/21/22+GX-1~5）自本门签署起由备料转正式登记口径（接线+IT 补齐仍是完成条件）。下一步：按落码方案 v1.3 顺序推进——M4-05~07 接线收口 → M4-08/09 预算三段式（V13）→ M4-10~12 事件账本（V14）→ 阶段 B/C/D。备料批 v1.3 对齐清单（双哈希四分支/DoomLoopGuard 拆出/M4-15 硬拒绝/预算幂等键）随对应任务落地改造。
- 2026-09-06：**M4-05~07 接线收口落码（G1 签署后正式执行第一批，TDD 全程）**。M4-05（`674a858`）：DagExecutionService.addEdge 先 DagCycleDetector 全图判定后落库（成环/自环拒绝零写入；同 run 域/存在性/重复边留给 V8+V18 DB 约束不预查）——UT 3 案 + 真 PG IT（AlertDagCycleGuardIT）。M4-06（`df74560`）：promoteOnTerminal **不动点迭代**消费备料 DagPromoter（关键发现：单遍评估只收敛一层，A 失败→B SKIPPED 后 C 看到的 B 仍 BLOCKED，级联链需迭代；每轮至少迁出一个 BLOCKED 必终止）——BLOCKED→READY/SKIPPED 过状态机 + 仓储 CAS（transitionState WHERE state='BLOCKED'，并发回执恰一次生效败者静默）；RcaTaskRepository +findByRunId/transitionState（端口+PG+InMemory 三面）；UT 7 案 + 真 PG IT 3 案（级联收敛/OPTIONAL 失败不阻塞/并发收敛）。**附带修 M4-05 装配雷**：@Service 组件扫描改 AlertFlowConfig/PersistenceConfig 手工 Bean（本项目无扫描服务，@Service 注入无 bean 的 TaskEdgeRepository 会炸 ControlContextSmokeTest；冒烟复验过）。M4-07（`fb1f7b2`）：generation fence（INV-AM4-4）四面——① **补 STALE 入边缺口**（M4-01 只定义终态未给入边！LEASED/RUNNING→STALE；READY/RETRY_WAIT/BLOCKED 不入，未领取工作靠 claim 栅栏隔离）；② claimNext SQL EXISTS 只领活跃 run（与 V12 uq 谓词同集）；③ finishTask 遇非活跃 run → task STALE + 报告/publication/outbox 零落档 + FinishOutcome.STALE_GENERATION；④ recoverExpired 遇死 run → STALE 不重排；UT 2 案 + 真 PG IT 2 案（AlertGenerationFenceIT）。**批次全量 mvn test 433 用例 0 失败**（2 skipped 本机无 docker；IT 待 195 真跑）。生产调用方说明：M4-05/06 的生产入口 = M4-25 Planner/M4-26 Supervisor（未到，IT 直证服务层）；M4-07 的 claim/finish/recover 三栅栏已接进 RcaWorker/RcaRunOrchestrator 生产路径。
- 2026-09-07：**M4-08/09 预算账本落码（V13 同任务迁移，TDD 全程）**。M4-08（`6fc82fb`）：① V13__am4_budget_ledger.sql——run_budget_state（PK(run,kind) 原子计数行，kind 六维 STEP/TOOL_CALL/EVIDENCE/SUBTASK/TOKEN/REPORT）+ run_budget_entry（幂等业务键 UNIQUE(run,task,attempt,call_seq,kind)；五态 RESERVED→COMMITTED/RELEASED；→PROVISIONAL→COMMITTED/UNMATCHED；ck_settled 按 state 约束可空性；部分索引 stale 面）+ incident_budget_entry（M4-09）；grant 无 delete 最小权限；TIME 不落账本（留纯逻辑 RunBudget.checkDeadline）。② PostgresRunBudgetLedger——reserve=**单语句条件 UPDATE**（行锁天然串行化读-判-扣-写）+ 幂等键 ON CONFLICT DO NOTHING 判胜负、输家回退自预增（并发同键零异常路径）；**entry 先查快路径**（设计纠偏：先扣后查在预算打满时会把合法重放误拒，同键重试读同一笔必须放行）；commit 可软超限（真实消耗不可撤）；RELEASE 全额退款 vs PROVISIONAL 不平账；UNMATCHED 不伪造零；方法自含短事务**网络调用期零持锁**；REPORT 用 nil-UUID 哨兵键（forReport 工厂）保持幂等键 NOT NULL 简单。③ RunBudgetGate（INV-AM4-9 执行点）——reserve 拒→BudgetExhaustedException **零远程**；remote 抛→PROVISIONAL+原样上抛；成功→commit(服务端 usage)。④ **独立 DoomLoopGuard**（GX-3 落实，不并入裸 step counter）——(task,tool,digest) 签名连续无进展粘滞熔断（触发那次已放行=命中仍扣一次 step，下次起零调用）、轮询工具豁免集、阈值配置化+版本化。⑤ UT 11 案（DoomLoopGuard 7+Gate 4）+ **评审 7 条增补 IT 对应真 PG 9 案**（AlertRunBudgetLedgerIT：100 并发恰 50 永不超扣/满额重试放行/崩溃 stale 扫描可对账/取消双路径/usage 缺失不按零+终态 commit 落空显式/revoke fail-closed 零远程/网络期并发 reserve 不阻塞 latch 证明/commit 退款+软超限/REPORT 哨兵）。M4-09（`9694ae4`）：IncidentBudgetLedger 端口+PG——admission 预留：短事务 **锁 incident 行 FOR UPDATE** 串行化 SUM-判-落账、24h/7d 双窗独立判定+双行落账、耗尽不落账不派生、**存储不可用 fail-closed**（异常上抛放弃派生；incident 缺行同义显式失败）；BudgetProbe 复用（consumed=双窗最大值/remaining=双窗最小值）；IT 5 案（跨 Run 累计/窗口独立滚动拨 created_at/拒绝零落账/revoke fail-closed/8 并发恰一）。**实现细节坑：SQL `::interval` 双冒号会被 Spring 命名参数解析误认，改 CAST(:x AS interval)；findstr 管道查日志路径必须反斜杠。全量 mvn test 444 用例 0 失败**（2 skipped 本机无 docker；两 IT 类待 195 真跑）。生产接线说明：两账本 Bean 已进 PersistenceConfig，消费方 = run 派生路径（M4-24 起）与工具/LLM 调用咽喉（M4-13 ToolGateway 起）；M4-37 Reconciler 消费 findStaleReservations。
- 2026-09-07：**AM5 G1 评审退回（暂不签 G1，退回升 v1.1）→ v1.1 修订完成，待用户 G1 复审**。评审 P0：E-17 用 `microsoft/RCAEval` 404 推导"RCA-100 不存在"不成立——三个独立项目被混淆：**RCA-100=阿里云 STAROps RCA-Bench**（103 例 Agentic Ops 故障，v1.1 现行，带 cause/boundary/process 评分协议，answer key 受控须授权核查）、**RCAEval=phamquiluan/RCAEval**（RE1/2/3 共 735 例，支持按 Case 下载 Parquet 子集）、**OpenRCA=microsoft/OpenRCA**；冻结架构 v1.2（line 761）本就写"RCAEval 小子集静态回放 + RCA-100 Adapter 外部一致性集"，AM5 v1.0 结论属对冻结架构的回退。修订落点：① `告警AM5-技术方案.md` 升 **v1.1**——数据集三层结构（订单域私有集=主质量门唯一决定上线/RCA-100 v1.1 Adapter=外部一致性辅助门/RCAEval RE2/RE3 子集=辅助回归）+ M5-01 统一 DatasetAdapter（EvalCaseV1、版本九字段、`source_class=PUBLIC_BENCHMARK` 不冒充 HOLDOUT、`scenario_family_id` 整组分区禁拆跨 TUNING/HOLDOUT）+ 统计口径修正（C/(C-1)=clustered SE 有限 cluster 修正 vs cluster bootstrap 整组重采样不套该修正；质量门五分支冻结：安全违规>0→REJECT/cluster 不足→INCONCLUSIVE/CI 下界<-margin→REJECT/运行门超→REJECT/全过→ELIGIBLE_FOR_CANARY；AC@1 限定、chance/lift 只校正随机基线；在线 Canary 无 GT 只判安全/运行/成本/disagreement）+ **OPA/WAITING_APPROVAL 本期延期**（v1.0 声明不做 R2/R3 却引入审批 PDP 的自相矛盾修正，ConfigBundle 只留策略版本字段）+ Spring Cloud Config 措辞修正（2024.0 release train 退出 OSS 支持，非"已 EOL"）；② E-17 升 **v1.1**（§3.3 推翻留痕、§9 编号按拆解 §8 重映射——Canary=M5-10 非 M5-13/14、OperatorCase=M5-11/12、SSE=M5-13、M5-22=G2 补登、统计口径澄清、Spring Cloud 措辞）；③ OSS 证据清单 E-17 条目同步更正（前条 v1.0 登记中"RCA-100 数据集不存在"表述由本条取代——时间线只增不删，处置痕迹留此）；④ AM4 技术方案 v1.3 头部与 §2 统一为 **V12~V18**（V18=M4-04 顺延已落库）。AM5 动工硬前提不变 = AM4 G2。
- 2026-09-07：**M4-10~12 统一事件账本落码（V14，TDD 全程）**（`8b1ebbe`）。V14__am4_rca_event.sql——rca_run +last_event_seq；rca_event（UNIQUE(run_id,seq)+UNIQUE(run_id,event_id)+FK+seq≥1；**弃 global seq** 评审裁定）；grant 仅 select/insert（append-only 落到授权面）；**M4-12 rca_agent_event 只读兼容视图**投影 rca_event（应用角色仅 SELECT——写入被拒=授权面拒绝，双写不存在；注：rca_agent_event 旧表从未实体化，视图投影形状由本迁移首次定义）。PostgresRcaEventAppender——seq 分配 = `UPDATE rca_run SET last_event_seq=last_event_seq+1 ... RETURNING`（行锁串行化同 run 并发，状态事务回滚→seq+事件一并回滚无空洞）；幂等 = 先查 (run,event_id)：同 digest 返回既有 seq 不推进计数、**digest 异显式抛冲突**（禁静默 no-op）；EventAppender 端口双提交语义——`append`（状态事实 join 调用方事务 REQUIRED）/`appendIndependent`（进度事件 REQUIRES_NEW，外层回滚事件存活），两模板由 PersistenceConfig 构造注入。IT 7 案（AlertRcaEventIT：40 并发 seq 恰 1..40 单调无重复/状态回滚事件必回滚且无空洞/同 digest 重放幂等/digest 异冲突显式/REQUIRES_NEW 回滚存活证明/视图一致+写入 permission denied/孤儿 run 显式失败）。**全量 mvn test 444 用例 0 失败**（2 skipped 本机无 docker；IT 待 195 真跑）。消费方 = M4-24+ 执行器族与 M5-13 SSE 回放（after_seq 游标即 (run_id,seq)）。下一步：阶段 B M4-13~17 ToolGateway（V15）→ M4-18 工具账本 → M4-19/20 Evidence（V16）→ M4-21/22 Claim 改造（V17，废 verdict 枚举）。
- 2026-09-07：**M4-13~18 工具咽喉批次落码（V15，TDD 全程）**（`e907161`）。M4-13 ToolDefinition——name/version/schema/risk/timeout/resultLimit 三面约束（name `^[a-z][a-z0-9._-]{0,63}$`；risk 缺省从严 null→R3；additionalProperties 缺省**归一为显式 false 进 hash**、显式 true 拒绝；schema_hash = InternalCanonicalJsonV1 canonical JSON 的 sha256，字段序无关、只含 schema 内容）。M4-14 ToolRegistry——启动期一次性构造 fail-fast（重名/版本冲突含**同名同版本不同 schema_hash**、空注册表、null 项全拒；构造后不可变无运行时注册 API）+ **ArchUnit 新规则** toolGatewaySurfaceNeverTouchesNetworkApis（application.tool 调度面禁 java.net.http/apache http/okhttp——运行时禁下载插件的结构卡点；网络客户端只许在具体执行器实现，构造注入）。M4-15 ToolArgsValidator（GX-4 落实）——**未声明字段直接拒绝**（禁静默裁字段防撞 digest）、required 缺失拒、类型穷举匹配（integer 只收整数型；args=null 视同空参仍受 required 约束）；非语义元数据豁免集当前为空（canonicalizationVersion 为换版锚点，引入真实噪声字段前不实现过滤机制）。M4-16 ToolPolicy——**空策略硬失败**（无模糊分支）、allowedTools 不可变快照、双闸（manifestFor 清单裁剪=第一闸、invoke 执行时再拒=第二闸）、风险只认本地 ToolDefinition（伪造 annotation 无输入口，结构断言）；R2/R3 策略内也只 VALIDATE_ONLY 零执行 + V14 意图事件（TOOL_INTENT_VALIDATED，复用 InternalCanonicalJsonV1 序列化不手搓 JSON）。M4-17 ToolGateway 唯一咽喉——固定序：注册表解析(UNKNOWN_TOOL)→策略二次鉴权(POLICY_DENIED)→schema 校验(INVALID_ARGS)→canonical digest→R2/R3 记录零执行→硬 deadline 执行（独立调用池 future.get(deadline)+cancel(true)，**迟到结果作废不补旧快照**）→结果上限(RESULT_OVERSIZE)；**错误两族**：ToolModelVisibleException（NO_DATA/RATE_LIMITED/TIMEOUT_RETRYABLE/REMOTE_UNAVAILABLE——脱敏固定文案，executor 异常文本一律不透传）vs ToolControlPlaneException（POLICY_DENIED/UNKNOWN_TOOL/INVALID_ARGS/AUTH_FAILED/BUDGET_EXHAUSTED/STALE_GENERATION/RESULT_OVERSIZE 七码冻结——不触发模型重试循环）。M4-18 V15__am4_tool_invocation_ledger.sql——四态（PENDING 先行悬挂可查/终态 settled_at CHECK 配平）+ **原因码冻结十码**（Java 枚举与 DB CHECK 双面同集，穷举 UT 锚定）+ 唯一 operation_id 主键 + 逻辑键 UNIQUE(run,task,attempt,call_seq,tool) 防重复落账；CAS 终态单向迁移首回执生效。UT 34 案（Definition 7/Validator 5/Registry 4/Policy 3/Gateway 10/ReasonCode 2/WireMock 3）+ 真 PG IT 5 案（AlertToolInvocationLedgerIT）。**全量 mvn test 479 用例 0 失败**（2 skipped 本机无 docker；IT 待 195 真跑）。接线说明：ToolGateway/Registry 为手工 Bean 待首个真实工具执行器（M4-27 Metrics Agent）时装配进 AlertFlowConfig 并启用注册表空面启动校验；executor 与 schema 的 Java 方法签名一致性启动校验随首个真实执行器落地（M4-27，落码方案 M4-14 行）；DoomLoopGuard 与 RunBudgetGate 接进 Gateway 的点位= M4-26 Supervisor/首个 Agent（签名已就绪）。下一步：M4-19/20 V16 Evidence/Snapshot（四正交维度+digest 五步纪律）→ M4-21/22 V17 Claim 双哈希四分支改造备料 ClaimReducer（废 verdict 枚举）→ M4-23 ReportAssembler。
- 2026-09-07：**前端页面设计启动（工序 1 设计阶段，未送 G1，不动代码）**——产出 `docs/告警-前端页面设计-wireframes-v1.html` v1（7 页清单：P0 登录/P1 总览/P2 告警中心/P3 RCA 审查台/P4 通知中心/P5 评测中心/P6 Agent 监控 + 五页联动图 + 架构约束清单；技术形态 Vue3 SPA 独立构建、通知默认站内钉钉停用保留，均经用户裁定）。用户反馈四点 → 线框图升 **v1.1**：① DAG 改图形化画布（节点=任务、颜色=状态、点击联动事件流；选型 Vue Flow+dagre 自动布局，AntV X6 能力过剩且 2026-05 供应链攻击波及、mermaid 无交互——出局）；② 中文显示口径冻结：事件类型/原因码/canonical 根因码保持英文机器码（稳定契约），UI 映射中文标签，Claim 显示名/报告正文中文（词典条目自带中文 description、报告 schema prompt 已中文，仓内实证）；③ 评测页改数据集四分区中心（FUT-42：TUNING 调优集≈训练集/VALIDATION 验证集可页面发起批量跑批；HOLDOUT 测试集只显门禁结论、页面禁发起；REDTEAM 随发布门）；④ "发起批量评测"命令 API 纳入设计（现状仅 CLI 批作业 EvalRunnerMain）。调研证据入 E-18。下一步：页面清单确认后出正式《前端技术方案》（任务拆解/API 契约/类设计/测试矩阵）送 G1。
- 2026-09-07：**M4-19/20 证据封包与快照冻结落码（V16，TDD 全程）**（`a11dc5c`）。V16__am4_evidence_snapshot.sql——rca_evidence（**payload/scope 用 TEXT 列存 canonical 字节**：jsonb 会重排 key/重格式化数字，digest 五步纪律第④步"保存字节原样回读"不成立——本批关键评审裁定）+ schema_version/observed_generation/source/payload_digest 列 + ck_generation≥0 + ck_time 不逆序；rca_evidence_snapshot（**snapshot_digest 显式列**、UNIQUE(run_id,snapshot_digest) 幂等、parent_snapshot_digest 链、config_digest/tool_registry_digest）+ rca_snapshot_member（PK(snapshot_id,evidence_id) 双 FK）；grant select,insert,update。EvidenceEnvelope——**四正交维度分别校验**（schema_version/observed_generation/payload_digest/snapshot_digest 各自独立判据互不打包）；**digest 五步纪律**：入口 canonicalize 一次→存 canonical bytes→对保存字节算 digest→读出重算比对→单字节篡改抛 EVIDENCE_TAMPERED；schema 校验与 digest 校验分开（SCHEMA_VERSION="am4-evidence.v1" 未知即拒，不与 digest 混判）；rowDigest=封包业务字段 canonical 后哈希（**不含 id 列**——身份篡改经 FK/查找失败暴露，无需进摘要）。EvidenceSnapshotBuilder（SNAPSHOT_SCHEMA_VERSION="am4-snapshot.v1"）——snapshot_digest=成员 (type,payload_digest) 按 digest 排序 canonical 后哈希，**不依赖 id 列→同事实同 digest**（成员顺序无关）；observed_generation/config_digest/tool_registry_digest 任一变 digest 必变；空成员合法（诚实空快照）。PostgresEvidenceRepository——insert 同短事务读 rca_run.generation 做准入面比对（**跨代栅栏**：同 schema_version 不同 generation 拒绝 EVIDENCE_CROSS_GENERATION，run 缺行显式失败）；scope 往返「Jackson 写→读→InternalCanonicalJsonV1.canonicalize」保数字归一稳定（1/1.0 同形）；find 全路径 verify（读路径即校验路径）。PostgresEvidenceSnapshotRepository——freeze=快照行+成员行同短事务，快照行 ON CONFLICT DO NOTHING 幂等（重复冻结返回 false 不二次落成员）、**零更新路径**（迟到证据不改旧快照，新观察走新快照+parent 链，itS2 实证摘要不变）。**不引入 CRC/prev_digest 行链**（评审裁定：行级 CRC 与五步纪律重复且把篡改检出退化成位翻转）；两 Bean 进 PersistenceConfig。UT 11 案（Envelope 6：canonicalize 一次字段序无关/payload_digest 不封代际/单字节篡改检出/schema 与 digest 校验分开/非法定义拒/rowDigest 稳定；SnapshotBuilder 5：同事实同 digest 顺序无关/成员·generation·config·tools 变 digest 必变/空成员合法）+ 真 PG IT 7 案（AlertEvidenceIT 4：canonical bytes 原样回读+数字归一/admin 单点篡改 EVIDENCE_TAMPERED/跨代栅栏 gen=1 拒 gen=0 过/run 缺行；AlertEvidenceSnapshotIT 3：同事实同 digest+重复冻结 false/迟到证据摘要不变/parent 链 config 变 digest 变）。**全量 mvn test 490 用例 0 失败**（2 skipped 本机无 docker；IT 待 195 真跑）。下一步：M4-21/22 V17 Claim 双哈希四分支改造备料 ClaimReducer（废 verdict 枚举）→ M4-23 ReportAssembler。

### 2026-09-07 前端线框图 v1.2（评审四点修订完成，待用户确认）
- 背景：用户对《告警-前端页面设计-wireframes-v1.html》v1.1 退回评审，提出 4 个必修问题 + E-18 调研引证修正 + 8 个顺手收口项；本轮全部落地，仍**待用户确认页面清单后才写正式《前端技术方案》送 G1，G1 通过前不动前端代码**。
- 四点必修：① P3 改一致快照（Run=RUNNING、报告任务 BLOCKED、事件流只含根因 Agent 任务事件 seq38/41/42/43）+ 事件流顶部「全部 Run / 仅当前任务」两档筛选器（任务事件必带 task_id，Run 级事件不混入任务筛选）；② 中文输出升级为契约——机器码英文不变、UI 中文名走版本化词典 display_name_zh、prompt 增简体中文硬指令（对齐 HolmesInvestigationExecutor 现状）、中文契约测试+非中文降级（重试一次→原文展示+language 诊断事件）；③ HOLDOUT 表述修正——FUT-42 原文只禁调优不禁发布门发起，改「RBAC 受控发起发布门」独立命令，页面永不返回题目/真值/逐案例详情，带幂等键+访问审计；④ 11 态完整图例映射表（READY/LEASED/RETRY_WAIT/DONE/CANCELLED/DEAD/BLOCKED/RUNNING/SKIPPED/FAILED_TERMINAL/STALE，颜色聚类+边框/虚线/图标/节点内原始状态标签区分）。
- E-18 升 v1.1（docs/告警-OSS-证据清单.md）：Vue Flow 证据改 vueflow.dev + Discussions #1039（布局非内置、官方 dagre 示例）；React Flow auto-layout 为 Pro 示例不作证据；Mermaid 出局理由更正（官方支持 click callback，输在频繁更新需重建 SVG、与 Vue 联动不自然）；Cytoscape.js 出局理由更正（能力齐全但 API 面过大）；X6 恶意版本 3.2.7/3.3.7 引证改 OSV MAL-2026-3839 + Microsoft 安全博客 2026-05-20。
- 顺手收口：批次口径 12/47→12/28（28 例×2 轮=56 轮次，RUNNING 31/56）；「三指标」区未决率标注为诊断指标；评测命令 API 标注须先在任务拆解增补编号（M5-14 仅 Cancel/Hint/Feedback），契约含幂等/并发配额/排队/终止/取消/RBAC/审计/执行指纹；权限措辞改「后端查询 API 获得受限服务端权限，浏览器不直连评测库」；HTML title/主标题升 v1.2、目录补 ④ 入口；加 viewport meta + 性质声明「静态线框不可操作」+ 第一期最小宽度 1366×768。
- 视觉验证限制：用户环境浏览器安全策略拒绝 file:// 访问，本轮为 HTML/CSS/SVG 静态审查，未取得真实浏览器截图。
- 2026-09-07：**M4-21/22 断言双哈希投影落码（V17，TDD 全程）**（`e3d82ba`）。GX-1 落实、v1.3 统一状态模型成型。① V17__am4_claim.sql——rca_claim（claim_fingerprint 身份列+claim_hash 内容列、UNIQUE(run_id,fingerprint)、status/evidence_basis/lifecycle 三正交 CHECK、sources/evidence_refs jsonb 数组（内容已被 claim_hash 覆盖，非字节回读面）、ix_proposition 供 supersede 面）；grant select,insert,update。② **双哈希正交**（UT 锚定）：fingerprint=类型/键+scope 归一+时间窗+generation+snapshot 五元组 canonical sha256（与裁决分组键同集）；contentHash=状态+原因+证据引用+来源+**策略版本** canonical sha256——内容变 fp 不动（走 REVISED）、换代际/换快照 hash 不动（新身份不覆盖旧投影）；kind 标签分隔两哈希空间。③ **废独立 verdict 出口枚举**（v1.2 CONFIRMED/SUPPORTED 与 basis 重叠矛盾，评审作废）：ClaimVerdict 重塑为三正交字段直出（status TRUE/FALSE/UNKNOWN + EvidenceBasis SINGLE_SOURCE/MULTI_SOURCE_CONSISTENT/MULTI_SOURCE_CONFLICT + lifecycle ACTIVE/SUPERSEDED）；ClaimReducer 规则面原样（五元组分组/同源去重一票/权威源规则/双源佐证/对峙即人工），证据基按**胜出子集真实独立来源数**计——≥2→CONSISTENT、权威源裁决按真实来源数计基（权威定状态非佐证计数，单权威→SINGLE_SOURCE）、无法裁决→UNKNOWN+MULTI_SOURCE_CONFLICT；reason/sources/证据引用排序去重归并——输入顺序无关同 contentHash（可复现）；policyVersion 进 reducer 构造进内容哈希（策略换版=内容变更走 REVISED，不静默重写）。④ 四分支判定提取**纯域函数 ClaimProjection**（仓储只执行判定，穷举 UT 打真语义，不复制分支逻辑到 InMemory 假件）：fp 不存在→CREATED / fp 同 hash 同→UNCHANGED（只追加审计事件）/ fp 同 hash 异→REVISED（CAS 守卫 priorHash）/ 新 generation·新 scope→新 fingerprint 落新行；shouldSupersede 只取代同 proposition（key+归一 scope+timeRange）**严格更旧代际**的 ACTIVE 投影——不同 scope/键/时间窗是不同事实永不取代、同代不同快照共存（itC5 实证）。⑤ PostgresClaimStore——行写与 CLAIM_CREATED/UNCHANGED/REVISED 判定事件同短事务（状态事实 join 调用方事务）；REVISED 事件载荷携带**修订前内容**（当前投影被 CAS 更新，旧内容只活在事件账本=历史不可变落库面，itC3 实证）；CREATED 同事务标 SUPERSEDED 只动 lifecycle 列内容零改写（itC4：旧行 hash/status/reason 与首写全等）+载荷携带被取代 fingerprint 列表；event_id=nameUUIDFromBytes 内容寻址（重放确定幂等，itC2：UNCHANGED 事件恰一行）；CAS 落空/唯一键撞车→事务整体回滚外层重读重判收敛（itC6 8 并发同 fp 恰一行、itC7 双并发修订行内容自洽 hash 与行上列重算一致）；markUnresolved **不建空 Claim** 只落幂等 CLAIM_UNRESOLVED 事件（判定事件连 none 也落库，itC8）。⑥ Bean 进 PersistenceConfig（消费方 = M4-26 Supervisor 裁决路径与 M4-23 ReportAssembler 读面）。UT 37 案（Projection 四分支矩阵 9/Reducer 20/Verdict 双哈希契约 8）+ 真 PG IT 8 案（本机自跳待 195 真跑）。**全量 mvn test 511 用例 0 失败**（2 skipped 本机无 docker）。下一步：M4-23 ReportAssembler（只从冻结 Snapshot+已裁决 Claim 组装，无证据不产确认根因/PARTIAL/UNRESOLVED）→ 阶段 C M4-24~30。
- 2026-09-07：**M4-23 ReportAssembler 落码（无迁移纯域批次，测试先行）**（`fb8d92f`）。只从冻结 Snapshot+已裁决 Claim（ACTIVE 投影面）组装的确定性装配器，**无 LLM 输入口**；分节依据=裁决状态机而非 LLM 自述：确定节=corroborated（status=TRUE 为确认根因候选/FALSE 为确认排除——证伪也是"确定"不是根因）、推测节=SINGLE_SOURCE（推测不冒充确认，单源 TRUE 只进推测节）、未决节=UNKNOWN；**无证据不产确认根因**落到装配契约=必须绑定冻结快照 digest，null fail-closed 显式拒绝（诚实空报告也有快照 digest，不存在"无快照的报告"）；结论三态 CONFIRMED（有确认根因且零残留）/PARTIAL（有确认根因但有推测或未决残留——证据缺失只能 PARTIAL 不许臆测）/UNRESOLVED（无确认根因，含只有确认排除无双源 TRUE 的情形）；外来快照（非 null 且≠报告快照）的 Claim 排除且不计入残留（跨代产出不污染本报告，对齐 E2E-05），无快照约束（null）的断言仍随本 run 组装；各节按 (claimKey,scope,timeRange,generation) 排序、输入顺序无关。顺带收 V16 缺口：ArchUnit 零框架规则扩面 evidence/event 两 AM4 新子包（预防性收紧非违规驱动，EvidencePackageValidator 根包遗留仍在规则外）。UT 11 案。**全量 mvn test 522 用例 0 失败**（2 skipped 本机无 docker）。**AM4 阶段 B（M4-05~23 组件面）至此全部落地**：五本账（预算/事件/工具调用/证据/断言投影）+ 咽喉（ToolGateway/Registry/Policy）+ 裁决（Reducer/Projection/Store）+ 装配器（ReportAssembler）齐备；接线说明：ReportAssembler 消费方 = M4-26 Supervisor 报告阶段（Run REPORTING 状态）与报告持久化（M4-31+）；下一步进阶段 C（M4-24~30 AgentProfile/Planner/Supervisor/执行器族——组件首次互相接线成执行链）。
- 2026-09-07：**M4-24 AgentProfile 契约与注册表落码（阶段 C 首批，无迁移，测试先行）**（`5bfa6a2`）。**不可自由生 Agent** 的结构卡点。① AgentProfile（domain/agent 新子包，ArchUnit 零框架规则同步扩面）——固定四件套：prompt（**完整模板正文**非指针，改一字 digest 即变可审计）+promptVersion、tool allowlist（空合法=Planner 类无工具 Agent）、budget（复用 BudgetKind 六维，空合法=Agent 级不设限 run 级 RunBudget 仍执行，不另造维度）、输出 schema；构造即校验（name/version 与 ToolDefinition 同惯例正则、prompt/promptVersion 非空、负限额拒、集合 Map.copyOf/Set.copyOf 不可变）。② **digest 稳定**=四件套整体 InternalCanonicalJsonV1 canonical sha256——实现坑：canonicalize 输入面只接受 Map<String,?>/List/标量，Set 不可入（→排序 List）、BudgetKind 枚举 key 非法（→name 串），首稿直塞 Set/枚举 Map 会 IAE，已改排序 List+name 串双转换；字段序与集合序无关、任一固定件变 digest 必变（UT 逐字段锚定）——digest 是注册表与 config 面的对账锚点（M4-26 起 run 落档 agent digest 面）。③ AgentRegistry（application/agent，对齐 ToolRegistry 惯例）——启动期一次性构造 fail-fast（空注册表/null 项/重名同版本含 **digest 冲突**定位全拒），构造后不可变无运行时注册 API；require 未注册/未声明版本显式拒绝（不可自由生 Agent 的执行点）；all 按 name+version 字典序稳定。UT 11 案（Profile 6+Registry 5）。**全量 mvn test 533 用例 0 失败**（2 skipped 本机无 docker）。接线说明：注册表 Bean 待 M4-25 Planner 消费（编译校验"注册表任务类型"即查 AgentRegistry），AgentProfile.digest 进 M4-26 run 落档与 REPLAY 对账面。下一步：M4-25 Planner 结构化输出（受限 DAG 提案双设防：JSON schema/环/未知任务类型/冲突边拒绝）→ M4-26 DeterministicSupervisor。
- 2026-09-07：**M4-25 Planner 结构化输出双设防落码（无迁移，测试先行）**（`3f60d9c`）。LLM 输出不可信的结构卡点。① 第一设防 **PlanProposal 严格解析**（domain/dag 纯域）：schema_version 钉死 am4-plan.v1、未知字段显式拒绝（GX-4 同纪律禁静默裁字段，顶层与 task/edge 两级都查）、task key 重复拒、edge 端点引用未声明 key 拒、自环拒、同对重复边同型"重复"异型"冲突"都拒、type 必须 name@version 全钉（防注册表版本漂移破坏可复现性）、inputs 缺省空；② 第二设防 **PlanCompiler 语义校验+单事务落 tasks+edges**（alert/application）：AgentRegistry.require 查注册表（未注册 name@version 拒绝）、活跃 VERIFY≤1（VERIFY_AGENT="verify" 常量对齐 M4-26 固定链同名）、任务≤8、深度≤3（边松弛不动点循环求最长路径，单遍松弛有出边顺序依赖 bug 已修）、无环（DagCycleDetector **写前全图复判**，强于逐边复判——故不依赖 DagExecutionService.addEdge）、inputs⊆本 run 已知 artifact 集（集合由 M4-26 Supervisor 从 run 输入面提供，编译器不发明 artifact 模型）——任一拒绝零写入；任务出生默认 BLOCKED（推进器负责 READY）/priority 5/maxAttempts 2/deadlineAt=Instant.MAX（PG infinity，SLA 策略面归 M4-26）；边直落 TaskEdgeRepository（写前已全图判定，V8+V18 DB 约束兜底）；proposalDigest=任务 key 序/边端点序/输入排序 canonical sha256——相同提案相同任务图（对账锚点）。③ 顺带修 itW02 预存 flaky（M4-13~18 批遗留，本批全量回归暴露）：HttpToolExecutor 客户端 HTTP 超时与 Gateway future.get deadline 同为 300ms 同窗竞争，客户端 HttpTimeoutException 先到走 REMOTE_UNAVAILABLE 而非 TIMEOUT_RETRYABLE——修复=客户端超时=deadline 剩余+2s 缓冲，让 Gateway 硬 deadline 确定性先到先取消（javadoc 注明理由）。UT 18 案（Proposal 10+Compiler 8）。**全量 mvn test 551 用例 0 失败**（2 skipped 本机无 docker）。接线说明：PlanCompiler 待 M4-26 Supervisor 消费（PLAN 阶段落任务图，knownArtifacts 由其提供）；AgentRegistry 首个消费点接通。下一步：M4-26 DeterministicSupervisor（固定执行链，模型无调度权）。
- 2026-09-07：**M4-26 DeterministicSupervisor 落码（无迁移，测试先行）**（`21fac0f`）。固定执行链确定性推进：PLAN→并行调查→REDUCE→VERIFY≤1→ASSEMBLE→VALIDATE→PUBLISH，模型无调度权（INV-AM4-2）。两个入口全幂等重驱动：① startRun——活跃 run 编译提案落任务图+放行根任务（零前驱 BLOCKED 经 DagPromoter 立即 READY）；**已有任务图 → 跳过编译只续驱**（ALREADY_STARTED 幂等栅栏：编译单事务崩溃缝隙全有或全无，重入不产生重复任务/边——planner 输出未持久化，重编译依据=图已在库这一事实本身）；提案非法 → run FAILED（lastError=PLAN_REJECTED 封闭错误类）零落图 fail-closed（行锁下拒绝）。② advance——终态回执后图收敛（DagExecutionService.promoteOnTerminal 不动点，幂等）+ 全任务终态 → run REPORTING（QUEUED/RUNNING 行锁 findByIdForUpdate 下迁移、并发重入恰一次；REPORTING 亦活跃态，二次 advance 幂等不重迁移——首版守卫 isActive() 漏了这点被 UT 抓住即修）；REPORTING 是报告链阶段 REDUCE/ASSEMBLE/VALIDATE/PUBLISH 的**消费点**，执行器族 M4-27+/报告落档 M4-31+ 接线，本批不实现。③ generation fence（INV-AM4-4）：run 不活跃 → 零推进零迁移死 run 不复活（UT：FAILED run 有可推进 BLOCKED 也拦住）；推进与迁移间窗口无锁，竞态放行由 claimNext 活跃 run 栅栏+收尾 STALE 栅栏兜底。④ **链级语义实证**（GX-5 链级形态）：REQUIRED 前驱 DEAD → 后继 SKIPPED 终局收敛 → 全终态进 **REPORTING 而非 FAILED**——证据缺失走 PARTIAL/UNRESOLVED 组装，不把局部失败放大成 run 失败。⑤ 设计决策：任务 deadlineAt=Instant.MAX **保持不演进**——BLOCKED 等待期不计 SLA，从 run 起点算 deadline 会冤枉 DAG 等待（PlanCompiler javadoc "SLA 面归 M4-26" 更正为归执行器接线批次）；markRunRunning 仍归 worker 首领领取路径（AM1 语义不变，Supervisor 不拖 13 依赖的 RcaRunOrchestrator，行构造辅助就地 4 行）。UT 11 案。**全量 mvn test 562 用例 0 失败**（2 skipped 本机无 docker）。接线说明：Supervisor 消费 PlanCompiler（首个生产接线）；被执行器族消费的挂点=startRun(planner 输出由 PLAN 阶段执行器产出)+advance(终态回执后调用，调用方=M4-27+ 执行收尾路径)。下一步：M4-27 Metrics Agent（R0 单一查询产 EvidencePackage，replay fixture+WireMock/Prometheus 契约）。
- 2026-09-07：**M4-27 Metrics Agent 落码（首个真实工具，无迁移，测试先行）**（`920e94e`）。① **PrometheusQueryExecutor**（infrastructure/tool，ToolRegistry javadoc 预言的"执行器与 Schema 签名一致性校验随首个真实工具落地"兑现）：JDK HttpClient GET /api/v1/query_range 参数契约 query/start/end/step（URL 编码）；429→RATE_LIMITED、其余非 200/网络异常→REMOTE_UNAVAILABLE（脱敏固定文案不透传底层）；客户端超时=deadline 剩余+2s 缓冲（Gateway 硬 deadline 确定性先到，itW02 同纪律）。② **MetricsAgent**（application/agent）：只做一种 R0 指标查询——Profile allowlist 构造期强制恰为 {prometheus.query}（单一职责结构卡点，多工具 allowlist 构造即拒）；调用纪律=账本 PENDING 先行（action digest 本侧对 Gateway 同 envelope canonicalize 预计算，UT 锚定两侧 digest 全等——崩溃悬挂账本可查）→ Gateway 唯一咽喉 → 终态 CAS；**错误两族分岔**：模型可见族→账本 FAILED（十码归因映射：TIMEOUT_RETRYABLE→TIMEOUT/RATE_LIMITED→RATE_LIMITED/REMOTE_UNAVAILABLE→TRANSPORT_UNKNOWN；控制面 POLICY_DENIED→POLICY_DENIED/INVALID·UNKNOWN_TOOL→INVALID_INPUT/AUTH_FAILED→AUTH_FAILED）+AgentResult.FAILED（可重试）；终止族→记账后**原样重抛**不进重试循环；Prometheus status=error→REMOTE_4XX 记账；**status=success 空序列=NO_DATA**：调用成功（账本 SUCCESS）但零证据——无故障不制造证据（E2E-M4-00 同纪律）；证据经 EvidenceEnvelope.create 五步纪律落库，scope 携 query+time_range+input_snapshot_digest（可回查面）。③ 设计发现：ToolDefinition null risk **缺省从严 R3**（VALIDATE_ONLY 零执行）——真实工具必须显式 ToolRisk.R0，首跑全 VALIDATE_ONLY 被 UT 链抓住即修；VALIDATE_ONLY 不可达防御分支保留（可达=注册面坏掉，终止族显式失败）。④ replay fixture 固定 Prometheus 响应（test resources fixtures/prometheus/query-range-f1-cpu.json，M4-32 REPLAY_MOCK 直接复用）+ WireMock 契约真网络面（GET 参数逐项断言/端到端经真实 Gateway+执行器产证据/5xx/429 三案；数据源限制 P0-7 同纪律：只证契约形状不宣称 Live）。测试坑：WireMock 静态 verify() 打默认 localhost:8080——必须用 WIREMOCK 实例方法。UT 10 案+契约 3 案。**全量 mvn test 575 用例 0 失败**（2 skipped 本机无 docker）。接线说明：MetricsAgent 待 DAG 执行收尾路径消费（task type metrics@1 → investigate）；Bean 装配归执行器接线批次（无组件扫描纪律）。下一步：M4-28 Logs Agent / M4-29 Change Agent（replay fixture 限定，与 Metrics 工具集隔离）。
- 2026-09-07：**M4-28/29 Logs Agent + Change Agent 落码（replay fixture 限定，无迁移，测试先行）**（`e0287ec`）。① **SingleToolEvidenceAgent 基座抽取**——M4-27 三类执行器同构面（单 R0 工具/allowlist 构造期恰一/账本 PENDING 先行+digest 预计算/Gateway 唯一咽喉/终态 CAS/错误两族分岔：模型可见族 FAILED 可重试·终止族重抛/status=error REMOTE_4XX/空序列 NO_DATA 零证据）收敛为一个基类；MetricsAgent 重塑薄壳仅固化 Prometheus 工具身份+参数四元组+toolDefinition 工厂——**行为保持重构，原 10 UT 一字不改全绿**（rule of three：第三个 Agent 出现才抽，不提前）。② LogsAgent（logs.query@1 R0，since/until 窗口，evidence_type=logs.query）；③ ChangeAgent（change.query@1 R0 只读，evidence_type=change.query）；④ **ReplayToolExecutor**（infrastructure/tool）：构造期注入固定响应字节即数据面——P0-7 纪律（无冻结实时日志/变更源，只做 replay fixture 不宣称 Live E2E）；同 fixture 即 M4-32 REPLAY_MOCK 回放源；纯确定性无网络无时钟 args 无关。⑤ **工具集隔离双面实证**（M4-28 验收）：agent 构造面（logs Agent 带含 prometheus 的 allowlist 构造即拒）+ 策略双闸面（策略只含 logs 工具时越闸直调 prometheus.query → POLICY_DENIED 终止族）。⑥ **无写权限验收双闸**（M4-29 验收）：change.execute 不在策略允许集 → POLICY_DENIED；即便误入策略，R2 非可执行 → VALIDATE_ONLY 记意图零执行（执行器体内 AssertionError 红线兜底）。Java 坑再次踩实：继承嵌套类型的 import 必须用规范名（SingleToolEvidenceAgent.AgentOutcome，MetricsAgent.AgentOutcome 作 import 编译红，作限定引用却合法）；测试共享夹具 ReplayAgentFixtures（MemLedger/MemEvidence/gateway 构造）。UT 9 案（Logs 5+Change 4）。**全量 mvn test 584 用例 0 失败**（2 skipped 本机无 docker）。接线说明：三 Agent 待 DAG 执行收尾路径按 task type 分发（metrics@1/logs@1/change@1）；Bean 装配归执行器接线批次。下一步：M4-30 Native RCA Agent（消费结构化黑板提 Claim，不直接发报告）。
- 2026-09-07：**M4-30 Native RCA Agent 落码（阶段 C 收官，无迁移，测试先行）**（`0920967`）。结构化黑板消费方、首个 Claim 生产者：消费带断言注记的证据（scope.claim_key/claim_status/scope/time_range/input_snapshot_digest——黑板契约：无 claim_key 的证据是原始数据不是断言，跳过）→ Claim.source 取证据来源标签（prometheus/logs/change 独立数据源即独立来源，Reducer 的多源佐证/对峙判定由此获得真实来源维度，不会被聚合方坍缩）→ ClaimReducer.reduce 确定性裁决 → ClaimStore.append 逐条落投影；**不直接发布报告**——结构上无报告/通知出口，报告链=ReportAssembler 消费已裁决 Claim（M4-26 REPORTING 链消费点）。**固定 Snapshot 绑定**（验收"回放可比较"）：快照 digest 必填 null fail-closed；外来快照证据排除（scope 声明异快照即丢，对齐 E2E-05——UT 实证排除后双源降单源 SINGLE_SOURCE 不冒充佐证）；确定性推导+Reducer 排序归并 ⇒ **相同黑板相同 Claim 指纹**（UT 两轮全等锚定；LLM 面在 M4-33 Replay Runner 以 mock 工具置换后精确比对，本 Agent 侧已确定性）；status 解析缺省 UNKNOWN。UT 6 案。**全量 mvn test 590 用例 0 失败**（2 skipped 本机无 docker）。**AM4 阶段 C（M4-24~30 组件互相接线）至此全部落地**：AgentProfile/注册表 → Planner 双设防 → Supervisor 固定链 → 三证据 Agent（Metrics 真 Prometheus 契约 + Logs/Change replay）→ Native RCA → Claim/Reducer/Store/Assembler 全链组件齐备；未完成面=195 真栈接线（Bean 装配/DAG 执行收尾路径按 task type 分发到 Agent/报告落档）与阶段 D（M4-31~38 Holmes Adapter/REPLAY/Shadow/Reconciler）。下一步：阶段 D M4-31 Holmes Baseline Adapter（M3-08 对接，Holmes 输出转 EvidencePackage/Claim 边界）。
- 2026-09-07：**M4-31 Holmes Baseline Adapter 落码（阶段 D 首批，无迁移，测试先行）**（`f2ffde5`）。Holmes（AM3 M3-08 报告包）输出转成与 Native 路径**相同**的 Evidence/Claim 边界——M4-34 Shadow Router 在同一 snapshot_digest 下对比两路的对齐面。① 报告包整体落一条证据信封（evidence_type=holmes.report、source=holmes、scope 绑 input_snapshot_digest）；② ReportClaim 逐条映射 Claim（claimKey=claim_type、scope=component、source=holmes、快照/代际继承本 run 绑定）；③ 空 evidence_refs 的因果型断言**桥接包证据信封**（Claim 契约禁空 refs，ReportClaim 允许空数组——两契约的缝在 Adapter 收口）；④ **Adapter 失败可见**（验收行）：包解析失败/claims 缺失/status 非法 → FAILED 结局携带原因显式返回，禁静默吞成零断言；合法空 claims → NO_CLAIMS（诚实空不是失败）；快照必绑 fail-closed。UT 5 案。**全量 mvn test 595 用例 0 失败**（2 skipped 本机无 docker）。接线说明：Adapter 待 M4-34 Shadow Router 消费（Holmes 基线侧），Holmes 包来源=InvestigationResult.packageJson（M3-08 落档面）。下一步：M4-32 REPLAY_MOCK 精确匹配（tool/version/args/scope/time/snapshot 全相同才回放，任一字段不同 REPLAY_MISS）。

### 2026-09-07 前端线框图 v1.5.1（纯视觉改版，内容语义不变）
- 背景：用户反馈"页面有点丑"，对 docs/告警-前端页面设计-wireframes-v1.html 做视觉 overhaul；**所有页面结构、文案、评审裁定一字未动**。
- 改版内容：引入 CSS 变量设计系统（--brand #1f6feb / 状态四色 / 线色分级）；卡片圆角 14px + 柔和双层阴影；目录改吸顶胶囊导航（sticky + 毛玻璃 + hover 填充）；page-head 改深色渐变；annot 注释表加重型首列+圆角白卡；线框件全面圆角化（frame/navbar 胶囊/tab 圆角/进度条渐变胶囊/step 卡片化）；tag 改全圆角药丸；SVG DAG 节点色（#0b5cad→#1f6feb 等 3 处）并入新色板；正文加 max-width 1520 居中；media query 同步新变量。
- 校验：12 个 section、div 配平 379/379、HTML 解析零未闭合、旧色板零残留。用户环境浏览器拒 file://，本轮仍为静态审查，视觉效果请用户打开文件肉眼确认。
- 状态：仍待用户确认页面清单后才出正式《前端技术方案》送 G1，G1 通过前不动前端代码。

### 2026-09-07 前端线框图 v1.6（结构改版，注释契约不变）
- 背景：v1.5.1 纯视觉改版后用户仍反馈"布局太丑"；本轮依据新完成的 E-19 前端 UI 对标调研（`docs/告警-调研-前端UI对标-v1.md`，20 款产品逐页对标）对 `docs/告警-前端页面设计-wireframes-v1.html` 做结构性改版，**所有 .annot 注释表文字（评审裁定契约：11 态图例、事件筛选语义、中文契约、HOLDOUT 发布门、脱敏红线等）一字未动**，机器校验逐块全等。
- 改版内容：① 新增「⓪ 全局应用壳」section——左侧深色导航（7 个一级入口+底部设置位）+ 顶栏（环境切换/全局搜索/SSE 状态点/用户菜单）混合模型（E-19 §4-1/§4-12），后续页面只画内容区；② 各页删除重复 topbar/navbar（由 6 组降为仅 ⓪ 壳保留 1 组），替换为面包屑条 `.crumb`，右侧保留该页环境/时间信息；③ P1 统计卡改 `.stat-card` 可点击、注明"点击→带筛选参数深跳"目标（腾讯三级下钻/Alerta ASI 先例）；④ P2-A 改行业标准"左 facet 栏 `.facet-rail`（分类/严重度/状态/来源/服务分组计数）+ 顶搜索栏 + 右真表格 `.grid`"，行首 4px severity 色条 `.sev-p0/1/2` + 状态文字 badge 不整行着色，九类视图/分面/修正说明全部重组保留；⑤ P3-B 改"头部摘要条 `.runhead`（状态 badge+严重度+进度 2/4+预算微条+主操作右置）+ 多视图 tab（DAG 图/事件流/Claim与证据/报告/历史Attempt）"，取消/Hint/Feedback 从底部上移，DAG tab=左画布（SVG+11 态图例原样）+右诊断抽屉，事件流/Claim/报告内容以 tab 内容平铺示意、语义不变；⑥ P3-A 队列改同款 `.grid` 表格+severity 色条；⑦ P5-D 配对比较区追加对比矩阵（实验为列、用例为行、✓/✗、回归红 `.cmp-reg`/改进绿 `.cmp-ip`、列头计数、"只看回归"开关、"设为基准实验"按钮——LangSmith 蓝本）；⑧ P0/P4/P5 其余子页/P6 仅做去壳，结构不动。
- 版本记录：title/h1 升 v1.6，meta 追加 v1.6 条目，目录补 ⓪ 链接，⑤ 调研证据表新增 E-19 行并逐条标注 v1.6 改动对应的 E-19 结论编号。
- 校验：13 个 section（≥13 ✓）、div 配平 401/401、python html.parser 零未闭合、红线关键串（仅当前任务/发起发布门/FAILED_TERMINAL/display_name_zh/Last-Event-ID/认领并查看）全部 grep 命中、topbar 类仅剩 ⓪ 壳 1 处 navbar 0 处。备份：`var/wireframe-v16/告警-前端页面设计-wireframes-v1.5.1-backup.html`。
- 状态：仍待用户确认页面清单后才出正式《前端技术方案》送 G1，G1 通过前不动前端代码；视觉效果请用户打开文件肉眼确认（file:// 浏览器限制同前）。

### 2026-09-07 AM7 前端与操作面 —— 正式开工（工序 3 编码）
- 门禁记录：用户裁定**线框图 v1.6 即方案，豁免正式 12 节技术方案文档，G1 视为通过，直接开工**；执行依据 = `docs/告警-前端页面设计-wireframes-v1.html` v1.6（7 页 + ⓪ 全局壳 + 各页 annot 契约）+ E-18（`docs/告警-OSS-证据清单.md` DAG 选型）+ E-19（`docs/告警-调研-前端UI对标-v1.md` 布局对标）。
- 里程碑编号：**AM7 前端与操作面**，任务编号 M7-xx；与 AM4（编码中）/AM5（v1.1 待 G1 复审）并行推进，用户当场授权。
- 任务拆解（线框图 §① 页面清单直接映射）：M7-01 工程脚手架（Vue3+Vite 独立构建、设计系统移植、路由/状态/API client 骨架）→ M7-02 ⓪全局壳+P0 登录 → M7-03 P1 总览 → M7-04 P2 告警中心 → M7-05 P3 调查队列与详情（Vue Flow+dagre DAG、SSE 事件流）→ M7-06 P4 处置中心 → M7-07 P5 评测中心 → M7-08 P6 Agent 监控 → M7-09 中文词典 display_name_zh 与契约 → M7-10 部署门（构建产物 + 部署验证）。
- 范围纪律：后端配套 API（GET /rca-runs 只读投影、评测命令 API 增补编号等）按 annot 标注登记为依赖项；前端先行以 API client + mock 适配层推进，不与 AM4 执行者在 control-app 内并发改同一后端文件。

### 2026-09-07 AM5 技术方案 v1.2 —— 独立 E2E 与真实业务场景门补齐
- 用户裁定“AM5 也必须有”端到端与真实场景测试；`docs/告警AM5-技术方案.md` 升 **v1.2**，状态仍为待 G1 复审、动工硬前提仍为 AM4 G2，未新增或改写 M5-01~22 权威任务编号。
- 新增 **INV-AM5-11** 与 M5-22 G2 硬门：AM4 Shadow E2E、公共 benchmark、单测和组件 IT 均不得替代 AM5 自身 E2E；冻结 B0 正常订单、B1/F1 幂等破坏、B2/F2 状态回跳、B3/F3 超时未知、B4 重复告警/预算耗尽五类订单域场景。
- 展开 **E2E-AM5-00~10**：真实私有质量门、RCA-100/RCAEval Adapter、安全硬门、ConfigBundle 原子激活/回滚、隔离 Canary、OperatorCase、SSE/命令、防自噬+Gatus、归档恢复/legal hold、FTS/pgvector 门；全部要求 195+2C4G 证据包且不得 skip。
- 引入 `LIVE_BUSINESS/HYBRID_BUSINESS/PUBLIC_REPLAY/CONTROL_FIXTURE` 真实性标签：日志/变更仍用 AM4 replay 时只能报 HYBRID；公共 benchmark 与确定性控制候选不得冒充 Native 业务质量。真实候选 REJECT/INCONCLUSIVE 可证明门禁正确，但 active pointer 必须零变化、零放量；AM5 只做隔离环境 Canary 演练，生产 1% Canary 仍归 M6-01。
- **M7-01 工程脚手架 ✅ 2026-09-07**：`alert-web/`（Vite 7 + Vue 3.5 + vue-router 4 + pinia 3 + @vue-flow/core + @dagrejs/dagre + axios，独立构建非 Maven 模块）；设计系统移植 `src/styles/tokens.css`（CSS 变量与线框图 v1.6 同源，含 severity 行首色条三档）；路由骨架 10 条（P0 登录 bare 路由 + 7 页 + incident/run 详情子路由）；API client `src/api/client.js`（VITE_USE_MOCK 开关，默认 mock 适配层，/api 代理 control-app:8080）；`npm install` + `npm run build` 绿（89.5KB js / 34.9KB gzip）。
- **M7-02 ⓪全局壳+P0 登录 ✅ 2026-09-07**：`alert-web/src/layouts/AppShell.vue`（172px 深色渐变侧栏 6 入口当前项高亮+顶栏环境切换/全局搜索占位/SSE 状态点/用户菜单，逐像素对标线框 ⓪）；`src/views/LoginView.vue`（FUT-34 契约注条：禁复用机器 Bearer、必须用户会话，mock 登录写 sessionStorage，路由守卫未登录重定向 /login?redirect=）；10 条路由全部指向真实 view 文件（P1~P6 为骨架占位，后续并行实现不再动 router/App.vue/AppShell）；mock 约定入 `src/mocks/README.md`；build 绿（98.6KB/38.2KB gzip），dev 四路由 200。事故注记：执行 agent 关 dev server 时误用 taskkill //IM node.exe 全量杀进程（波及宿主其他 node 进程），后续一律按 PID 定点结束。

### 2026-09-07 AM4 E2E 与真实业务场景补充（v1.4 尾部增量）
- 按用户“AM4 也补齐，尾部追加”裁定，未改写 AM4 既有正文与历史修订：在 `docs/告警AM4-技术方案.md` 尾部新增 §15，在 `docs/告警AM4-落码技术方案.md` 尾部新增执行附录；不改变已签 G1 功能裁定、不新增 M4 编号，只收紧 M4-38/G2。
- 冻结 B0 正常订单、B1/F1 幂等破坏、B2/F2 状态回跳、B3/F3 延迟未知、B4 信号冲突、B5 重复告警/预算耗尽六类订单域场景；细化 **E2E-M4-00~11**，覆盖真实业务全链、证据缺失/冲突、generation 交替、五杀点恢复、prompt injection、精确 Replay、Holmes/Native Shadow 隔离、预算/doom loop、来源中断与 Reconciler。
- 明确真实性：业务入口、Prometheus、Alertmanager、control、Holmes/Native、LiteLLM、PG 必须真跑；M4-28/29 Logs/Change 当前为冻结 replay，统一标 `LIVE_METRICS_REPLAY_LOGS_CHANGE`，不得宣称全数据源 LIVE；核心依赖失败后换 mock、skip、直写结果表、只验 HTTP 200 均不能报通过。
- 修正验收边界：AM4 签机制与隔离，不强迫 Native 单例命中 GT；命中失败但诚实进入 UNRESOLVED/NEEDS_REVIEW/PARTIAL 可证明机制正确，无证据确定根因则失败。真实单例质量原样移交 AM5 HOLDOUT 统计门，Candidate 发布增量必须为 0。
- 落码附录冻结 `docs/测试证据/AM4/e2e-脚本` 与 runs 证据布局、M4-31~38 随件交付表、执行顺序和 G2 一票否决项；M4-38 必须无 skip 复跑 00~11，`mvn clean verify`、V12~V18 IT、195 E2E 三层证据互不替代。
- **M7-03~08 六页面并行实现 ✅ 2026-09-07**（六 agent 并行，各自名下文件零冲突）：P1 总览（5 统计卡可点击带筛选深跳/交接视图认领 mock/30s 轮询）；P2 告警中心三页（A=左 facet 九类+搜索 DSL+severity 行首色条表格/P0 恒置顶/分类修正审计提示，B=头部摘要条+八 tab+九步 step-lane 轨迹+Attempt 下钻，C=历史档案检索+ARCHIVE_UNAVAILABLE 分支+归档证明）；P3 核心页（队列 tab/URL 持久化筛选/SLA banner；详情 runhead+五视图 tab，Vue Flow+dagre DAG 11 态四重编码单一事实源 `RunDagStatus.js`，点节点联动事件流「仅当前任务」，SSE 定时器模拟+真实接入点注释：stream ticket/Last-Event-ID/after_seq/FUT-33）；P4 处置中心（Case 队列+详情证据工作区+expected_revision/idempotency_key 命令+REVISION_CONFLICT 冲突分支+通知 tab 已读/深链）；P5 评测中心（9 子视图组件化：四分区卡/HOLDOUT 发布门红线/批次/六维/逐案例/评分器校准/人工复核/对比矩阵实验列×用例行+只看回归+基准实验/发布门）；P6 监控（六指标卡深链/预算风险双进度条/doom-loop 熔断记录/失败原因码分布/成本对账，只读无操作按钮）。
- **M7-09 中文词典 ✅ 2026-09-07**：`src/dict/displayNameZh.js`（DICT_VERSION=zh-display-v1，EVENT_TYPE_ZH/NOTIFY_TYPE_ZH/VERIFY_STATUS_ZH 三表 + zh() 未收录回退原文；修订须升版本）；RunDetailView/CasesView/CaseDetailPanel 三处硬编码中文 map 全部收口到词典。
- **总验收**：统一 `npm run build` 绿（195 modules，js 495KB/175KB gzip）；dev server 十条路由（/login /overview /alerts /alerts/inc-81a /history /runs /runs/3f2a /cases /eval /monitor）全部 HTTP 200。
- 遗留依赖（登记在案不实现）：后端配套 API（GET /rca-runs 投影、OperatorCase 命令/通知、评测命令 API 增补编号、HOLDOUT 发布门命令、投递对账视图路由）、真实 SSE 接入（ticket+EventSource 替换定时器模拟）、P0 正式 HttpOnly 会话替换 sessionStorage 占位（守卫已留 hasSession() 单点替换位）。
- **M7-10 部署门 ⏳ pending**：工序 4 部署验证（目标服务器静态托管 + 与 control-app 联调 VITE_USE_MOCK=false）待执行。

### 2026-09-07 晚 并行会话 AM4 收口核验 + BUGLOG 纪律补登记（主会话）
- 核验属实：提交 `5dd272e`（E2E-M4-06 故障演练 IT 六案 + runall v1.4 + common v1.4）与 `363b4be`（生产缺陷 #7 修复 + 195 真跑证据归档）均在库，证据件（195-真跑-mvn-verify-it06.log、it06 摘要）已归档 `docs/测试证据/AM4/`；两笔提交**未 push**。
- 纪律缺口补齐：AM4 期 7 项生产缺陷 + 10 项测试层缺陷此前只入迭代修复证据文档、未入 BUGLOG——本轮补登记 **BA-27~BA-33**（生产：ClaimStore jsonb cast/settleFrom 悬挂态/**sumWindow 限额 fail-open**/**DagExecutionService CAS 败者死循环**/findStaleReservations Instant 绑定/probe 拒绝路径自炸/**deadline infinity 写侧越界——BA-05 修复方案被真 PG 证伪并取代**）+ **BA-34**（测试层 10 项合集），每条含回归证据指针。
- 待用户决策（未执行）：① runall 12 场景真栈执行前置=195 control-app AM3 版原地升级 AM4 版 + flyway V12~V19 apply——共享栈替换，可能与并行会话干扰，需确认时机；② 两笔 AM4 提交 + 工作区未提交文件（AM5 v1.1/v1.2 修订、AM4 双方案 v1.4、E-19、线框图、alert-web/ AM7 全量代码）的 commit/push 授权；③ `am4-tech-diff.tmp` 并行会话遗留临时件，未动。
- **更正（主会话认账）**：前条"AM4 G2 终审材料现在齐了"表述错误——三层证据中 195 E2E 层（runall 00~11 十二场景无 skip 复跑）尚未执行，AM4 版镜像升级与 V12~V19 迁移也未在 195 apply。M4-38 一票否决项要求 `mvn clean verify` + V12~V18 IT + 195 E2E 三层互不替代，E2E 缺席即 G2 条件不满足。用户指出后更正，G2 终审待 runall 真栈跑绿后方可提请。

### 2026-09-07 晚 模型切换：deepseek-v3 → qwen3.7-plus（用户裁定）
- 用户在百炼专属实例可用模型清单中选定 **qwen3.7-plus**（qwen-max/qwen-plus-2025-07-28/deepseek-r1-distill-qwen-7b 为候选，7B 小模型已排除）。
- 配置面四处已改：litellm `config.yaml`（model_name+upstream）、`deploy/alert/docker-compose.yml`（holmes MODEL）、`deploy/docker-compose.yml`（APP_ALERT_HOLMES_MODEL 默认值）、`EvalRunnerConfig`（eval 元数据模型默认值）、`.env.example` 同步。**费率字段暂留 deepseek-v3 旧值并标 TODO**——须按 qwen3.7-plus 刊例价校准，低估会放宽 per-EvalRun max_budget 硬拦（BA-29 同族风险），归 AM5 运维项。
- **195 实测探针（仅回 HTTP 码与模型输出，密钥零暴露）**：HTTP 200；`response_format=json_object` 下 content=纯 JSON 合规（BA-14 之坑——deepseek-v3 忽略该参数——在 qwen3.7-plus 上不复发）；该模型为 thinking 型，`reasoning_content` 独立字段、reasoning_tokens 计入 completion_tokens（预算账本口径不变）；max_tokens 过小会被思考耗尽的坑已实测确认，调用侧需留足额度。
- 注意：195 的 `/opt/projects/pr_agent/.env` 当前 AGENT_MODEL=qwen-plus（Native 路由），部署时需同步 sed 为 qwen3.7-plus（不打印文件内容）。
- 影响声明：模型切换后既有 eval 基线（AM3 47 案例命中率 0，deepseek-v3 产出）与新模型不可比，AM5 质量门基线须以 qwen3.7-plus 重测重建。

### 2026-09-07 晚 AM5 G1 签署 + 并行形态裁定
- **AM5 v1.2 G1 用户签署通过**（"签署 G1，AM5 正式开工"）；动工硬前提"AM4 G2"由用户当场豁免为**与 M4 E2E 并行推进**。AM5 进入工序 3（编码），任务编号 M5-01~22，迁移自 V19 起。
- **195 共享栈归属裁定：并行会话独占**（M4 E2E=栈升级+V12~V19 apply+runall 十二场景由其执行）；本会话不碰 195，M5 编码在本地进行，部署验证排在 M4 E2E 收口之后。
- 模型切换的 195 侧落盘（.env AGENT_MODEL sed、litellm/holmesgpt 重建）随并行会话的栈升级一并执行——仓库 HEAD 2fca37d 已含全部配置变更，探针已实证 qwen3.7-plus 可用且 JSON 合规。
- **AM5 执行交接文档出具**：`docs/告警AM5-执行交接文档.md`（用户裁定"M5 执行者由用户指定"）。要点：G1 已签/硬前提豁免并行、195 归并行会话独占 M5 不碰、**迁移边界实测已到 V19（AM4 工具回放占用）→ AM5 从 V20 起**、HEAD 快速移动须先同步、模型 qwen3.7-plus 四坑（thinking 型/max_tokens/费率 TODO/基线重建）、M5-01~22 导读与 v1.1/v1.2 关键裁定、工序 3 纪律（IT 跳过不计证据）。

### 2026-09-07 深夜 AM5 开工前基线验证（M5 执行会话）
- 交接文档口径"634 UT 绿"在 HEAD 4e4941b 实测**不成立**：全 reactor `mvn -B -ntp clean test` = control-app 638 run / **1F**（EvalRunnerProfileIsolationTest 断言旧默认模型 deepseek-v3，2fca37d 切 qwen3.7-plus 漏改钉值断言）→ 登记 **BA-35** 并修复（测试期望值同步，装配钉值语义不变）。
- 修复复验：全 reactor BUILD SUCCESS，**control-app 639 run / 0F / 2 skip**（skip=本机无 Docker 的 PostgresModelCallLedgerRepositoryTest，IT 真证据仍待 195 释放后统一补）；shared-kernel 15 / order-arena 30 / arena-chaos-admin 14 / notify-app 19 全绿。测试数 634→639 为并行会话 AM4 提交所致。
- **共享工作树竞态实录**：并行会话编辑 Am4ShadowTrigger 主类/测试期间（22:16~22:17），本会话基线重跑撞上中间态编译错（WORKER_SLOT_SCOPE 符号缺失）；等其编辑落定（mtime 静默 2 分钟+）后重跑即绿——后续每个任务的全量验证前须先确认无并行编辑在途。
- 迁移边界实测复核：control-app 迁移最大号 **V19__am4_tool_replay**（order-arena 自有 V001~V035 独立编号不混），AM5 自 **V20** 起，与交接文档一致。
- M5-01 编码**未启动**：按用户指示待《AM5-落码技术方案》补齐后开工（DoD 第 5 条"落码方案先行"）。
- **AM5 落码技术方案 v1.0 出具**：`docs/告警AM5-落码技术方案.md`（525 行，AM4 落码方案同格式）——M5-01~22 逐任务施工图（落点清单/迁移 DDL 要点/字段级 API 契约/验收命令/依赖与并行）；迁移号段重冻结 **V20~V29**（AM4 实占至 V19）；起草发现方案与代码现状 5 处矛盾（C-1~C-5：V19 已占、rca_run 缺 engine/bucket 列且活跃唯一索引与 Canary 双活跃冲突、rca_attempt 无采样指纹列、M5-14 无命令表、评测查询/发起 API 在拆解无编号须增补）+ 6 开放项全部入文档附录封存；主会话抽查头部/迁移表/M5-01/02 节质量合格（真实代码面引用核实、无虚构类名）。

### 2026-09-07 AM5 落码方案 E2E 执行附录补齐（v1.1 尾部增量）
- 按用户“补充”裁定，在 `docs/告警AM5-落码技术方案.md` 尾部追加 E2E/真实业务执行附录；不改 M5-01~22 编号、依赖和 V20~V29 迁移表。修正前文“M5-22 无新代码”歧义：业务功能无新增，但总控 runner、断言器和证据封装为 M5-22 必交测试资产。
- 冻结 `docs/测试证据/AM5/` 布局：preflight/common/runall + E2E-AM5-00~10 十一个场景脚本，fixtures 分 CONTROL/redteam/public benchmark，runs 按批次保存 manifest/results/assertions/sha256/commands/topology/sql/stats/raw。
- 总控契约：00 正常基线先行，01~10 顺序执行；`SKIP/NOT_RUN/缺结果`、核心依赖失败后换 mock、digest/真实性标签/trial 原始行缺失均非零失败；退出码区分断言、环境授权和证据完整性。`BLOCKED_EXTERNAL` 仅 RCA-100 answer key 授权分支允许，且不得声明外部一致性通过。
- E2E 责任按 M5-01~22 随件分配：01/02 数据与 Adapter、01/03 统计安全门、04/05 ConfigBundle/Canary、06/07 OperatorCase/SSE/命令、08 双故障域观测、09 归档恢复、10 检索门；M5-22 只补齐 common/preflight/runall 并无 skip 复跑封存。
- 一票否决冻结：HOLDOUT/GT 泄漏、family 拆分、未过门仍激活/放量、CONTROL 与真实候选混证、未授权 RCA-100 伪造答案、OperatorCase/SSE/防自噬/归档/检索安全断言失败、AM5 越权接生产 1% Canary。195+2C4G E2E、真 PG IT、`mvn clean verify` 三层证据互不替代。

### 2026-09-07 深夜 M5-01 DatasetVersion/CaseVersion + 统一 DatasetAdapter 落码（M5 执行会话）
- 两段式推进：先按技术方案 v1.2 口径实现并全绿（eval/domain/dataset 包面、eval_ 前缀表、(source,name,version) 唯一）；复验中发现主会话出具《AM5-落码技术方案》v1.0，遂按其 §M5-01 施工图**返工对齐**（落码方案为编码权威）。教训入档：M5-02 起开工前先核对落码方案对应节，避免二次返工。
- 返工内容：域模型迁 eval/domain/model（DatasetVersion/CaseVersion/EvalCaseV1 + SourceClass/PartitionClass 枚举）；端口 eval/domain/port/DatasetAdapter，签名对齐落码方案 `importCases(ImportRequest)`（req 含 datasetRef{name,version,manifestDigest}）；三适配器迁 eval/infrastructure/adapter 并去 Dataset 前缀（OrderArena/Rca100/RcaEval）；仓储改名 DatasetVersionRepository（实现留 infrastructure.persistence，沿 EvalRunRepository 惯例）。
- V20 重写：表名 dataset_version/case_version；dataset_version 增 partition_class varchar(16) 四值封闭 + scenario_family_digest char(64)（族清单摘要，族增删即换版本锚）；case_version 增 source_artifact_ref text（原始 artifact 引用，不覆盖来源九字段）；唯一约束改 uq(name,version) 与 uq(dataset_version_id,case_key)——纠错语义改为"新 dataset_version 携带修正行"；补 comment on table/column（落码方案风格要求）。
- family 整组分区断言归 M5-01（落码方案 §M5-01④ 明文）：DatasetAdapter.assertFamilyPartition——同 scenario_family_id 拆分跨 TUNING/HOLDOUT 直接拒绝；M5-02 V21 再以 case_version 上 unique(scenario_family_id,partition_class) DB 兜底。RCA-100 fail-closed 双铁律保持：锚定 v1.1 禁浮动 latest（datasetRef.version 检查）、answer key 未授权 → NOT_AVAILABLE_AUTH 且 dataset 行与 cases 全拒。
- TDD 红绿留证：首版增量 RED 32 跑 9F/6E（全"feature missing"）→ 32 绿；返工增量 RED 位 = familyDigest/assertFamilyPartition stub → scoped 37 跑 1F+6E 精确全中 stub → 实现 → scoped 37 全绿（迁移契约 3 案静态门 + ArchUnit 零框架扩面 eval.domain.model/port 同绿）。
- 全 reactor 复验 BUILD SUCCESS：control-app **669 run / 0F / 7 skip**（skip = 5 新 PostgresDatasetVersionRepositoryTest + 2 旧 ledger IT，本机无 Docker 自动跳过、不计证据，真证据待 195 释放）；shared-kernel 15 / order-arena 30 / arena-chaos-admin 14 / notify-app 19 全绿。
- 提交纪律：只 stage 本任务 22 文件（12 主 + 10 测试）；告警-PROGRESS/BUGLOG 混有并行会话未提交条目，不入本提交。

### 2026-09-07 深夜 M5-02 数据集四分区权限（M5 执行会话）
- V21__am5_dataset_partition：case_version.partition_class 冗余直挂（V9 FUT-50 惯例，禁 JOIN 推导）+ `ck_case_version_partition` 四值封闭 + 复合 FK (dataset_version_id, partition_class) → dataset_version(id, partition_class)（新增 uq_dataset_version_partition，单列 FK 由复合取代）——冗余列与头表一致性由 DB 兜底。RLS 落地（落码建议采纳）：case_version enable row level security；eval_app（SCORING 评分身份）select/insert 同谓词 `partition_class <> 'HOLDOUT'`（封存门前不可见不可写，RLS 写路径无策略即默认拒绝）；四分区角色 eval_tuning_ro / eval_validation_ro / eval_holdout_gate / eval_redteam_gate（NOLOGIN 授权目标，V9 条件化建角色同构）各见单分区；control_app 族（Agent/RAG/调优界面身份）零策略 = 默认拒绝（即使未来误 grant 也恒 0 行）；INV-AM5-1 触发器兜底 trg_dataset_benchmark_not_holdout（PUBLIC_BENCHMARK × HOLDOUT 插入硬拒绝）；GT 延迟授权沿用 V3/V11 不触碰。
- **C-6 决策（偏离落码方案字面、保留其不变量，供主会话复核回写勘误）**：§M5-02② "case_version 上 unique(scenario_family_id, partition_class) 兜底"不可实施——①同族多 Case 是设计常态（V20：同一故障模板的重复 Case 同族），(family,partition) 行面天然多行；②纠错 = 新 dataset_version 携带修正行（§M5-01② 自定语义），跨版本同分区同族再次多行。改为登记面表 case_family_partition（PK(scenario_family_id)，insert-only）：同族二次登记不同分区 = DuplicateKey 拒绝，同一不变量 "family → 至多一个 partition"；应用层前置门为 M5-01 DatasetAdapter.assertFamilyPartition。已入 V21 头注。
- EvalIdentityGuard（eval/infrastructure/identity）：查询侧身份矩阵单点——AGENT/RAG/TUNING_UI 见 TUNING+VALIDATION，HOLDOUT/REDTEAM/GT 恒 0；SCORING 见 TUNING/VALIDATION/REDTEAM（HOLDOUT 封存门前不可见）；HOLDOUT_GATE / REDTEAM_GATE 各单分区最小特权；GT 可读 = SCORING/HOLDOUT_GATE（V3 延迟授权沿用，报告封存后）。DB 角色映射（M5-06 起接线）入 javadoc。
- 仓储适配：PostgresDatasetVersionRepository.insertCaseVersion 改 INSERT..SELECT 自 dataset_version 派生 partition_class（零 API 变更、单一事实源）；INSERT..SELECT 在 dataset 不存在时静默 0 行，补受影响行数守卫 fail-loud（原 VALUES 形态由 FK 报错兜底），insertCaseVersion 契约真实性不破。
- TDD 红绿留证：RED = guard 矩阵 stub ×4 + V21 缺失 ×4（scoped 11 跑 8E 精确全中）→ 实现 → scoped 45 全绿（含 M5-01 回归 34+ 与 ArchUnit 13）；IT 面 PostgresDatasetPartitionAccessTest 5 案（control_app 零访问、eval_app 封存面含 RLS 写拒绝、四角色单分区 SET LOCAL ROLE 行为面、benchmark×HOLDOUT 拒绝、family 拆分登记拒绝）本机无 Docker 跳过待 195。
- 全 reactor 复验 BUILD SUCCESS：control-app **682 run / 0F / 12 skip**（skip = partition IT 5 + dataset IT 5 + ledger IT 2，真证据待 195）；shared-kernel 15 / order-arena 30 / arena-chaos-admin 14 / notify-app 19 全绿。
- 工具教训：中文源文件严禁 PowerShell 字符串管道处理（Set-Content 编码面曾把 IT 文件中文注释打成 mojibake，整文件重写恢复）；台账与源文件编辑一律 Read+Edit 工具。
- 提交纪律：只 stage 本任务 6 文件（V21 + Guard + 仓储 + 3 测试）；台账不入本提交。

### 2026-09-08 凌晨 M5-03 Golden Candidate 工作流（M5 执行会话）
- V22__am5_golden_candidate：golden_candidate（状态机 DRAFT→REVIEW→PUBLISHED/REJECTED/WITHDRAWN 五值封闭 + revision 乐观锁 default 0 + ix(state,updated_at)）+ golden_review_event（append-only 复核时间线，ck_golden_review_action 五值 + **uq_golden_review_event_idem unique(idempotency_key) 幂等重放锚**）。INV-AM5-2 DB 双兜底：**ck_golden_dual_review**（reviewer_a=reviewer_b 非空即拒，同人双签）+ **ck_golden_terminal_reviewers**（PUBLISHED/REJECTED 必须双签齐全——终态结论不允许单签产生，为落码方案之外的自加强化；WITHDRAWN 提案人单方动作除外）。授权面：eval_app 候选 select,insert + **列级 UPDATE（V7 惯例，仅 state/reviewer_a/reviewer_b/revision/updated_at 五列）**；事件表只 select,insert；control_app/publisher/notify/public/arena 域零权限。
- 域面：GoldenCandidate/GoldenReviewEvent record（payload/proposedGt Map.copyOf 不可变）+ GoldenCandidateState/GoldenReviewAction 枚举 + GoldenCandidateStateMachine（**复用 alert 域 TransitionTable 泛型**，矩阵唯一权威；AlertStateMachineTest 反射穷举同款测试）+ GoldenCandidateRepository 端口（insertDraft/find/eventExists/casTransition/events——无 update/delete 命名面，insert-only 纪律的候选例外仅列级 UPDATE 落在实现内）。
- 服务编排 GoldenCandidateService（**不加 @Service**，装配归 EvalRunnerConfig M5-06+，避免无仓储 bean 面破坏 ControlContextSmokeTest）：propose 建 DRAFT revision 0 + PROPOSED 事件（key="propose-"+id）；decide 双签位同人/blank 在进仓储前 IAE 拒（更早报错，V22 CHECK 为 DB 兜底第二道）；**幂等重放先于状态机门**——同 key 重试发生在状态已迁移之后，是正常重试而非旁路（REVIEW→REVIEW 假阳性教训入档：门序颠倒会把重放误判为非法迁移）；新操作（新 key）撞已迁移状态仍被矩阵拒绝。casTransition 三重 CAS（id + expectedState + revision）。
- PostgresGoldenCandidateRepository：事务边界在仓储——insertDraft（候选 INSERT + PROPOSED 事件）与 casTransition（状态 UPDATE + 事件 INSERT）同事务提交，"两签同事务"落地面；CAS 0 行 setRollbackOnly 返回 false（零痕迹）；CAS 命中后事件撞幂等唯一键 = 非重放面盗键，IllegalState 整事务回滚 fail-loud。
- TDD 红绿留证：RED = service 状态机/CAS/dual-sign 8 案 + 状态机穷举 2 案，stub 位 "（RED 位）" 精确全中 → 实现 → scoped 10/10 绿；V22 契约测试 2 案入 Am5MigrationContractTest（双签 CHECK/终态 CHECK/列级 grant 字面断言 + 事件 append-only/幂等唯一/无 update 授权），scoped 契约 9/9。
- IT 面 PostgresGoldenCandidateRepositoryTest 5 案（eval_app 单连接池真实角色）：propose→submit→publish 全流程 PROPOSED/SUBMITTED/PUBLISHED 时间线 + revision=2；同人双签/终态单签 DB CHECK 拒绝；CAS 旁路（期望态错配）false 且零痕迹；同 key 二次 submit 服务层短路 + DB 直插 DuplicateKey；事件表 UPDATE/DELETE 授权拒绝（append-only）+ 候选非授权列 UPDATE 拒绝（列级 UPDATE 面）+ control_app 零权限。本机无 Docker 全跳过，真证据待 195。
- 全 reactor 复验 BUILD SUCCESS：control-app **699 run / 0F / 17 skip**（skip = partition IT 5 + dataset IT 5 + golden IT 5 + ledger IT 2）；order-arena 30 / arena-chaos-admin 14 / notify-app 19 全绿。
- 提交纪律：只 stage 本任务 13 文件（9 主 + 4 测试）；台账不入本提交。

### 2026-09-08 凌晨 M5-04 模型采样指纹（M5 执行会话）
- V23__am5_sampling_fingerprint（落码方案 §M5-04② **方案甲**）：`alter table rca_attempt add column sampling_fingerprint jsonb` + `ck_rca_attempt_fingerprint_keys`（五顶层键 requested/effective/provider_fingerprint/model/trial_no 缺一即违约；字段级完整性留给域面 isGateEligible——jsonb 键存在性对嵌套 null 不过问，诚实留空 ≠ 缺键）。rca_attempt 写方 control_app 已有表级 UPDATE（V7），新列零新增授权面。
- SamplingFingerprint VO（eval/domain/model，domain 零框架）：请求态+生效态两态各 temperature/top_p/max_tokens/seed + provider fingerprint + model + trial 序号；`isGateEligible()` = 两态四字段全非空 + 身份齐全 + trial≥1（**INV-AM5-3 fail-closed 字面实现：缺任一字段不进正式门禁**）；`canonical()` 固定字段序规范串 + `digest()` sha256（**BA-22 实测校准：测试钉值 b38e7bde… 以 Python sha256 对拍预计算，一次命中**）；`toMap()` 键序固定 = V23 ck 键集序（jsonb 落库形态稳定）。
- **C-7 决策（落码方案落点修正，供主会话复核回写）**：§M5-04① 指名"AM4 Native Agent 的 ChatClient 封装处"经 grep 实证不存在（全库无 ChatClient）——真实 LLM 调用面 = HolmesInvestigationExecutor.execute()（每 Attempt 一次 HTTP，§4.1 时序）。指纹回写不走执行器直写（"执行器只写 external_invocation_ledger"纪律不破）：AttemptArtifact 携带 Map 形态（alert 域零 eval 类型依赖）→ RcaRunOrchestrator.finishAttempt 复制 → rca_attempt 行随收尾事务单次落库（无二次写、无新事务边界）。
- **C-8 决策（生效 seed 的诚实形态）**：holmes /api/chat 响应无 seed 生效值回传字段 → 生效态 seed 恒 null → 当前 provider 组合 isGateEligible 恒 false = INV-AM5-3 的正确执行（不可复现的运行不得进正式门禁）；trial_no=0（非配对试验）同样拒绝。max_tokens 生效值 = 请求值经成功调用背书（qwen3.7-plus thinking 型的实际预算，交接文档 §四）。M5-05 配对试验经 run 面携带真实轮次、M5-06 接回传链路后自然放开——门禁消费方（M5-08 EvalGateRunner）落地时按 VO 判定。
- 装配：AlertFlowConfig 注入 `app.alert.holmes.sampling.{temperature,top-p,max-tokens,seed}`（未配置=null 诚实留空）+ provider 指纹复用 `app.alert.eval.provider-fingerprint`（与 eval_run 头 V10 单一事实源）；SamplingSpec 嵌套 record 校验 providerFingerprint 非 blank（E-17：指纹必须单列）。
- TDD 红绿留证：RED = SamplingFingerprintTest 6 案（缺类编译错）+ V23 契约 1 案（文件缺失）精确全中 → 实现 → scoped 16 绿；回写链路 WireMock 新案 m504（artifact 两态键集/键序/trial_no=0/生效 seed null 断言）。构造契约修正一处：身份 null/blank 在构造期 NPE/IAE 拒绝而非构造后判 ineligible（与 DatasetVersion.requireText 共用语义）。
- 全 reactor 复验 BUILD SUCCESS：control-app **707 run / 0F / 17 skip**（skip = 17 IT 本机无 Docker；真证据待 195）；shared-kernel 15 / order-arena 30 / arena-chaos-admin 14 / notify-app 19 全绿。测试数 699→707（+8：fingerprint UT 6 + WireMock 指纹案 1 + 契约 V23 案 1）。
- 提交纪律：只 stage 本任务 15 文件（9 主 + 6 测试）；台账不入本提交。

### 2026-09-08 凌晨 M5-05 配对重复试验与方差（M5 执行会话）
- PairedTrialStats（eval/domain/service，纯函数不触网）——冻结统计口径逐条落地（技术方案 §3.1 v1.1/E-17）：①配对差值按 independence group 聚类 + bootstrap **整组有放回重采样**（默认 1000 次，重采样单位是簇非对）；②**不再套 C/(C-1) 修正**（clustered SE 的有限 cluster 校正项与 bootstrap 两算法不混写，E-17 Inspect std.py 实证）；③点估计与重采样同口径 = 簇内均值跨簇**无权平均**（池化到对会破坏与区间的一致性）；④95% **nearest-rank** 百分位区间；⑤独立 cluster 数 < **MIN_CLUSTERS=5**（C-9 裁定入常量待 195 校准）→ INCONCLUSIVE 且区间置 null——**禁止收窄到零的假区间**（INV-AM5-3），溯源面 stats_seed/algorithmVersion/resamples/ciMethod 任何结论形态全记录。可复现锚：Random LCG 跨 JVM 规范冻结，同 (pairs,seed,resamples) 必同输出；ALGORITHM_VERSION=cluster-bootstrap-v1（改动口径必须升版）。
- PairedTrialReport（eval/domain/model）：rounds/flipRate/difference 三件全记录；flipRate = baseline 与 candidate 判定不一致的对占比；方向约定 difference = candidate − baseline（正 = 候选更好）。不占迁移号（落码方案 §M5-05②），持久化面随 M5-08 门禁消费装配。
- **C-10 决策（runner 侧落点裁剪，供主会话复核）**：§M5-05① "EvalBatchRunner 批间隔离三件套（等 resolve 投递/清 nflog/临时放宽 group_interval）"——runner 的 rounds>1 循环 M3-17 起已存在；三件套是 order-arena/Prometheus 管理面操作（无既有 driver 面），只在真栈 E2E 显形，其唯一消费方是 M5-08 质量门的 A/B 配对装配——**归 M5-08/M5-22 落地**，本任务不空转改 runner。
- TDD 留证：测试先写（8 案：簇均值无权平均点估计 0.25 精确断言/同种子复现/全正差值区间严格大于 0/区间包点估计+全记录字段/4 簇 INCONCLUSIVE 区间 null 且溯源照记/5 簇边界 CONCLUSIVE/flipRate 0.5/空对 blank 簇超上限重采样拒绝），实现后 scoped 8/8 绿（RED 位为缺类编译错未单独截屏，序列如实记录）。
- 全 reactor 复验 BUILD SUCCESS：control-app **715 run / 0F / 17 skip**；shared-kernel 15 / order-arena 30 / arena-chaos-admin 14 / notify-app 19 全绿。测试数 707→715（+8）。
- 提交纪律：只 stage 本任务 3 文件（2 主 + 1 测试）；台账不入本提交。




### 2026-09-08 凌晨 M5-06 六维 Evaluator（M5 执行会话）
- SixDimEvaluator（eval/domain/service，纯函数 L0）+ EvalCaseInput/DimensionCounts/SixDimResult（eval/domain/model，零框架面过 am5DatasetDomainZeroFrameworkDependency）——六维原始计数：结果（TP/FP/FN/support/hit/UNRESOLVED/静默罚，委托 ScenarioEvaluator 冻结语义透传）/过程（tool_name+params_digest 同键再现=重复等价查询，首现不计）/工具（注册命中/幻觉/拒绝=approval_required）/成本（latency+token 三项，usage 缺失诚实记 0+usage_missing=true 不猜补）/协作（claims 按 TRUE/FALSE/UNKNOWN 三态）/安全（策略拒绝计数+红队旗标）。
- 每维 {rawCounts, traceRefs[]}（落码方案 §M5-06③ 契约形态）：claim:{i} / tool_call:{i}（下标=输入序列位）指回证据面——"每项原始计数可追溯"拆解验收；成本维 traceRefs 为空（run 级观测，身份引用归 M5-08 记录面）。
- **不聚合成单一分**（§3.1）：SixDimResult 无任何 score/aggregate 面——反射锁定 UT（禁名集合 score/aggregate/combined/total/overall + 无 Double 字段）；score 有意缺席（v1 无任何维出分）。
- **AC@1 限定**（v1.1 裁定）：acAt(1)=命中 1/0；acAt(3)/acAt(5) 无 candidate_root_causes[] 契约直接 UnsupportedOperationException（fail-closed 禁算禁收窄假口径）；acAt(0) IAE。
- **C-11 决策（演进非推翻落地形态，供主会话复核回写）**：§M5-06① 指名"修改 ScenarioEvaluator/SingleCaseScorer"——本任务两者零改动：SixDimEvaluator 构造注入 ScenarioEvaluator（AM3 三维冻结语义仍是 canonical 判定面，六维加镜头不换镜头=字面演进非推翻）；SingleCaseScorer 接六维需 EvalCaseResult 落库扩面，计数列落表随 M5-08 门禁记录面（§M5-06② 原文"计数列若需落表随 M5-08"），提前改是无处安放的投机面。包向说明：domain/service → application 经构造注入（现无 L0 规则禁止该向；若后续收紧，迁移点=构造签名不变）。
- 工具/安全两维同源不混算：approval_required 在工具维=rejectedCalls（可用性镜头）、安全维=policyRejections（拦截面镜头）；注册面命中与策略拒绝独立计数（write_dns 案：注册解析命中+审批拒绝两边都计）。
- TDD 红绿留证：RED = SixDimEvaluatorTest 11 案缺类编译错精确全中 → 实现 → scoped 11/11 绿。中途修正两处：①Usage.missing() 静态工厂与 record 组件访问器撞名编译错，改 absent()；②write_dns 注册命中期望 1→2（注册面解析与审批拒绝两镜头，测试预期错非实现错）。
- 全 reactor 复验 BUILD SUCCESS：control-app **727 run / 0F / 17 skip**（skip=IT 本机无 Docker；真证据待 195）；shared-kernel 15 / order-arena 30 / arena-chaos-admin 14 / notify-app 19 全绿。测试数台账 715→727（本任务 +11；排除法实测基线 716，台账差额 1 疑前序总数笔误，供主会话复核）。
- 提交纪律：只 stage 本任务 5 文件（4 主 + 1 测试）；台账不入本提交。

### 2026-09-08 凌晨 M5-07 硬安全门（M5 执行会话）
- SafetyGate（eval/domain/service，纯函数 L0）+ SafetyFace（eval/domain/model，五面分类码机器码英文+中文口径）+ EvalCaseInput 扩 SafetyRejection 拒绝记录载体——五面消费不重判（落码方案 §M5-07①）：越权工具/写意图从 tool_call 观测落档影子消费（registered=false=Registry UNKNOWN_TOOL 拦截、APPROVAL_REQUIRED=Gateway 写意图拦截）；非法 schema/注入/跨租户经 safetyRejections 拒绝记录搬运（Assembly 面从 investigation 拒绝记录折算，M5-08 装配）。
- 契约形态 §M5-07③ check(SixDimResult, EvalCaseInput) -> SafetyVerdict{violations[], verdict PASS|REJECT}：任一违规即 REJECT（INV-AM5-4 fail-closed）；violations 逐条 face+引用位（tool_call:{i}/claim:{i}）+机器码原因原样透传——"拒绝理由与原始计数可回溯"（E2E-AM5-03 断言面）；result 参数为契约冻结位（五面判定不读质量计数），"结果维全中不抵安全违规"有 UT 锁定。
- EvalCaseInput 演进（M5-06 自有文件兼容扩面）：+safetyRejections 组件与嵌套 SafetyRejection(face,ref,reason)（reason 禁原始 payload，§7.9.3 红线入构造注释）；SixDimEvaluatorTest 构造位同步 List.of() 适配，11 案不动语义。
- TDD 红绿留证：RED = SafetyGateTest 8 案缺类编译错精确全中（SafetyGate/SafetyFace/SafetyRejection 三面）→ 实现 → scoped 19/19 绿（SafetyGateTest 8 + SixDimEvaluatorTest 11 回归）；中途修正一处：ScenarioEvaluator import 漏引编译错。
- E2E-AM5-03 脚本骨架随件交付：docs/测试证据/AM5/e2e-脚本/e2e-am5-03-safety-gate.sh（五面逐面装载→过真实门禁→断 REJECT→拒绝理由回溯 + 零 ConfigBundle 激活/零 Canary/零外部写副作用终检；SQL 断言点标 [M5-08] 待记录面表名落定，真栈断言待 195 部署段——骨架期不宣称 LIVE）。
- 全 reactor 复验 BUILD SUCCESS：control-app **735 run / 0F / 17 skip**；shared-kernel 15 / order-arena 30 / arena-chaos-admin 14 / notify-app 19 全绿。测试数 727→735（+8），716 基线+11+8 对平（M5-06 台账差额判断成立：M5-05 总数笔误 -1）。
- 提交纪律：只 stage 本任务 6 文件（3 主 + 2 测试 + 1 e2e 骨架；其中 EvalCaseInput/SixDimEvaluatorTest 为 M5-06 自有文件兼容扩面）；台账不入本提交。

### 2026-09-08 上午 M5-08 质量门与运行门（M5 执行会话）
- QualityGate（eval/domain/service）五分支冻结逻辑逐字执行（v1.1 裁定，分支序即裁定序有锁定案）：①安全违规>0→REJECT（M5-07 合流，质量再好不抵违规）→②独立 cluster 不足/统计 INCONCLUSIVE/无配对试验→INCONCLUSIVE（小样本不自动放行，无统计面不伪造）→③配对差值 CI 下界<-margin→REJECT（边界恰等不触发，严格小于才拒）→④运行门延迟/token 预算/工具错误率→REJECT→⑤全过→ELIGIBLE_FOR_CANARY 且 reasons 空。机器码未过原因六码冻结；GateDecision 契约 ELIGIBLE⇔reasons 空（构造器双向校验=解释完整性硬约束）。
- GateThresholds（eval/domain/model，版本化）：version blank 拒绝构造（阈值随 ConfigBundle 走的版本锚，M5-09 bundle 携带）；ciMargin/minClusters/maxLatencyMs/maxTotalTokens/maxToolErrorRate 全参数校验。
- EvalGateRunner（eval/application）：复用 EvalBatchRunner 跑批产物（逐案 EvalCaseInput 观测面 + PairedOutcome）→ SixDimEvaluator 逐案 → SafetyGate 逐案安检跨案聚合 → PairedTrialStats（stats_seed=请求携带）→ QualityGate → EvaluationRecordV1 落档（insert-only）。跨案聚合口径冻结：latency=max（最坏面 fail-closed）、tokens=sum（批预算）、错误率=全批比、traceRefs 全批拼接可回溯不丢案。
- EvaluationRecordV1（eval/domain/model）+ EvaluationRecordRepository（insert-only 接口）：真实性标签四值写入（§12.2 原文）；统计溯源快照 stats_seed/算法版本/重采样次数/CI 方法全带（INV-AM5-3）；违规快照 face/ref/reason 逐条可回溯；digest 集（dataset/config/engine）；值快照形态有意不复用 service 层类型（避免 model↔service 包环），映射归 runner。
- **O-1 升级（开工核对结论，需主会话/用户裁定）**：§M5-08② 原默认"不加新迁移号，扩展列挂 V23 同任务窗口"已不可执行——V23 随 M5-04 发布（INV-AM5-10 已发布迁移不得追加）；独立表路径按原文"并入 V24 之前的空档需用户确认"，而 V24 已被 §M5-09 指名占用（V24__am5_config_bundle.sql，本会话不得越占重排）。裁定落地：本任务交付 QualityGateTest+EvalGateRunnerTest 验收面（§M5-08④ 全 UT，无 IT 要求）+ 仓储接口先行；Postgres 记录仓储实现与 DDL（扩展列新迁移号 or 用户确认独立表）待 O-1 裁定后小步补齐。
- **C-12 决策（runner 复用形态，供主会话复核）**："复用 EvalBatchRunner 跑批"落为消费其产物（GateBatchRequest 携带逐案观测+配对结局）而非内嵌跑批循环——真栈 EvalCaseResult→EvalCaseInput 装配适配器依赖 rca_tool_invocation/usage 台账读面与记录表落点（同一 O-1 消费面），随 O-1 裁定后接线，避免无落点的投机装配。
- **C-13 演进（M5-06 自有文件兼容扩面）**：DimensionCounts.Process +errorToolCalls（分支4 错误率数据面），SixDimEvaluator.processDim 计数 ERROR 状态；SixDimEvaluatorTest 过程维案同步断言（4→5 案例锁新计数）。
- TDD 红绿留证：RED = QualityGateTest 13 案 + EvalGateRunnerTest 4 案缺类编译错精确全中 → 实现 → scoped 28/28 绿（中途修正：测试侧双 Verdict 枚举撞名歧义 import ×3 处 + 烟测案一行占位残句重写为真断言；主侧 runner 首稿逐案重复评估改单循环）。
- 全 reactor 复验 BUILD SUCCESS：control-app **752 run / 0F / 17 skip**；shared-kernel 15 / order-arena 30 / arena-chaos-admin 14 / notify-app 19 全绿。测试数 735→752（+17：QualityGateTest 13 + EvalGateRunnerTest 4）。
- 提交纪律：只 stage 本任务 9 文件（4 主新 + 2 主改[M5-06 自有] + 2 测试新 + 1 测试改）；台账不入本提交。

### 2026-09-08 凌晨 M5-09 ConfigBundle 发布/回滚（M5 执行会话）
- ConfigBundle（release/domain/model，新顶层包 `com.objwww.pr.control.release/`，落码方案 §3.1 划面）：不可变配置束——digest 锚 = InternalCanonicalJsonV1.canonicalize（键序无关）的 sha256（发布幂等锚 + 重跑一致锚双职能）；content 深冻结（构造期递归 unmodifiable 拷贝，外部改写原映射不回流）；INV-AM5-5 密钥材料**键名**递归扫描 fail-closed（apikey|api_key|authorization|bearer|secret|password|privatekey，嵌套 Map/List 全扫，命中即拒发布）；policy_version 必带（v1.1 裁定：本期不引入 OPA/WAITING_APPROVAL，仅保留策略版本字段，R2/R3 再评估）。
- V24__am5_config_bundle.sql（O-1 裁定后 V24 号位按落码方案 §M5-09① 归位）：config_bundle 只授 select,insert（immutable 的 DB 面；同 digest 唯一 = 幂等锚 DB 兜底；content jsonb 只锁对象形状——密钥检测归发布面）；config_bundle_active 单行 pointer（id=1 check + 种子行未激活态；ck_config_bundle_active_unactivated 禁半激活态）；激活/回滚 = CAS UPDATE `WHERE bundle_digest IS NOT DISTINCT FROM :expected`（null 期望匹配未激活态）；revoke publisher/notify/eval/public 全零。契约案 +3 入 Am5MigrationContractTest。
- ConfigBundleService（不加 @Service，装配归 PersistenceConfig 唯一装配点——ReleaseConfig 不另设，沿 GoldenCandidateService/PersistenceConfig 既定惯例）：publish 幂等序 = 先查 digest 命中即重放（revision 不烧号）→ nextRevision() 仓储分配 → insert 竞败（并发同内容）落回既有行；activate/rollback → movePointer（重复激活当前 digest = 幂等重放零 CAS；CAS 竞败 moved=false 携最新指针投影）；activePointer 查询面。
- ConfigBundleController（release/interfaces，@Profile("docker")）：四端点 §M5-09③ 契约——POST /api/config-bundles（body {content{...}, expectedRevision?}，缺 content 400）、POST /{digest}/activate、POST /rollback（toDigest 必填 400）、GET /active（未激活 404 never_activated）；RBAC=release bearer（`app.release.api.bearer` ← APP_RELEASE_API_BEARER，常量时间比较，AlertWebhookController 同构；O-4 用户体系落地前的共享凭证形态，401 面即切换点）；**409 = CAS 竞争败者**携当前指针零状态改写（200 replayed=true 幂等重放与之分离）；404 = 未知 digest（先验目标存在不盲移指针）；激活/回滚审计日志行（digest/参数键）决策可回溯；compose `:?` 必填 + .env.example 键面随件（防静默空值）。
- PostgresConfigBundleRepository（infrastructure/persistence）：activate 单语句单事务（TransactionTemplate 包 CAS UPDATE = 无半激活态落地面）；findByDigest 读路径经 canonical 构造（重冻结 + 重扫密钥，防御纵深）；insert DuplicateKeyException→false（幂等锚消费面）；activeDigest 查询 SQL 内滤 NULL（种子未激活态不作 Optional 空/值歧义）。
- ArchUnit +1：am5ReleaseDomainZeroFrameworkDependency（release.domain.model/repository 零 Spring/Jackson/JDBC/HTTP；canonical JSON 复用 InternalCanonicalJsonV1 正为不引 Jackson；ControlArchitectureTest 13→14）。
- **配置清点清单（方案 §11 技术债条款；落码交付物 = 本清单入台账不另立文档）**：散在 yml/env/compose 的键 → bundle 首版候选 = `app.model.route.*`、`app.model.max-call-retries`、`app.model.budget.*`（4 键）、`app.model.gateway.total-deadline-ms/inline-retry-max-delay-ms`、`app.model.per-call-timeout-ms`、`app.model.circuit.*`（2 键）、`app.model.ledger.recovery-after-seconds/scan-interval-ms`、`app.alert.eval.*` 阈值族（M5-08 GateThresholds 对应面）、`app.artifact.cas-dir`、`APP_ALERT_HOLMES_MODEL`；**密钥永不入 bundle（INV-AM5-5）** = `AGENT_MODEL_API_KEY`、`HOLMES_API_KEY`、`ALERTMANAGER_WEBHOOK_BEARER_TOKEN`、`APP_RELEASE_API_BEARER`、`OPENAI_COMPAT_BASE_URL`（寻址含凭证语义，与 assertNoCredentialFields 同族口径）——保持 env-only，bundle 只携带非密钥值。
- **C-14 决策组（供主会话复核）**：①发布幂等键 = 客户端 Idempotency-Key 头仅随审计日志留痕不入库——发布幂等锚 = digest 唯一已足，不为它加存储列；②activate body 的 canaryPercent/whitelist 与 publish body 的 expectedRevision 本期**只收不用**（前者 M5-10 CanaryRouter 放量语义消费面、后者无语义定义——契约先收拢防后期破面，启用时补语义与 UT）；③409 语义 = CAS 竞争败者专属（乐观锁冲突 REST 惯例），幂等重放走 200。
- TDD 红绿留证：RED = ConfigBundleTest 6 + ConfigBundleServiceTest 7 缺类编译错精确全中 → 实现 → scoped 20/20 绿 + ControllerTest 7（standalone MockMvc + InMemory fake，AlertWebhookControllerTest 同构）；绿阶段修正三处：①of() 发布形态 revision 恒 1 而服务未分配 revision（发布序重构为先查重放再分配再落库，withRevision 组装）；②深冻结案异常预期错（unmodifiableMap 写路径 = UnsupportedOperationException 非 IAE，测试侧改正实现不动）；③controller 首稿 CAS 败者判位 digest 自我比较恒真重写 + List 缺 import（自查捕获于提交前）。
- E2E-AM5-04 脚本骨架随件：`docs/测试证据/AM5/e2e-脚本/e2e-am5-04-config-bundle-atomic.sh`——phase1/2（发布幂等/64hex/密钥键拒收 400/原子激活指针落位/伪 bearer 401）API 真调面已可执行；phase3 运行中回滚+老 Run 固定旧 digest、phase4 DB 历史行对拍标 [195]（后者待 M5-10 V25 config_digest 列后强化）。
- IT：PostgresConfigBundleRepositoryTest（PostgresITBase 真 PG 4 案：发布-激活-回滚全程历史行 to_jsonb 对拍零改写/digest 唯一锚 DB 面 DuplicateKey + CAS 竞败零副作用 + ghost 不成行/V24 授权面 bundle 免 UPDATE·DELETE + pointer 免 DELETE + eval_app 全零）本机无 Docker 自动跳过——真证据待 195 释放统一补，跳过不计证据。
- 全 reactor 复验 BUILD SUCCESS：control-app **780 run / 0F / 21 skip**（run 口径含 skip：+24 UT +4 IT skip，与 752+28 对平）；shared-kernel 15 / order-arena 30 / arena-chaos-admin 14 / notify-app 19 全绿。
- **提交纪律附注（M5-08 缺陷补救）**：本任务开工盘点暴露 a2657b8 stage 清单遗漏 EvalGateRunner.java（测试在库主类未跟踪，HEAD 单独 checkout 不可编译，详见 BUGLOG BA-36）——先于本提交单独补交 **1adb940**（内容 = M5-08 时点原样）。自本任务起 commit 前执行 `git diff --cached --name-status` 与落点清单**逐文件名**对拍。
- 提交纪律：只 stage 本任务 17 文件（6 主新 + 3 主改[PersistenceConfig/application.yml/compose] + 4 测试新 + 2 测试改 + 1 e2e + 1 .env.example）；台账不入本提交。

### 2026-09-08 凌晨 M5-10 Canary Router 稳定分桶与路由四列（M5 执行会话）
- CanaryBucketer（release/domain/service）：murmur3_x86_32(seed=0) 标准实现（C1=0xcc9e2d51/C2=0x1b873593/fmix 收尾）作用于规范化键 `groupId:id`（trim + Locale.ROOT 小写）；分桶 = `(hash_u32 * totalWeight) >>> 32`（flagd fractional.go:196-207 同式，无模偏）；键 null/blank = IAE 拒绝放量原始面。正确性锚 = python mmh3 参考向量交叉验证（hash("")=0、hash("hello")=613153351、hash("g:abc")=1566941584）——独立实现对拍非同源复读。
- CanaryRouter（release/application）：route() 判定序 ①无 active bundle → NO_ACTIVE_BUNDLE ②content.canary 缺失/percent null/负 → CANARY_DISABLED ③规范化键 IAE → NO_STICKINESS_KEY（无 stickiness key 拒绝放量——Unleash random 回退坑）④桶位恒算 + 白名单集判定（bucket=percent 无关恒记，拒绝放量也留路由事实）⑤NATIVE 意愿 × nativeReady=false → NATIVE_DEFERRED 降级 ⑥countNativeDecisions() ≥ max_native_runs → WARN + BLAST_RADIUS_STOPPED（爆炸半径上限 = 审计表无状态重查即自愈，无开关行可绕）⑦终判 whitelisted>bucketed → HOLMES/NATIVE。决策行 append-only 全审计（canary_route_decision 八值枚举，run/bucket/percent/digest/stickiness_key 齐备）。
- V25__am5_canary_route.sql：rca_run +4 列（engine varchar(16) not null default 'HOLMES' check、config_digest char(64)、stickiness_key text、canary_bucket integer）+ ck NATIVE⇒digest；uq_rca_run_active_incident 重建为 (incident_id, engine) WHERE state IN ('QUEUED','RUNNING')——同引擎双活跃仍 23505，异引擎活跃共存（Shadow=HOLMES 缺省行为零变化）；canary_route_decision append-only（bigserial pk；八值 check；grant select,insert to control_app + revoke update,delete + 其余角色全零）。契约案 +2 入 Am5MigrationContractTest（路由四列 + 索引/决策表形态）。
- RcaRunRouting（alert/domain/model）+ RcaRunRepository.insertRouted default（默认退化 insert——既有 fake 测试点零扰动）+ PostgresRcaRunRepository.insertRouted override（路由四列落行）；RcaEngine 枚举（HOLMES/NATIVE）。
- 铸造点接线（IncidentProjector/RcaRunOrchestrator）：**重载构造器模式**——旧构造器委托新构造器传 `CanaryRouter.holmesOnly()`（NullBundles/NullDecisions 惰性实现，零写入），AM4 冻结测试点全数原样绿；castRunAndTask 单 runId 共享（router 审计行与 run 行同键），insertRouted 落库。AlertFlowConfig 增 canaryRouter bean：**nativeReady=false**（O-3 未裁定 + NATIVE 执行引擎 M6 才落——NATIVE 意愿一律 NATIVE_DEFERRED 降级 HOLMES，路由/桶位/决策照记）。Am4ShadowTrigger trigger() javadoc 增 O-3 注记：Shadow 面仍 plain insert（engine 缺省 HOLMES）不咨询 router。
- C-15 决策组（供主会话复核）：①holmesOnly() 重载构造器 = 路由惰性缺省（冻结测试点零改 + docker 装配显式升级，无 ReleaseConfig 新装配点）；②percent=0 语义 = BUCKETED_HOLMES 且桶位照记（0 是合法关闭不是禁用——CANARY_DISABLED 保留给 canary 段缺失/null/负；放量节奏归运维面不停代码面）；③爆炸半径上限 = 决策审计表无状态 COUNT 重查（进程重启/多实例天然一致，无状态行可被绕过或漂移）；④nativeReady=false 硬编码于 bean = NATIVE 执行引擎未交付前的诚实降级（M6-01 生产 1% 灰度时随引擎落位翻转，本任务不预埋开关）。
- **O-3 升级（未裁定，不阻塞）**：Canary 与 Shadow 关系——Shadow Run 现走 plain insert（HOLMES 缺省），不咨询 router；若裁定 Shadow 也须带路由四列/进比例分母，改动面 = Am4ShadowTrigger 单点切 insertRouted，接口已备。
- TDD 红绿留证：RED = CanaryBucketerTest 7（参考向量/公式点查 weight=7 非二次幂/千次黏性/规范化等价/均匀性 10000 键 100 桶各 60~140/拒绝 IAE/值域覆盖）+ CanaryRouterTest 11（八决策各态 + provenance 行 + holmesOnly 零写入 + runId null NPE）缺类编译错精确全中 → 实现 → scoped 18/18 绿；CanaryRouterTest 绿阶段修正三处（record 助手 7 参对齐三处调用点/percent=0 语义重裁/双 UUID 铸造改单 runId 共享）。
- IT：PostgresCanaryRoutingIT（真 PG 3 案：insertRouted 四列落行 + 决策审计 BUCKETED_NATIVE 计数 1 而全量 2 / 老 Run 固定旧 digest——pointer 移动不改历史行、新 Run 读新指针 / uq 索引 (incident_id, engine) 粒度——同引擎双活跃 23505 + 异引擎活跃共存）本机无 Docker 自动跳过——真证据待 195 释放统一补，跳过不计证据。
- E2E-AM5-05 脚本骨架随件：`docs/测试证据/AM5/e2e-脚本/e2e-am5-05-canary-bucket.sh`——①黏性 ②bucket 值域 [0,100) ③决策八值域 ④白名单直达 ⑤percent=0 照记 ⑥python3 纯实现 murmur3 独立对拍 DB 桶位 ⑦max_native_runs 到顶 BLAST_RADIUS_STOPPED；**边界：禁接生产告警流量**（合成注入面），生产 1% 灰度属 M6-01（方案 §12.3 原文）；全 phase 标 [195]（路由判定在 Run 铸造点进程内，无独立 HTTP 面）。
- 全 reactor 复验 BUILD SUCCESS：control-app **800 run / 0F / 21 skip**（+18 UT：Bucketer 7 + Router 11；+2 契约案；IT 3 案本地 skip 不入 run 数）；shared-kernel 15 / order-arena 30 / arena-chaos-admin 14 / notify-app 19 全绿。
- 提交纪律：commit 前 `git diff --cached --name-status` 与落点清单逐文件名对拍（BA-36 纪律首用）。

### 2026-09-08 凌晨 M5-11 OperatorCase 状态机（M5 执行会话）
- 新顶层包 `com.objwww.pr.control.ops/`（方案 §3.1 划面）：OperatorCase 聚合根（ops/domain/model）——(tenant,fingerprint) 幂等合并键（V26 uq）；快照冻结（snapshot_digest/observed_generation 固定创建时值，迟到证据进新快照）；evidence_refs N≥1（构造器 + DB check 双兜底）；activities/audits 内嵌 jsonb 投影（P4 mock audits[] 形状 {at,actor,action,revision,key}，key 兼命令幂等重放锚）；Resolution 结构化结案 {code,note,at}；状态全集 = OPEN/ACKED/RESOLVED（与前端 mocks/cases.js status 值核对，中文显示名归 view 词典）。
- OperatorCaseStateMachine（ops/domain/statemachine）：**ISA 18.2 式 action 驱动**（E-17 alerta isa_18_2.py 同构）——(状态×动作) 矩阵唯一权威，Transition{to,rule} 规则名（T00_CREATE…T61_ESCALATE）随 StructuredLog 落账可回溯；RESOLVED 吸收态（仅 MERGE 可落：复发只记 activity 不复活结案）；矩阵外组合 IllegalCaseActionException 穷举拒绝（UT 反射全组合对齐）。命令四类 claim/ack/resolve/assign + 生命周期 CREATE/MERGE + SLA 升级 ESCALATE（不改状态，审计行+revision+1）。
- OperatorCaseService（ops/application，无 HTTP——API 归 M5-12）：openOrMerge 以 (tenant,fingerprint) FOR UPDATE 串行化（E-17 keep db.py 同构）——再发生 = revision+1 + activity 追加永不新建单；命令面 expected-revision CAS（Postgres update WHERE revision=:expected），败者 CaseRevisionConflictException 零副作用（并发认领恰一人成功的物理前提）；幂等重放 = 审计行 (action,key) 命中即零副作用返回当前投影（MERGE→CASE_MERGED、ESCALATE→SLA_ESCALATED 审计名映射）；**结案不改历史 = 结构性保证**：本服务零 Run 依赖，不 import 任何 alert 域仓储。
- V26__am5_operator_case.sql：operator_case（uq (tenant,fingerprint)；priority P0~P2；status 值域 check；evidence_refs N≥1 jsonb check；ix status+resolve_due 部分索引供 SLA 扫描）+ notify_outbox +case_id 关联列（AM7 IN_APP 渠道预留缝，本任务只落列不写通知行——落码方案 §M5-11① 原文）；授权 select,insert,update（命令 CAS 需 update）+ revoke delete + 其余角色全零。契约案 +2 入 Am5MigrationContractTest（17 案）。
- PostgresOperatorCaseRepository（infrastructure/persistence）：update **只写可变列**（status/owner/activities/audits/resolution/revision/updated_at）——快照列与来源列 SQL 面零开口；jsonb 数组/对象投影 Instant 一律 ISO-8601 字符串（不引 JavaTimeModule）；lockByTenantAndFingerprint FOR UPDATE（调用方事务包裹语义——单语句 autocommit 下锁即取即放，M5-12 API 层接线 TransactionTemplate；merge 并发冲突由 uq 兜底）。
- C-16 决策组（供主会话复核）：①activities/audits 内嵌 operator_case jsonb 列（不另立审计表）——P4 mock 形状直配 + 命令幂等重放锚同源，行内数组有界增长（合并频度低）；②ESCALATE 升级恰一次 = 审计 (action,key) 重放锚（notify_outbox 实写归 AM7：V9 outbox publication_id/report_id NOT NULL 报告绑定，预算/通知侧 Case 无报告可绑，放松约束超 M5-11 面）；③merge 不复活 RESOLVED（复发只记 activity——规格原文只说 revision+1+activity 追加，重开语义留 M5-12 API 裁定）；④ACK 仅 OPEN→ACKED（已 ACKED 再 ack = 非法迁移；幂等诉求由 idempotency-key 重放面承接）。
- TDD 红绿留证：RED = OperatorCaseStateMachineTest 4 + OperatorCaseServiceTest 15 缺包编译错精确全中 → 实现 → scoped 19/19 绿；绿阶段修正四处：①状态机静态表 CREATE 的 from=null 键入 EnumMap 即 NPE（行改 HashMap，null 键合法）；②replayed() 动作→审计名缺映射（MERGE/ESCALATE 重放永不命中，mergeReplay 案红捕获）；③stale-revision 案原误置 ACKED 态（状态机先拒非 CAS 冲突——改 assign 先行保持 OPEN 态让 CAS 真判）；④resolve 缺 structured reason 先于查单校验（IAE 面）。架构测试 +1：am5OpsDomainZeroFrameworkDependency（model+statemachine+repository 零框架，ControlArchitectureTest 14→15）。
- IT：PostgresOperatorCaseIT（真 PG 3 案：连续三次聚集单行 revision=3 + jsonb 往返 audits 形状 / 双线程并发 claim 恰一人 revision=2 落行 / 升级同 key 三次重放恰 bump 一次 + RESOLVED 吸收态拒绝 + resolution 往返）本机无 Docker 自动跳过——真证据待 195 释放统一补，跳过不计证据。
- E2E-AM5-06 脚本骨架随件：`docs/测试证据/AM5/e2e-脚本/e2e-am5-06-operator-case.sh`——①B2/B3/B4 来源开单 ②三次合并单行 ③双人并发认领+ack/resolve 契约+结案不篡改 Run 对拍 ④SLA 升级恰一次；M5-11 无 HTTP 面（命令 API 归 M5-12），phase3~4 命令驱动标 M5-12 后升级 API 真调，全 phase [195]。
- 全 reactor 复验 BUILD SUCCESS：control-app **822 run / 0F / 21 skip**（+19 UT：状态机 4 + 服务 15；+2 契约；+1 ArchUnit；IT 3 案 failsafe 本地 skip 不入 run 数）；shared-kernel 15 / order-arena 30 / arena-chaos-admin 14 / notify-app 19 全绿。
- 提交纪律：commit 前 `git diff --cached --name-status` 与落点清单逐文件名对拍。

### 2026-09-08 凌晨 M5-12 Operator API 最小集（M5 执行会话）
- OperatorApiController（ops/interfaces，@RestController+@Profile("docker")，ConfigBundleController 同构）：查询三面 + 命令四类——GET /api/cases/summary → {updatedAt, tabs{mine,all,unassigned,overdue,notifyUnread}}；GET /api/cases → **数组直返**（SLA 风险排序：逾期优先→优先级→resolve_due nulls-last→first_seen；bucket/status/priority/reasonCode 服务端过滤）；GET /api/cases/{id} → 工作区投影；POST claim/ack/resolve/assign 全部 expected_revision + idempotency_key——409 = revision 冲突携 {ok,conflict,error:"REVISION_CONFLICT",case:最新投影}，422 = 非法迁移（IllegalCaseActionException 携 currentStatus/action），404 = 未知 Case，400 = 缺参（expectedRevision/idempotencyKey/resolve reason/assign toOwner）。
- OperatorQueryService（ops/application 只读投影，无写路径）：summary tab 计数沿用 view 语义（各桶排除 RESOLVED，mine=owner==actor）；slaLabel（已逾期/Xm/Xh）+ ackOverdue（"ACK 超时 Xm"）+ overdue 面即时计算；detail 工作区投影 = 列表投影 + headline/summary/sourceRefs{count,text}/snapshot/generation/evidence[]/claims[]/conflictNote/activities/audits/evidenceRefs/resolution——**证据只回安全摘要与计数，canonical payload 不出 API 面**（P4 annot）。
- **C-17 决策组（供主会话复核）**：①summary 键名 = `tabs`（spec 原文 `buckets`；"字段级以 mocks+CasesView.vue 实际调用为准"条款裁定，view 读 summary.tabs.*）；②列表 = 数组直返（spec 原文 {cases,nextCursor} 游标分页——view 现状一次拉全量直接赋值，游标面等 view 消费 nextCursor 后补）；③resolve 双形状同收：契约 {reason:{code,note}} 与 view 实发 {reason:<code>,remark:<note>}；assign 双形状同收：toOwner 与 view 实发 assignee；④activities 回显示字符串行（"at actor ｜ text"，panel 直接 {{a}} 渲染；结构化 {at,actor,text} 保留存储面）、audits 用 `time` 键（mock 形状）；⑤evidence[]/claims[] 明细归 run 侧（RunDetailView 已承载），本最小集回空数组 + evidenceRefs 引用列表，明细面挂后续任务；⑥操作者身份 = X-Operator-Id 头（缺省 operator，审计 actor 面）+ 静态 bearer（`app.operator.api.bearer` ← APP_OPERATOR_API_BEARER，常量时间比较，compose `:?` + .env.example 三方随件）——O-4 用户体系落地切换点。
- 仓储面 +1：OperatorCaseRepository.findAll()（全量无锁扫描，Case 规模有限过滤排序内存做；Postgres ORDER BY created_at,id）。
- TDD 红绿留证：OperatorApiControllerTest 12（standalone MockMvc + InMemoryOperatorCases 测试夹具上提共享）——RBAC 正反（缺/伪 bearer 401）、summary 桶计数（mine=2/all=4/unassigned=2/overdue=1、RESOLVED 全排除）、SLA 风险排序（overdue→P0→P2）、status/priority 过滤、detail 审计链（CASE_CREATED→CLAIM key 齐备）、claim owner 落位、stale revision 409 携最新投影、resolve 缺 reason 400 + 双形状、assign 双形状、非法迁移 422 + 未知 404。scoped 红→绿（编译错 RED 留证）；实现侧修正三处：list 泛型不匹配（ResponseEntity<Object>→List<Map>）、401 面泛型、assign lambda 变量须 effectively final。
- 全 reactor 复验 BUILD SUCCESS：control-app **834 run / 0F / 21 skip**（+12 ControllerTest）；shared-kernel 15 / order-arena 30 / arena-chaos-admin 14 / notify-app 19 全绿。
- 提交纪律：commit 前 `git diff --cached --name-status` 与落点清单逐文件名对拍。

### 2026-09-08 凌晨 M5-13 rca_event 查询/SSE 只读面（M5 执行会话）
- EventQueryService + EventPayloadSanitizer（alert/application）：读端口 RcaEventReader（readAfter/latestSeq，事件域与追加域分离——append-only 写侧 M4-10、读侧本任务新增）；**gap 判定三分**：seq 缺口（首行 seq > after_seq+1）/客户端超窗（after_seq > latest_seq）/drained（空页但 after_seq < latest_seq——事件归档后计数器仍是锚）→ gap=true 停增量，客户端全量重同步（Unleash delta API 先例）；limit 钳 1..200。Sanitizer 白名单字段集对齐 notify-app NotificationRenderer 口径（机器码英文摘要/引用/digest 可出，thought/prompt/tool_args/secret 全拒）+ SECRET_LIKE 正则 [REDACTED] 遮蔽 + 控制字符剥离 + 64 字符截断；非对象 JSON/嵌套对象一律丢弃——**§17.9.3 红线（thought/原始 prompt/secret/完整工具参数永不进前端）由过滤器结构性保证，不靠调用方自觉**。
- SseStreamService：stream ticket（TTL 30s、单次、绑 run+subject，consume-then-check——失败不区分原因对探测者零信息；签发时顺手清过期票）；drain 单次有界页读经 Sink（accept/resync/heartbeat）即回，零长驻线程——INV-AM5-8（断线/慢客户端不拖慢 Run）结构性成立；SSE 侧只把"游标 ≤ latestSeq 仍现缺口/被清"判 resync——SSE 游标来自服务端自签 id:seq，超窗=暂无新事件走心跳（REST 轮询面则超窗即 gap，两处语义分工）。帧信封 {seq,type,taskId,summary,payload} 对齐 mocks 事件行形状。
- PostgresRcaEventReader（infrastructure/persistence）：latestSeq 读 **rca_run.last_event_seq 计数器**而非 max(seq)——事件行未来归档/清理后计数器仍是缺口判定正确锚；readAfter 按 (run_id, seq > :after) LIMIT 有界。
- RunQueryService（runs 队列/详情只读投影，字段级以 mocks/runs.js 为准）：bucket 由 run+task 状态推导（活跃+BLOCKED/RETRY_WAIT/LEASED 超期 → stuck；FAILED/EXPIRED → failed；SUCCEEDED/PARTIAL → review；CANCELLED/SUPERSEDED → done）；summary.buckets + sla{overSla,oldestReadyWait,projectionLag}；detail = run 头（V25 路由四列经新读端口 RcaRunRepository.findRoutingById——**读视图 RoutingView 独立于铸造校验面 RcaRunRouting**（后者 decision 必带，存量 plain-insert 行没有决策语义））+ rca_task/rca_task_edge DAG 投影（边 source/target 以 taskKey 表达，未知 UUID 原样回退）。
- **C-18 决策组（供主会话复核）**：①severity/owner：rca_run/rca_incident 均无该列 → **如实 null**（owner 认领面属 AM7 operator_case 域）；②engine/config 经 findRoutingById 读 V25 列；③budget/projectionLag：预算账本无读面、投影延迟无观测面 → 如实 null（不回填示意值）；④bucket 只由状态推导——summary.mine 恒 0、nextCursor 恒 null（认领/游标分页列**开放项 O-5**，runs 规模有限全量返回）；SUCCEEDED/PARTIAL 落 review 为诚实近似（暂无复核状态机）；⑤鉴权沿 O-4 过渡形态：operator bearer（与 M5-12 同键 app.operator.api.bearer）+ SSE 换票面 X-Operator-Id 留主体；⑥SSE 连接形态 = **单次排水后 complete，客户端 retry(2s) 自动重连 + Last-Event-ID 续传**（服务端把 Last-Event-ID 归一为 after_seq 游标）——每连接零长驻线程，PG NOTIFY 唤醒面不落（表+游标即真相源，NOTIFY 本就只作可选唤醒）；与方案"浏览器重连以 Last-Event-ID 续传"原文同构。
- EventQueryController（alert/interfaces，@Profile("docker")）：GET /api/rca-runs（summary+rows）、GET /api/rca-runs/{id}（404 面）、GET /api/rca-runs/{id}/events?after_seq=&limit=（events[].type/level/taskName 面——level 由 eventType 派生：FAILED/DEAD→error、RETRY/BUDGET→warn，供"仅错误"筛选）、POST /api/rca-runs/{id}/events/stream-ticket、GET /api/rca-runs/{id}/events/stream?ticket=&subject=（票 401 先于 emitter 创建；SseEmitter 桥 EmitterSink，resync 事件携 latestSeq）。
- RcaStateContract +parseEngine（HOLMES/NATIVE 字面量双读，fail-closed 同口径——不 Enum.valueOf）；RcaRunRepository +findAll（created_at DESC, id 稳定序）+findRoutingById；AlertInMemoryStores.Runs 同步两方法（findRoutingById 如实 empty——fake 无路由语义）。
- PersistenceConfig +4 bean（rcaEventReader/eventQueryService（Sanitizer 64 字符截断）/sseStreamService（30s）/runQueryService）——唯一装配点惯例不变。
- TDD 红绿留证：**25 案全绿**（EventQueryServiceTest 5 + EventPayloadSanitizerTest 6 + SseStreamServiceTest 4 + RunQueryServiceTest 4 + EventQueryControllerTest 6；scoped 编译错 RED 留证）。ControllerTest 以 standalone MockMvc asyncDispatch 实证 SSE 断线重连面（Last-Event-ID=1 → 仅续传 seq=2 帧 id:2/event:rca_event）+ 票单次/错主体 401。实现侧修正：SSE 帧初版只回 payload 丢 type（mock 契约每行事件带 type——信封化修复）；gap 判定与心跳语义分工（空仓超窗在 SSE 面走心跳不 resync）。
- E2E-AM5-07 半脚本随件：`docs/测试证据/AM5/e2e-脚本/e2e-am5-07-runs-events-sse.sh`（phase1 投影对拍/2 游标/3 换票-开流-重连/4 resync/5 慢客户端；命令面 phases 归 M5-14 随件补齐；全 phase [195]，IT 跳过不计证据）。
- 全 reactor 复验 BUILD SUCCESS：control-app **859 run / 0F / 21 skip**（+25）；shared-kernel 15 / order-arena 30 / arena-chaos-admin 14 / notify-app 19 全绿。
- 提交纪律：commit 前 `git diff --cached --name-status` 与落点清单逐文件名对拍。

### 2026-09-08 凌晨 M5-14 Cancel/Hint/Feedback 命令（M5 执行会话）
- V27__am5_operator_command.sql：operator_command（uq (run_id,command_type,idempotency_key) 幂等锚；command_type/state 值域 check；payload jsonb 对象面；状态单向推进 PERSISTED→APPLIED/REJECTED_STALE/REJECTED_FORBIDDEN）+ ix (run_id,created_at)；授权 select,insert + **列级 update (state,applied_at)**（V9/V22 同构——账本行正文落库零开口）+ revoke delete + 其余角色全零。契约案 +2 入 Am5MigrationContractTest（19 案）。
- **C-19 决策组（供主会话复核）**：①expected_revision 修订锚 = **rca_run.last_event_seq**（M4-10 计数器状态事实同行 +1、无洞单调 = run 天然修订号；RcaRunRepository +currentRevision 读锚）——rca_run 无 revision 列，updated_at 有同秒碰撞面，计数器是唯一无洞锚；②生效面：CANCEL = RcaRunStateMachine 迁移 CANCELLED + finished_at 收尾 + RUN_CANCELLED 状态事实事件；HINT/FEEDBACK 不迁移 Run，生效 = OPERATOR_COMMAND_APPLIED 事件留痕（事件 payload 只含 {"summary","state"} 白名单面）；③**Hint 标 UNTRUSTED**（§3.2）：文本只落 operator_command.payload（上下文组装面读表时以 UNTRUSTED 身份注入——M5-14 只落账本，消费面挂 AM2+/native 调查上下文），**文本不复制进事件流**（§17.9.3 攻击面最小化）；④"越权" = 终态 Run 上的一切命令（O-4 无用户体系，RBAC 面=bearer；REJECTED_FORBIDDEN 403）；裁决序 = 幂等重放（终态行原样返回含原拒绝态）→ 修订锚（REJECTED_STALE 零副作用）→ 适用性；⑤批量评测命令不在本期（O-2，落码方案 annot 原文禁扩展）。
- CommandService（alert/application）：**先持久化再生效**两阶段（FUT-33 结构面）——phase1 PERSISTED 行独立提交；phase2 生效（校验→迁移/事件→终态推进）。崩溃窗口双恢复面：生效前崩溃 = 同键 retry 续走 apply；生效已落标记未落 = 重放走恢复面补标 APPLIED 不重放事件；并发同键 = uq 败者读胜者行原样结算；updateState 带 state='PERSISTED' 谓词（DB 面二次推进 0 行）。
- RunCommandController（alert/interfaces，@Profile("docker")）：POST /api/rca-runs/{runId}/commands——body {type,idempotencyKey,expectedRevision,payload} → {commandId,state,replayed}；HTTP 面 APPLIED=200/STALE=409/FORBIDDEN=403/未知 run 404/缺参或非法 type 400；actor = X-Operator-Id（≤64 截断，缺省 operator）；O-4 过渡 bearer 常量时间比较。
- AlertInMemoryStores +Commands（uq 模拟/PERSISTED 谓词）+RcaEventLog（追加记录面）——M5-14 IT 与后续任务共享夹具；RcaRunRepository +currentRevision（Postgres 读 last_event_seq；fake 恒 0 如实无事件账本语义）。
- TDD 红绿留证：**CommandServiceTest 8 + RunCommandControllerTest 4**（先持久化调用序断言/STALE 零副作用/终态越权/Hint UNTRUSTED 文本不进事件/幂等重放零二次生效/双崩溃窗口恢复/并发败者；Controller 面 RBAC/400/404/200/409/403 全覆盖）+ 契约 +2。实现侧修正：Jackson TypeReference 导入包路径（core.type 非 databind）。
- E2E-AM5-07 命令半脚本随件补齐：phase6（CANCEL 生效对拍/幂等/STALE/FORBIDDEN/Hint 文本零泄漏/FUT-33 断线自洽，全 [195]）。
- 全 reactor 复验 BUILD SUCCESS：control-app **873 run / 0F / 21 skip**（859+12+2 契约）；其余模块全绿。
- 提交纪律：commit 前 `git diff --cached --name-status` 与落点清单逐文件名对拍。

### 2026-09-08 凌晨 M5-15 三支柱完整化（M5 执行会话）
- **C-20 决策组（供主会话复核）**：①落点隔离——deploy/alert/ 下既有 otelcol 属 OTel demo 栈（AD 演示环境）配置面，**非控制面资产**；M5-15 新建 deploy/otelcol-control/ 独立 collector（contrib:0.104.0），与 demo 栈零共享零依赖（防 demo 配置漂移污染控制面、防 tail_sampling 需求反向侵入 demo compose）。②零改动接通（M3-29 延续）——control-app **不加 OTLP exporter 依赖**：OTEL_EXPORTER_OTLP_ENDPOINT/APP_TRACE_SAMPLING 声明式 env 先落（brave 桥已在，span 内存态；真外发待 exporter 依赖上类路径，开放项挂 AM5 运维段），collector 端到端面就绪等上游。③静态校验门 = 落码方案 §M5-15④"静态校验脚本/装配测试"的落地形态：ControlCollectorConfigContractTest 3 案直读 deploy 文件断言（资源上限/采样策略/env-only 凭据/loopback 面/加固面/sampling probability face）；真栈证据 = E2E-AM5-08（骨架随件，全 [195] 部署段执行——IT 跳过不计证据同纪律）。
- otelcol-config.yml 三支柱：**资源支柱** memory_limiter 256M（check_interval 1s/spike 64M）+ 容器 384M/0.5CPU 双上限；**采样支柱** tail_sampling（decision_wait 10s/num_traces 5000）keep-errors(status_code ERROR) + keep-slow(latency 5000ms) + sample-healthy(probabilistic 10%)——错误/慢全保、健康抽 10%；**出口支柱** otlp/upstream 凭据 **env-only**（${env:OTEL_UPSTREAM_ENDPOINT}、authorization ${env:OTEL_UPSTREAM_TOKEN}、INSECURE 开关）零明文；prometheus/control 9465 本地拉面；health_check 13133；三 pipeline（traces 走 tail_sampling，metrics/logs 直出）。
- docker-compose.yml：otelcol-control 服务——env OTEL_UPSTREAM_ENDPOINT/TOKEN **`:?` 必填**（防静默空值出向裸奔）、INSECURE 缺省 true；端口**仅 loopback**（127.0.0.1:9465/13133，4317/4318 内网不发布）；加固面与 control-app 同构（read_only/tmpfs/cap_drop ALL/no-new-privileges/internal 网）；**无 depends_on**（INV-AM5-8：遥测故障只丢遥测，不挂业务依赖链）；control-app 侧 +OTEL_EXPORTER_OTLP_ENDPOINT（缺省 http://otelcol-control:4317）+APP_TRACE_SAMPLING（缺省 0.1）。
- application.yml：management.tracing.sampling.probability ${APP_TRACE_SAMPLING:0.1}（app 侧采样概率面）；.env.example +OTEL_UPSTREAM_* 三键 + APP_TRACE_SAMPLING 注释键。
- TDD 红绿留证：ControlCollectorConfigContractTest 3（collector 配置面/compose 装配面/application.yml 采样面，normalized 小写+空白折叠对拍，显式断言 4317/4318 **不发布**）；E2E-AM5-08 骨架随件：`docs/测试证据/AM5/e2e-脚本/e2e-am5-08-telemetry-outage.sh`（phase1 基线/2 断出口业务零漂移/3 collector 上限/4 恢复面，全 [195]）。
- 全 reactor 复验 BUILD SUCCESS：control-app **876 run / 0F / 21 skip**（873+3）；其余模块全绿。
- 提交纪律：commit 前 `git diff --cached --name-status` 与落点清单逐文件名对拍。

### 2026-09-08 凌晨 M5-16 控制面防自噬路由（M5 执行会话）
- **C-21 决策组（供主会话复核）**：①三面门裁定序 = 身份（独立控制面 bearer，常量时间比较；**配置空 = 恒拒** fail-closed，与 release/operator bearer 空值面不同——入口是安全门，宁拒勿放）→ AM route 白名单（envelope.receiver ∈ allowed-receivers）→ monitoring_scope 白名单（组内 scope 值全 ∈ allowed-scopes，**声明组必须有可校验 scope**——RCA_SYSTEM alertname 无 scope label = 400 不放行）；②"不信 webhook body 同名 label"（方案 §3.2）的实现语义 = **label 单独不作数**：伪造 monitoring_scope/RCA_SYSTEM 而无控制面身份 → 401 零落库（若放行业务路即借 RCA 自噬控制面的投毒面）；有身份但 route/scope 越白名单 → 400；③ROUTED 留痕形态 = alert_inbox **直写 PROCESSED+SUPPRESSED**（V7 decision check 预留枚举的消费者；无迁移约束下的投递记录锚——payload_raw 审计唯一权威照存，claimNext 领取面结构性不可达 = "不创建 Incident/Run"的结构性证明）；值班通道投递 = 结构化事件 **CONTROL_ALERT_ONCALL**（仅 inbox_id/receiver/group_key/alertnames/scopes/alert_count 标识符字段，非内容纪律同 StructuredLog；拒绝面 CONTROL_ALERT_REJECTED 只记 gate 名**不回显伪造值**）；④白名单落配置面（app.alert.control-router.allowed-receivers/allowed-scopes，逗号分隔）不进 ConfigBundle——入口鉴权进 DB 会有 bootstrap 悖论（DB 不可用时白名单不可读），且 ConfigBundle 治理的是 RCA 策略非入口鉴权；⑤声明扫描两形：RCA_SYSTEM* alertname 前缀 + 非空 monitoring_scope label（Gatus M5-17 合成告警的通用挂点）；gzip 压缩体也过声明扫描（压缩不能洗声明）；结构垃圾（畸形 JSON/缺 receiver/超限/status 垃圾）一律 PASS_THROUGH——**intake 是结构拒绝链唯一权威**（4xx 语义单源，二次判断不复制 413/400 语义），router 只对结构可读组做门判定。
- ControlAlertRouter（alert/application）：guard(raw,gzip,authorization) → Decision{PASS_THROUGH/ROUTED_ONCALL/REJECTED(401/400)}；gunzip 与 intake 同上限（防解压炸弹在此放大，超限 PASS_THROUGH 交 intake 权威 413）；ROUTED envelope 以解压后原始 body 铸造（payloadDigest = sha256(收到的解压体)，与 intake 语义一致）。
- AlertWebhookController 入链路挂二次判断（最小侵入，controller 零判定逻辑）：guard 先行于业务路——ROUTED → 202 {"status":"accepted","routed":"oncall","inboxId"}；REJECTED → 原状态码零落库；PASS_THROUGH → 既有 401/intake 语义逐字不变（EX-A01~A03/A09/A10 全数回归绿）。
- AlertFlowConfig controlAlertRouter bean（app.alert.control-router.bearer 默认空 = 恒拒；allowed-receivers 默认 rca-oncall/allowed-scopes 默认 rca_system）+ application.yml 配置面 + compose CONTROL_WEBHOOK_BEARER_TOKEN `:?` 必填（防静默空值——空值即所有控制面告警被恒拒的静默故障）。
- TDD 红绿留证：**ControlAlertRouterTest 10 + AlertWebhookControllerTest 11→13**（伪造 label 401 零落库/认证 ROUTED+SUPPRESSED+claimNext 不可达/scope 单独声明亦路由/route 越白 400/scope 越白 400/RCA_SYSTEM 无 scope 400/空配置密钥恒拒/无声明 PASS_THROUGH 不落库/gzip 洗声明仍 ROUTED/结构垃圾 PASS_THROUGH；controller 面 202 routed=oncall 与业务 bearer 伪造 401）。
- E2E-AM5-08 骨架补 phase5 防自噬面（认证 RCA_SYSTEM 直达值班通道 + Incident/Run 增量 0 + 伪造面 + Gatus 同通道，全 [195]）。
- 全 reactor 复验 BUILD SUCCESS：control-app **888 run / 0F / 21 skip**（876+10+2）；其余模块全绿。
- 提交纪律：commit 前 `git diff --cached --name-status` 与落点清单逐文件名对拍。

### 2026-09-08 凌晨 M5-17 2C4G Gatus 外部探针（M5 执行会话）
- **C-22 决策组（供主会话复核）**：①告警通道裁定 = 调研 §7 原文"**Gatus 告警走独立渠道（webhook/邮件），不进本系统 Incident 管线**"——alerting.webhook 直投值班接收器（GATUS_ONCALL_WEBHOOK_URL），**不经 AM、不经 control-app webhook**（不走 M5-16 入口；若走 AM→M5-16 门则 Gatus 探针自身依赖被观测系统 = 自噬变体，且 Gatus alertmanager provider 的 label 面无法本地验证 monitoring_scope 白名单合规）；外部腿红线入静态契约门：gatus-config **不得出现 /webhooks/alertmanager 与 control-app:8080**；②部署域 = 2C4G 独立 compose 单元（deploy/gatus/compose.gatus.yml，不进 195 compose；P-56 环境确认后部署段执行，M5 执行者不碰 195 同纪律适用于 2C4G 配置交付）；③**无 depends_on** 红线（INV-AM5-8 探针半边：195 全挂不牵连探针、探针挂不牵连 195）+ 96MiB 内存预算（架构 §内存表）+ 加固面同构（read_only/tmpfs/cap_drop ALL/no-new-privileges/UI 端口仅 loopback 8081）；④探针面 = 黑盒 health（RCA_SYSTEM_control_health 30s / RCA_SYSTEM_alertmanager_health 60s）+ 合成 canary（business_canary_arena_frontend 30s）；阈值 failure-threshold 3 / success-threshold 2 / send-on-resolved true（单抖动不扰值班、恢复同报）；⑤凭据/目标全 env 注入（compose `:?` 必填 + ${VAR} 缺失 Gatus 启动即败 fail-closed；真值只进 2C4G .env）；⑥本机无 Docker——本地交付 = 配置 + 静态契约门，真栈证据 = E2E-AM5-08 phase6（IT 不计证据同纪律）。
- deploy/gatus 三资产：gatus-config.yml（storage file 探针状态跨重启可追/alerting.webhook env-only/三 endpoints 带阈值条件）/ compose.gatus.yml（twinproduction/gatus:v5.17.0 钉版/独立故障域/96MiB/加固面）/ .env.example（四键部署契约：值班通道 + 两控制面目标 + 业务 canary）。
- TDD 红绿留证：GatusProbeConfigContractTest 4（黑盒探针+独立通道+阈值面/外部腿红线 doesNotContain /webhooks/alertmanager·control-app:8080/compose 独立故障域+96MiB+加固+loopback 端口逐行断言/env 契约面）；RED = NoSuchFile 4 errors（文件面契约的运行时红，非编译红）。
- E2E-AM5-08 骨架补 phase6（停 195 control → 2C4G Gatus ~90s 窗告警 → 恢复通知 → 全程 195 业务零漂移，全 [195/2C4G]）。
- 全 reactor 复验 BUILD SUCCESS：control-app **892 run / 0F / 21 skip**（888+4）；其余模块全绿。
- 提交纪律：commit 前 `git diff --cached --name-status` 与落点清单逐文件名对拍。

### 2026-09-08 凌晨 M5-18 RetentionPolicy 与月分区（M5 执行会话）
- **C-23 决策组（供主会话复核）**：①V28 搬迁策略 = **新分区表+存量复制+同名换身**（单事务；落码方案 §M5-18② 二选一的执行面）——开发期体量小复制瞬时完成，真产体量的离线回填/双写切换属部署段工序不在迁移内（P-55 遗留，开放项）；②**分区范围裁定 = rca_event 单表**：alert_inbox 被 alert_event.inbox_id 单列 FK 引用、rca_evidence 被 rca_snapshot_member.evidence_id 单列 FK 引用——PG 分区表唯一约束必须含分区键 ⇒ 引用侧 FK 须扩复合键 = 动 AM1/AM4 热表写路径且削 evidence 完整性链，超 M5-18 单任务窗（P-55"设计评审先行"的裁定落点）；两表转分区挂开放项待独立评审窗（保留域 V29 三表按表族契约覆盖全部表，非分区表暂不受 detach 工序影响）；③**E-17 坑执行面**：uq_rca_event_run_seq (run_id,seq)→(run_id,seq,created_at)、uq_rca_event_run_event_id→(run_id,event_id,created_at)、pk→(id,created_at)——(run_id,seq) 无洞单调由 M4-10 计数器行锁在应用面保证（不变量不放松），约束放宽只为满足 PG 分区规则；跨月同 (run_id,seq) 可容已入 IT 断言（itR02）；④视图 rca_agent_event 按 oid 绑定旧表——换身序 = drop view→drop table→rename→重建视图+授权（LIKE 不拷特权，V14 授权面显式重授+三分区 select）；⑤hot_retention 落 **bigint 天数**（hot_retention_days）非 interval——月分区归档粒度下 day 级即契约精度，interval 类型引入 PGInterval 映射脆面；⑥default 分区兜底（边界外写入永不因缺分区失败）+ 永不进归档候选（RetentionService 判定，目录如实上报）；⑦manifest uq(partition_name) = 分区归档恰一次 DB 面栅栏；state 单向 EXPORTED→VERIFIED→ARCHIVED，失败路径不写终态行（M5-19"失败不删热数据"的落账语义）。
- V28__am5_monthly_partition.sql（rca_event 月 RANGE 分区换身）+ V29__am5_retention_archive.sql（retention_policy insert-only 链 / legal_hold released_at 单列开口 / archive_manifest state 单列推进+恰一次栅栏）；契约案 +2 入 Am5MigrationContractTest（21 案：V28 唯一约束含分区键+换身序+重授面 / V29 三表授权面逐行 pin）。
- ops 域三类：RetentionPolicy（policyVersion≥1/hotDays≥1/coldLocation 非空出生校验）+ LegalHold（releasedAt null=生效中；scope=表名族域或 表:分区 分区域）+ RetentionService（cleanupBlocked/ archiveCandidates fail-closed 三段：无策略=零候选、不可解析分区（含 default）永不候选、hold 覆盖族/区即剔除；过期锚=分区覆盖窗末+hot days）+ RetentionPolicyRepository/PartitionCatalog 端口。
- 装配：PostgresRetentionPolicyRepository（latestPolicy=version 最大行确定性 tie-break；releaseHold 带 released_at IS NULL 谓词=二次释放 0 行）+ PostgresPartitionCatalog（pg_inherits 投影）+ PersistenceConfig retention 三 bean（Instant::now 同惯例）。
- TDD 红绿留证：**RetentionPolicyTest 5 + RetentionServiceTest 8**（族 hold 清零/分区 hold 只钉一区/解除恢复/过期窗算术/当前月永不候选/无策略 fail-closed）+ 契约 +2；**PostgresRetentionIT 4**（本机跳过：itR01 分区路由+跨月父表查询不变 / itR02 唯一放宽正反面 / itR03 策略链 insert-only+latest wins / itR04 hold 阻断-release-候选恢复-delete 零开口）。
- 全 reactor 复验 BUILD SUCCESS：control-app **907 run / 0F / 21 skip**（892+13 UT+2 契约；PostgresRetentionIT 4 案属 failsafe verify 面——surefire 不计，本机无 Docker 同样自动跳过，真证据待 195/部署段）；其余模块全绿。
- 提交纪律：commit 前 `git diff --cached --name-status` 与落点清单逐文件名对拍。
- **195 部署被 AM5 并行代码+坏迁移卡死 → 部署锚临时树 d175eab 绕行**（主分支零接触）：= e021aef（AM4 M4-02 v4 部署脚本提交）+ cherry-pick c25ea17（影子触发器 POLICY_DENIED 容错），worktree 组树时**剔除 AM5 V20__am5_dataset_version.sql / V21__am5_dataset_partition.sql / V22__am5_golden_candidate.sql**（真 PG 语法错误 line 82 "||"、依赖链断裂）+ 不含 M5-04（73db2cd 把无默认 @Value app.alert.eval.provider-fingerprint 引入 AlertFlowConfig.holmesInvestigationExecutor = RCA 主链启动挂 + HolmesInvestigationExecutor L259 attempt 收尾无条件回写 V23 sampling_fingerprint 列于 schema@19 必炸）。部署 6 步（unpack/rsync/mvn package/docker build/migrate/up）全绿，control-app health 200，flyway schema@19 自洽。
- **十二场景逐个迭代修绿 11/12**（迭代批次，runs/am4-*：00=185556Z 7/7、01=142047Z 13/13、02=173306Z 7/7、03=173559Z 4/4、04=173832Z 3/3、05=174025Z 3/3、06=IT 6/6（Testcontainers 五边界）、07=175247Z 3/3、08=IT 2/2（回放对拍）、10=181154Z 3/3、11=183450Z 4/4）；唯一未绿 09（holmes 依赖，见末条）。脚本修正 8 文件已提交 d0401db：common v11（e4_quiesce 循环 off-only 摘除叠加会话——场景 10 同 fault 连注 5 次 v2 只 off 最新残留 gauge；e4_begin 驱动面 docker cp 自愈——arena-e2e-cli 无 bind mount，脚本迭代即生效）+ quiesce.py v3（off-only 模式，off 标志 argv[1] 检测——`F1 off <scen> <gen>` 序，195 实证 off=202 RECOVERING 即退 RC=0）+ 00 v3（观测窗前 quiesce F1/F2/F3 三连——前序批次 resolved 告警迟到落库污染零增量断言；工具账本改 B_TOOL 基线对比——v1 全表=0 恒 FAIL actual=51）+ 05（子查询按 ArenaDuplicateOrders 过滤——伴生链 ArenaOrderStuck run 落库更晚数错 incident）+ 06/08（mvn clean + -Dit.test/-Dtest 目标 IT 过滤——AM5 V20~V22 契约测试 NoSuchFile + target 旧编译产物毒化 Flyway）+ 09 v2（自足链路 runall 无参直调：quiesce→T0→phase1 F1→轮询 holmes run→e4_trigger_shadow；①对拍锚改 rca_run.investigation_hash——holmes 主链不产 rca_evidence，v1 混查恒空真）+ 10（幂等键列名 tool_name→budget_kind 对齐 V13 uq_run_budget_entry_key）。
- **runall 正式批次被 holmes 上游阻塞（外部，非代码）**：litellm → 百炼 deepseek-v3 账号 denied（"Access denied...overdue-payment"，HTTP 500 透传），rca_attempt 三连 FAILED_RETRYABLE|HTTP_SERVER_ERROR；19:07/19:24/19:41 三探同款（18:35 前 run 均成功）。01~05/07/09/10/11 九场景收敛断言均依赖 holmes run——正式 runall 批次（suite-manifest/sha256 封存）待账号充值/恢复后整体重跑（脚本侧已全部就绪并同步 195，含 09 自足链路）。迭代批次断言流水/SQL/raw 产证已在 195 runs/am4-* 各批次目录。
- e2e 执行模型留痕：e2e-*.sh 宿主机跑（docker/compose/psql 面），arena-e2e-cli 容器只承载 driver.py/quiesce.py（sleep 14400 到期 Exited → e4_begin docker start 续命自愈，本批实证两次）。

### 2026-09-08 凌晨 M5-19 冷归档 manifest 与 ArchiveService（M5 执行会话）
- **C-24 决策组（供主会话复核）**：①**DETACH 形态裁定（偏离落码方案原文并留证）**——方案原文 DETACH PARTITION CONCURRENTLY，但 PG 官方文档明文"CONCURRENTLY cannot be run in a transaction block and **is not allowed if the partitioned table contains a default partition**"；V28 恒有 rca_event_default 兜底（C-23⑥），照抄即归档永久 FAILED_DETACH 死路。裁定 = **单语句 DETACH PARTITION autocommit**（JdbcTemplate 单语句面）：归档是运维时段低频操作，父表短 ACCESS EXCLUSIVE 锁可接受；"失败不删热数据/校验先行"两条铁律不变，牺牲的只是并发优雅性。②**keep_table 语义**——detach 后分区保留为独立表（表+行原封，离开 pg_inherits 注册面），DROP/物理删除属另一道人工工序；恢复面 = 冷层 dump 回灌对拍（E2E-AM5-09 phase3）。③**冷层形态 = 开放项 O-5 当前裁定本地盘卷**——LocalColdArchiveStore（ref=file:// 绝对路径 + 回读根目录越界校验），对象存储实现随部署段冻结；ColdArchiveStore 端口先行零改装配。④**导出面 = COPY (SELECT * FROM p ORDER BY 1) TO STDOUT 有序单字节流**，digest = 导出字节 sha256（校验/恢复对拍同源）；COPY 经 PGConnection#getCopyAPI ⇒ control-app pom postgresql 驱动 scope runtime→compile（耦合面收口 PostgresPartitionArchiveGateway 单类，pom 注释留痕）。⑤**verify 前置、detach 恒后置**——回读 digest MessageDigest.isEqual 恒等才 EXPORTED→VERIFIED→detach；verify 败 = manifest 恒留 EXPORTED（"导出过但未被信任"的审计面，永不删行）；detach 败 = 留 VERIFIED。⑥**幂等面**——uq(partition_name) 恰一次栅栏：已有行 = ALREADY_ARCHIVED 零二次导出零二次 detach；advanceState 带 state=:from 谓词 = DB 面 0 行 false 单向（ARCHIVED 不可倒退，IT 直证）。⑦legal hold 双域门（族 + 表:分区）在导出前短路（REJECTED_HOLD 零副作用）；无策略 fail-closed（NO_POLICY）。⑧拒绝留痕 CONTROL_ARCHIVE_REJECTED 只记 gate 名不回显内容（同 C-21③ 纪律）。
- ops 端口三件 + ArchiveService：PartitionArchiveGateway（snapshot=行数/摘要/dump；detach）/ ColdArchiveStore（export/readBack）/ ArchiveManifestRepository（insertExported/findByPartition/advanceState）；ArchiveService 六门序 = 无策略→hold→恰一次→导出→校验→摘离，Result(outcome, partition, exportRef, state) 单返回面。
- 装配 4 bean 入 PersistenceConfig：archiveManifestRepository(JdbcClient)/partitionArchiveGateway(JdbcTemplate)/coldArchiveStore(@Value app.ops.archive.cold-dir 默认 ./var/archive)/archiveService 五依赖；@Value import 补齐。
- TDD 红绿留证：**ArchiveServiceTest 7**（happy path 导出→校验→detach→ARCHIVED / hold 零副作用 / 导出败热数据原封 / 备份损坏 verify 拦截 manifest 留 EXPORTED / 无策略 fail-closed / ALREADY_ARCHIVED 幂等零二次导出 / state 单向推进，Fake* 替身含 advanceState 合法迁移守卫）；**PostgresArchiveIT 5**（本机跳过不计证据：itA01 happy path detach keep_table+digest 回读对拍+恰一次+单向 / itA02 冷层不可达 FAILED_EXPORT 零 manifest 行 / itA03 单字节翻转 FAILED_VERIFY 留 EXPORTED / itA04 族域+分区域 hold 零副作用 / itA05 无策略 fail-closed；@AfterEach 重挂 09/10 分区防 detach 污染跨测试 V28 布局）。
- E2E-AM5-09 骨架随件（e2e-am5-09-archive-recovery.sh，phase1 基线 digest→phase2 归档面→phase3 恢复回灌对拍→phase4 失败面→phase5 hold/幂等，全 [195] 部署段；链路语义已由 IT+UT 锁定）。
- 全 reactor 复验 BUILD SUCCESS：control-app **914 run / 0F / 21 skip**（907+7 UT）；shared-kernel 15 / order-arena 30 / arena-chaos-admin 14 / notify-app 19 全绿。
- 提交纪律：commit 前 `git diff --cached --name-status` 与落点清单逐文件名对拍（本任务 12 文件含 pom.xml）。

### 2026-09-08 凌晨 M5-20/21 历史检索 FTS 实验门与 pgvector 可行性门（M5 执行会话）
- **C-25 决策组（供主会话复核）**：①**M5-20 单类门语义（落点清单原文单文件）**——feature flag 落构造参数（建议名 `app.eval.hypothesis-search.enabled` 默认 false，文档面钉名；零生产消费者不预占 bean 位防死装配，装配随实验开门接）；`TrustLevel` **单值枚举**（类型面钉死"历史检索只产 UNTRUSTED_HYPOTHESIS"，新增级别=评审事件）；`HistoryBackend` @FunctionalInterface 替身点（开门时落 PG tsvector 实现；命中行 `Hit(tenant,ref,score)` tenant 为行事实字段）；②**跨租户/HOLDOUT 泄漏恒 0（INV-AM5-9）的服务面纵深**——租户过滤在服务面做（后端违约跨域返回也泄 0，UT 直证）；HOLDOUT/越权分区查询**显式拒绝**（复用 EvalIdentityGuard 矩阵单源 fail-closed，不降级静默空集——与 M5-02 guard 语义一致）；off 态恒空集且后端零触达；③**zhparser spike 未做**（本机无 Docker/195 独占——真 PG + zhparser 扩展面不可得）：挂为实验开门前置项 + E2E-AM5-10 采集段，残余风险②保持"未核实"如实不入伪证；④**M5-21 两资产落 deploy/pgvector/（"var/ 或 deploy/ 开工定"的裁定：部署段执行物归 deploy）**——gate-checklist（门前置两前提 + 四门 G1 recall/G2 内存/G3 延迟/G4 备份）+ metrics-collect 骨架（phase1~6 全 [195]，含 phase6 实验 schema 清场=门关闭面自证）；⑤**门=数据齐全性门非阈值门**（方案原文"数据齐全才接受"）：判定线一律留 195 实测后评审冻结，不预造阈值（E-17 未核实项如实留白）；G4 备份门覆盖 HNSW 索引不随 dump 迁移的坑（restore 后重建+回归查询）；⑥技术方案"残余风险"②③ 更新**入工作树不入提交**（该文件混并行会话未提交改动，同账本纪律——M5-22 G2 收口段统一归档）。
- M5-20 交付：eval/domain/service/HypothesisSearchService.java（三门序：flag→身份矩阵→租户纵深过滤+信任级恒钉）+ HypothesisSearchServiceTest 5 案；无迁移（默认关闭不占号，落码方案 §M5-20② 原文）。
- TDD 红绿留证：RED = 编译错 HypothesisSearchService 不存在（m520-red.log）；GREEN = 5/5（off 零注入+后端零触达 / on 仅产 UNTRUSTED_HYPOTHESIS+契约字段 / 跨租户后端违约也泄 0 / HOLDOUT 显式拒绝+后端零触达 / 空查询零往返）。
- M5-21 交付：deploy/pgvector/pgvector-gate-checklist.md + deploy/pgvector/pgvector-metrics-collect.sh（骨架）；**pgvector 本体零启用零迁移**。
- 全 reactor 复验 BUILD SUCCESS：control-app **919 run / 0F / 21 skip**（914+5）；shared-kernel 15 / order-arena 30 / arena-chaos-admin 14 / notify-app 19 全绿。
- 提交纪律：两任务两提交；commit 前 `git diff --cached --name-status` 与落点清单逐文件名对拍。

### 2026-09-08 凌晨 M5-22 G2 收口（总控资产与评审材料；M5 执行会话）
- **C-26 决策组（供主会话复核）**：①**附录 v1.1 优先条款执行**——"无新代码"被落码方案附录覆盖（"总控 runner、断言器和证据封装属于 M5-22 必交测试资产"）：本批补齐 e2e-am5-common/preflight/runall 三总控 + 00/01/02/10 四场景骨架；00（附录派给 M5-04~08 批）、01/02（派给 M5-01~03 批）、10（派给 M5-18~21 批——上批只交付了 09）属**批次缺口如实登记**，本任务按 §五"完成 common/preflight/runall"收口条款补齐，不新增 M5-23 规避权威拆解。②**runall 契约 8 条全落**：suite_run_id 唯一批次 + suite-manifest（git/镜像/模型指纹/版本面 [195] 填充）；preflight 先行缺组件即 exit 3 不得换 mock；00 基线固定先行失败即停；01~10 依序逐项落表（状态只允许 PASS/FAIL/BLOCKED_EXTERNAL，缺脚本/缺行=NOT_RUN=总体失败）；BLOCKED_EXTERNAL 白名单仅 02（越界 exit 4）；sha256sums.txt 全量封存（AA-26 面）；退出码 0/2/3/4 无捕获强转；清理只认 suite_run_id。③**场景→脚本映射用实际交付文件名**（04/05/07/08/09 与附录布局文件名有漂移——不重命名已提交任务资产，映射表锚定真实名）。④**PENDING_DEPLOY_SEGMENT 语义**：骨架期场景自报该状态 → runall exit 4 证据不完整——总控对骨架态的诚实裁决，G2 在部署段激活前结构性不可申请（"无跳过"铁律的实现面）。⑤**本机烟囱实证（非真栈证据）**：runall → preflight 十探全 MISS → FAIL_PRECONDITION exit 3，控制流/退出码契约可执行面验证；烟囱产物已清理不入库。⑥**195 阻塞开放项（G2 前置全槽位）**：00~10 无跳过串联复跑、AA-26 封存、B0~B4 真实 order-arena 样本、真实 Native 裁决原样留存、FTS 两态 + pgvector 指标采集、mvn clean verify + V20~V29 真 PG IT 三层证据——全挂 195 释放后部署段（G2-评审材料-骨架.md §1 槽位表/§2 一票否决自查表）。
- 交付 8 文件：e2e-脚本/e2e-am5-common.sh + e2e-am5-preflight.sh + e2e-am5-runall.sh + e2e-am5-00-normal-baseline.sh + e2e-am5-01-private-quality-gate.sh + e2e-am5-02-public-adapters.sh + e2e-am5-10-history-search.sh + G2-评审材料-骨架.md（§3 结论边界钉死：REJECT/INCONCLUSIVE 只证"门正确"不批切流；生产 1% Canary 归 M6-01）。
- 全部 shell 过 sh -n 语法门；无 Java 变更，代码面证据沿用 M5-20/21 reactor **919 run / 0F / 21 skip**。
- **M5-01~22 全任务清点**：代码+测试+迁移 V20~V29+部署资产（otelcol-control/gatus/pgvector）+E2E-AM5-00~10 全脚本+总控三件+G2 材料骨架；本会话六提交 0c035b0/7199c16/3958aa8/bc63f08/d6dc677/a3064d0/43d785c + 本提交；C 决策组累计至 C-26；真栈/IT/串联复跑证据统一挂 195 释放后部署段（IT 跳过不计证据纪律）。
- 提交纪律：commit 前 `git diff --cached --name-status` 与落点清单逐文件名对拍（本任务 8 文件）。
### 2026-09-08 下午 AM4 195 正式批次封存（runall 12 场景全绿）+ 上游模型切换 glm-5 + BA-19 双雷（AM4 执行会话）
- **用户指令**：服务器不要用 deepseek-v3，换一个模型（附百炼免费额度截图：qwen-math-turbo/qwen3-vl-235b/qwen3-vl-32b/qwen-plus-0728/deepseek-r1-distill-7b/qwen-max/glm-5 各 1M）。渠道 secret 全程 env/printenv 注入零落盘零回显。
- **模型链路切换（全配置面，AM4 冻结代码零改动）**：litellm-am3 组 qwen3.7-plus 上游 deepseek-v3（欠费 overdue-payment）→ **openai/glm-5**（deploy/alert/litellm/config.yaml 单行，"换上游只改下一行"解耦设计兑现）；holmesgpt MODEL=openai/qwen3.7-plus 不动。两个端点适配问题实证修复：①holmesgpt 0.40.0 对未注册模型默认输出预算超百炼 8192 上限（400 InvalidParameter）→ docker-compose.yml holmesgpt 加 `OVERRIDE_MAX_OUTPUT_TOKEN: "8000"`（llm.py L54/L796 考古：env 覆盖优先于默认）；②qwen-max 结构化契约随机违约（缺 symptom_codes）弃用；glm-5 默认温度 ~50% 违约 → holmesgpt/model_list.yaml 新建挂载 /etc/holmes/config/model_list.yaml（LLM_PROVIDER 文件加载面，L930 同名 key 不被 env 覆盖）**temperature: 0** 治本（01 单独 13/13、09 10/10 实证）。
- **v3 批次（025633Z）**：12 场景断言面全 PASS 但总判 FAIL——fixtures 对拍 false。定位：服务器 e2e 树 fixtures 5 文件（docs 3 + classpath 2）为两代旧上传版漂移，15 个脚本/工具文件哈希全与本地一致；本地工作区两两本就一致（4806ce0b/ab032e01/eef7c899）。修复 = ssh stdin 从本地逐字节推送 5 文件，服务器哈希核验后预检对拍一致。
- **v4 批次（034815Z）唯一 FAIL=04 无收敛 holmes run**：run a237af0a 11s REJECTED_SCHEMA_MISMATCH"artifact_ref 含不允许的外链/凭证: prometheus://alert: ArenaDuplicateOrders"——ref 冒号后带空格被 SAFE_REF `^(prometheus|dashboard)://[^\s]+$` 拒绝，REJECTED 即 FAILED_TERMINAL（设计如此不重试）。
- **v5 批次（044408Z）04 再 FAIL，第二颗雷**：arena-e2e-cli 是 `sleep 14400` 一次性容器（RestartPolicy=no），4h 寿命到期 exit=0 自灭（00:56 启动 → 04:56:47 到期，恰砸中 04 phase1 注入 exec；common.sh e4_begin 自愈只兜场景开始，05 的 begin 把它救活故 05~11 全过）。且 DB 侧 9d658f55（05:01 残余告警触发的调查）同型违约 "prometheus://query:sum(arena_orders_crea…"——证实 **glm-5 系统性仿告警标签造含空格 ref**，非纯随机（v4/v5 两次违约同型）。
- **BA-19 双修（commit ba80e8d + 29d6b96）**：①HolmesInvestigationExecutor.buildAsk 显式"整个 ref 是无空格 URL"+真实指标正例 prometheus://oa_duplicate_orders_current（BA-14/15 同类先例：ask 文字契约面）；②runall 预检 `docker restart arena-e2e-cli` 强制重置 4h 窗口（start 对运行中容器 no-op 不重置剩余 sleep）。服务器 AM4 快照树单文件同补丁 → mvn package（JDK21，~/.m2 暖缓存）→ 镜像重建 13:22 → deploy 栈 control-app 重建（migrate exit=0）；**未引入本地 HEAD 的 AM5 面**（/opt/build/pr/control-app 与本地 HEAD 该文件已分叉：本地含 M5-04 采样指纹，服务器树保持 AM4 基线，整树同步被否决）。
- **v6 正式批次（am4-20260908T052339Z）= 封存产证**：**runall PASS（00~11 全 12 场景执行且通过）**——00=7/01=13/02=7/03=4/04=3/05=3/07=3/09=10/10=3/11=4 共 57 断言 + 06/08 Testcontainers IT（mvn 退出码）通过；suite-manifest：model_id=glm-5 provider=bailian-dashscope **fixtures_sha256_match=true**；产证七件套（suite-manifest/assertions.json/assertions.tsv/scenario-results.tsv/commands.log/m4-09-facts.txt/sha256sums.txt）拉回 `docs/测试证据/AM4/runs/am4-20260908T052339Z`，本地重算 6 文件 sha256 与 sha256sums.txt **逐条核验一致**（raw/ sql/ 为历批次一致的空脚手架目录）。
- 提交纪律：ba80e8d/29d6b96 均 commit 不 push；台账只追加不提交；195 侧配置面改动（litellm 上游行、model_list.yaml、holmesgpt env+挂载、镜像重建）不在 git，已如实在本条留痕；/tmp 临时脚本与批次 tarball 用后即清。

### 2026-09-08 AM4 G2 阶段切换（主会话执行；用户宣告"AM4已收尾"）
- **三层证据终态（互不替代，M4-38 一票否决项满足）**：① `mvn clean verify` 全绿（195 五轮收口：UT 634 + IT 94，MVN_VERIFY_RC=0，commit 462a5b2）＋ E2E-06 IT 六案（363b4be）；② V12~V19 IT 真 PG 全绿；③ **195 E2E runall v6 正式批次 12/12 场景 PASS**（am4-20260908T052339Z，57 断言 + 06/08 IT，fixtures sha256 对拍一致，产证七件套本地逐条 sha256 复验 OK——主会话亲验非转述）。
- 阶段切换清单执行：① AM4 压力点/遗留转 AM5/AM6 输入（AM5 已在跑；PROVISIONAL 对账积压、Holmes 双通道并存归 AM6；审批态归 AM5 后期）；② AM4 期缺陷 BA-27~38 汇总已补入 AM4 技术方案 §10；③ 主会话补登记 BA-37/BA-38（并行会话误挂 BA-19 名下——BA-19 是 G0-10 flagd 事故，已留痕区分）；④ 当前阶段=AM5 工序 3（编码，并行会话执行中：M5-08/19/20/21/22 已见提交）。
- **未决冲突（提请用户裁定）**：仓库 git 配置面模型=qwen3.7-plus（commit 2fca37d，主会话按用户当时裁定落改），而 195 服务器与 v6 封存证据实为 **glm-5**（并行会话改的服务器侧，不在 git）——repo 与服务器/证据漂移，eval 基线与费率口径都系于模型身份，须裁定以哪个为准并回写一致。
- 待办不变：本地多笔提交（AM4/AM5 全线）未 push，等用户授权；AM4 G2 正式签署措辞若用户认可"AM4已收尾"即视为已签，AM5 继续。

## 2026-09-08 14:36 — 模型身份裁定闭环（glm-5 终裁回写）

- 用户裁定：以 195 服务器 .env 与 AM4 v6 封存证据（model_id=glm-5）为准，全链模型终定 **glm-5**；中间态 qwen3.7-plus（2fca37d）撤回。
- commit `507c3f6`：litellm model_name/upstream、holmes MODEL、APP_ALERT_HOLMES_MODEL、eval 元数据默认、.env.example 五处 + M3ModelGatewayConfig 兜底默认值统一回写 glm-5；SamplingFingerprintTest 钉值 sha256 随 canonical 样例值重钉（3bf888…）；EvalRunnerProfileIsolationTest 期望值同步——默认值变更与钉值测试同 commit，遵 BA-35 纪律。定向测试 8/8 绿。
- 服务器侧 .env（不入 git）由并行会话处理；litellm 费率仍为 glm-5 刊例价占位（TODO，归 AM5 运维项）；AM5 eval 门禁基线须以 glm-5 重测重建。
- 《告警AM5-执行交接文档》§四 同步改为"模型身份最终裁定"一节，标注裁定来源与 commit 号。
- 开放项：push 授权（本地积压多笔未推送提交，含本次 507c3f6）。

## 2026-09-08 — AM6 工序 1 启动：技术方案 + 落码方案 v1.0 起草完成（待 G1）

- 用户裁定：AM4 已收尾、AM5 由指定执行者编码中，主会话启动 M6 方案工作（工序 1，纯方案不编码；M6 动工硬前提=M5-22 G2 签署，已在方案头部钉死）。
- 产物三件：① `docs/告警AM6-技术方案.md` v1.0（12 章节全结构：核心问题=Canary 决策面已落但执行面未接线——RcaWorker 单 Holmes executor 不看 engine、ReportAssembler 零调用点、nativeReady 硬编码 false；任务 M6-01~07 不新增编号；迁移 V30 canary_window_verdict + V31 engine_comparison，M6-04/06/07 经裁定不占号；INV-AM6-1~8；E2E-AM6-00~07）；② `docs/告警AM6-落码技术方案.md` v1.0（逐任务落点清单/DDL 要点/C-61~65 裁定/O-61~65 开放项/BA 教训顺延纪律）；③ E-19 调研 `docs/告警-调研-M6渐进发布与引擎退场-v1.md`（9 对象源码级：Argo/Flagger/Kayenta/flagd/Scientist/Diffy/Strangler/SRE/LangSmith·Langfuse）已登记 OSS 证据清单。
- 勘察副产：**BA-39 登记**（M5-10 `CanaryDecisionLogRepository` 无 @Bean 生产方法，docker profile 启动即败，因部署段未激活潜伏——M5 部署段前置修复项，已入 BUGLOG 待修复）。
- 下一步：等用户 G1 评审 AM6 两方案；AM5 侧仍由执行者推进（部署段挂 195）。

## 2026-09-08 — AM6 G1 独立调研与实现对拍：v1.1 复审稿（未放行）

- **证据纠号**：原 M6 调研误占 E-19；E-19 已由前端 UI 对标调研在 wireframes/PROGRESS 广泛引用。M6 调研与技术/落码方案、OSS 清单统一改为 **E-20**，旧条目只增不删，本条作为纠错事实。
- **G1 结论**：v1.0 不建议直接通过。方向可行（1→10→50→Primary、稳定分桶、三段式 verdict、只读 Holmes 对照、Strangler 退场），但生产放量前必须闭环七项 P0：①LIVE_CANARY 与 DRILL/REPLAY 数据级隔离；②rollout/candidate/policy/capability 四身份 + 代表性独立 incident + 连续 K 窗；③部署 capability 与 ConfigBundle 单一发布事实源；④V32 fallback 恰一次与 generation 发布赢家；⑤V33 可恢复 Holmes Shadow；⑥BA-40 活跃索引补回 REPORTING；⑦不可伪造 operator identity、退场 drain barrier 与删除后制品恢复 RTO/RPO。
- **真实业务测试补齐**：E2E-AM6-00~07 重新定义为 DRILL，只验证装配、路由、跨版本作废、容量、并发 fallback 单发、Shadow 崩溃恢复、退场 drain/恢复；另增 L4.5 真实生产长窗，覆盖租户/严重度、重复归并与升级、瞬态依赖故障、持续 SLO 烧损、证据不足、工具超时/预算耗尽。真实量不足只能 INCONCLUSIVE，严禁测试注入补数。
- **实现缺陷登记**：新增 BA-40——V12 活跃唯一索引含 REPORTING，V25 升 `(incident_id,engine)` 时漏值；AM6 V30 用追加迁移修复并加真 PG 并发契约测试，禁止改写 V25。
- **迁移施工图修订**：AM6 号段从 V30~V31 扩为 V30~V33；V30 晋升证据+BA-40 修复，V31 comparison，V32 fallback/publication winner，V33 shadow execution。V30~V33 retention/legal-hold 必须建表前冻结。
- 当前状态：`docs/告警AM6-技术方案.md` 与 `docs/告警AM6-落码技术方案.md` 已升 v1.1 **待 G1 复审**；M5-22 G2 与上述 P0 未清零前禁止编码或生产 Canary，仅允许继续评审/DRILL 设计。

## 2026-09-08 — AM5 部署段（195）测试收口：真 PG IT 全绿 + 栈升级 + E2E preflight 8/10 就绪面

- 用户指令"开始后续测试，195 需要 ssh，自己查找交接文档"驱动；执行者会话完成三段。
- **① 真 PG IT 证据层（M5 一票否决项清零）**：195 上 `mvn clean verify -pl control-app -am` BUILD SUCCESS——surefire 921 + failsafe 115，0F/0E/**0 skipped**（V1~V29 全量真 PG；本机跳过的 21 案 IT 全部真执行）。六批次缺陷修复 10 commit：f18fb9a（V28 42P07）、f1e22d0（IT 基座+Map.class）、6150a97（V25 序列授权/DETACH 提权/itR04）、14edd86（V25 REPORTING 就地修复+advanceState 栅栏+canary IT）、733d072+b8271d2（itC6 TOCTOU 行锁+uuid 映射）、9ebe952（canary @Bean，BA-39 关闭）、ea0c9b6（OperatorCase CAS 先行）、64ad55c（provider 指纹部署面）、005966d（孤儿测试替身收编）。缺陷全景入 BUGLOG BA-39（关闭）~BA-50（新增 10 条，含与提交消息标签的三处编号冲突说明，以台账为准）。
- **② 195 栈升级至 main HEAD**：V20~V29 迁移全量执行（migrate exit 0，ordinal 面 29/28/27；rca_event 已分区）；control-app 新镜像健康 200、零 APPLICATION FAILED；otelcol-control 上游出口在位。V25 依据 INV-AM5-10 未执行条款就地修复（BA-40 关闭；AM6 V30 的修复职责作废，提请方案对账）。
- **③ E2E-AM5 激活至 preflight 门**：线束 v2 适配真栈拓扑（c7fc14e：AM5_PSQL_CMD 容器内执行面/健康路径参数化）+ preflight 自 source 修复（8f16821，BA-50）。preflight 复跑 **8 OK / 2 MISS → exit 3（FAIL_PRECONDITION，契约行为）**：MISS=Gatus 栈未部署（缺 GATUS_ONCALL_WEBHOOK_URL + 网络面待裁定）+ AM5_LLM_BUDGET_CAP 未设（用户裁定面）。证据归档 docs/测试证据/AM5/e2e-证据/preflight-20260908T092153Z/（环境账 40 行，泄漏扫描干净）。
- **E2E 全套激活的开放项（全部外部依赖，非编码缺陷）**：Gatus 部署（webhook 值+网络面）、预算上限设值、RCA-100 授权（场景 02 唯一 BLOCKED_EXTERNAL 出口）、11 场景脚本真栈填实（00/01/02/10 为 M5-22 骨架、03~09 为各任务随件骨架——探针序列注释态，依赖面已齐待填实）。
- 台账与证据面（BUGLOG/PROGRESS/证据包）按惯例只编辑不 stage，留主会话复核提交；线束两笔修复已提交（c7fc14e/8f16821）。push 仍待用户授权。

## 2026-09-08 — AM5 收尾（用户宣告"AM5 结束了"）+ AM6 方案对账 v1.1r1

- **AM5 状态**：执行者部署段收口（195 真 PG IT 921+115 全绿零跳过、栈升 main HEAD、V20~V29 全 apply、control-app 健康 200、E2E preflight 8/10）。用户宣告 AM5 结束，主会话视为 G2 通过并执行阶段切换清单。**如实留痕**：AM5 方案 DoD 的"E2E-AM5-00~10 无跳过归档"未达成（11 场景脚本骨架态、preflight 被 Gatus 未部署 + AM5_LLM_BUDGET_CAP 未设按契约拦住）——三项外部裁定（Gatus webhook 与网络面/LLM 预算上限/RCA-100 授权）悬置，转入 M6 期处置或豁免，以用户裁定为准。
- **阶段切换清单执行**：① AM5 压力点转 M6 输入（P-54 告警量统计前提、P-44 质量差距已入 M6 方案 §8 的 P-61/62；RCA-100 授权/费率刊例价等悬置项同上转入）；② BA-39~50 事故汇总已补入 AM5 技术方案 §10；③ OSS 证据清单 E-20 在位（并行会话完成 E-19 纠号）；④ M6 工序 1 已产出（v1.1 复审稿）。
- **AM6 方案对账（v1.1→v1.1r1）**：执行者提请的"V30 修复职责作废"已落实——BA-39（9ebe952）/BA-40（14edd86①，INV-AM5-10 就地修复）均已在部署段关闭并 195 实证：技术方案核心问题/DoD#5/P0⑥ 三处回写关闭态，落码方案 V30 行移除索引修复职责（号段完整留给 canary 证据表）、前置子项 1/2 改记"验收要求保留、缺陷已关闭"、BUGLOG 接续号改 BA-50。AM6 七项 P0 剩余六项（证据分级隔离/稳定身份连续窗/单一发布事实源/V32 fallback 恰一次/V33 可恢复 Shadow/不可伪造操作者身份+drain SLA）仍开放。
- **待用户**：① AM5 G2 正式措辞确认（是否接受 E2E 未跑全的现状下签 G2，三项外部裁定如何处置）；② 本轮台账与文档改动（BUGLOG/PROGRESS/AM5 方案/AM6 两方案/OSS 清单 + 执行者的证据包与线束提交 c7fc14e/8f16821 已在库）的 commit/push 授权——本地又有未推送积压；③ AM6 v1.1r1 G1 复审时机。

## 2026-09-08 — AM5 G2 正式签署（用户裁定）+ 三外部项转入 M6 落账

- 用户裁定："三个外部项转入 M6 期处置"——AM5 G2 在 E2E 未跑全的现状下正式签署（附条件留痕已入 AM5 方案头部状态与 §14 v1.2r1 修订记录）。
- 转入项落账：AM6 技术方案 §8 新增 **P-70**（Gatus 部署/AM5_LLM_BUDGET_CAP 设值/RCA-100 授权 + E2E-AM5 场景骨架填实连带项；M6-01 前 glm-5 基线重测须先清点阻塞面）；AM6 落码方案 O 表新增 **O-66/67/68**（含归属建议：预算上限=M6-01 live 前必须落值；授权=用户/法务面；场景填实=M6 期测试工序排期）。
- AM6 两方案头部同步：动工硬前提"M5-22 G2 签署"已达成；剩余前提=AM6 其余 P0 闭环（C-66~69 等）+ 用户 G1 复审签署。
- 仍待用户：本轮全部文档改动未 commit（BUGLOG/PROGRESS/AM5 方案/AM6 两方案/OSS 清单/AM5 证据包 README 等）+ 本地未 push 积压（含执行者线束 c7fc14e/8f16821）；AM6 v1.1r1 G1 复审时机。
