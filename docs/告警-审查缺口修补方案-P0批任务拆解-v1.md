# 告警-审查缺口修补方案-P0批任务拆解-v1

> 依据：`docs/告警-审查缺口修补方案-v1.md`（总方案）、`docs/告警R7-真LLM多Agent技术方案.md` §21.2（MC21~36 用例规格原文）、2026-09-12 对账裁定（P0=MC21~24+MC31/32，捎带 digest 资产登记）。
> 前置审计：2026-09-12 只读审计结论（六卡现状逐卡 file:line），本文「现状」节即其浓缩，执行者上手前应先核对锚点未漂移。
> 迁移号段纪律：V95 已占用（dr05），本批新迁移从 **V96** 起；Flyway 乱序部署已配 `-outOfOrder=true`，仍须按号段申请，禁止复号。
> 本批**无新迁移**（全部改动在域/应用/装配面，复用既有表）；若实施中发现必须加列，先回报再申请 V96。

## 〇、六卡现状裁定（审计浓缩）

| 卡 | 现状 | 核心证据 |
|---|---|---|
| MC21 子任务回执幂等/乱序/有界 | **大部分已实现**：同 gap 唯一（V47:61 uq_r7_delegation_gap）、重复裁决拒绝（DeterministicSupervisor:312-314）、DuplicateKey 竞态兜底（:436-449）、唤醒 CAS 恰一次（:406-408）、round 栅栏防乱序（:399-401）、工具结果上界（ToolGateway:100-104） | 缺口=question 自由文本无长度闸 |
| MC22 同源去重/反证保留/禁投票 | **域层已实现**（ClaimReducer:34 禁投票明文、:70-115 同源一票/对峙 UNKNOWN）；**R7 主模式绕行 Reducer**（NativeInvestigationExecutor:242-243），且 ClaimStore 同 fingerprint 异内容=REVISED 覆盖（ClaimStore:10-14）——反证被"覆盖"而非"对峙呈堂" | 缺口=PrimaryFinalClaimProjector 无冲突检测 |
| MC23 resolved 后新 episode | **已实现**（IncidentProjector:263-279 新 episode 铸新 run、IncidentStateMachine:31-36 generation+1、STALE_GENERATION 栅栏、发布赢家 per(incident,generation) CAS） | 缺口=再现时序缝隙待验证（见 P0-4） |
| MC24 查询规范化去重/新鲜度 | **指纹面已实现**（ActionDigest:9-38 canonical sha256、冻结窗+inputDigest 使新现场指纹必变）；**去重消费面真空**（账本唯一键含 call_seq，重复调用各自落行；DoomLoopGuard:55-78 是熔断非去重） | 缺口=规范化器+同现场复用 |
| MC31 人工补充材料 | **入口已存在**（RunCommandController:46-91 HINT/FEEDBACK，CommandService:117-124 标 UNTRUSTED 落台账）；**消费面真空**（ContextAssembler 全文无 operator 读取，人工文本到不了模型上下文） | 缺口=消费缝+payload 闸+Claim 准入 |
| MC32 人工等待超时/并发 | **无"等待人工"态=合规真空**（RcaRunState:16-18 九态无人工态）；CONFIG_SWITCH 的 WAITING_SAFE_POINT 有界性+CAS 已实现（RunConfigSwitchService:210-214/258-264） | 缺口=验收断言钉死即可 |
| digest 资产登记 | **prompt 原文已登记**（AlertAm4Config:416-444 registerPromptAssets 启动时内容寻址幂等；InputReplayVerifier:78-90 rolePromptByDigest 可反查） | 缺口=压缩策略/compaction prompt 未登记（V92 只有行级 summary_prompt_digest） |

## 一、批次总览

| 卡 | 内容 | 可并行性 | 预估 | 状态（2026-09-12 收官） |
|---|---|---|---|---|
| P0-1 | MC22 对峙呈堂 + MC21 question 上界 | 与 P0-2 并行；**先于 P0-3**（同碰投影面） | 1~1.5 天 | ✅ 全绿（含 MC21~23 回执信封集成面） |
| P0-2 | MC24 查询规范化器 + 同现场证据复用 | 与 P0-1 并行 | 1 天 | ✅ 全绿 |
| P0-3 | MC31 人工补充消费缝（UNTRUSTED 入信封） | P0-1 合并后启动 | 1.5~2 天 | ✅ 全绿（V96 operator_material 表+API+CAS+消费缝） |
| P0-4 | MC23 时序缝隙验证 + MC32 断言钉 + 压缩资产登记（digest 小活） | 全程可并行 | 0.5~1 天 | ✅ MC23=钉案（缝隙表征+修法候选待裁定）；MC32 钉绿；资产登记 195 落库 |

> 验证梯（2026-09-12）：本地定向绿 → 195 全量 UT **1636/1636 BUILD SUCCESS** → 195 真 PG IT 9/9 → 195 真机部署（V96 应用、release_asset PROMPT 行 ff846b82、端点 401/403、启动 ERROR=0/WARN=0）。用例台账见 §八，偏差裁定见 §七。

执行纪律：每个执行者只提交自己名下文件；共享工作区禁用 `git add -A`；真 PG IT 本地一律 NOT_RUN 标留 195 窗；诚实语义（未知显「未统计」，无假实现）。

## 二、P0-1：MC22 对峙呈堂 + MC21 question 上界

**目标**：R7 主模式报告中，同 claimKey 的 TRUE/FALSE 双断言不再 REVISED 覆盖，强制 UNKNOWN+MULTI_SOURCE_CONFLICT 并集呈堂；委派 question 自由文本加长度闸。

**改动落点**：
1. `alert/application/agent/PrimaryFinalClaimProjector.java`（投影 :80-97）：投影前对同 claimKey 组内做状态冲突检测——复用 `ClaimReducer.reduceGroup`（:70-115）裁决规则或直接注入 ClaimReducer 调用；冲突组强制 UNKNOWN+CONFLICT，证据全组并集呈堂（对齐 ClaimReducer:122-125 语义）。EXCLUSION→FALSE 的"确认排除"语义（:90-94）不动。
2. `alert/domain/agent/DelegationDecision.java:24`：紧凑构造器加 question 长度上限（≤512，对齐 OperatorCommand.idempotencyKey ≤128 的惯例量级，理由写 javadoc）；超限行为=截断并标注还是拒绝，按 DeterministicSupervisor:306-332 校验链的 BATCH_SHAPE 族拒绝码风格裁定，**选定后在 javadoc 钉死**。
3. `infrastructure/persistence/PostgresDelegationDecisionRepository.java`：列注释同步（若选定截断语义）。
4. `ClaimStore.java:10-14` 四分支**不改**（REVISED/SUPERSEDED 是修订语义，保留）；冲突检测只加在投影缝，保证存储面历史不丢。

**测试落点**（新案补进 `PrimaryFinalClaimProjectorTest`/`DeterministicSupervisorTest` 同族文件）：
- MC22 主案：两专家同源复述（同 prometheus 源）→ 不升级根因（sources 折叠单源，进推测节）；第三专家反证（同 claimKey FALSE）→ UNKNOWN+CONFLICT 且反证证据 id 在报告引用集中。
- MC21 主案：重复裁决同 gap → REJ_GAP_ALREADY_ADJUDICATED 不重复唤醒（既有案回归钉）；question 超长 → 按裁定语义截断/拒绝。
- 回归钉：EXCLUSION→FALSE、MULTI_SOURCE_CONSISTENT 既有行为不翻面。

**验收口径**：MC22 E、MC21 E 按 §21.2 原文断言；权威源表（AlertAm4Config:537-541）在 R7 主模式不生 effect 是**已知诚实面**，验收不得按 Reducer 权威源语义断言 R7 路径（审计④）。

**相关文档批注**：R7方案 §18.3 能力 05/06（证据质量）、§21.2 MC22；修补方案 v1 R1 卡（ContextAssembler 证据进上下文是本卡的输入面）；ClaimReducer javadoc（域语义唯一权威）。

## 三、P0-2：MC24 查询规范化 + 同现场复用

**目标**：同参数不同措辞（空白/语序差异）的工具查询识别为重复；同 run 同 actionDigest 的重复查询复用已有证据行而非重打工具；新现场（新冻结窗）指纹必变，不受影响。

**改动落点**：
1. 新类 `alert/domain/tool/ArgsNormalizer.java`（或同族命名）：args 语义规范化（初版范围=字符串字段空白折叠/trim + Map 键序无关——canonical JSON 已管键序，规范化器只管字符串值）；javadoc 钉死"规范化范围白名单，逐字段扩面需评审"。
2. `ActionDigest.of`（domain/agent/ActionDigest.java:9-38）计算前接规范化器；**两计算点同源**：`ToolGateway.invoke`（:89-92）与 `SingleToolEvidenceAgent.investigate`（:132-134）。
3. **MCP 面并列防漂移**：`McpToolInvoker.java:57,106` 的第二套 actionDigest（args-only）同接规范化器，或裁定向主路径 ActionDigest 收敛——**实施时先裁定收敛还是并列，结论写进本文件偏差节**。
4. 同现场复用：`BoundedLlmRoleRunner.driveToolCall`（:201-247）invoke 前按 actionDigest 查本 run 证据账本（读面 `evidence.findByRunId`，:413-418 已有），命中则回喂已有 evidenceId 并落一行"复用"台账（不新扣工具预算？——**扣预算语义须裁定**：建议复用仍计一次工具调用防 DoomLoopGuard 豁免被绕，理由写 javadoc）。
5. 与 DoomLoopGuard（:55-78）协调：复用命中不得重置其"连续无进展"计数语义。

**测试落点**：
- MC24 主案 A：同参数换措辞（多余空白）→ digest 相同 → 第二次调用复用证据、零新工具执行（假件计数断言）。
- MC24 主案 B：同查询新现场（不同冻结窗/inputDigest）→ digest 不同 → 正常重查（新鲜度区分）。
- 规范化器边界案：值大小写/数字格式差异**不**折叠（白名单外不动）。
- 既有 DoomLoopGuard 案回归不翻面。

**验收口径**：MC24 E；新现场必须重查由冻结窗结构性保证，验收只需断言面（审计③）。

**相关文档批注**：R7方案 §18.3 能力 06、§21.2 MC24；增强线方案（⑤共享记忆/⑥查询去重为 P1 后期项——本卡只做到"同 run 内复用"，跨 run 共享不在本批）；InternalCanonicalJsonV1 javadoc。

## 四、P0-3：MC31 人工补充消费缝

**目标**：HINT/FEEDBACK 人工补充以 UNTRUSTED 身份段进入模型信封；人工根因判断不覆盖事实、不冒充独立来源；payload 有界。

**前置**：P0-1 已合并（同碰 PrimaryClaimAdmission/Projector 面）。

**改动落点**：
1. 消费缝：`ContextAssembler.assemble` 增 operator_command 读面——新端口方法按 runId 取 APPLIED 的 HINT/FEEDBACK（`OperatorCommandRepository` 增查询方法，Postgres 实现只读 SELECT 授权内）；以 UNTRUSTED 身份段入信封，条数/字符双上限（仿 MEMORY_SLOT_LIMIT 截断面 ContextAssembler:41,362-378）；空集=零段落，不造占位。
2. `OperatorCommand.java` 紧凑构造器：payload 大小闸（≤2048 字符，超限 400）；`RunCommandController.submit`（:46-91）同步校验。secret 不得入 payload 既有注释（:14-16）升级为**发送前扫描密钥词形拒绝**（对齐 R2 redacted 档惯例，`RcaModelGateway` 脱敏面同款词表）。
3. Claim 面（仅当人工补充含根因判断时）：必须走 `PrimaryClaimAdmission.admit`（:76-79 零有效引用→降级 HYPOTHESIS）同等准入；来源标签加 `operator:<actor>`（PrimaryFinalClaimProjector:80-88 同源推导点）——人工来源**不计入** MULTI_SOURCE 独立源数（与 MC22 同源折叠同律）。
4. 缝合既有不一致：`OperatorCommand.java:14-16`/`CommandService.java:31-32` 注释承诺的"上下文 UNTRUSTED 注入"由本卡兑现，注释语义与实现对齐。

**测试落点**：
- MC31 主案：HINT 含无引用根因判断 → 信封含 UNTRUSTED 段；若进 Claim 面 → 降级 HYPOTHESIS+operator 来源标签，不覆盖既有 TRUE/FALSE。
- 界案：payload 超限 400；密钥词形拒绝；条数/字符截断可追溯（omitted 标注）。
- 并发案：同 revision 双提交 → expectedRevision CAS（CommandService:89-91）REJECTED_STALE（MC32 顺带钉）。

**验收口径**：MC31 E、MC32 E（CAS 部分）。

**相关文档批注**：R7方案 §18.3 能力 08（调度与介入）、§21.2 MC31/32；MA-07 卡（MC29~32 家族）；CommandService javadoc（UNTRUSTED 承诺面）；修补方案 v1 R2 卡（redacted 词表同款）。

## 五、P0-4：验证与 digest 小活（可全程并行）

**5.1 MC23 时序缝隙验证（先测后修）**
- 审计发现：incident 再现（resolved→firing）时旧 run 通常仍在跑，`castRunIfFree`（IncidentProjector:324-326）返回 null 不铸新 run——新 episode 要等旧 run 收尾后的下一条事件才铸 run，期间新现场无 run。
- 落点：`IncidentProjectorTest`（或同族 IT）构造"旧 run 活跃中再现"场景断言行为；若红（新 episode 长时间无 run），修法候选=再现即铸新 run（uq_rca_run_active_incident 部分唯一索引是否允许双活跃需先核 V 迁移定义）或旧 run 主动取消——**修法先回报裁定再动**。
- 注意审计④：RcaRunOrchestrator:60-61 自陈 RERUN 同代不加 1（伪代码笔误修正），验收不得按伪代码断言 RERUN 加代。

**5.2 MC32 断言钉**
- 等待人工态不存在=合规：在 `RcaRunStateTest` 钉"九态无人工等待态"契约案；CONFIG_SWITCH 有界性（EXPIRED/cancelWaiting/双切 UNIQUE）既有案回归。

**5.3 压缩策略资产登记（digest 小活，半天）**
- 现状：prompt 原文已登记（AlertAm4Config:416-444），但 ContextCompactionService 的压缩 prompt/策略组合只有 V92 行级 `summary_prompt_digest`，资产级不可反查。
- 落点：压缩 prompt 模板按同款 registerPromptAssets 模式登记 release_asset（PROMPT kind，content 含策略标识+模板原文+digest）；`InputReplayVerifier` 反查面扩展（rolePromptByDigest 同法查压缩模板）。
- 登记失败维持 log-warn 降级（与 :439-442 同款诚实面）；若验收要求 fail-closed，先回报裁定（审计③）。

**相关文档批注**：R7方案 §21.2 MC23/MC32/MC36；EN-07（release_asset kind 扩面先例）；修补方案 v1 R2 卡第 3 条（prompt 落快照原文）。

## 六、完成定义（本批 DoD）

1. MC21/MC22/MC24/MC31 E 案 + MC32 断言钉全部绿（本地单测）；MC23 缝隙验证有结论（绿=钉案，红=修复后绿）。
2. 全量 `mvn -q -pl control-app test` 0 失败 0 错误；无真 PG 环境的新 IT 标 NOT_RUN 留 195 窗并登记。
3. 无新迁移（如破例，V96 起+回报记录）；无假实现、无 UNKNOWN 当 PERFORMED。
4. 偏差与裁定（P0-2 规范化范围/MCP 收敛、复用扣预算语义、question 截断 vs 拒绝、MC23 修法）逐条回填本文件偏差节。
5. 落账：PROGRESS 追加一行 + 本文件状态表翻面。

## 七、偏差与裁定（执行期回填，2026-09-12）

| # | 类别 | 内容 |
|---|---|---|
| 偏差-1 | 迁移破例 | 拆解原文「本批无新迁移」**破例**：本批按用户「继续实现，将未实现的部分全部实现」指令扩面至审计 9 未实现项的 MC21~23 回执台账与 MC31/32 人工材料，二者无表可落 → 申请 **V96**（`V96__mc2123_receipt_and_operator_material.sql`：rca_delegation_receipt + incident_operator_material 两表及约束），按号段纪律申请、195 真 PG Flyway 应用并验证（schema_history + 约束清单见 §八 D-19/D-20）。 |
| 偏差-2 | 批面扩容 | 受指令扩面影响，实际落点超出拆解 §二~五所列行：新增 DelegationReceiptService/DelegationReceipt/OperatorMaterialService/OperatorMaterialController 等 11 个新文件 + ContextAssembler/NativeInvestigationExecutor/AlertFlowConfig/PersistenceConfig 等集成面；逐文件清单以本批提交为准。 |
| 裁定-1 | P0-2 规范化范围 | ArgsNormalizer 白名单=**字符串值空白折叠仅此一项**（trim + `\s+`→单空格）；大小写、数字格式、null、空容器**不**折叠（语义面差异保留）。javadoc 钉死于 `ArgsNormalizer`，逐字段扩面需评审。 |
| 裁定-2 | MCP 并列 vs 收敛 | 裁定**并列不收敛**：McpToolInvoker 的 args-only digest 保留自有形态（无 DelegationEnvelope 上下文可拼，收敛会改指纹兼容性），但同接 ArgsNormalizer 保证语义等价查询归一；javadoc 注明与 ActionDigest 的并列关系与防漂移约束。 |
| 裁定-3 | 复用扣预算语义 | 同 run 内 actionDigest 命中复用：**仍扣 1 次 TOOL_CALL**（防复用面成为绕开预算/熔断的豁免通道）；`doomLoopGuard.record` **不**调用（复用不是新进展也不是无进展重试，连续无进展计数不动）；复用台账行照落（open→markResultRef(原 evidence)→succeed，正常 call_seq），审计可回放。 |
| 裁定-4 | question 超限语义 | **截断非拒绝**：>512 字符 → 前 480 字符 + ≤25 字符中文标注（含原长），严格 ≤512 上界。理由：question 是委派意图描述而非结构性输入（BATCH_SHAPE 族拒绝码针对形状违规），截断保调查可继续且显式留痕；javadoc 钉死于 `DelegationDecision`。 |
| 裁定-5 | MC23 时序缝隙 | 表征测试钉死（`ExA4bIncidentProjectorTest.mc23_reproductionDuringActiveRunSeamCharacterization`）：旧 run 活跃中 incident 再现 → `castRunIfFree` 返回 null 不铸新 run（uq_rca_run_active_incident 禁双活跃），新 episode 的 run 铸造推迟至旧 run 收尾后的**下一事件+材料变化**。判定：设计内行为、非崩溃缝，但存在「新 episode 短窗无 run」面。修法候选（①再现即取消旧 run 铸新；②旧 run 收尾补偿铸造）**待用户裁定，未实施**——按拆解 §5.1「修法先回报裁定再动」纪律。 |
| 裁定-6 | 资产登记契约 | 压缩指令资产按 ReleaseAsset PROMPT kind 契约登记：`messages_template` 非 blank + `{{var}}` 占位符（source_snapshot_digest/event_seq_from/event_seq_to/required_refs）全部在 `variables_schema` 声明（P02）；run 专属值以占位符进模板、行级 summary_prompt_digest 承载随行原文，资产级 target_ratio 为构造值。登记失败维持 log-warn 不阻断（审计③诚实面），195 实测 WARN=0、行落库。 |

## 八、用例台账（caseId | 状态 | 证据路径，2026-09-12）

| caseId | 说明 | 状态 | 证据路径 |
|---|---|---|---|
| MC21-E1 | 同 messageId 幂等：重复提交恰一次入账，duplicate=true 不重复合并 | PASS | control-app/src/test/java/com/objwww/pr/control/alert/application/agent/DelegationReceiptServiceTest.java |
| MC21-E2 | raw 先量后裁：>64KB 拒 OVERSIZED，量裁前原文 digest/bytes 落行 | PASS | 同上 |
| MC21-E3 | LATE 栅栏：run 终态后迟到回执拒收 | PASS | 同上 |
| MC21-E4 | FAILED 回执必带 missingInformation 缺口 | PASS | 同上 |
| MC21-E5 | 身份面：非 ACCEPTED 审计行 primary_task_id 可空（V96 列放宽） | PASS（195 真 PG） | control-app/src/test/java/com/objwww/pr/control/it/PostgresDelegationReceiptIT.java |
| MC21-E6 | question 截断：>512 → 480+标注，严格上界不破 | PASS | control-app/src/test/java/com/objwww/pr/control/alert/domain/agent/DelegationDecisionQuestionCapTest.java |
| MC21-P | 回执生产面：DAG 任务终态路径（成功/失败/异常）全量发单，messageId 确定性幂等 | PASS | control-app/src/main/java/com/objwww/pr/control/infrastructure/nativeexec/NativeInvestigationExecutor.java（submitDelegationReceipt）+ R3ProtocolConsistencyTest |
| MC22-E1 | 同 claimKey 状态冲突 → 单 UNKNOWN+MULTI_SOURCE_CONFLICT 行、证据/来源并集呈堂 | PASS | control-app/src/test/java/com/objwww/pr/control/infrastructure/nativeexec/PrimaryFinalClaimProjectorConflictTest.java |
| MC22-E2 | 一致组合并单行；单行路径与既有行为字节级不变 | PASS | 同上 |
| MC23-E1 | 再现时序缝隙表征钉：活跃旧 run 不铸新 run，收尾后下一事件+材料变化才铸 | PASS（钉案，裁定-5） | control-app/src/test/java/com/objwww/pr/control/alert/application/ExA4bIncidentProjectorTest.java |
| MC32-E1 | 九态封闭集无「人工等待」态断言钉；活跃/终态补集完备 | PASS | control-app/src/test/java/com/objwww/pr/control/alert/domain/model/RcaRunStateTest.java |
| MC24-E1 | 换措辞同语义 → digest 同 → 复用证据零重打（工具执行计数=1） | PASS | control-app/src/test/java/com/objwww/pr/control/alert/application/agent/Mc24QueryReuseTest.java |
| MC24-E2 | 新冻结窗 → digest 变 → 正常重查（执行=2，新鲜度区分） | PASS | 同上 |
| MC24-E3 | 复用不触 DoomLoopGuard 计数、仍扣 TOOL_CALL | PASS | 同上 |
| MC24-E4 | 规范化白名单边界：空白折叠生效；大小写/数字格式不折叠 | PASS | control-app/src/test/java/com/objwww/pr/control/alert/domain/tool/ArgsNormalizerTest.java |
| MC31-E1 | 材料提交→APPLIED→ContextAssembler `operator_materials` UNTRUSTED 段入信封（JUDGMENT 带身份标注） | PASS | OperatorMaterialServiceTest + ContextAssemblerTest |
| MC31-E2 | payload/kind/source 约束面（超限/非法 kind 拒绝） | PASS | control-app/src/test/java/com/objwww/pr/control/alert/application/OperatorMaterialServiceTest.java |
| MC31-E3 | expectedRevision CAS：同 revision 双提交 REJECTED_STALE（MC32 并发面） | PASS | 同上 |
| MC31-IT | V96 operator_material 表+约束真 PG 读写 | PASS（195 真 PG） | control-app/src/test/java/com/objwww/pr/control/it/PostgresOperatorMaterialIT.java |
| MC31-EP | 端点路由认证面：GET→401 / POST→403（非 404/500） | PASS（195 部署后） | /opt/build/mc-p0-verify.sh §[6/6] |
| EN02-ASSET | 压缩指令 PROMPT 资产：P02 契约直证（ReleaseAsset.of 过闸）+ 内容寻址幂等 | PASS | ContextCompactionServiceTest.directiveTemplateIsStableAssetContent |
| D-19 | V96 真 PG 迁移：flyway schema_history 含 V96，rca_delegation_receipt+operator_material 建表 | PASS（195） | mc-p0-verify.sh §[1/6][2/6] + deploy-postgres-1 flyway_schema_history |
| D-20 | V96 约束清单：ck_mc21_receipt_payload_size（OVERSIZED 豁免 64K）/ck_mc31_* /uq_mc32_material_revision 等 | PASS（195） | mc-p0-verify.sh §[3/6] |
| D-21 | 资产行落库：release_asset PROMPT 行 digest=ff846b82…，模板含 {{source_snapshot_digest}}，variables_schema 四变量 | PASS（195） | /opt/build/asset-check.sh 输出 + deploy-control-app-1 启动日志（登记 WARN=0） |
| D-22 | 195 全量 UT：1636/1636 BUILD SUCCESS（含本批全部新案） | PASS（195） | 2026-09-12 mvn -pl control-app test 远端日志 |
| D-23 | 195 真机部署：V96 应用 + 应用 Up + 启动 ERROR=0 | PASS（195） | deploy-control-app-1 docker logs + mc-p0-verify.sh §[4/6][6/6] |

## 九、审计 9 未实现项续批清账（2026-09-12 第二窗）

| 项 | 处置 | 证据 |
|---|---|---|
| ⑤ 共享历史记忆（P1） | **改判：已交付**。审计判"未实现"不成立——EN-07 `history_rca_search`（22e67ae）即"经审查摘要按服务/时间检索"的拉取面：service allowlist 先行过滤 + ≤30d 窗 + 仅 STRUCTURE_VALIDATED 判例 + 参考区红线 note + 50+1 截断 + NO_DATA 诚实面，已装配（AlertAm4Config/DirectReadToolCatalog）且 E 面全绿。残余缺口如实登记 NOT_IMPLEMENTED：版本检索键、语义相似度召回、免工具调用的推送面（归 RAG 后续，不重复建设结构化面） | infrastructure/rag/HistoryRcaSearchExecutor.java + EN-07 用例（R01~R07/R10/R12） |
| ⑧ R7-X10 部署版本代码工具 | **完成**（81b6827+ac7c28e）：code.search/code.read 双工具——宿主 `service=repo@commit` 绑定授权（模型不可指定仓库/绝对路径，未授权 service 零触盘拒）+ 路径沙箱（禁 ../、反斜杠、越界逃逸）+ 三重上限（2000 文件/512KB 单文件/32MB 累计，触限 truncated 如实）+ SECRET_LIKE 秘密词形脱敏（口径对齐 EventPayloadSanitizer）+ 字面量匹配无正则面 + NO_DATA/SOURCE_UNAVAILABLE 诚实两族 + binding 溯源块（provenance=host-declared）。扫描两段式（收集排序→顺序扫描）= 确定性结果。验证梯：本地 14/14 → 195 全量 **1647/1647**（1 skip=门控探针）→ 195 真树探针 `/opt/build` **5/5 PASS** → 真机部署（override compose 新文件 `docker-compose.x10.yml` 不动 base：`/opt/build/pr:ro` 只读挂载 + env 注入 + 容器内源版本 grep 核验 + 启动 ERROR=0），绑定 `control-app=pr@ac7c28e` | deploy/docker-compose.x10.yml + CodeSearchExecutorTest.realTreeProbe（-Dr7x10.probe.root 门控） |
| ⑦ O01/O10 演练 | 维持**不可编码**：需真模型专项资源窗，非代码缺口 | — |
| ⑨ R7-X7/X8 后期卡 | 维持**后期**：X7 需真模型同预算对照 E2E；X8 需质量证据后接 Reviewer/SkillCurator——条件未满足，不伪实现 | R7 方案 L206-207 |
| ③ EN-08 Skill 自沉淀链 | **完成首期**（a76d70f，2026-09-12 第二窗）：V97 rca_skill_candidate 候选生命周期（DRAFT→VALIDATING→EVALUATING→QUALIFIED→ACTIVE→DEPRECATED/RETIRED，封闭 CHECK）+SkillCandidateService（S01 生成器仅写 DRAFT/S02 未复核隔离不落资产/S03 确定性提炼清洗+验泄漏/S04 manifest 静态校验远端零调用/S05 权限交集+预算上界/S09 资格门 passQualified/S10 激活 digest 重算对账/S14 幂等重放）+SkillMatcher（S06 selector 双维命中/S07 冲突字典序取一+conflictSuppressed 可观测/S08 生产只读 ACTIVE）。验证梯=本地 13/13→195 全量 1660→真 PG IT 2/2→真机部署（flyway 97|t+五 CHECK+ERROR=0）。边界：S11 归 EN-03；S13 B 面留真模型窗；selector 接进调查 Run 装配面为下一卡（本批零 Run 行为漂移） | V97 + release/application/SkillCandidateService.java + domain/service/SkillMatcher.java |
| ④ 真 grader 评分引擎 | **切片拆分**：规则面评分引擎**已在位且绿**——ScenarioEvaluator 纯函数（三维 root_cause_hit 同义词白名单+reason_code→fault_type 从属校验/症状 TP-FP-FN 混淆矩阵/silence_penalty/UNRESOLVED 谨慎拒答哨兵）+ SingleCaseScorer verdict 分派（STRUCTURE_REJECTED/TIMEOUT_OR_ABSENT/DECIDABLE/UNRESOLVED），审计若判"评分器是假的"不成立。EN-09 E01~12 的真实增量按可码性拆：模型评分复合面=**需真模型资源**（不可编码，与 O01/O10 同窗）；消融三臂（MC34）=**可编码面已齐**（compaction enabled/soft_threshold 旋钮表达三臂+EvalCompareService CaseDelta 差值报告承载对照读数），对照执行需真模型专项窗 NOT_RUN；新查询覆盖分类=**回放完整性分类面已交付**（InputReplayVerifier FULL/REDACTED/DIGEST_ONLY+complete() 诚实语义，R2 批验证），E01~E12 隔离 runner B 面需真模型窗 NOT_RUN | eval/application/ScenarioEvaluator.java + alert/application/replay/InputReplayVerifier.java + eval/application/EvalCompareService.java |

**X10 第一期偏差（如实登记）**：绑定=宿主静态声明（compose env），部署树↔commit 的一致性由同步流程保证、未经运行时核验——镜像 digest→commit 的绑定核验属后续卡；结果 note 已向模型如实标注该 provenance 面。**R7-A1 门**（真实日志+代码闭环 E2E 验收）依赖真模型窗，NOT_RUN 如实登记，不以 X10 落码冒充通过。
