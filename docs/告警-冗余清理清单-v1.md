# 告警 冗余与技术债清理清单（ponytail-audit）v1.1

> 文档信息：2026-09-05；产出方式 = ponytail-audit skill（整仓过度设计/死代码扫描，只列不改）。
> 依据方案：`docs/告警AM1-技术方案.md` v2.3（§2.1 T00 死代码清除清单 + §14 修订记录）、`docs/告警G0-收口技术方案.md` v1.0（G0-01~12 + BA-09~13）。
> **v1.1 增补**：按用户要求，将全部"条件删除/保留件"条目逐一对照前瞻三文档复核——`架构设计-告警Agent-v1.2.md`（FUT-01~55）、`告警Agent-增量实现任务拆解-v1.md`（G0~AM6 全 96 任务）、`告警AM4-技术方案.md` v1.1 / `告警AM4-落码技术方案.md`（GX-1~5 + M4-01~37）、`告警AM3-落码技术方案.md`（M3-08/09）。核对记录见 §9，两处改判见 §3。
> **纪律：本文档只记录，未修改/未删除任何代码、配置、文件。所有清理动作等用户逐批指令后执行。**
> 证据口径：引用计数 = 全仓 `rg -w 类名/方法名`（生产 src/main、测试 src/test、yml、pom、docs），核实日期 2026-09-05，基线 = 当前工作区（含未提交 AM3 eval WIP）。

---

## 0. 审计总结论（TL;DR）

1. **AM1-T00 死代码清除已基本干净**：sandbox-broker/publisher-app 两模块已整删、根 pom 只剩 4 模块、control-app PR/GitHub/review/sandbox 域无残留（剩余 "publisher/github/sandbox" 字样均为合法引用：历史迁移授权、注释、架构测试黑名单）。
2. **G0 收口项全部已落码**（BA-09~13 全关）：配置三名统一、Holmes key 桥接、rca_report 自检、四台状态机接线（Inbox/Run/Task/Incident）、episode 乱序修复、Holmes 响应限读/UNKNOWN 账本/Hikari 显式配置、BA-13 四条优化——本次审计逐一核实无回退。
3. **经前瞻三文档交叉核对后（v1.1）**：真正"确定可删"的范围收窄为——A 组 10 项死件（约 230 行）+ R-C05 snapshot 三件套（无任何前瞻承接，约 435 行）。v1.0 初判的四处债修正为：① AM4 预制四包（dag/claim/budget/tool）**改判确认保留**（AM4 在案任务 M4-05/06/08/15/21/22 直接承接，GX-1~5 修正已落码）；② `ReportPublicationStateMachine` **改判保留**（消费者 = 尚未落码的 M3-20 Outbox Claimer）；③ ModelGateway/ExecutionLedger 保留件出现**方案冲突**（AM1 v2.3 明文保留 vs 前瞻方案零承接：模型路由走 LiteLLM、事件走全新 rca_event），需用户在 AM4 动工前裁定；④ 构建/部署面死依赖、死配置、过期 README 维持原判。
4. **净可删估计（v1.1 修订）**：确定项 ≈ 230 行（main ≈ 130 + test ≈ 65）+ snapshot 三件套 435 行（待拍板）+ pom/yml/gitignore 17 行；过渡件（ModelGateway 体系 ~3,005 main + ~1,180 test、ExecutionLedger 链）去留待 AM4 裁定，前瞻方案倾向 AM4 落地 rca_event/LiteLLM 时退役。

### 0.1 待用户拍板清单（本清单全部待决项，裁定后回填）

| # | 待决项 | 选项 | 对应条目 | 裁定结果 |
|---|---|---|---|---|
| 1 | 批 1 死件清理是否执行 | 执行 / 暂缓 | §1 A 组 10 项（~230 行 + pom/yml/gitignore 17 行） | ______ |
| 2 | snapshot 三件套删除 | 删（推荐：前瞻三文档零承接，git 历史可捞回）/ 保留 | R-C05（435 行，连带 shared-kernel commons-compress） | ______ |
| 3 | ModelGateway 体系去留（AM4 动工前须定） | a. AM4 落地 ToolGateway/LiteLLM 后整链退役（推荐）/ b. 立即删（需先核实 execution_event/model_call_ledger 无其他写入方）/ c. 保留为 Java 侧账本参考实现 | R-C07（~3,005 main + ~1,180 test；连带 R-B05/R-B10） | ______ |
| 4 | B 组修改瘦身执行范围 | 全做 / 只做 R-B02~B04、B06~B09（R-B01 可选、R-B10 随 #3） | §2 | ______ |
| 5 | E 组 WIP 内建议 | AM3 提交时顺手处理（交 AM3 执行者） | §5 | ______ |
| 6 | F/G 组文档归档与本地磁盘清理 | 执行 / 用户自理 | §6、§7 | ______ |

> 不需要拍板的项：§3.1 四包（确认保留）、R-C08（保留等 M3-20）、§4 D 组全部（明确不动）。

---

## 1. A 组：确认死件，可直接删除（等用户指令）

> 全部条目已核实生产零调用，且前瞻文档无承接任务。删除后需 `mvn -q test` 全绿回归。

| 编号 | 对象 | 类型 | 位置 | 证据（引用计数） | 预计削减 |
|---|---|---|---|---|---|
| R-A01 | `ModelRouteCatalog` 接口 + `ModelGateway` 的 implements 子句与 `findContractIdentityByModel` 实现 | 接口+实现 | `domain/ai/ModelRouteCatalog.java:19`、`application/ModelGateway.java:110-121` | 唯一方法全库仅声明+实现两处命中，0 调用方；javadoc 声称的消费者（ReviewStepExecutor/CheckpointResumeService）已随 AM1-T00 删除。属 AM4 保留件内部的死分支，删它不伤保留件主体。**联动**：若 R-C07 裁定整链删除，本项自动包含 | ~30 行 |
| R-A02 | `ExecutionEventRepository.findByRunIdOrdered`（接口声明 + PG 实现 + 测试 fake 实现） | 方法 | `domain/service/ExecutionEventRepository.java:17`、`infrastructure/persistence/PostgresExecutionEventRepository.java:72-107`、测试 `support/ExecutionEventsFake.java:23` | 0 生产调用（"供 Projector fold"注释所指消费者不存在）。**联动**：同 R-A01 | ~45 行 |
| R-A03 | `MockModelGateway` 整类 | 测试类 | `control-app/src/test/java/com/objwww/pr/control/domain/ai/MockModelGateway.java` | 全库 0 消费者——连 `ModelGatewayTest` 都不用它，仅自身文件 5 处命中 | 65 行（test） |
| R-A04 | `PersistenceConfig.evalRunRepository` bean（docker profile） | Spring bean | `infrastructure/config/PersistenceConfig.java:142-146` | 生产 0 消费者：eval 线用 eval profile 的 `EvalRunnerConfig` 自建 bean（`@Profile("eval")` 装配根，非死代码）；`AlertV10EvalRunIT.java:36` 直接 `new PostgresEvalRunRepository` | 5 行 |
| R-A05 | 根 pom dependencyManagement 的 `control-app` 条目 | Maven 依赖管理 | `pom.xml:41-46` | 全仓库无任何 pom 声明依赖 control-app（原消费方 publisher-app 已删） | 6 行 |
| R-A06 | control-app 的 `commons-compress:1.27.1` 直接依赖 | Maven 依赖 | `control-app/pom.xml:57-62` | control-app src 零 import（`org.apache.commons.compress` 无命中）；shared-kernel 已声明同一依赖并传递（与 R-C05 联动：若将来删 snapshot 三件套，shared-kernel 的该依赖一并失效） | 6 行 |
| R-A07 | `app.arena.probe.max-staleness-ms`、`app.arena.chaos.recovery-grace-ms` | 死配置键 | `order-arena/src/main/resources/application.yml:24、:31` | 代码零读取（真实现分别是 `app.arena.probe.stuck-threshold-seconds`、f2-batch 无宽限参数） | 4 行 |
| R-A08 | `.gitignore` 的 `deploy/probe-sync-state/` | gitignore 行 | `.gitignore:14` | probe-sync-daemon 与整个 M2 探针同步机制已随 publisher 链路删除，仓库无此路径 | 1 行 |
| R-A09 | 仓库根空目录 `-p` | 杂物目录 | `E:\kimiCode\-p` | 空目录（BA-18 记录的 cmd 劫持产物），git 不追踪不忽略 | 目录 |
| R-A10 | `Digest.hex()` | 方法 | `shared-kernel/src/main/java/com/objwww/pr/shared/Digest.java:23-26` | 与 `value()` 完全等价；0 生产调用，仅 `DigestTest.java:17,115` | 4 行 + 测试 2 处改 `value()` |

> v1.0 的 R-A01（ReportPublicationStateMachine）/R-A02（withState）经前瞻核对**改判保留**，移入 §3 R-C08，理由见该条。

小计：main ≈ 130 行，test ≈ 65 行，另 pom/yml/gitignore 17 行。

---

## 2. B 组：保留类、修改/瘦身项（等用户指令）

| 编号 | 对象 | 修改内容 | 位置 | 证据与代价 |
|---|---|---|---|---|
| R-B01 | 四台活状态机的 `public static allowed()` | 生产 0 调用，可降包内私有或删除；**但** `AlertStateMachineTest` 用 `IncidentStateMachine::allowed` 等方法引用做迁移表穷举（`AlertStateMachineTest.java:59/92/120/143`），删除需同步改写断言方式（如改走 requireTransition+catch） | `InboxStateMachine.java:27`、`IncidentStateMachine.java:23`、`RcaRunStateMachine.java:27`、`RcaTaskStateMachine.java:28` | 收益 12 行，代价改 4 处测试——可选低优先 |
| R-B02 | 孤儿端口方法 `findByReportId` / `findByPublicationId` | 从端口接口 + PG 实现 + 内存 fake + 测试断言中成组删除（生产 0 调用，仅测试在用）。**注**：两者是 M3-20 Outbox Claimer 未来可能要用的查询面，若 M3-20 落码马上要用则保留 | `ReportPublicationRepository.java:15` + `PostgresReportPublicationRepository.java:51-57`；`NotifyOutboxRepository.java:15` + `PostgresNotifyOutboxRepository.java:58`；测试 `AlertM3RepositoryIT.java:245-269`、`ReportCompletedNotifierTest.java:49-93`、`AlertInMemoryStores.java:689,747` | ~16 行 main + 测试断言修剪 |
| R-B03 | `AlertIdentityFactory()` 无参构造 | 删除；生产用双参构造（`AlertFlowConfig.java:71-78`），仅 4 个测试文件用无参版，测试改双参 | `alert/domain/service/AlertIdentityFactory.java:44-46` | 3 行 + 4 处测试 |
| R-B04 | `SlaPolicy.defaults()` | 删除；生产用配置化构造（`AlertFlowConfig.java:87-91`），仅 3 个测试文件在用 | `alert/domain/service/SlaPolicy.java:31` | 5 行 + 5 处测试 |
| R-B05 | `CircuitBreaker.currentState()` / `consecutiveFailures()` | 删除或降包内可见；生产 0 引用，仅 `CircuitBreakerTest` 18 处。**注**：属 R-C07 保留件内部，若整链裁定删除则随之消失，可先不动 | `domain/ai/CircuitBreaker.java:113-123` | 7 行 + 测试改写 |
| R-B06 | `M3ModelGatewayConfig.deriveScope` 手搓 SHA-256 hex | 换 shared-kernel 现成 `Digests.sha256Hex(...)`（stdlib 标记） | `infrastructure/config/M3ModelGatewayConfig.java:244-252` | 净 -6 行 |
| R-B07 | `spring-boot-maven-plugin` 注释过期 | 注释仍称"主 artifact 保持瘦 jar 供 publisher IT 反应堆解析"——publisher-app 已删；classifier=exec 本身保留（三个 Dockerfile COPY `*-exec.jar`），只改注释为真实理由 | `control-app/pom.xml:107-112` | 注释 6 行 |
| R-B08 | 根 `README.md` 重写 | 仍自称 "AI Code Review Agent" 并列已删的 publisher-app 模块 | `README.md` | ~净 -40 行 |
| R-B09 | `deploy/README.md` 重写 | ~150/195 行引用已不存在资产（publisher-app、github-stub、smoke-test.sh、m2-lib.sh、e2e-m2.sh、bt-m2.sh、e2e-35-real.sh、probe-sync-daemon.sh、db/dp19-seed-v3.sql、docs/M2-技术方案.md） | `deploy/README.md` | ~净 -150 行 |
| R-B10 | `spring-ai-starter-model-openai` 降级为裸 `spring-ai-openai` 构件 | 代码全手工装配 OpenAiApi/OpenAiChatModel，starter 的自动配置被四处显式压制（application.yml `spring.ai.model.chat: none` 等 + 启动断言）；换裸构件后压制配置与断言一并删除。**需 mvn 验证一次**；因属 R-C07 保留件链路且前瞻方案可能整链退役，**建议随 R-C07 裁定一起做，不单独动** | `control-app/pom.xml:52-55`、`application.yml` spring.ai 段 | 依赖瘦身 + 删 ~4 处压制配置 |

---

## 3. C 组：前瞻核对后的分档结论（v1.1 重写）

> 对照 `架构设计 v1.2`（FUT-01~55）、`任务拆解 v1`（M4-01~37/M5/M6）、`AM4-技术方案 v1.1`/`AM4-落码技术方案`（GX-1~5）逐条核对后的三档结论。核对证据行见 §9。

### 3.1 确认保留——AM4 在案任务直接承接，**不可删**

| 编号 | 包/类 | AM4 承接任务（在案证据） | 备注 |
|---|---|---|---|
| R-C01 | `alert/domain/dag/` 6 类（DagPromoter/DagCycleDetector/DagTaskState/DagPromotion/TaskEdge/DependencyType，288 行 main + 325 行 test） | 任务拆解 M4-05（DAG 环检测）/M4-06（READY/BLOCKED 推进器）；AM4-技术方案 §domain 明列 TaskEdge/DagCycleDetector/DagPromoter，时序图含"DagPromoter 推进后继"；V8 已含 rca_task_edge"实锤，M4-04 不得重建" | GX-5（终局收敛 SKIPPED/冲突边拒绝）已由 commit 32ec891 落码，当前代码即 GX 修正后形态 |
| R-C02 | `alert/domain/claim/`：ClaimReducer/Claim/ClaimVerdict（250 main + 311 test） | 任务拆解 M4-21（Claim 契约与仓储）/M4-22（Claim Reducer）；AM4-技术方案 §domain 明列 | GX-1（分组键/平局 NEEDS_REVIEW）已落码。**同包 `ClaimStatus` 是活代码**（`ReportClaim.java:3` AM3 报告契约在用）——任何时候删包须先迁走它 |
| R-C03 | `alert/domain/budget/`：RunBudget/BudgetExhaustedException（112 main + 140 test） | 任务拆解 M4-08（RunBudget 账本，V12）；AM4-落码方案 GX-3 | 现有纯函数件是 M4-08 的基底（将扩成 DB 原子预留账本），GX-3 修正已落码 |
| R-C04 | `alert/domain/tool/`：InternalCanonicalJsonV1/ActionDigest/ActionEnvelope（198 main + 240 test） | 任务拆解 M4-15（canonical args 与 action digest）；AM4-落码方案 GX-4 | GX-4 正是"更名 InternalCanonicalJsonV1 + envelope 化"，已落码。遗留矛盾：`ControlArchitectureTest.java:75,89` 称"零 Jackson 域"但 `EvidencePackageV2`/`EvidencePackageValidator` 已 import Jackson——M4-15 接线时此前提需重估 |

### 3.2 无承接孤儿——前瞻三文档零提及，**建议删除**（等用户拍板）

| 编号 | 对象 | 核对结论 | 行数 |
|---|---|---|---|
| R-C05 | shared-kernel `snapshot/` 三件套：SafeTarExtractor/SnapshotTree/SecurityRejectionException + 测试 | v1.2 FUT-01~55 全文零提及 tar/解包/SafeTar/SnapshotTree；AM4-技术方案/落码方案零提及（V15 的 Evidence/Snapshot 是证据快照契约 = M4-20 EvidenceSnapshot Builder，冻结排序+digest，与 tar 解包无关）；任务拆解 96 任务零提及。AM1 v2.3 的保留注记是笼统"通用件保留"，无落码任务承接 → 属无承接孤儿。真需要时从 git 历史捞 | 357 main + 78 test；删后连带 shared-kernel pom 的 commons-compress（R-A06 联动） |

### 3.3 过渡件——前瞻方案已安排替代，替代落地时"退役"而非"接线"

| 编号 | 对象 | 核对结论 | 处置建议 |
|---|---|---|---|
| R-C06 | `ExecutionEventType`（26 常量中 22 个僵尸常量，emit 点已随 T00 消失）+ ExecutionLedger/ExecutionEventRepository/PostgresExecutionEventRepository 链 | AM4-技术方案 M4-10/V13 建**全新统一 rca_event**（append-only + last_event_seq 分段原子分配 + EventAppender），且明写"旧 rca_agent_event 只读兼容视图、禁双写"——旧事件表（实际表名 `execution_event`，V1:289 建，PR 线产物）AM4 后不再接新写入。前瞻方案不复用本链 | 维持现状（AM1 v2.3 保留决定仍有效）；AM4 M4-10 落地时随 rca_event 切换整链退役。现在只删其中纯死分支（R-A02），僵尸常量届时随链走 |
| R-C07 | ModelGateway 体系：domain/ai 全包 + ModelGateway + M3ModelGatewayConfig + infra/model + ModelCallLedgerRecovery + ModelCallLedger 仓储（~3,005 main + ~1,180 test） | **方案冲突，需用户裁定**：AM1 v2.3 明文保留（理由"AM4 Java 替换 HolmesGPT 时直接用"）；但前瞻三文档对 ModelGateway/ModelRouter/SpringAiRouteClient **零提及**——模型路由改走 LiteLLM proxy（任务拆解 M3-24"proxy、私网、密钥和模型路由"），Native 路径是全新 ToolGateway/AgentProfile/Planner/Supervisor（M4-13~32），AM6 M6-06/07 Holmes 退场。即 AM1 保留理由所对应的"AM4 直接复用"已不成立 | 三个选项（AM4 动工前裁定）：a. **推荐**：AM4 落地 ToolGateway/LiteLLM 后整链删除（届时 R-A01/R-B05/R-B10 一并消失）；b. 立即删（需先核实 execution_event/model_call_ledger 无其他写入方、启动自检不依赖）；c. 保留为 Java 侧账本参考实现。**保留期间内部债清单**（接线/退役时修，现在不动）：ModelRouter.primaryRoute()/fallbackRoute() 零调用 getter（:175-181）；ModelGatewayPort 零消费者接口（被 `ControlArchitectureTest.java:240` 钉住签名）；`M3ModelGatewayConfig.java:100-102` 读 PR 时代键 `app.review.model-provider/model-version`（有默认值，语义漂移）；`SpringAiRouteClient.java:81` 每次 complete() 新建虚拟线程池 |
| R-C08 | `ReportPublicationStateMachine` + `ReportPublication.withState`（**自 v1.0 A 组改判移入**） | 消费者 = **尚未落码的 M3-20 Outbox Claimer**（任务拆解 :210 在案，SKIP LOCKED/租约/退避，claim 时需要状态机校验迁移）；M3-09 冻结该状态机是给 M3-19/20 的契约前置。另：AM3-落码方案 M3-08 已定"publication(READY) 同事务出生"（`ReportCompletedNotifier.java:68` 实锤），PENDING 目前无生产写入方——M3-20 落码时复核 PENDING 是否有用例，无用例则届时删该态 | **保留等 M3-20 接线**；若 AM3 通知支线最终改设计（如纯 SQL CASE 迁移），届时连测试一起删。删 `AlertStateMachineTest` publication 段的方案作废 |

### 3.4 明确保留勿删（登记防误删）

- domain/ai 中被 R-C07 覆盖的全部类当前均在装配链上（PersistenceConfig/M3ModelGatewayConfig，docker profile），删除需整体裁定而非逐类摘除。
- `AlertClock`（java.time.Clock 手搓替身）：生产 10+ 处在用，替换波及大——观察项，随 AM4 顺手处理。

---

## 4. D 组：明确不动清单（防止误删）

| 对象 | 不动理由 |
|---|---|
| Flyway 迁移 V1~V10 及 195 上全部表 | 项目纪律：已部署迁移历史不可变。⚠️ 附带记录一条漂移：`V7__am1_alert_domain.sql:337` 注释引用 `app.alert.worker.slots` 配置，代码已无此键（槽位并发由 scheduler_slot 表行数决定）——因迁移文件不可改（Flyway checksum），此漂移仅记录在案不改 |
| `deploy/db/01-roles.sh` 的 `publisher_app` 角色 | V2~V6 历史迁移的授权对象（V7 起显式 REVOKE），角色必须存在否则迁移失败（compose 注释已自述） |
| `deploy/policy/*.rego` 8 文件 | 非死资产：文档化手工部署门禁（`docker compose config | conftest test`，deploy/policy/README.md:14），25/25 单测 |
| `deploy/alert/otelcol/*`、`prometheus/rules/empty-groups.yml` | 195 外部栈宿主侧配置的 SHA-256 对拍存档（deploy/alert/README.md），删了破坏镜像可复现性 |
| `deploy/alert/eval/*` | AM3 在途资产：`EvalRunnerConfig` 默认读 `classpath:eval/eval-scenarios.yml`，目前 compose 未挂载、main resources 无此目录——**提醒 AM3：接线（挂载或入 resources）前它是事实死配置** |
| `eval/application/` 未提交 WIP 主体 | git untracked 在途代码，不判债（组内具体建议见 §5） |
| 13 个告警域仓储端口（单实现） | port/adapter 是架构冻结决策（ArchUnit 守），单实现不构成债；生产全部有装配与调用方，无孤儿端口 |
| `execution_event` 表（195 已建，V1:289，不可变 trigger V1:537） | 已部署不可删；Java 写入链的去留随 R-C06 在 AM4 M4-10 裁定 |
| `Digest` vs `Digests` | 不重复：record 值对象 vs 静态工具，互补（仅 hex() 冗余，见 R-A10） |
| 架构设计 v1 与 v1.2 | 都在引用链上（v1.2 被 AM2/评审引用，v1 被 AM0~AM3 与 v1.2 引用），非互相掩盖的重复 |

---

## 5. E 组：未提交 WIP 内建议（AM3 提交前顺手处理，不阻塞）

> 对象 = 当前 git untracked/modified 的 AM3 eval 代码。下列仅记录，AM3 收尾时自行取舍。

| 编号 | 建议 | 位置 | 说明 |
|---|---|---|---|
| R-E01 | `InfrastructureScenarioDriver`（24 行纯抛 IllegalStateException 占位）+ `EvalRunnerConfig.java:165-168` 装配 + EvalBatchRunner drivers map 注册项 | `eval/application/InfrastructureScenarioDriver.java` | 首批 5 场景不用（自注释），要用时再写，~35 行 |
| R-E02 | `AlertProbe.awaitSessionClosed` 恒返回 true，导致 `ArenaChaosScenarioDriver.java:63-67` 的 `session_not_closed_in_cleanup_window` 分支不可达 | `PrometheusAlertProbe.java:50-53`、`AlertProbe.java:18` | 删方法+死分支，等真有会话收口探针再加，~15 行 |
| R-E03 | `EvalBatchRunner.quote()` 手搓 JSON 转义，且把 `"` 替换成 `'`（改变原文，违背"原值"意图） | `EvalBatchRunner.java:236-239` | 用项目已在用的 Jackson `writeValueAsString` |
| R-E04 | 三个等价的自建睡眠抽象（PrometheusAlertProbe.Sleeper / PostgresRcaRunResolver.Sleeper / EvalBatchRunner.EvalClock） | 各文件 :35/:37/:52 附近 | 归一为一个共享 `@FunctionalInterface Sleeper`，净 -12 行 |
| R-E05 | 根目录 `eval-test-out.txt` 测试输出杂物 | `E:\kimiCode\eval-test-out.txt` | untracked，删 |

---

## 6. F 组：文档归档候选（零入链，去留用户裁定）

| 对象 | 位置 | 引用情况 |
|---|---|---|
| 接入层可视化产物 8 文件（archify.json/html/4 png/visual-check.html+json） | `docs/接入层当前流程.*` | 全仓库零入链，浏览器生成的一次性产物 |
| rca 可视化产物 32 个 tracked 文件 | `docs/diagrams/` | 目录外零入链 |
| `架构评审-WebSocket与对账方案裁定-v1.md`、`架构评审-评测安全反馈核查-v1.md` | `docs/` | 零入链；`架构评审-外部Harness图未采纳项-v1.md` 仅 1 处引用 |

---

## 7. G 组：本地未跟踪杂物（git 外，仅磁盘清理，用户自清）

- `var/`：m4-review~m4-review-6 六份旧快照（内含已删模块 publisher-app/sandbox-broker 拷贝）、G0 期脚本与日志（g0-*.sh/log、t07-*.log、pr-sync-*.tar.gz 等）、mall_R-dp13 三份全量克隆。**注意：var/ 内含 BA-16 固化的 195 密钥恢复脚本（含密钥字面量，有意不入 git）——清理时必须保留。**
- `backups/`、`.scheduler-research/`（六个上游仓库整克隆：alertmanager/conductor/dbos-transact-java/holmesgpt/keep/robusta）：GB 级，已 gitignore。
- `scp/`：空目录（untracked）。

---

## 8. 执行建议（等用户指令后按批执行）

| 批次 | 内容 | 验收 |
|---|---|---|
| 批 1 | A 组全部（Java 死分支 + 死 bean + pom 依赖 + 死配置 + gitignore + `-p` 目录） | `mvn -q test` 全绿；`git grep` 复核零残留 |
| 批 2 | R-C05 snapshot 三件套 + shared-kernel commons-compress（用户拍板后） | 全 reactor 绿 |
| 批 3 | B 组（R-B01 可选；R-B05/R-B10 建议随 R-C07 裁定一起做） | 同上 + README 人工过目 |
| 批 4 | E 组（AM3 提交前顺手） | AM3 提交自带测试绿 |
| 批 5 | R-C07 裁定（AM4 动工前）：整链退役 or 保留；R-C06 随 M4-10 rca_event 落地执行 | 全 reactor 绿 + ArchUnit 同步 |
| 批 6 | F/G 组（文档归档、本地磁盘） | 无构建影响 |

预估净削减：批 1~3 ≈ 800 行（含 snapshot 435）+ 2 依赖 + 3 配置键 + 2 份 README 重写；R-C07 若裁定退役再 ≈ 4,200 行（main+test）。

---

## 9. 前瞻文档交叉核对记录（v1.1 新增）

| 核对项 | 结论 | 证据 |
|---|---|---|
| dag 六件套 | AM4 在案任务承接 | 任务拆解 :236-238（M4-05/06）；AM4-技术方案 :61/:66/:67/:110/:202；AM4-落码方案 :17（GX-5）；commit 32ec891 即 GX-1~5 修正落码 |
| claim 包 | AM4 在案任务承接 | 任务拆解 :255-256（M4-21/22）；AM4-技术方案 :65/:69；AM4-落码方案 :13（GX-1） |
| budget 包 | AM4 在案任务承接 | 任务拆解 :237（M4-08，V12）；AM4-落码方案 :15（GX-3）、:30（M4-08 V12 RunBudget 账本） |
| tool 包 | AM4 在案任务承接 | 任务拆解 :249（M4-15）；AM4-落码方案 :16（GX-4） |
| snapshot 三件套 | 前瞻三文档零承接 | rg 全 docs（除 archive）"SafeTar/SnapshotTree/解包" 零命中（AM4 方案/任务拆解/v1.2 均无）；AM4 V15 = Evidence/Snapshot 证据契约（M4-20 Builder），与 tar 解包无关 |
| ExecutionLedger 链 / ExecutionEventType | AM4 M4-10 用全新 rca_event 替代 | AM4-技术方案 :87/:150/:203（"旧 rca_agent_event 只读兼容视图、禁双写"）；任务拆解 :239（M4-10）、:297（M5-13 回放）；旧表实为 `execution_event`（V1:289） |
| ModelGateway 体系 | 前瞻零提及，方案冲突 | v1.2 全文 rg "ModelGateway/ModelRouter/SpringAiRoute" 零命中；任务拆解 M3-24（LiteLLM proxy 模型路由）、M4-13~17（ToolGateway 体系）、M6-06/07（Holmes 退场）；AM4-技术方案零提及 ModelGateway |
| ReportPublicationStateMachine | M3-20 接线件，非死件 | 任务拆解 :210（M3-20 Outbox Claimer 未落码）；AM3-落码方案 :21-22（M3-08 born-READY 同事务、M3-09 状态机冻结）；仓库无 Claimer 类（rg 零命中）；控制面出生即 READY（ReportCompletedNotifier.java:68） |
| AM1 v2.3 T00 残留检查 | 干净 | sandbox-broker/publisher-app 已整删（根 pom 仅 4 模块）；残余字样均为合法引用（迁移授权/注释/架构测试黑名单）；var/m4-review* 为 gitignored 本地快照 |

---

## 修订记录

| 日期 | 版本 | 变更 |
|---|---|---|
| 2026-09-05 | v1.0 | 初版：ponytail-audit 整仓扫描 + AM1 v2.3/G0 v1.0 对照，A~G 七组分档 |
| 2026-09-05 | v1.1 | 按用户要求对照前瞻三文档（架构 v1.2/任务拆解/AM4 方案 v1.1/AM3 落码方案）逐条复核：dag/claim/budget/tool 四包改判**确认保留**（M4 在案任务承接）；ReportPublicationStateMachine/withState 自 A 组改判**保留**（M3-20 接线件）；snapshot 三件套升级**建议删**（前瞻零承接）；ModelGateway 体系识别为**方案冲突**（AM1 v2.3 保留 vs 前瞻零承接），列三选项待用户裁定；新增 §9 核对记录与修订表；A 组重编号（原 R-A01/A02 移入 §3 R-C08） |
