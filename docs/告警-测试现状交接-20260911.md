# 告警平台测试现状交接（2026-09-11 午后）

> 写给接手的测试执行者。本文是当前唯一权威状态面；历史方案文档里的 NOT_RUN 统计以本文为准。
> 上一轮的教训：某预研只看了本地 `docs/测试证据/R7/`（那里是**脚本源**）就判"0 执行"——真窗证据在 195 `/opt/r7-e2e/runs/`。**每次跑完必须把证据回拉本地对应目录。**

## 一、总账：各线状态一览

| # | 测试面 | 状态 | 下一步动作 |
|---|---|---|---|
| 1 | R7 A0 门（供应商回执链，195 真窗） | **已实跑 7 次，截 6 雷全修（BA-108~113）；第 8 跑待发** | 部署 BA-113 → 第八跑（见 §二） |
| 2 | R7 RX13/RD08 真 PG 原子性 | 脚本就绪，未跑 | A0 PASS 后：`R7_ALLOW_DESTRUCTIVE=1 sh /opt/r7-e2e/e2e-r7-pg-atomicity-rx13-rd08.sh` |
| 3 | R7 RX02/RD09 worker 重启 | 脚本就绪，未跑 | `sh /opt/r7-e2e/e2e-r7-worker-restart-rx02-rd09.sh` |
| 4 | R7 B 门 armC 动态委派 | 未跑 | 原子性+重启过后：`sh /opt/r7-e2e/e2e-r7-b-gate-three-arm.sh armC` |
| 5 | R7 B 门 armB 固定三角色 | 未跑 | 切姿态 OFF（.env PRIMARY_ENABLED=false + recreate，留重启证据）→ `R7_BGATE_SESSION=<armC session> sh ... armB` |
| 6 | R7 B 门 joint 汇总+盲评封存 | 未跑（依赖 4+5） | `sh ... joint`；blind-cases.json 交独立评分者 |
| 7 | R7 B 门臂A 零委派 | **BLOCKED_EXTERNAL 已解除（代码面）**：旋钮化完成于分支 `r7/armA-knob`@5526ecd（E:\kimiCode-bgate 工作区，1342 例绿，未合 main） | 评审合入 main → 部署加 `app.alert.r7.primary.max-delegation-batches=0` → 补测臂A |
| 8 | EV/UX/DR 六卡 Postgres IT 真值 | **已实跑 42 例：PASS 28 / FAIL 12 / ERROR 2**；主会话定责：12 红=断言形态错（产品/迁移无罪，授权矩阵变相验证生效），4 红另查（见 §三） | 测试断言改整链文本形态 → 195 复跑 7 个 IT 类 |
| 9 | EV-08 数据集与评审（V87） | 编码完成：ev/eval-center@**d92634e**（24 文件，1301 例绿）；IT 4 例 NOT_RUN | 评审 → 合 main → 195 跑 PostgresEvalReviewIT |
| 10 | EN 增强线 | **未收口**：worktree E:\kimiCode-en 存在未解决合并冲突 `UU NativeInvestigationExecutor.java` | EN 执行者收口 → 评审 → 合 main → 部署 → EN 真值 |

## 二、A0 门：第七跑后精确现场（最优先接续项）

**代码态**：main 已到 `59cad8f`。BA-113 修复（`7fd5705`）= INVALID_ARGS 的 reason 串归族 `TOOL_RETRYABLE:INVALID_ARGS`（裸串撞 executor 可重试封闭集被判 DEAD）。本地 control-app 全量测试绿（1338 例基线）。

**195 现场**：
- `/tmp/r7fix5.tar.gz` 已传（= 7fd5705 的 control-app/src + exec.jar）；
- A0 脚本已同步最新版（BA-111 套件唯一 summary 修复在内），`sh -n` 已过。

**接续命令**（直接可抄）：
```bash
ssh -i ~/.ssh/id_ed25519 root@146.56.195.225
cd /opt/build/pr && tar xzf /tmp/r7fix5.tar.gz
cd deploy && docker compose build control-app && docker compose up -d control-app
sleep 16 && curl -s -o /dev/null -w '%{http_code}\n' http://127.0.0.1:8080/actuator/health   # 期望 200
nohup /opt/r7-e2e/run-a0.sh > /opt/r7-e2e/runs/a0-console-8.log 2>&1 &
# 5~10 分钟后：
tail -16 /opt/r7-e2e/runs/a0-console-8.log
```

**结果分支**：
- 全 PASS → BUGLOG BA-108~113 改"已关闭"，R7 执行日志 §14 补记，证据回拉 `docs/测试证据/R7/runs/`，进入窗序第 2 步（原子性）。
- phase4 仍 INVALID_ARGS 循环到步数耗尽→确定性兜底 FINAL：这是**已登记的合规出口**（盲重驱无错误反馈是已知限制），如实记录，**禁改口径放行**；是否立项"信封加 last_error 反馈环"由主会话裁定。
- 新缺陷 → 开 BUGLOG BA-114 起（表格格式照既有行），修复→本地全量绿→提交→部署→重跑。

**已修复雷表**（全部"修复待真窗复核"，A0 PASS 后关闭）：

| 雷 | 内容 | 修复提交 |
|---|---|---|
| BA-108 | openRun 缺 TOKEN 维 → 真 PG fail-closed 预算耗尽 | 86788f0 |
| BA-109 | RCA→平台账本 23503 FK；裁定 RCA 侧独立账本山（NoOp 旁路） | 002c664 |
| BA-110 | 缺省 prompt 零输出协议 → glm-5 自然语言作答耗尽步数；修=协议后缀 runner 拥有 | 70badb9 |
| BA-111 | A0 脚本场景身份恒同被产品正确去重；修=注入套件唯一 summary | 59cad8f |
| BA-112 | 工具 schema 未下发 + INVALID_ARGS 即 DEAD；修=schema 钉版随信封下发+计步重驱 | 128cda7 |
| BA-113 | BA-112 的 reason 串撞 executor 可重试封闭集仍 DEAD；修=归族 TOOL_RETRYABLE:* | 7fd5705 |

glm-5 能力曲线（真窗实测）：自然语言乱答 → 按协议输出 tool_call → 卡在参数形状修正。每轮实质推进。

## 三、EV/UX/DR IT 真值：28/42 PASS，14 红——**主定责已落地（主会话复核）**

agent-97 在 195 用 Testcontainers（postgres:16-alpine + Flyway V1~V86 全迁移 + 真实四角色）实跑 7 个 IT 类 42 例，**Skipped=0**。证据已回拉 `docs/测试证据/IT-EVUXDR真值-20260911/`（SUMMARY.md + 7 个 mvn 全量 log + failsafe-reports）。

**12 例授权矩阵红 = 断言形态错，产品与迁移无罪**（主会话 2026-09-11 午后定责，推翻 agent-97 的"列/授权面不一致"猜想）：

- 证据链：①failsafe 堆栈显示异常经 `SQLStateSQLExceptionTranslator` 翻译（PG 驱动 errorCode=0，厂商码翻译链兜不住）；②仓内早有定论——`AlertV7MigrationContractIT.java:59-60` 注释明载"PG 的 42xxx(含 42501 permission denied)被 Spring 译成 BadSqlGrammarException，'permission denied' 只在根因链——断言走整链文本（195 真跑首发现的翻译层坑）"，老 IT（AlertV7/V10/AlertRcaEventIT）全部用整链文本断言且在 195 真跑通过；③`PermissionDeniedDataAccessException` 全仓只有本批三个新 IT 类在用，没有任何在 195 通过过的先例。
- 即：PG 确实按授权矩阵 42501 拒绝了（授权设计生效），是新 IT 断言了 Spring+PG 驱动组合下**永远不会产生的异常类型**（`isInstanceOf(PermissionDeniedDataAccessException.class)`）或**只看顶层 message**（"permission denied" 在根因 PSQLException 链上）。
- **修法**（照 `AlertV7MigrationContractIT.java:150-151` 现成形态）：整链遍历 `for (Throwable c = e; c != null; c = c.getCause())` 拼 message 后断言含 "permission denied"；把三处新 IT 类的 `isInstanceOf(PermissionDeniedDataAccessException.class)` 与顶层 `hasMessageContaining("permission denied")` 全部改为此形。改动仅限 src/test，零产品面。
- 复核抽查：`phaseEventGrantMatrix` 的 publisher_app SELECT count(*) 被 42501 拒=V80 revoke 生效；`commandTableGrantMatrix` 的 control_app UPDATE 被 42501 拒=V81 零 update 开口生效——**授权矩阵本身经此变相验证为真**。

**剩余 4 例非授权矩阵红，另定责**（未复核，执行者须单独查）：
- EV-07 `compareCasesProjectIdentityColumns`（ERROR）、DR-02 `claimSkipLocked`（ERROR）——ERROR 非断言失败，看堆栈定因。
- UX-02 `clientMessageIdUnique`（expected 4L was 3L）、`checkConstraints`（ck_role 未触发先撞 ck_intent）——疑似测试种子/约束顺序假设问题。

逐卡：EV-03(5/6)、EV-04(3/5)、EV-05(6/7)、EV-07(4/5)、UX-01(4/6)、UX-02(2/5)、DR-02(3/8)。修复后 195 复跑命令见 SUMMARY.md。

## 四、环境与纪律（违反=返工）

- 195 访问：`ssh -i ~/.ssh/id_ed25519 root@146.56.195.225`；构建现场 `/opt/build/pr`（源码直挂）。
- **严禁动 `deploy/.env`**（真实文件，R7 四键已配好：PRIMARY_ENABLED=true、AGENT_MODEL=glm-5、OPENAI_COMPAT_BASE_URL=http://litellm-am3:4000 无 /v1 段、key=litellm master）；只动 control-app 容器；DB 只读（`docker exec deploy-postgres-1 psql -U postgres -d pr_agent`）。
- 本地脚本源 `docs/测试证据/R7/e2e-脚本/` ↔ 195 `/opt/r7-e2e/` 必须同步（改了一端 scp 另一端 + `sh -n`）。
- 记账三处：BUGLOG（新雷）、`docs/告警-R7执行日志-20260911.md` §14（跑次）、`docs/告警-PROGRESS.md`（交付条目）。提交只 add 自己的路径。
- NOT_RUN 禁 PASS 占位；FAIL 取证修复重跑，禁改口径。
- RV08 红线：`model_call_ledger` 字面量在 src/main 只允许 PostgresModelCallLedgerRepository 与 ControlSelfCheck 两文件（javadoc 里也不许）。
- 账面口径：rca_model_call 是 RCA 唯一账本山（V48）；平台 model_call_ledger 对 RCA 恒零行（A0 phase6 断言面）。
- 回滚资产：195 上 `pr-agent/control-app:rollback-pre-mergeA` 镜像 + `/opt/build/pr/backup-pre-mergeA-20260911-1158.sql.gz`。

## 五、合并风险提示（给做主会话的人）

1. **EN worktree 冲突文件 = BA-112/113 刚动过的同一文件**（NativeInvestigationExecutor.java），EN 合 main 时必须以 main 新面为准重新解冲突，不能简单取 EN 侧。
2. EV-08（d92634e）在 ev/eval-center 未推送；合 main 时注意 V87 号段（V84 空置已确认，无撞）。
3. r7/armA-knob 基于 128cda7，落后 main 两个提交（59cad8f/7fd5705），rebase 后需复跑全量。
