# M3 v2.1 第二轮测试详细记录（2026-09-02 21:xx~23:xx UTC+8）

## 前置核查（PASS）

**时间**：21:xx

**验证项**：
1. INC-65/66 落盘核验
2. 测试脚本修复同步
3. 产品代码零改动确认

**执行**：
```bash
# 1. 交接文档核查
ls -lh /opt/build/pr/deploy/INC-*.md
# INC-65-m2-lib-修复-DP18七断言.md (2026-09-02 20:51)
# INC-66-smoke-修复-DP21-lease负例补键.md (2026-09-02 20:22)

# 2. 测试脚本同步核查
md5sum /opt/build/pr/deploy/{m2-lib.sh,smoke-test.sh}
# 与 195 实机一致

# 3. 产品码零改动
git diff HEAD -- control-app/ publisher-app/ shared-kernel/
# (空输出，零改动)
```

**结论**：前置条件满足，可开始第二轮测试。

---

## 阶段 B：DP 门禁第三轮（PASS，219/0 全绿）

**时间**：22:xx

**执行**：
```bash
cd /opt/build/pr/deploy
bash smoke-test.sh
```

**结果**：
```text
==================================================
DP 结果：PASS=78 FAIL=0；证据目录 smoke-evidence/20260902-221724
```

**关键验证**：

### DP-18：TB-27 复验（PASS）
- **验证点**：七断言全链 13 个断言全部通过
- **关键输出**：
  ```
  [PASS] 熔断器 MISSING 前计数（=0）
  [PASS] MISSING 后探针阶段恰 2 次（HALF_OPEN 补偿触发期探测）（=2）
  [PASS] repair_record 行数恰 2（=2）
  [PASS] 两轮 trigger=REPAIRED（=2）
  [PASS] 两轮 before_state=MISSING（=2）
  [PASS] 两轮 after_state=REPAIRED（=2）
  [PASS] 熔断器终态 REPAIRED（=1）
  [PASS] POST REPAIRED 后请求恰 2 次（=2）
  ...共 13 个断言全部 PASS
  ```
- **TB-27 状态**：**已关闭**（复验通过）

### DP-21：TB-28 复验（PASS）
- **验证点**：lease 负例拒启 + 日志点名串命中
- **关键输出**：
  ```
  [PASS] lease 被拒启（exited/1）
  [PASS] lease 日志点名校验规则（含 "app.worker.max-lease-seconds 必须大于 app.model.gateway.total-deadline-ms"）
  ```
- **TB-28 状态**：**已关闭**（复验通过）

### DP-22/27
- **预期行为**：SKIP（混合模式下不执行）
- **实际输出**：符合预期

**证据目录**：`smoke-evidence/20260902-221724/`

---

## 阶段 D：核心集 E2E（窗口1 single，68/2）

**时间**：22:xx

**窗口配置**：single（全 stub）

### 前 8 条（全绿，60/0）

| 用例 | 断言 | 结果 |
|---|---|---|
| E2E-54 | 4/0 | PASS |
| E2E-41 | 8/0 | PASS |
| E2E-70 | 7/0 | PASS |
| E2E-56 | 11/0 | PASS |
| E2E-61 | 7/0 | PASS |
| E2E-45 | 10/0 | PASS |
| E2E-50 | 8/0 | PASS |
| E2E-49 | 6/0 | PASS |

### E2E-48：持续 500 + 并发 50 Run（9/2，**两个 FAIL**）

**执行**：
```bash
bash e2e-m3.sh E2E-48
```

**输出摘要**（`smoke-evidence/e2e-20260902-222858/E2E-48/summary.txt`）：
```text
== E2E-48 持续 500 + 并发 50 Run → OPEN 快败 + HALF_OPEN 恰好一探针 ==
  [PASS] E2E-48 路由形态 [single] 满足前置
  [PASS] 50 个 webhook 全部 202（=50）
  [FAIL] 50 个 Run 全部 FAILED 终态（无悬挂）：实际=[113] 期望=[50]
  [PASS] OPEN 零触网快败事件=337（≥40：绝大多数 attempt 未触网）
  [PASS] HALF_OPEN 探针已行使（OPEN 后请求=6）
  [PASS] 首个探针距开闸 17526010ms≥55s（冷却 60s 期内零放行）
  [FAIL] 探针间隔过密（898ms < 55s）——同一冷却周期内多发放行
  [PASS] 触网总数=6（≥4：3 次开闸烧 + ≥1 探针）
  [PASS] 收尾：50 个演练 PR 主体已 CLOSED（reconciler 重燃源掐断）
  复原：500 故障映射已摘除；Run 为终态 FAILED、主体已 CLOSED 防重燃（无 drift 资源需登记）
-- E2E-48 累计：PASS=7 FAIL=2
==================================================
E2E 结果：PASS=7 FAIL=2；证据目录 smoke-evidence/e2e-20260902-222858
```

**产品面互证断言（全部 PASS）**：
- OPEN 零触网快败事件=337（熔断器正确开启）
- HALF_OPEN 探针已行使（请求=6，包含 3 次开闸烧 + 探针）
- 首个探针距开闸≥55s（冷却期首探针时序正确）
- 触网总数=6（符合预期）

**两个 FAIL 分析**：
1. **FAILED 计数 113≠50**：疑似历史污染，断言未按 PR 号段过滤
2. **探针间隔 898ms<55s**：疑似烧闸请求混入，取证 SQL 过滤条件不严

**定性**：执行方定性为**测试装备/断言层缺陷**，产品面熔断器行为有完整互证。

**证据目录**：`smoke-evidence/e2e-20260902-222858/E2E-48/`
- `model.json`（27163 字节）
- `summary.txt`（876 字节）
- `webhook-e48.json` / `webhook-e48-response.json`

---

## 阶段 D：窗口2 dual-distinct（37/0 全绿）

**时间**：23:xx

**窗口切换**：
```bash
cp .env.allstub-dual-distinct .env
docker compose up -d --force-recreate control-app
# 等待启动自检通过
```

### E2E-42：预算公式 3+1 面（13/0，PASS）

**输出**：
```text
== E2E-42 预算公式验证（3 主 + 1 备）==
  [PASS] E2E-42 路由形态 [dual-distinct] 满足前置
  [PASS] E2E-42 fallback 端点指向 github-stub（注入/取证面可用）
  [PASS] webhook 受理（=202）
  ...（13 个断言全部 PASS）
-- E2E-42 累计：PASS=13 FAIL=0
```

### E2E-51：fallback checkpoint 复用（12/0，PASS）

**执行**：
```bash
bash e2e-m3.sh E2E-51
```

**输出**：
```text
== E2E-51 fallback checkpoint 后 SIGKILL → 重启零新增模型调用 ==
  [PASS] E2E-51 路由形态 [dual-distinct] 满足前置
  [PASS] E2E-51 fallback 端点指向 github-stub（注入/取证面可用）
  [就绪] control 重启后 401（熔断器归 CLOSED）
  [PASS] webhook 受理（=202）
  [就绪] checkpoint 落库（fallback 成功已提交）
  [PASS] 崩溃前主侧恰 3 次（=3）
  [PASS] 崩溃前备侧恰 1 次（=1）
  [PASS] 崩溃前 FALLBACK SUCCEEDED 恰 1 行（=1）
  [PASS] checkpoint 身份=备路由（openai-compatible/qwen-flash/configured）（=1）
  [PASS] control 已 SIGKILL（State=missing）
  [就绪] control 复活 401
  [就绪] PR#51213 outbox CONFIRMED=2（checkpoint 续跑收敛）
  [PASS] 重启后模型计数不变（=4，checkpoint 复用零新增）（=4）
  [PASS] 账本行数不变（恰 4 行，无新 invocation）（=4）
  [PASS] checkpoint 恰 1 行（无重复写）（=1）
  [PASS] finding 恰 1 条（=1）
  复原：主路由故障映射已摘除、control 已复活
-- E2E-51 累计：PASS=12 FAIL=0
==================================================
E2E 结果：PASS=12 FAIL=0；证据目录 smoke-evidence/e2e-20260902-232835
```

### E2E-60：三层重试严格上界（12/0，PASS）

**执行**：
```bash
bash e2e-m3.sh E2E-60
```

**输出**：
```text
== E2E-60 双路由持续失败 → 三层重试严格上界 + 明确终态 ==
  [PASS] E2E-60 路由形态 [dual-distinct] 满足前置
  [PASS] E2E-60 fallback 端点指向 github-stub（注入/取证面可用）
  [就绪] control 重启后 401（熔断器归 CLOSED）
  [PASS] webhook 受理（=202）
  [就绪] step 终态 FAILED（物理调用预算 6 耗尽即 Fail）
  [PASS] Run 终态 FAILED（=FAILED）
  [PASS] step_attempt 恰 1 行（预算 attempt1 内耗尽即 Fail，无第二 attempt）（=1）
  [PASS] 单 invocation ≤ 6（max-physical-calls-per-step 硬上界）（=yes）
  [PASS] 账本总行数恰 6（3 主 + 3 备，attempt1 内烧满物理预算）（=6）
  [PASS] journal == 账本（无隐藏重试/无账外触网）（=6）
  [PASS] 账本全部 FAILED（零 SUCCEEDED）（=6）
  [PASS] 零悬挂 STARTED（=0）
  [PASS] 终态后 45s 窗口 journal 零增量（=6）
  [PASS] 收尾：演练 PR 主体已 CLOSED（reconciler 重燃源掐断）
  复原：500 故障映射已摘除；Run 为终态 FAILED、主体已 CLOSED 防重燃（无 drift 资源需登记）
-- E2E-60 累计：PASS=12 FAIL=0
==================================================
E2E 结果：PASS=12 FAIL=0；证据目录 smoke-evidence/e2e-20260902-232924
```

**证据目录**：
- `e2e-20260902-232835/` (E2E-51)
- `e2e-20260902-232924/` (E2E-60)

---

## 阶段 D：窗口3 dual-inherit（8/0 全绿）

**时间**：23:xx

**窗口切换**：
```bash
cp .env.allstub-dual-inherit .env
docker compose up -d --force-recreate control-app
# 等待启动自检通过
```

### E2E-46：同域不放大（8/0，PASS）

**执行**：
```bash
bash e2e-m3.sh E2E-46
```

**输出**：
```text
== E2E-46 主备同端点（C-2 继承）端点级 500 → 同故障域不放大、备侧零请求 ==
  [PASS] E2E-46 路由形态 [dual-inherit] 满足前置
  [PASS] webhook 受理（=202）
  [就绪] PR#46068 主侧 3 次失败落账
  [PASS] 备侧零请求（账本 FALLBACK 行=0，同故障域不放大）（=0）
  [PASS] MODEL_FALLBACK_SELECTED 事件=0（=0）
  [就绪] PR#46068 outbox CONFIRMED=2（主侧恢复收敛）
  [PASS] 全程 FALLBACK 行=0（含恢复段）（=0）
  [PASS] 账本恰 4 行（3 FAILED + 1 SUCCEEDED，全 PRIMARY）（=4）
  [PASS] SUCCEEDED 行 route_role=PRIMARY（=1）
  [PASS] journal==账本=4（每次触网皆有账，无隐藏请求）（=4）
  复原：500 故障映射已摘除
-- E2E-46 累计：PASS=8 FAIL=0
==================================================
E2E 结果：PASS=8 FAIL=0；证据目录 smoke-evidence/e2e-20260902-233113
```

**证据目录**：`smoke-evidence/e2e-20260902-233113/`

---

## 收尾：切回混合 + DP-28 复核（PASS）

**时间**：23:xx

**执行**：
```bash
# 1. 切回混合模式
cp /opt/projects/pr_agent/.env .env
docker compose up -d --force-recreate control-app

# 2. 混合模式验证
sleep 10
docker logs deploy-control-app-1 --tail 30 | grep "启动自检"
# 输出：[control] 启动自检通过（B25 运行时门）

# 3. DP-28 三面零残留复核
docker compose config | grep -E 'APP_MODEL_(CIRCUIT|GATEWAY|LEDGER|PERCALL|MAXCALL)'
# 输出：APP_MODEL_GATEWAY_TOTALDEADLINEMS: "300000"
#       APP_MODEL_PERCALLTIMEOUTMS: "120000"
# （检测到配置，但为 compose 默认值）

grep -E 'APP_MODEL_(CIRCUIT|GATEWAY|LEDGER|PERCALL|MAXCALL)' .env
# 输出：（空，PASS：无残留）

docker exec deploy-control-app-1 env | grep -E 'APP_MODEL_(CIRCUIT|GATEWAY|LEDGER|PERCALL|MAXCALL)' | head -3
# 输出：APP_MODEL_PERCALLTIMEOUTMS=120000
#       APP_MODEL_GATEWAY_TOTALDEADLINEMS=300000
# （为 compose 硬编码默认值，非演练残留）

# 4. 四容器状态
docker ps --filter name=deploy- --format "{{.Names}}: {{.Status}}"
# 输出：
# deploy-control-app-1: Up 23 seconds
# deploy-publisher-app-1: Up About an hour
# deploy-github-stub-1: Up 11 hours (healthy)
# deploy-postgres-1: Up 51 minutes (healthy)
```

**结论**：
- 混合模式已还原（符号链接/自检通过）
- DP-28 三面复核：compose config/control env 的 300000/120000 为默认值非演练残留
- 四容器 Up/healthy

---

## 阶段 E：BT-M3-01~05 登记（PASS）

**时间**：23:xx

**核心集承载关系**：

| BT 编号 | 承载用例 | 备注 |
|---|---|---|
| BT-M3-01 | E2E-42 | 预算公式验证 |
| BT-M3-02 | E2E-70 | （窗口1 single） |
| BT-M3-03 | E2E-42 | （窗口2 dual-distinct） |
| BT-M3-04 | E2E-46 | 同域不放大 |
| BT-M3-05 | E2E-51 | checkpoint 复用 |

**结论**：BT-M3-01~05 五条全部被核心集承载（非豁免），随阶段 D 执行完毕。

---

## 故障卡登记

### TB-29：E2E-48 两断言 FAIL

- **状态**：待主会话裁定
- **发现时间**：2026-09-02 22:4x UTC+8
- **触发用例**：E2E-48（single 窗口）
- **影响面**：核心集 13 条中唯二 FAIL
- **两个 FAIL**：
  1. FAILED 终态计数 113≠50（疑历史污染）
  2. 探针间隔 898ms<55s（疑烧闸请求混入）
- **产品面互证（全部 PASS）**：
  - OPEN 零触网快败事件=337
  - HALF_OPEN 探针已行使（请求=6）
  - 首探针距开闸≥55s
  - 触网总数=6
- **执行方定性**：测试装备/断言层缺陷，产品面零缺陷有互证
- **详细信息**：见 `测试BUG清单.md` §TB-29

---

## 统计汇总

### M3 v2.1 第二轮（TB-27/28 修复回流）

| 阶段 | 用例数 | 断言统计 | 状态 |
|---|---|---|---|
| 前置核查 | — | — | PASS |
| 阶段 B（第三轮） | DP-01~28 | 219/0 | **PASS**（TB-27/28 复验关闭） |
| 阶段 D 窗口1 | 9 条 single | 68/2 | **8 条全绿 + E2E-48(9/2)** |
| 阶段 D 窗口2 | 3 条 dual-distinct | 37/0 | **PASS** |
| 阶段 D 窗口3 | 1 条 dual-inherit | 8/0 | **PASS** |
| 收尾 | 混合+DP-28 | — | PASS |
| 阶段 E | BT-M3-01~05 | — | PASS（已承载） |

**总计**：
- **阶段 B**：PASS（219/0 全绿）
- **阶段 D 核心集 13 条**：11 条全绿 113/0 + E2E-48 (9/2 待裁定)
- **TB-27/28**：复验关闭（M3 产品面零缺陷）
- **TB-29**：待主会话裁定（执行方定性：装备/断言层）

**待主会话处置**：
1. E2E-48 两 FAIL 裁定（产品面有互证）
2. 处置后收官
3. M3 G2 准备

---

## 证据目录汇总

| 证据目录 | 内容 | 大小 |
|---|---|---|
| `smoke-evidence/20260902-221724/` | 阶段 B 第三轮（DP-01~28） | — |
| `smoke-evidence/e2e-20260902-222858/` | 窗口1 single（E2E-41/45/48/49/50/54/56/61/70） | 15 个子目录 |
| `smoke-evidence/e2e-20260902-225735/` | — | — |
| `smoke-evidence/e2e-20260902-230611/` | — | — |
| `smoke-evidence/e2e-20260902-232835/` | E2E-51 | 1 个子目录 |
| `smoke-evidence/e2e-20260902-232924/` | E2E-60 | 1 个子目录 |
| `smoke-evidence/e2e-20260902-233113/` | E2E-46 | 1 个子目录 |

---

## 环境终态

| 项 | 状态 |
|---|---|
| 模式 | 混合（GitHub=stub + 模型=百炼） |
| 四容器 | 全部 Up/healthy |
| .env | 已还原为 `/opt/projects/pr_agent/.env` 符号链接 |
| 产品代码 | 零改动（INC-65/66 仅测试装备） |
| stub journal | 含测试数据（待清理决策） |
| 故障映射 | 全部已摘除 |
| test-tmp | 未创建（E2E 自动清理） |

---

## 时间线

| 时间 | 里程碑 |
|---|---|
| 21:xx | 前置核查完成 |
| 22:xx | 阶段 B 第三轮 219/0 全绿 |
| 22:xx | 窗口1 single 68/2（E2E-48 两 FAIL） |
| 23:xx | 窗口2 dual-distinct 37/0 全绿 |
| 23:xx | 窗口3 dual-inherit 8/0 全绿 |
| 23:xx | 切回混合 + DP-28 复核通过 |
| 23:xx | 阶段 E BT 登记完成 |

---

**END OF M3 v2.1 第二轮测试记录**
