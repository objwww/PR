# 告警-EN 执行日志（增强线 Tool/MCP/RAG/Skill）— 2026-09-11

> 执行者单线日志：按 `docs/告警-增强线-Tool-MCP-RAG-Skill技术方案-v1.1.md` §十二 卡序执行，
> §6.1 并行规约约束全程。工作区 = `E:\kimiCode-en`（worktree，分支 `en/enhance-line`，
> 基于 main@db262b9）；**本线禁止在 `E:\kimiCode` 主工作区写代码**（R7 线正在该处作业，
> 其 V46/V47 未提交变更与本线无关）。
>
> 诚实纪律：ABC v2.0 §七防假绿 + §十 测试模式三分（E/L/B）全部适用；
> 真模型/真 PG/真机部分本机不可证 → 标 NOT_RUN/NOT_IMPLEMENTED，禁止 PASS 占位。

## 〇、三线协调备忘（2026-09-11，用户裁定落档）

- **Flyway 号段三线定死**：R7=V46 起、EN=V60 起、EV/DR（评测与故障演练）=V80 起。
  本线首迁移 V60（已 `ls` 核实：worktree 迁移至 V45 止，V46/V47 系 R7 主工作区未提交件）。
- **195 验收窗三线串行**：真 PG/真模型/部署窗口 R7/EN/EV-DR 排队，不并发部署
  control-app；演练注入执行期间其他两线真机验证等窗。本线 L0（`mvn test`）本地跑，
  L1 真 PG IT 与 L2 部署验收全部排 195 窗。
- **EV 线第一批（EV-01/EV-02）**：只动 alert-web（主工作区），与本线零交集；
  第二批 EV-03/04/05 将动 control-app eval 读面与持久化作业——与 EN-09（评测补强）
  相邻，届时需向其提供本卡资产/组合身份契约（digest 身份、release_manifest 结构），
  迁移号走 V80 段，互不抢号。

## 一、开工自检（EN-01/02/03、EN-09 契约部分，§6.1 表行 1）

**条件：EX 13/13 + C 门（用户裁定已满足；自检只需引用 EX 交接包锚点）**

| 检测锚 | 实测结果 |
|---|---|
| EX 交接包 13/13 收口索引 | `docs/告警-EX执行日志-20260910.md` L1265-1287：EX 线十三卡（A0/A1/A2/A3/A4a/A4b/B1/B2/C3a/C2a/C1/C2/C4）全部收口，逐卡块行号在册 |
| EX L0/L1/L2 证据锚 | 同文件 L1289-1310：L0 control 1086/0/21-skip；L1 195 真 PG `mvn-verify-m718-control.log`（173 IT 全绿）+ notify 14 IT；L2 `docs/测试证据/AM7/exc4-mig02-20260910/` 等 |
| C 门 | §6.1 表行 1 用户裁定"已满足"；main@7d12920 提交记录「EX 线 13/13 卡 C 门通过、195 真 PG IT 全绿（control 173 IT/notify 14 IT）、基线 1104+ 测试绿」 |
| 工作区隔离锚 | `git worktree list`：`E:/kimiCode  db262b9 [main]` + `E:/kimiCode-en db262b9 [en/enhance-line]`（本线）；主工作区 `git status` 实测 R7 未提交件在册（V46/V47、AgentPhase 等），本线零触碰 |
| Flyway 号段锚 | worktree `ls db/migration`：V1~V45 在库；**V46~V59 留 R7，EN 自 V60 起**（下方 EN-01 首迁移前二次 `ls` 复核） |

**结论：EN-01/02/03（及 EN-09 契约同步部分）开工条件满足，动工。**
EN-04（热更新命令）依赖 R7 X1/X4 编码完成+测试绿——截至本日志时刻 R7 上述文件未提交、
锚点②（R7 日志对应卡块记录完成且测试绿）未核实 → EN-04 不动工，留待复查。
EN-05（真实工具）依赖 R7 卡 7（R7a-2 ActionGuard 组装）→ 同上未动工。

## 二、EN-01 资产与组合（§8.2 数据契约 + §8.3 文件级改法）

**范围**（对照卡行：扩 ConfigBundle manifest、资产校验与版本依赖；关联用例 P01/02/04、S03/04/10）：

- `release_manifest` typed 段进 bundle content（8 成员：schema_version/角色→Prompt 映射/
  Skill 允许集/工具 schema 集/模型路由/RAG 语料版本/上下文规则/Harness 兼容范围）；
  结构校验进 `ConfigBundle` 构造期（fail-closed，零框架 L0 规则不破）；
  **依赖闭包校验进 `ConfigBundleService.publish`**（引用资产缺失 → 发布拒绝，指针天然不动）。
- `ReleaseAsset`（PROMPT/SKILL/TOOL_SCHEMA 一表三类）：身份=内容 canonical digest（P01/S10 锚），
  密钥键名扫描与 bundle 同源（INV-AM5-5），PROMPT 模板占位符 ⊆ variables_schema（P02 缺变量拒绝）。
- `release_asset` 表（V60）：UNIQUE(kind,digest)，DB 面 select,insert（immutable 惯例同 V24）。
- 不做（越界推迟）：资产 REST 控制器（EN-02/EN-10 面）、Skill manifest 语义级 DAG 校验（EN-08）、
  向量召回（§三阶段 2）、第二个 active 指针（§8.2 明令禁止——复用 config_bundle_active CAS）。

### TDD 红绿记录

**红轮**（测试先行，新面编译失败为红）：
`mvn -B -ntp -pl control-app test-compile` → COMPILATION ERROR 全集为"找不到符号"
（ReleaseManifest/ReleaseAsset/ReleaseAssetRepository/新构造器尚未存在），
涉及 ReleaseManifestTest/ReleaseAssetTest/ConfigBundleTest 新案/ConfigBundleServiceTest 新案。

**中途红**（首绿轮 46 run / 1F / 1E，均为测试自身缺陷，非实现缺陷）：
- `ConfigBundleServiceTest.registerAssetIdempotentPerKind`：TOOL_SCHEMA 形状校验拒绝
  PROMPT 形状内容——跨 kind 用例改用三形状皆合法的混合内容载体（IT 侧同修）；
- `ConfigBundleTest.invalidManifestSectionRejected`：成员解析先于 schema_version 报错——
  实现改为 `fromRaw` 先验 schema_version 再解析成员（版本协商先行，报错确定性更好）。

**绿轮**：
- scoped：`mvn -pl control-app test -Dtest="ConfigBundleTest,ReleaseManifestTest,
  ReleaseAssetTest,ConfigBundleServiceTest,ConfigBundleControllerTest"` → 0F/0E；
- 全量：`mvn -B -ntp -pl control-app test` → **Tests run: 1146, Failures: 0,
  Errors: 0, Skipped: 25, BUILD SUCCESS**（en01-green-full.log；25 skip 全部为
  it/ 包真 PG IT 无 Docker 自动跳过——含新增 PostgresReleaseAssetRepositoryTest
  4 案，真值待 195 窗，NOT_RUN 不作 PASS 计）。

**改动清单**（新增/修改，全部 EN 号段 V60）：

| 文件 | 性质 |
|---|---|
| `release/domain/model/ReleaseManifest.java` | 新增：typed manifest 8 成员结构校验（schema_version 先行） |
| `release/domain/model/ReleaseAsset.java` | 新增：内容寻址资产（PROMPT 缺变量拒绝/SKILL/TOOL_SCHEMA 形状） |
| `release/domain/model/SecretKeys.java` | 新增：密钥键名检测单一事实源（ConfigBundle/ReleaseAsset 共用） |
| `release/domain/model/ConfigBundle.java` | 修改：manifest 段构造期校验 + `releaseManifest()` 视图；密钥检测换 SecretKeys |
| `release/domain/repository/ReleaseAssetRepository.java` | 新增：insert/findByDigest(kind,digest) |
| `release/application/ConfigBundleService.java` | 修改：两参构造（资产仓储）、`registerAsset`、发布面依赖闭包校验（P04） |
| `infrastructure/persistence/PostgresReleaseAssetRepository.java` | 新增：JdbcClient 实现（读路径重过域校验） |
| `infrastructure/config/PersistenceConfig.java` | 修改：releaseAssetRepository bean + service 装配 |
| `db/migration/V60__en01_release_asset.sql` | 新增：release_asset 表（UNIQUE(kind,digest)，select,insert 授权面同 V24） |
| 测试 5 文件 + PostgresITBase（ALL_TABLES 入 release_asset） | 新增/修改：46 案面 |

**闭包与身份不变量**（本卡建立，EN-02/03 续用）：组合身份=bundle canonical digest
（manifest 段参与内容，依赖变动即新身份）；资产身份=(kind, 内容 digest)；
密钥仅引用（键名 fail-closed）；发布组合不设第二个 active 指针（复用 config_bundle_active CAS）。

### 用例映射现状

| 用例 | 状态 | 落点 |
|---|---|---|
| P01 canonical 同 digest | UT 可落 | ConfigBundleTest 键序稳定案 + manifest 段案 |
| P02 缺变量/错误 schema | UT 可落（资产注册拒绝 + manifest 结构拒绝） | ReleaseAssetTest / ReleaseManifestTest |
| P04 依赖闭包失败指针不变 | UT 可落（服务层闭包拒绝；E2E 待 EN-09 基建） | ConfigBundleServiceTest |
| S03/S04/S10 | NOT_IMPLEMENTED（S03 污染面/S04 DAG 校验归 EN-08；S10 篡改=内容寻址免费锚，UT 部分 E2E 部分） | — |
