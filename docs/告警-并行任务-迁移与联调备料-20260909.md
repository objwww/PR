# 并行任务备料：迁移线 + 联调线可立即开工项（2026-09-09）

> 用途：主会话当前占用面 = ①195→127 rsync 备份传输（带宽独占，预计还剩 ~1.5h）②BA-63 litellm/alertmanager 重启（等用户确认）③AM7 值班通知方案（等 G1）。以下任务与这三个面**零冲突**，可分派其他执行者立即开工。
> 全局纪律（所有执行者必读）：生产侧操作先备份后变更；git 变更先获用户确认；Bug 当场入 `docs/告警-BUGLOG.md`（下一可用编号 BA-64 起）；证据入 `docs/测试证据/`；密钥不进仓库/文档/日志。
> 机器锚点：195 = `ssh -i ~/.ssh/id_ed25519 root@146.56.195.225`；127 = `ssh -i ~/.ssh/hotel_deploy root@117.72.208.68`。隧道：195=10.250.250.1、127=10.250.250.2（已通，勿重配）。

## P1【127】Gatus v5.17.0 三项契约冲突修正 + 镜像实测启动（MIG-02 前半）

- 背景：评审文档 Phase 1 已裁定 `deploy/gatus/gatus-config.yml` 有三项契约冲突：①`alerting.webhook` 应为 `alerting.custom`；②`storage.type: file` 非法（官方只认 memory/sqlite/postgres）；③endpoint 未声明 `alerts`。**"填变量就能用"不成立，必须镜像实测**。
- 执行面：127 本机。拉 `twinproduction/gatus:v5.17.0`（记录 digest，pin 进 `deploy/gatus/compose.gatus.yml`），逐项修正配置后以临时占位 webhook（如指向 127 本机 8081 之外的一个 nc/httpbin 监听）启动，验收：启动日志明确读 /config/config.yml、UI 只含本项目 3 endpoint 无镜像示例、失败/恢复告警按阈值触发（可临时把某 endpoint 指向必败地址验证）。
- 隔离边界：**不动 195 任何东西**；值班接收器真实地址留占位变量，通知腿归 AM7 M7-17，不在本任务范围；拉镜像期间自觉限速/错峰（rsync 在跑，127 入网带宽共享）。
- 产出：修正后的 `deploy/gatus/gatus-config.yml` + `compose.gatus.yml`（pin digest）+ 启动验收证据 `docs/测试证据/HOST2-127/gatus-v5170-契约修正/`；Gatus 官方文档依据摘入 `docs/告警-OSS-证据清单.md`（E-23）。

## P2【195】BA-54 三档内存闸 Prom rules 落码（运维面选项②的前半）

- 背景：BA-54 开放——三档内存闸无自动判定面。数据源缺口 2026-09-09 已闭合（`node_memory_MemAvailable_bytes{job="node-exporter-host1"}` 入库可查）。
- 执行面：先在 Prometheus API 上 dry-run PromQL 表达式（`curl -G 127.0.0.1:9090/api/v1/query`），确认取数正确后写三条 alert rules 到 `/opt/build/pr/deploy/alert/prometheus/rules/memory-gate.yml`：tier1 `MemAvailable < 1.2GiB 持续 5min`、tier2 `< 768MiB 持续 2min`、tier3 `< 512MiB`（滞回 1536MiB×10min 用 `for` + 反向规则表达，或如实记录"滞回需 recording rule 辅助"的取舍）。rules 是**目录 mount**，新文件即刻可见；写文件前备份目录快照，写后用 `docker cp` 进容器 `promtool check rules` 校验，再 `docker kill --signal=HUP prometheus-am0` 热加载（rules 热加载可靠，无需 restart——但加载后必须 `curl /api/v1/rules` 实证规则在场）。
- 隔离边界：只新增一个 rules 文件，不改 prometheus.yml、不动 alertmanager.yml（alertmanager 路由是否接这三条告警=另一决策，本任务**不接**，rules 只在 Prom 面可见即可）。
- 产出：rules 文件 + dry-run/加载/查询证据 `docs/测试证据/容量/memory-gate-rules-2026MMDD/`；BA-54 条目追加进展注记（不关单，动作面/事件面仍缺）。

## P3【195 只读】磁盘深查与处置建议清单

- 背景：195 磁盘 74%（余 14G）；缓办项 = 37 个未使用 docker volume（约 3.42G）+ /root 约 2.0G 未勘察。
- 执行面：纯只读。`docker system df -v` 导出 37 个 volume 明细，逐卷 `docker run --rm -v <vol>:/v alpine sh -c 'du -sh /v; ls -la /v | head'` 取样判内容族（注意：alpine 镜像若不在本地改用已存在的小镜像，**禁新拉大镜像**）；/root 下 `du -sh */` 分级。
- 隔离边界：**只勘察不删除**，任何删除动作回主会话裁定；rsync 在跑，避免大目录递归读打到 IO 峰值（nice/ionice 或分批）。
- 产出：`docs/测试证据/容量/195-disk-deep-dive-2026MMDD.md`（逐卷裁定建议表：可删/需留/待问）。

## P4【本机只读】FE-01 前后端端点缺口盘点（联调前置）

- 背景：用户要求前后端尽可能联通并联调（评审文档 Phase 12）。alert-web 各页面经 `src/api/client.js` 的 `api(path,{mock})` 调后端，control-app 已实现端点有限（/api/rca-runs、/api/cases、/api/config-bundles、/api/canary/status 等）。
- 执行面：仓库本地。全量扫 `alert-web/src/**/*.vue|js` 里所有 `api('...')` 调用点 → 与 control-app 现有 Controller 端点对账 → 输出矩阵：每个页面每调用 = 已通（真接口存在且契约对齐）/ 契约偏差（存在但字段不符）/ 纯 mock（后端无端点）；含每个缺口的建议落点（新增投影 API or 复用现有）。
- 隔离边界：只读盘点，不改码；产出直接喂 Phase 12 任务排序。
- 产出：`docs/告警-前端联调-端点缺口盘点-v1.md`。

## P5【本机只读】M7-17 duty-adapter 预备调研（AM7 增量 G1 待定，先备料不动码）

- 背景：AM7 增量方案（`docs/告警AM7-值班通知与值班表增量技术方案.md`，待 G1）里 127 duty-adapter 需承接 Gatus custom webhook → 企微/钉钉（业务码校验）→ 排班快照拉取。
- 执行面：查 Gatus v5.17.0 `alerting.custom` 官方文档（出站请求体模板、可用占位符、重试语义）；查企微/钉钉 markdown 消息体的确切字段约束；把事实摘入 `docs/告警-OSS-证据清单.md`（E-24，标注【明示】来源链接）；对 adapter 的最小内存足迹给出 Java/Spring 与 Go 两个形态的对比结论（127 只有 2.8G available——若 Go 形态明显更省，把"引入新栈代价"如实列出让 G1 裁定）。
- 隔离边界：调研与证据清单更新，**不写实现代码**（G1 未过）。

## 明确不可并行的面（防撞车）

1. 195 `/opt/backups` 删除与 holmes 镜像 rmi——等主会话 rsync 校验完成。
2. litellm-am3 / alertmanager-am0 重启（BA-63）——等用户确认，且执行时不得与 P2 的 Prometheus HUP 同时打（通知链短暂间隙叠加）。
3. AM7 值班通知/值班表任何编码——G1 未过。
4. 195 上任何新起容器/新开端口——内存水位紧张（available 3438MiB），新动作先回主会话对账。
